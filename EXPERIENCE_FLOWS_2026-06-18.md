# Setnayan — End-to-End Experience Flows
Date: 2026-06-18 · code @ 09ec9601 (fresh origin/main) · ✅ works · ⚠️ partial · ⛔ broken

## COUPLE — account → launch editorial

✅ **1.** _P1 — Account creation + onboarding_ — Tap a 'Start planning · free' / 'Plan your wedding' CTA on the marketing homepage → Anonymous navigation straight into the onboarding wizard; no DB write  (/ (marketing homepage))
✅ **2.** _P1 — Account creation + onboarding_ — Walk the wizard screens (role → kind → faith → name → date → love story → region → pax → budget → team → songs → mood) → All answers held in React state + localStorage (lazy-commit); read-only marketplace vendor counts + reception venues shown; nothing persisted to Postgres  (/onboarding/wedding (anonymous))
⚠️ **3.** _P1 — Account creation + onboarding_ — Reach the account gate; pick Continue-with-Google OR Use-email-instead inline form → auth.users created → trigger handle_new_auth_user inserts public.users(account_type='customer'); email auto-confirmed; welcome email fired. No event rows yet  (/onboarding/wedding → screen 11 'account' gate)
⚠️ **4.** _P1 — Account creation + onboarding_ — (EMAIL PATH ONLY) Forced to /login; must manually re-enter the just-set password and Sign in → signInWithPassword authenticates, stamps last_login, redirects to ?resume=1. Draft survives in localStorage (no data loss) but seamless 'create account → keep planning' breaks; Google OAuth avoids this entirely  (/login?ready=<email>&next=/onboarding/wedding?resume=1)
✅ **5.** _P1 — Account creation + onboarding_ — Wizard auto-resumes past the satisfied account gate; finish congrats → plan → bundle → services → summary → localStorage draft restored; advances to 'congrats'. Still no events write — commit deferred to final button  (/onboarding/wedding?resume=1 (authenticated))
⚠️ **6.** _P1 — Account creation + onboarding_ — Tap terminal CTA ('Go to my dashboard' / 'Purchase Now' / bundle) → commitOnboardingWedding → ONE lazy commit: events (event_date=NULL written explicitly), event_members(couple), event_moderators(role_subtype), best-effort song picks / shortlisted considering vendors / seed guests / opt-in inquiry fan-out. Idempotent guard prevents dup event  (/onboarding/wedding final button → commitOnboardingWedding (server action))
✅ **7.** _P1 — Account creation + onboarding_ — Automatic — blocking 'Creating your dashboard' overlay (~2.2s) prefetches tabs then routes → Lands on event-scoped dashboard (or checkout drawer for a paid pick); localStorage draft cleared. Countdown/date surfaces render empty because event_date is NULL  (/dashboard/<eventId> (couple dashboard arrival))
✅ **8.** _P1 — Account creation + onboarding (ALT entry)_ — Sign up via standalone /signup → /login bounce → /dashboard empty state → Create event → Wedding → Same trigger-driven users(account_type='customer') write; authed user skips the in-wizard account screen; converges on the same lazy commit (step 6)  (/signup → /login → /dashboard/create-event → /onboarding/wedding (authed))
✅ **9.** _P2 — Plan & Build_ — Add a guest (name/side/group/role + optional email/meal/plus-one), Submit → guests row inserted (rsvp_status=pending); blocked by guard_guest_edits_when_locked once pax finalized  (/dashboard/[eventId]/guests/new)
✅ **10.** _P2 — Plan & Build_ — Paste CSV and Submit bulk import → Up to 200 rows bulk-inserted with dedup against existing + within-file; skipped/error rows reported  (/dashboard/[eventId]/guests/import)
✅ **11.** _P2 — Plan & Build_ — Guest opens invite link, picks role, verifies OTP, submits attending/meal/dietary → guests.update(rsvp_status, meal, dietary); join path mints guest_claims(pending) → couple's claims queue  (/[slug] (guest-facing) / /join/[eventId])
✅ **12.** _P2 — Plan & Build_ — Couple reviews a pending self-claim → Approve (matched/new) or Reject → finalize_guest_claim links claimer→guest_id; confirmation email; claim leaves queue (privacy gate)  (/dashboard/[eventId]/guests/claims)
✅ **13.** _P2 — Plan & Build_ — Browse marketplace, tap Save (Add to plan) on a vendor card → event_vendors considering row (source=host_manual); venue picks re-anchor events.venue_lat/lon; idempotent  (/explore (marketplace card))
✅ **14.** _P2 — Plan & Build_ — Open vendor profile, tap Inquire (single sends; multi opens service-pick sheet) → chat_threads upsert + first message + vendor_inquiry_received; AUTO-creates a considering event_vendors row if none exists (inquiring from profile implicitly saves)  (/v/[slug] (vendor profile))
✅ **15.** _P2 — Plan & Build_ — Open inbox, read vendor reply, continue thread → chat_messages append; vendor's first reply trips reveal_vendor_name_on_chat (real business name surfaces marketplace-wide)  (/dashboard/[eventId]/messages → /messages/[threadId])
✅ **16.** _P2 — Plan & Build_ — Set overall budget, add per-vendor milestone, log a payment with receipt screenshot → events.estimated_budget_centavos + line items + payments rows. Payments are OFF-PLATFORM, NOT Setnayan-verified (proof is host's own evidence)  (/dashboard/[eventId]/budget)
✅ **17.** _P2 — Plan & Build_ — Add tables, assign guests/groups, arrange floor plan, Publish → event_tables + table_assignments; publishSeating stamps qr_published_at → table-QR print pack + find-my-table go live  (/dashboard/[eventId]/seating)
✅ **18.** _P2 — Plan & Build_ — Open Monogram (Studio → Branding), generate bespoke/Cipher marks, Apply one → events.monogram_custom_svg set → propagates event-wide (QR center, hero, save-the-date, signage)  (/dashboard/[eventId]/add-ons → /monogram)
✅ **19.** _P2 — Plan & Build_ — Pick location-feel/dress-code/floral palettes, recolor, save selections + reception design → moodboard palette snapshot + events.reception_design saved; palette feeds the couple-website theme (contrast-safe)  (/dashboard/[eventId]/add-ons/mood-board)
✅ **20.** _P2 — Plan & Build_ — Upload couple photos via multi-file picker, Save → events.our_photos = deduped r2:// refs (capped); revalidates public /[slug] so the gallery goes live  (/dashboard/[eventId]/website/our-photos)
✅ **21.** _P3 — Buying in-app services (apply-then-pay)_ — Open a SKU detail page; in BUY state click 'Unlock · ₱X' to open the checkout drawer → Price resolves ONLY from admin V2 catalog. Setnayan AI BUY surface is DORMANT (SETNAYAN_AI_PAYWALL_ENABLED default OFF → shows owned/available branch, never the buy CTA)  (/dashboard/[eventId]/add-ons/<sku> (e.g. setnayan-ai, papic, bundle))
✅ **22.** _P3 — Buying in-app services (apply-then-pay)_ — In the drawer: optionally apply a voucher, choose GCash/BDO channel, view account + QR → applyVoucherAction read-only validates discount inline; channel toggle renders the rail's name/number/QR. Native (Capacitor) opens checkout in EXTERNAL browser instead (App Store compliance)  (InlineCheckoutDrawer (overlay))
✅ **23.** _P3 — Buying in-app services (apply-then-pay)_ — Pay externally on GCash/BDO, enter reference number, upload payment screenshot (REQUIRED), Submit request → Server re-resolves the AUTHORITATIVE charge from catalog (client price ignored), writes orders(status='submitted', reference 'SN'+8hex) + payments(status='pending', screenshot) + order_ledger near-atomically; payment-instructions email best-effort  (InlineCheckoutDrawer → submitOrderAction)
✅ **24.** _P3 — Buying in-app services (apply-then-pay)_ — See success card; click 'Track this order' or Close; then WAIT for admin reconciliation → Order stays 'submitted' / payment 'pending'. Reference code + 'reconciles within one business day' shown. MANUAL latency — no automated matching  (/dashboard/[eventId]/orders/[orderId])
✅ **25.** _P3 — Buying in-app services (apply-then-pay)_ — (Admin/Setnayan Team) Review screenshot vs reference → Approve · matched / Request resubmit / Reject → approvePayment: payment→matched, order→paid, activateOrderSku, app receipt issued, notification + email. Single-admin authority in V1. Reject flips order→cancelled + revokes access  (/admin/payments (gating step, outside couple view))
✅ **26.** _P3 — Buying in-app services (apply-then-pay)_ — Couple revisits order/feature after approval; capability unlocked → activateOrderSku flips the stored flag (e.g. events.setnayan_ai_active=true); order 'Paid' / payment 'Matched'; receipt link; feature works  (/dashboard/[eventId]/orders/[orderId] + the SKU surface)
✅ **27.** _P4 — Website 4-path → editorial launch_ — Anyone opens the public URL far before the wedding (>90d out); no action — date picks the phase → getLifecyclePhase(event_date) → STD: renders the Save-the-Date film + reveal opening, countdown, add-to-calendar; RSVP/widgets suppressed  (/[slug] (lifecycle phase 'save_the_date'))
✅ **28.** _P4 — Website 4-path → editorial launch_ — Guest opens personal link in the run-up (≤90d, T-3d..T-1h) → WIDGET_PHASES gates to the RSVP set (greeting, qr_card, countdown, schedule, dress_code, rsvp, venue_map); RSVP submit writes guests rows  (/[slug] (lifecycle phase 'rsvp'))
✅ **29.** _P4 — Website 4-path → editorial launch_ — Guest at the venue opens the page on the wedding day (live, T-1h..T+8h); couple can ?phase=event preview → getDayOfPhase 'live': 'Happening now' badge, Panood watch-live, Live Photo Wall mirror, per-guest tagged photos, table + schedule  (/[slug] (lifecycle phase 'event'))
✅ **30.** _P4 — Website 4-path → editorial launch_ — Couple opens the Editorial editor, edits auto-composed copy + section toggles, Save draft → saveEditorial(publish=false) upserts event_editorial draft_json (status='draft'); merge preserves unmanaged keys; revalidates /[slug]; first save fires scanEditorial via after()  (/dashboard/[eventId]/website/editorial)
✅ **31.** _P4 — Website 4-path → editorial launch_ — Couple clicks 'Preview your editorial' (host-only ?phase=editorial override) → Host-verified phase override renders the full newspaper front page (masthead, headline, hero, By-the-Numbers, gallery, wishes, vendor team, thank-you) even while status='draft'. No write  (/[slug]?phase=editorial (host preview))
⚠️ **32.** _P4 — Website 4-path → editorial launch_ — Couple clicks Publish → event_editorial.status='published' + published_at. DECORATIVE for go-live — drives only the OG share-card + admin review queue; does NOT trigger or gate public launch  (/dashboard/[eventId]/website/editorial — Publish)
✅ **33.** _P4 — Website 4-path → editorial launch [THE ENDING]_ — Anyone opens the URL after the wedding date passes (T+8h onward); no action — the date flips the phase → getLifecyclePhase → 'editorial' renders the live public front-page story from event_editorial + live counts. DATE-GATED, status-agnostic. event_date NULL → stays 'rsvp', editorial NEVER launches  (/[slug] (lifecycle phase 'editorial'))
✅ **34.** _P4 — Website 4-path → editorial launch_ — Public browses Real Stories; couple opts in earlier via Privacy & Data consent → Separate RA 10173 gate: public_summary_consent_at NOT NULL + account live + event_date ≤ today-30d + public slug. Returns [] today → curated samples; entries link to the couple's OWN /[slug], never a copy  (/realstories + /realstories/[slug])

**The ending:** The journey terminates at the couple's permanent public front-page EDITORIAL rendered at /[slug] (apps/web/app/[slug]/page.tsx → EditorialContent). It is the masthead/headline/hero/By-the-Numbers/gallery/wishes/vendor-team/thank-you newspaper front page of the wedding. The EXACT gate to reach it is purely date-driven: getLifecyclePhase(events.event_date) must return 'editorial', which happens once the wedding date has passed (day-of 'post' / past, T+8h onward). showEditorialPlaceholder = phasesEnabled && lifecyclePhase==='editorial' (phasesEnabled is ALWAYS true for event_type='wedding', independent of the WEBSITE_PHASES_ENABLED env flag). Critically, the gate is status-agnostic — EditorialContent does NOT check event_editorial.published, so a draft goes fully public the instant the date flips. The Publish button is decorative for go-live (it only sets the OG share-card + admin queue). The single hard prerequisite is a non-NULL events.event_date: getLifecyclePhase(null) returns 'rsvp', so an undated wedding never reaches the editorial phase and the public auto-launch can never fire. Inclusion in the public /realstories gallery is a SEPARATE consent gate (public_summary_consent_at NOT NULL + event_date ≤ today-30d grace + public slug), not part of reaching the /[slug] ending.

**Breaks blocking the end-to-end flow:**
- BLOCKER (the ending): event_date is NULL after onboarding. commitOnboardingWedding writes events.event_date = null explicitly (the wizard collects date_candidates / date_window / date_mode but never resolves a committed date). getLifecyclePhase(null) returns 'rsvp', so the public /[slug] page can NEVER advance to the 'editorial' phase — the terminal artifact never auto-launches — until the couple sets a real date later. This is the one break that truly blocks reaching the end.
- FRICTION (Phase 1, email path only): the in-wizard 'Use email instead' account gate posts to signUp, which ALWAYS redirects to /login?ready=…&next=/onboarding/wedding?resume=1 (signup/actions.ts ~L243-249) — never back into the wizard. The just-created email signer is ejected to a login screen and must manually re-enter the password they just set before the localStorage draft resumes. Draft survives so no data loss; Google OAuth round-trips cleanly via auth/callback. Breaks the seamless 'create account → keep planning' promise for the email path.
- NON-BLOCKING but misleading (Phase 4): the Publish button is decorative for public go-live. Public launch is date-gated and status-agnostic, so (a) a couple who clicks Publish sees no immediate public change, and (b) a couple who never clicks Publish still gets a live editorial after T+8h. Publish only affects the OG share-card choice and the admin review/scan queue.
- NON-BLOCKING dormant surface (Phase 3): the Setnayan AI BUY drawer never renders in prod because SETNAYAN_AI_PAYWALL_ENABLED defaults OFF — the page always shows the owned/available branch and the buy CTA, checkout, and entitlement chain stay unreachable for that SKU even though fully built. Does not block the editorial ending (Phase 3 is optional spend), but blocks that one purchase path.
- NON-BLOCKING funnel leak (Phase 2): a vendor the couple only Follows (card Follow → vendor_follows) never enters Budget/Build — only Save creates the considering pick, and the marketplace CARD has no Inquire CTA at all. Following is an orphan write that bypasses the pick/budget chain. Does not block the ending.

_Note: Single end-to-end couple journey, 34 ordered steps across 4 phases, audited read-only at commit 09ec9601b4f13afa24481d68b2ede029a91e7a6b (Merge PR #1723) in /Users/icecasasola/Setnayan/.claude/worktrees/sn-audit-main. Verified directly in code: getLifecyclePhase(null) returns 'rsvp' and STD_THRESHOLD_DAYS=90 (apps/web/lib/invitation-widgets.ts L334, L400, L405/L422), confirming the event_date-NULL editorial blocker. The flow is otherwise fully wired: anonymous lazy-commit onboarding → couple dashboard (Home/Guests/Explore/Studio/Budget) → apply-then-pay in-app SKUs (InlineCheckoutDrawer → submitOrderAction → admin /admin/payments manual reconciliation → activateOrderSku) → the one time-gated public /[slug] page that re-renders STD → RSVP → EVENT → EDITORIAL by date. Two getLifecyclePhase functions exist (day-of-mode.ts = plan/dayof/after menu phase; invitation-widgets.ts = STD/rsvp/event/editorial website phase); [slug]/page.tsx imports the website one. The single lever for the whole ending is events.event_date. Of all flagged items, only the NULL event_date hard-blocks reaching the published editorial; the rest are friction, dormant, or funnel-leak issues that do not gate the terminal state. Apply-then-pay carries an inherent manual-reconciliation latency (stated SLA: within one business day, single-admin in V1) but that gates SKU activation, not the editorial ending._

## VENDOR — account → close transaction → editorial input

✅ **1.** _P1 Account + Verification_ — Toggle 'I'm a vendor' on signup, enter email/password (min 8), submit 'Create account · free' → auth trigger handle_new_auth_user writes users.account_type='vendor'; second trigger on_users_vendor_created auto-INSERTs a starter vendor_profiles row (is_published=FALSE, public_visibility='coming_soon'); auto-confirmed; redirect to /login?ready=<email>; welcome email via Resend  (/signup?as=vendor)
✅ **2.** _P1 Account + Verification_ — Sign in and land on Vendor Home (Overview) — no first-run wizard → layout gate redirects to /dashboard unless hasVendor; page loads profile + 6 stat fetches; shows 'Verification pending' amber chip, '0% commission', 'Finish your profile' nudge, and 'business name hidden in browse' banner (hybrid-anonymity, name_revealed_at NULL)  (/vendor-dashboard)
✅ **3.** _P1 Account + Verification_ — Fill the single flat profile form (business name required, logo, services, event types, ceremony/venue compatibility, location, contact) and Save → saveVendorProfile UPDATEs the pre-provisioned row by user_id (never inserts); writes services[] (the Explore discoverability tag set), logo_url, slug (Pro-gated), etc.; best-effort Nominatim geocode; 'Profile saved.' flash. Logo is advisory/completion-bar driven, not hard-enforced  (/vendor-dashboard/profile)
✅ **4.** _P1 Account + Verification_ — Pick application type (Initial — FREE recommended) and click 'Start application' → ensureDraftApplication idempotently returns/creates one vendor_verification_applications draft (status='draft', doc_uploads={}); 12-document checklist + status card render  (/vendor-dashboard/verify)
✅ **5.** _P1 Account + Verification_ — Upload the 8 vendor-fillable doc slots (DTI, BIR 2303, Mayor's permit, bank proof, portfolio, client refs, live selfie, social URL); 4 slots are admin/external-run → updateDocUpload merges each slot into doc_uploads JSONB; files land in secure 'vendor-verification' R2 bucket; per-slot card flips complete; '{n} of 12 items complete' banner advances  (/vendor-dashboard/verify)
⚠️ **6.** _P1 Account + Verification_ — Once ≥8 of 12 slots complete, the 'Submit for review' button enables; click submit → submitApplication re-checks completeCount>=8 server-side (REQUIRED_TO_SUBMIT=8, verified both UI page.tsx:536-537 and actions.ts:245-246), sets status='pending_review', stamps sla_due_at=+5 business days, bumps verification_state='pending_review', writes admin_audit_log. STALE COPY: page still advertises '100 complimentary tokens on approval' (verify/page.tsx:39/149/152/635) though the grant was retired 2026-06-17  (/vendor-dashboard/verify)
⚠️ **7.** _P1 Account + Verification_ — Setnayan admin reviews the queue and clicks Approve (also runs the 4 external slots: Persona ID, AMLC, Google Meet, OTP) → decideVerification (service-role) sets application.status='approved', vendor_profiles.verification_state='verified', public_visibility='verified', last_verified_at/next_renewal_due_at, vendor_tier_history + admin_audit_log rows. NO token grant fires — migration 20270110320020 dropped both vendor_verified_bonus_trigger triggers (confirmed). ApprovedCard still falsely claims '100 tokens loaded'  (/admin/verify)
✅ **8.** _P1 Account + Verification_ — Verified vendor returns to dashboard; published profile is now live + verified → Home chip flips amber→green 'Verified vendor'; profile unlocks verified badge, Pro/Enterprise subs, Boosted/Sponsored ads; appears in marketplace only when is_published=TRUE; pre-verify published profile shows 'Coming soon' badge  (/vendor-dashboard (+ marketplace))
✅ **9.** _P2 Build services + discoverability_ — Open Profile, use ServicesPicker to tick categories (max MAX_SERVICES), save business name/slug/city → saveVendorProfile writes vendor_profiles.services[] (the discoverability tag set Explore filters on via .contains); slug gated to Pro. This is a SEPARATE surface from the priced services builder and is NOT auto-synced with it  (/vendor-dashboard/profile)
✅ **10.** _P2 Build services + discoverability_ — Click a category in the 'All categories' rail, fill title/starting price/crew/capacity + the required Setnayan Exclusive perk, submit 'Add service' → createVendorService INSERTs a vendor_services row as is_active=FALSE (draft); enforces tier caps server-side (servicesPerLeaf + parentCategories, founder bypass); new listing shows as a 'Hidden' card  (/vendor-dashboard/services?add=<category>)
⚠️ **11.** _P2 Build services + discoverability_ — On a service card edit price/perk ('Save changes'), separately tick linked categories ('Save links'), separately add a time slot ('Add slot', Enterprise only) → THREE distinct server actions on THREE separate forms per card (updateVendorService / setServiceLinks / addServiceTimeSlot); each is its own submit + round-trip with no unified 'Save service', so links/slots are easy to leave unsaved (UX friction, not a data bug)  (/vendor-dashboard/services)
✅ **12.** _P2 Build services + discoverability_ — Click the eye icon on a draft card to publish it → toggleVendorServiceActive GATES publish on a non-blank exclusive_perk_text (else error); on success sets is_active=TRUE so the service feeds schedule pools + the public profile's bookable listings  (/vendor-dashboard/services)
✅ **13.** _P2 Build services + discoverability_ — Open Calendar; optionally set per-pool capacity, block dates, import an outside client, or merge/split shared-team categories → No explicit 'create pool' — fetchVendorPools auto-materializes pools from active services via resolve_schedule_pool RPC (bootstraps vendor_schedule_pools + _categories). A category with only a profile tag and no ACTIVE service gets no pool: discoverable in Explore but un-bookable  (/vendor-dashboard/calendar)
✅ **14.** _P2 Build services + discoverability_ — Couple opens Explore and filters by category (vendor-side payoff, no vendor action) → Explore reads the vendor_market_stats view filtered to public_visibility ∈ {verified, coming_soon} + non-empty business_name, narrowing via .contains('services',[category]) against vendor_profiles.services[] (NOT vendor_services). Discoverability needs BOTH the visibility gate AND a matching services[] tag; FREE-tier search-gate is flag-dark/default-OFF  (/explore?category=<leaf>)
✅ **15.** _P2 Build services + discoverability_ — Couple taps the card to the public profile; for verified vendors the inquiry composer renders → v/[slug] looks up by business_slug; notFound unless coming_soon/verified; renders priced active listings + 'comes with' chips; only public_visibility='verified' (admin-set) is bookable (isBookable); inquiry snapshots live pax onto chat_threads.pax_at_inquiry  (/v/[slug])
✅ **16.** _P3 Inquiry → accept → book_ — Vendor receives + taps the inquiry notification → Couple's first message fires emitNotification 'vendor_inquiry_received' (priorMessageCount===0). chat_threads exists with inquiry_status='pending'; no event_vendors/unlock write yet. Couple's personal name/email stay masked; only event display_name + date show; returning client enriched via findPriorLockedEventName  ((push/email/in-app) → /vendor-dashboard/notifications)
✅ **17.** _P3 Inquiry → accept → book_ — Vendor opens Messages to triage pending inquiries → fetchVendorThreads lists one thread per couple; pending shows mulberry 'New inquiry · accept to reply' pill; returning clients get a terracotta pill + 'accepting costs just 1 token' hint — the ONLY pre-tap token-cost disclosure anywhere  (/vendor-dashboard/messages)
✅ **18.** _P3 Inquiry → accept → book_ — Vendor opens the pending thread; reads inquiry but cannot reply (composer hidden); sees Accept/Decline → Thread must match own vendor_profile_id; couple messages SSR-rendered; live pax recomputed (resolveLivePax); vendor's chat_messages insert is accept-gated server-side ('Accept the inquiry first to reply'). The 'Accept inquiry' button shows NO token price pre-tap  (/vendor-dashboard/messages/[threadId])
✅ **19.** _P3 Inquiry → accept → book_ — Vendor taps 'Accept inquiry' → RPC unlock_vendor_event (atomic + idempotent, ONE unlock per vendor+event covering all services) burns region-banded ₱100/200/300 = 1/2/3 tokens (returning customer = flat 1); writes vendor_event_unlocks + inquiry_status='accepted'; reveal_vendor_name_on_thread_accept stamps name_revealed_at; revealExclusivePerks posts system messages; notifies couple. Insufficient balance → tx rolls back, no phantom unlock. Token cost is realized HERE, post-tap  (/vendor-dashboard/messages/[threadId] → acceptInquiry)
✅ **20.** _P3 Inquiry → accept → book_ — Vendor and couple chat, quote, negotiate; vendor may cross-sell or confirm a pax surcharge → sendChatMessage inserts chat_messages, stamps vendor_first_reply_at on first reply; offerServiceInterest records thread_service_interests; acceptPaxSurcharge/declinePaxSurcharge confirm guest-count price deltas; realtime takes over after SSR batch  (/vendor-dashboard/messages/[threadId])
✅ **21.** _P3 Inquiry → accept → book_ — COUPLE locks/contracts the vendor and records the downpayment (couple action; vendor is notified) → finalizeVendor lock UPDATE writes ONLY {status, updated_at} (verified); 'contracted' = commitment, no pool consumed; at DOWNPAID (deposit_paid+) acquireSchedulePools runs acquire_schedule_pools RPC FIRST (all-or-nothing; a 'full' date blocks the deposit flip), inserting vendor_schedule_pool_bookings; then emitNotification 'booking_confirmed' to the vendor (fail-soft)  ((couple) /dashboard/[eventId]/vendors → finalizeVendor)
✅ **22.** _P3 Inquiry → accept → book_ — Vendor opens Clients / Calendar to see the confirmed booking → fetchVendorPoolBookings reads vendor_schedule_pool_bookings WHERE released_at IS NULL; Clients groups booked events under 'Booked via Setnayan' (vs 'In conversation' accepted-not-booked + 'Outside clients'); Calendar increments per-day booked counts toward capacity. booking_confirmed exists because event_vendors is couple-RLS-only — the vendor has no read path to it  (/vendor-dashboard/clients + /vendor-dashboard/calendar)
✅ **23.** _P4 Deliver → mark complete → close_ — Vendor opens Clients and clicks 'Event brief' on a 'Booked via Setnayan' row → Read-only; fetchVendorPoolBookings groups live reservations by event; three buckets render (Booked / In conversation / Outside clients)  (/vendor-dashboard/clients)
✅ **24.** _P4 Deliver → mark complete → close_ — Vendor delivers off-platform, then reviews the live Event Brief (headcount/palette/monogram/timeline/seat plan) → SECURITY DEFINER RPC get_vendor_event_brief enforces booked-gate, returns aggregates only (never guest rows); completion card resolves to not-marked / vendor_marked / confirmed / disputed from the admin read of event_vendors completion columns  (/vendor-dashboard/clients/[eventId])
✅ **25.** _P4 Deliver → mark complete → close_ — Vendor clicks 'Mark service complete' (handshake step 1) → event_vendors.service_marked_complete_at=now(), completion_status='vendor_marked' (admin client, ownership-gated, idempotent); best-effort 'review_request' notification to the couple; card flips to 'Waiting on the couple… (auto-confirms after 7 days)'  (/vendor-dashboard/clients/[eventId] → vendorMarkServiceComplete)
✅ **26.** _P4 Deliver → mark complete → close_ — Couple opens the review page from the notification; sees the ConfirmReceiptState handshake gate → reviewState() resolves 'awaiting_confirm' (<7d since mark, no confirm, not disputed), mirroring the review-gate RLS in migration 20270101000000; shows [Yes, I got everything] / [Something's missing] + 'auto-confirms after 7 days' footnote  ((couple) /dashboard/[eventId]/vendors/[vendorId]/review)
✅ **27.** _P4 Deliver → mark complete → close_ — Couple clicks 'Yes, I got everything' (handshake step 2 — closes the transaction) → event_vendors.customer_confirmed_received_at=now(), completion_status='confirmed' (idempotent, gated to couple membership, only after mark); vendor brief reads 'Service complete. The couple confirmed…'; review form unlocks. Cron-free safety valves: M=7d couple auto-confirm + N=30d post-event auto-complete, evaluated read-side  ((couple) /dashboard/[eventId]/vendors/[vendorId]/review → coupleConfirmReceived)
✅ **28.** _P4 Deliver → mark complete → close_ — Couple submits the public review (5 axes + optional 500-char body); vendor is notified → RLS-gated INSERT into vendor_reviews (vendor_reviews_couple_insert re-enforces the SAME handshake gate, correlated to marketplace_vendor_id; dispute freezes); 'review_received' notification to vendor; vendor can post a one-time public reply at /vendor-dashboard/reviews; self-review block gates abusers  ((couple) /dashboard/[eventId]/vendors/[vendorId]/review → submitCoupleReview)
✅ **29.** _P4 Deliver → mark complete → close_ — (Dispute branch) Couple clicks 'Something's missing' instead of confirming → completion_status='disputed', completion_disputed_at=now(); review-gate RLS HARD-FREEZES the review; vendor brief shows 'The couple reported a problem'. Admin resolves at /admin/completions (forceCompleteVendor unfreezes → 'confirmed'; upholdNonDelivery stays disputed + stamps completion_resolved_at). Transaction does NOT close until resolved  ((couple) review → coupleReportNonDelivery)
✅ **30.** _P4 Deliver → mark complete → close_ — Vendor reviews the booking ledger + standing off-platform money disclosure → Read-only; fetchVendorEarnings rolls up matched orders (YTD/monthly/per-booking). Money close is record-only — Setnayan Pay rails + 3-stage payout RETIRED 2026-05-28; vendors keep 100%, settle directly off-platform; disclosure states '0% / never touches money' + vigilance caution  (/vendor-dashboard/earnings + /vendor-dashboard/payment-options)
⛔ **31.** _P5 Editorial input (From Your Vendors)_ — Vendor opens a booked event brief and looks for the 'From your vendors' entry tile to the editorial-media studio → findRecommendedEventVendorId reads event_vendors WHERE selection_match_rank=1. Verified: that column is NULLABLE-no-DEFAULT and NEVER written (finalizeVendor's lock + revert UPDATEs write only {status, updated_at}; whole-repo grep finds only comments/readers — data.ts:721-723, editorial-vendor-media.ts:38). Lookup returns null → entry tile never renders  (/vendor-dashboard/clients/[eventId])
⛔ **32.** _P5 Editorial input (From Your Vendors)_ — Vendor opens the studio URL directly; the eligibility gate runs before any upload UI → findRecommendedEventVendorId returns null → EditorialMediaStudio not mounted; fallback 'Not available for this event yet' card renders with no upload controls  (/vendor-dashboard/clients/[eventId]/editorial-media)
⛔ **33.** _P5 Editorial input (From Your Vendors)_ — (Unreachable) Stage up to 3 photos + 3 clips (clips trimmed ≤5s, baked to boomerang in-browser), each POSTed to R2 → Never reached — step 32 blocks mounting. Would be client-side staging only, no DB write yet  (/vendor-dashboard/clients/[eventId]/editorial-media (studio))
⛔ **34.** _P5 Editorial input (From Your Vendors)_ — (Unreachable) Click 'Submit N to their editorial' → Action re-runs findRecommendedEventVendorId as a hard gate → returns null on a real DB → {ok:false}, inserts nothing, error 'Only the couple's recommended vendor for a category can add editorial media'  (server action submitVendorEditorialMedia)
⛔ **35.** _P5 Editorial input (From Your Vendors)_ — (Automatic, never fires) NSFW screen on each inserted row → Would set editorial_vendor_media.moderation_state clean/blocked (public fails closed to 'clean'). Never reached — no insert ever happens  (after() → screenEditorialVendorMedia)
⛔ **36.** _P5 Editorial input (From Your Vendors)_ — Anyone views the couple's published editorial 'From Your Vendors' strip → data.ts reads editorial_vendor_media WHERE moderation_state='clean' AND hidden_by_couple=false, then a LIVE selection_match_rank=1 re-check. Empty for TWO independent reasons: (a) no rows ever inserted; (b) even a hypothetical row is dropped by the rank re-check. Block never appears  (/[slug])

**The ending:** A vendor reaches the true end only at the per-vendor two-party completion handshake closing the transaction — NOT a payout (Setnayan Pay rails + 3-stage payout retired 2026-05-28; vendors keep 100%, money settles off-platform). The exact gate: vendor delivers off-platform → vendor 'Mark service complete' (event_vendors.service_marked_complete_at=now(), completion_status='vendor_marked') → couple 'Yes, I got everything' (customer_confirmed_received_at=now(), completion_status='confirmed') → review unlocks (RLS-gated). Two cron-free read-side safety valves auto-close a silent couple: M=7d auto-confirm after the mark, N=30d post-event auto-complete. An open dispute ('Something's missing' → completion_status='disputed') hard-freezes the close+review until an admin resolves at /admin/completions. The FINAL state — editorial media contributed to the couple's 'From Your Vendors' editorial block — is NEVER reachable for any vendor: the entire Phase-5 path is gated four times on event_vendors.selection_match_rank=1, a NULLABLE-no-DEFAULT column whose COMMENT claims it is 'Written by finalizeVendor' but which has ZERO write path anywhere in the repo (every occurrence is a comment, the column definition, or a reader). So the documented ending (transaction closed AND editorial media contributed) is only HALF reachable: transaction-close works end-to-end; editorial contribution is dead code.

**Breaks blocking the end-to-end flow:**
- PHASE 5 IS DEAD CODE — editorial contribution never activates for ANY vendor. event_vendors.selection_match_rank gates the entire 'From Your Vendors' path four times (entry tile clients/[eventId]/page.tsx:321; studio eligibility editorial-media/page.tsx → findRecommendedEventVendorId; submitVendorEditorialMedia action gate; public reader data.ts:723). The column is NULLABLE with NO DEFAULT (migration 20260912000000) and its COMMENT claims 'Written by finalizeVendor' — but finalizeVendor's lock + revert + archive UPDATEs write only {status, updated_at} (verified at actions.ts:285/lock block). Whole-repo grep: zero .insert/.update/.upsert/object-literal/trigger/RPC ever sets it. Every event_vendors row is selection_match_rank=NULL → findRecommendedEventVendorId always returns null → no vendor can see the entry, reach the studio, submit media, or have anything surface. FIX: wire finalizeVendor to set selection_match_rank=1 on the locked #1 leaf-match pick (per the column's documented intent; needs owner sign-off). Same NULL column also starves the M2 'first-pick rate' Editorial metric (migration 20260912000000:77).
- STALE TOKEN COPY (does not block reaching the end, but is a confirmed shipped-copy lie at the verification milestone). The verify page still advertises '100 complimentary tokens on approval' in four places (verify/page.tsx:39 metadata, :149/:152 ApprovedCard, :635) and lib/help.ts frames it as a founder bonus, but migration 20270110320020_remove_verification_token_grant.sql (owner directive 2026-06-17) dropped both vendor_verified_bonus_trigger + _insert triggers, and the approval action grants no tokens. A newly-approved vendor is told 100 tokens were loaded into their wallet when zero were — and since accepting the first inquiry burns 1-3 tokens (unlock_vendor_event), this directly mis-sets expectations for the very next paid step in Phase 3.

_Note: Assembled and re-verified against worktree /Users/icecasasola/Setnayan/.claude/worktrees/sn-audit-main at HEAD 09ec9601b4f13afa24481d68b2ede029a91e7a6b (read-only, no edits). The 36-step flow spans five phases: P1 account+verification (no first-run wizard — vendor_profiles auto-provisioned by the on_users_vendor_created trigger; saveVendorProfile only ever UPDATEs; 8-of-12 submit floor enforced both UI verify/page.tsx:536 AND server verify/actions.ts:245), P2 build-services+discoverability (two un-synced category surfaces: vendor_profiles.services[] tag drives Explore .contains, vendor_services rows drive /v/[slug] + auto-materialized schedule pools; categories hardcoded in lib/vendors.ts VENDOR_CATEGORIES — violates the 'categories=taxonomy-DB-driven' memory lock; 3-forms-3-saves UX friction per service card), P3 inquiry→accept→book (token cost ₱100/200/300 burns silently inside acceptInquiry/unlock_vendor_event AFTER the tap — no pre-tap price on the Accept button except the returning-client '1 token' hint; 'accepted' ≠ 'booked'; pool slot acquired only at deposit_paid+; booking_confirmed notification is the vendor's only signal because event_vendors is couple-RLS-only), P4 deliver→close (two-party handshake, cron-free M=7d/N=30d auto-paths, dispute hard-freeze, admin resolution, record-only earnings ledger + off-platform disclosure), P5 editorial input (BROKEN — dead code). Verified this session: (1) selection_match_rank has no write path — grep returned only comments/column-def/readers (data.ts:516/721/723, editorial-vendor-media.ts:24/38); (2) finalizeVendor lock UPDATE writes only {status, updated_at}; (3) token-grant triggers dropped by migration 20270110320020; (4) stale '100 tokens' copy still live at verify/page.tsx:39/149/152/635; (5) REQUIRED_TO_SUBMIT=8 in both UI and server. Net: the transaction-close half of theEnding works end-to-end; the editorial-contribution half is unreachable for every vendor — flag both the dead Phase-5 feature and the stale token copy for owner sign-off (the selection_match_rank wiring is a one-line, owner-gated fix)._
