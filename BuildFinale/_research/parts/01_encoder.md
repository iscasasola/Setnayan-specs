## WORKSTREAM: Desktop encoder (the S-series: couples go live from the Setnayan desktop app instead of OBS)

Plan of record: `build-sessions/encoder/README.md` in the code repo (origin/main @ 9663550cb, corrected 2026-09-08/09). It is current. The E0–E9 prompts in the same folder and `encoder-plan.html` are the RETIRED E-series plan (it still talks about `decideWatermark` / `ProgramFrame.overlay`, which were retired), so do not act on them. Merged: S0–S12, S14, S18, S19, W1 (PRs in the SUPERSEDED section). S13 merged only as a pre-flight check. S17 was partly run. S15 and S16 are gated on the owner. The README says "What is left is one publish, two measurement runs and the physical rehearsal." Code reading turned up two more gaps (rows 1–2) that no register lists.

| Item | What a person would get (plain English) | State | Evidence | What's needed to finish | Owner question (if any) + choices the docs already list |
|---|---|---|---|---|---|
| Own-channel (the DEFAULT tier) has no ingest address, so the desktop app cannot start a broadcast | A couple streaming to their own YouTube channel pastes their key into the desktop app, presses go live, and the encoder refuses ("no_stream_key"). Only the paid hosted-channel add-on can reach YouTube from the app. | PARTLY BUILT | Found by reading the code, not by running it. `src-tauri/src/stream_key.rs:197` stores `rtmps_url: String::new()` for a pasted key. `:147` `RtmpEndpoint::parse(&held.rtmps_url,…).ok()?`, and `crates/encoder/src/rtmp.rs:88-94` rejects "" (UnsupportedScheme), so `destinations()` returns None and `encoder_ipc.rs:311` returns `no_stream_key`. `lib/desktop-stream-key.ts:32-35` sends the key only. `encoder-key-panel.tsx:143-150` has one input (the key) and no server field. `git grep -n "youtube.com/live2" origin/main -- src-tauri/src apps/web/lib apps/web/app` finds only redaction-test strings (stream_key.rs:60-61, 281-284) and no default ingest. Also: both key-panel mounts sit inside active-broadcast branches (`panood/control/[eventId]/page.tsx:1659`, `go-live-card.tsx` `active` block), so a manual-watch-link couple with no `panood_broadcasts` row never sees the paste field. | Give Rust an ingest URL for pasted keys. Options: YouTube's standard RTMPS primary as the default, an OBS-style "Server" field, or, for an OAuth-connected own channel, the server-stored `ingestion_url` via the existing claim/exchange. Also render the paste field when the couple is on the manual route. Add a guard that a pasted key yields non-None `destinations()`. | None. This is engineering. |
| Hosted channel publishes to the plain-RTMP address, and there is never a backup ingest | The hosted-channel couple's stream goes out on port 1935 (plain RTMP), which venue and hotel firewalls often block, not TLS on 443. If the primary YouTube ingest drops, the app retries the primary only and never switches to YouTube's backup. | PARTLY BUILT | `createYoutubeStream` returns the RTMPS pair (`panood-youtube.ts:316,445`). But persistence stores only `stream.ingestionAddress` (`studio/panood/setup/actions.ts:327`, `live-studio-roam-provision.ts:874`). The exchange hands that value to Rust as `rtmps_url` (`live-studio-encoder-claims.ts:118-124`) with `rtmpsBackupUrl: null` ("not persisted anywhere yet", :123; docblock :30-35). Rust accepts `rtmp://` (`rtmp.rs:90`) and keeps `rtmps_backup_url` as `#[allow(dead_code)]` (`stream_key.rs:181`). `git grep -n -i "backup_ingest\|rtmps_ingest\|backup_url" origin/main -- supabase/migrations` shows comments only and no column. S7's backup alternation is proven (memory lead) but gets no data. | Add columns for the RTMPS primary and backup to `panood_broadcasts` and `live_studio_roam_streams` (with column grants, per house rules), persist both, return them from the exchange, store the backup on `HeldStreamKey`, and pass `Destinations` with a backup. | None. |
| S12 auto-updater | A couple would get Rust-encoder fixes without reinstalling, and never mid-broadcast (the check runs at launch and again after a broadcast stops). | BUILT-NOT-LIVE | Merged in PR #5252. `src-tauri/src/lib.rs:31,45,55`; `updater.rs:60` pins host `pub-37d64fe…r2.dev`; `tauri.conf.json:57-59` endpoint; `encoder_ipc.rs:529` `recheck_after_stop`. The updater is driven from Rust, so no capability entry is needed. No build containing it has ever been produced: the last successful `build-desktop` is run 34016548173 at 2026-09-06T06:27Z, before S12 merged at 06:55Z (`gh run list --workflow build-desktop.yml`, 2026-09-11). No `latest.json` has been published (R2 secrets absent, see the X0 table). | S15 (below), then S13 step 9: bump to a test version on R2 and confirm the app does not restart mid-stream. | None. |
| S15: the first real publish, so `/download` stops saying "no build" | Anyone could download and install the Mac app from setnayan.com. Today nobody can: both download routes answer 503. | BLOCKED ON OWNER | `build-sessions/encoder/S15.md` step 0. On 2026-09-11 `gh secret list -R iscasasola/setnayan-platform` shows zero `R2_*` secrets. X0-TRACKER item 6 measured the 503s and the R2 `desktop/` 404 on 2026-09-08. `build-desktop.yml:37` is `workflow_dispatch` only (no push trigger), so every release needs a manual dispatch. `download/page.tsx:263` still says "This build isn't Apple-notarized yet", which becomes false once a notarized build is served. | Owner sets the 4 GitHub Actions secrets plus `R2_PUBLIC_URL` in Vercel. Then dispatch, verify the manifest by object (non-empty signature per platform, artefact URLs return 200, version matches), and change the notarized copy. Est. 0.5 day. | Owner action, not a question (see OPEN OWNER QUESTIONS). |
| S16: the real 20-minute YouTube publish, and the measured grace window | Proof that a broadcast from the app actually appears on YouTube over TLS and survives a network drop. Until then, no frame has reached YouTube. | BLOCKED ON OWNER | `S16.md` ("NEEDS OWNER ACTION 3 — one live YouTube stream key"). `crates/encoder/src/reconnect.rs:65` `DEFAULT_GRACE = 120 s` is still a guess. `examples/publish_probe.rs` exists. S6, S7 and S7-acceptance all ran against a local ffmpeg ingest. DECISION_LOG 2026-09-09 row: "No frame has reached YouTube". | Owner provides one stream key (unlisted, ideally a secondary channel). Run publish_probe for 1200 s realtime, kill the network twice to time the grace window, replace the constant, test extended timestamps (`--start-ms 16770000`), and play the `.flv` from `--record` in VLC. Est. 1 day. Parallel-safe. | Owner action: one stream key. |
| S13: the physical end-to-end rehearsal (the acceptance run) | Someone runs a whole wedding the way a couple would, on real machines with phones as cameras, and writes down what happens. This produces the couples' rehearsal script. | BLOCKED ON DEVICE | S13.md SETUP needs a clean Apple-silicon Mac, a clean Windows 11 laptop, a macOS 13/Intel machine expected to fail, two phone cameras and a guest phone. It also needs a network kill and lid close on the broadcasting Mac, so it cannot run from the coding session on the owner's only MacBook (S13-PREFLIGHT.md). `git ls-tree -r origin/main build-sessions/encoder \| grep -i report` finds no `S13-REPORT.md`. Also depends on S15 (install from /download) and S16. | Once S15 and S16 are done: a human operator on a machine that is not the coding Mac (or the coding session on another machine), with scope cut to the hardware that exists. Hardware conflict to settle: S13-PREFLIGHT says there is no Windows machine, but S17-FINDING §4 says the owner has a Windows laptop. Only one of own-channel or hosted can be run per Mac. Est. 2–3 days. | See the hardware question in OPEN OWNER QUESTIONS. |
| S17: visible-window 60-minute thermal and throughput run | Evidence that a Mac can encode a long wedding with the window on screen without overheating or dropping frames. | BLOCKED ON OWNER | `S17-FINDING.md` §0 and §1: the run went a full hour but the window was hidden (358 of 359 windows reported `hidden`), so it does not answer the question. "Owner instruction (2026-09-09): defer that run." Needs a human at the keyboard for the hour. | The owner sits at the Mac for 60 minutes (load ≤ 5) and runs the exact resume command in S17-FINDING §1. | When will the owner run it? (scheduling) |
| S17: the Windows leg (IPC plus encode on WebView2) | Evidence that the Windows app works at all. Every encode and transport measurement so far is macOS-only. | BLOCKED ON OWNER | S17-FINDING §4: "Deferred by owner instruction (2026-09-09)". `git ls-tree origin/main src-tauri/probe/` shows detach.py, encoder-probe.js, local-shell and run.sh, with no Windows runner (`run.ps1`). | Write `src-tauri/probe/run.ps1` (PowerShell sampler for `msedgewebview2.exe`), then the owner runs it on the Windows laptop. | When will the owner run it? |
| WebContent memory growth 439→910 MB is unattributed | Knowing whether a six-hour wedding will run the laptop out of memory. | NOT STARTED | S17-FINDING §2: "The 439→910 MB attribution question from S0 remains open". The `run.sh` sampler sorts by CPU and loses zero-CPU processes, which is a known harness defect left unfixed in both S0 and S17. | Fix `run.sh` to sample by PID (or attach `leaks`/Instruments), then re-run. | None. |
| Minimised or hidden window degrades the broadcast | If a couple minimises the app, their stream may drop to about 12 fps or be suspended. | UNVERIFIED | S0-FINDING §4.1: a hidden page halves the pipeline within about 3 s, and WebKit suspended it after about 8 min. S17-FINDING §1: a hidden hour with no suspension (window pinned on top), but fps dips to 12.01. Nobody has measured a real minimise with the production app. | Covered by S13 step 3 and the S17 visible run. The mitigation (S10 keep-awake assertion) exists but its effect on hidden throttling is unmeasured. | None. |
| OS matrix cells beyond the one Mac | The /download floor ("macOS 14 or later, Safari 26 … Windows 10/11") would be tested, not assumed. | BLOCKED ON DEVICE | S0-FINDING §2.3 and §9 item 1 are LEFT UNDONE. S17-FINDING §3: "The floor … stays assumed for every macOS version except 26.6.2 arm64." | macOS 14/15 machines plus a macOS 13 or Intel machine expected to fail at the readiness gate. | None (the owner has 1 MacBook). |
| S4 average-bitrate figure | Confirmation that the stream runs at the planned 2.5 Mbps. | UNVERIFIED | `changelog.d/encoder-s4-video-encode.md:84-89`: measured about 1.79 Mbps under heavy load and "left open". The S17 hidden hour logged 2340–2371 kbps at 30 fps (inside ±10% of 2.5 Mbps), but nobody recorded the item as closed. | Formally close it using S17's figures, or re-run the S4 harness at load ≤ 5. | None. |

## WORKSTREAM: Encoder owner and infrastructure checklist (X0) — Apple notarization, Windows code-signing, R2 release secrets, prod flags

Register: `build-sessions/encoder/X0-TRACKER.md` (current, last touched 2026-09-10, commit 2bfb779db), with checklist `X0.md`. Items 1, 2, 7 and 8 are closed and listed under SUPERSEDED. Item 5 is also closed, but its own row is stale. The open items are below.

| Item | What a person would get (plain English) | State | Evidence | What's needed to finish | Owner question (if any) + choices the docs already list |
|---|---|---|---|---|---|
| X0 #6: R2 release secrets (5 values in 2 stores) | The desktop app becomes downloadable and auto-updatable. This is the single blocker for S15, S12-live and S13. | BLOCKED ON OWNER | X0-TRACKER.md:14: "still open — RE-VERIFIED 2026-09-08 by `gh secret list`: none of the four exists". Re-checked 2026-09-11: `gh secret list` shows zero `R2_*` (only APPLE_* and TAURI_SIGNING_*). DECISION_LOG 2026-09-09 row: "Owner action, ~10 minutes". Whether `R2_PUBLIC_URL` is set in Vercel is UNVERIFIED today (last measured unset 2026-09-08; this sweep may not read Vercel). | Owner sets `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_PUBLIC_URL` in GitHub Actions, plus `R2_PUBLIC_URL` in Vercel. | Owner action. |
| X0 #3: Windows OV code-signing certificate plus cloud signer | Windows couples install without "Windows protected your PC" (SmartScreen) in their wedding week. The Windows auto-update is signed too. | BLOCKED ON OWNER | X0-TRACKER.md:11: "⏳ open — order now (CI gate ready, waiting on the cert)". The CI gate exists (`build-desktop.yml:151-161` switches signing on when `SSL_COM_ESIGNER_*` exist). On 2026-09-11 `gh secret list` shows no `SSL_COM_*`. Windows builds ship unsigned, and the Windows updater signature is written empty (`build-desktop.yml:365`), so Windows gets no auto-update. The docs disagree on status: S15.md:194 says "deferred by owner ruling 2026-09-06", while the memory launch plan says "order 2026-10-12". No DECISION_LOG row. | Owner orders the cert (3–4 week validation) and adds the 4 `SSL_COM_ESIGNER_*` secrets. No code change needed. | Yes. See OPEN OWNER QUESTIONS (route plus whether and when to order). |
| X0 #4: prod value of `NEXT_PUBLIC_LIVE_STUDIO_POOL_ONLY` (PANOOD_STREAMING is recorded) | Certainty about whether couples can still connect their own YouTube channel, or broadcasts run only on Setnayan's channel | BLOCKED ON OWNER | ⚠ Corrected by the orchestrator of this sweep: `NEXT_PUBLIC_PANOOD_STREAMING_ENABLED` and `NEXT_PUBLIC_LIVE_STUDIO_ROAM_ENABLED` were both recorded `true` in prod on 2026-09-06 (`/Users/icecasasola/Documents/Claude/Projects/Setnayan/LAUNCH_CHECKLIST_2026-09-06.md:91`, "This closes S13 pre-flight blocker #4"); X0-TRACKER.md:12 is stale on that. Only POOL_ONLY is open: P0-b register measured it unset (2026-08-30) while DECISION_LOG 2026-09-02 (l.3700) says it "stays ON" with no measurement. Reader `lib/live-studio-pool-only.ts:72`; its docblock (:30-44) says do not flip until a Setnayan channel is live (owner gate G1). Full detail in the Production flags table. | Someone reads Vercel and records POOL_ONLY; owner confirms whether the Setnayan YouTube channel (G1) exists | Record the value; flip only after G1 (Launch Plan §6 default: pool-only ON once the pool can broadcast) |

## WORKSTREAM: Live Studio owner decisions outside the encoder

Sources: corpus `Live_Studio_Unified_Spec_2026-07-25.md` and `Live_Studio_Cast_and_Roam_2026-07-23.md`, read only for still-open owner questions, plus the DECISION_LOG Live Studio rows. Most open markers in the spec are already ruled or built (see SUPERSEDED). One genuine open question remains.

| Item | What a person would get (plain English) | State | Evidence | What's needed to finish | Owner question (if any) + choices the docs already list |
|---|---|---|---|---|---|
| Pool channel: reuse it, or retire it after one wedding? | A couple on a Setnayan-supplied channel cannot lose their archived film to another couple's copyright strike. | BLOCKED ON OWNER | DECISION_LOG row 2026-09-03 (line 3713): "OPEN — OWNER DECISION, NOT MADE IN CODE". Today the risk is only stated, via `POOL_CHANNEL_SHARED_STRIKE_NOTICE` (`lib/live-studio-pool-only.ts`), PR #5136. No later ruling (grepped "strike", "retire.*channel", "one-couple-per-channel" after line 3713). | An owner ruling, then either the current reuse or a retire-after-event flow in `live-studio-roam-provision.ts` and the admin channel board. | Yes, see below. |

## OPEN OWNER QUESTIONS

- **Windows code-signing: is the certificate being ordered, and through whom?** The docs' own words conflict. X0-TRACKER item 3 says "⏳ open — order now (CI gate ready, waiting on the cert)". S15 says "Windows is expected to be empty until the owner buys the OV certificate (X0 item 3, deferred by owner ruling 2026-09-06)". live-studio-system.html says "Decide: Windows code signing — A purchase and a few days. Without it, every Windows couple sees 'Windows protected your PC'". Choices listed: SSL.com OV (~$129/yr) plus eSigner, the default (Launch Plan §6); Certum (SimplySign); keep shipping unsigned with the "More info → Run anyway" copy. Azure Trusted/Artifact Signing is excluded because it does not serve PH organisations. — Source: /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/X0-TRACKER.md:11 (origin/main); /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S15.md:194; /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/live-studio-system.html:147. Checked DECISION_LOG: grepped "Windows", "OV cert", "SSL.com", "code-sign" → the only mention is the 2026-09-05 row (line 3735), which lists "Windows signing route (default SSL.com OV + eSigner)" as OPEN. No ruling found, and no 2026-09-06 deferral row exists.
- **Prod value of `NEXT_PUBLIC_LIVE_STUDIO_POOL_ONLY`** (the other two flags of this X0 item are answered: ROAM and PANOOD_STREAMING both `true`, LAUNCH_CHECKLIST_2026-09-06.md:91). "`NEXT_PUBLIC_PANOOD_STREAMING_ENABLED` and `NEXT_PUBLIC_LIVE_STUDIO_POOL_ONLY` have no anonymous surface and remain open" — Source: /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/X0-TRACKER.md:12 (origin/main; stale for PANOOD_STREAMING). Checked DECISION_LOG: 2026-09-02 l.3700 asserts POOL_ONLY "stays ON" (unmeasured) vs P0-b 2026-08-30 unset → contradiction, no measurement since.
- **Pool channel reuse vs retire:** "should a pool channel be ONE-COUPLE-PER-CHANNEL-FOREVER (retired after the wedding, never checked back in) rather than reused?" The choices are the two it names: keep reusing (the current code, risk stated to buyer and admin), or retire each channel after its wedding (a new channel per event, each with YouTube's one-time 24-hour live-activation wait). — Source: /Users/icecasasola/Documents/Claude/Projects/Setnayan/DECISION_LOG.md:3713. Checked DECISION_LOG: grepped "strike", "retire.*channel", "one-couple-per-channel" after line 3713 → only the 2026-09-03 correction row 3716 (about who lands on a shared channel), no ruling.
- **S17's two deferred runs: when?** "Still open, both by owner instruction, not by failure: the visible-state 60-minute run (needs a human physically at this Mac's keyboard …) and the entire Windows leg (owner has the laptop, will run it personally later)." This is a scheduling decision, not a design one. — Source: /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/README.md:97 (origin/main); /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S17-FINDING.md:120. Checked DECISION_LOG: grepped "S17", "visible", "thermal" → no row.
- **S13 hardware: which machines will the rehearsal actually use?** S13-PREFLIGHT: "the owner's available hardware (1 MacBook, 1 phone, 1 iPad, no separate Windows or legacy machine, no third 'guest' device) can't cover the Windows leg or the macOS-13/Intel expected-fail leg at all". S17-FINDING §4, later: "The owner has a physical Windows laptop". The rehearsal also needs the operator on a device that is not the coding Mac. Choices implied by the docs: run it with scope cut (Windows and legacy legs marked "NOT RUN — no hardware", iPad as the guest phone), or source the hardware. — Source: /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S13-PREFLIGHT.md:5-10; /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S17-FINDING.md:120-125. Checked DECISION_LOG: grepped "S13", "rehearsal" → no row.
- Owner ACTIONS on the critical path (not questions, listed so they are not lost): (1) set the 4 R2 GitHub Actions secrets plus `R2_PUBLIC_URL` in Vercel, which unblocks S15, the live updater and S13 (DECISION_LOG.md:3815: "Owner action, ~10 minutes"); (2) provide one live YouTube stream key, which unblocks S16 (S16.md:7).

## SUPERSEDED / ALREADY DONE

- E0–E9 encoder prompts: RETIRED for the S-series. Evidence: DECISION_LOG 2026-09-05 row (line 3735) "THE E0–E9 PLAN RETIRED FOR THE S-SERIES"; README.md header "The E0–E9 prompts remain on disk as history — do not act on one." `encoder-plan.html` is the E-era plan (it mentions the retired `decideWatermark` / `ProgramFrame.overlay`).
- Merged S-sessions (register lists some as open; all are on origin/main): S0 #5200 · S1 #5195 · S2 #5235 · S3 #5224 · S4 #5236 · S5 #5232 + #5239 · S6 #5213 (+ crate move #5215) · S7 #5223 · S8 #5210 · S9 #5243 · S10 #5209 · S11 #5240 · S12 #5252 · S13 pre-flight only #5250 (rehearsal still owed, in the table) · S14 #5334 (+ docs #5335) · S17 partial #5340 · S18 #5376 (d248f01d4) · S19 #5400 (27408d265) · W1 #5212.
- "`createProgramCanvas` has no call site" / "STUB SINK" / "nothing calls encoder_start" (S3, S4 and the memory note): FIXED by S18. `apps/web/app/panood/control/[eventId]/_components/desktop-encoder-host.tsx:121` calls `createProgramCanvas`, and it is mounted at `panood/control/[eventId]/page.tsx:1464`. `lib/encoder/encoder-session.ts:193` invokes `encoder_start`. The README's own caveat still applies: every hop is now wired, but no broadcast has been observed.
- "S12 updater does not exist" (S13-PREFLIGHT item 1, memory note): FALSE now. Merged in #5252 (see the table: built but not live).
- X0 #1, the Apple Program License Agreement (the notarization 403): RESOLVED 2026-09-06. Run 34016548173 notarized ("Notarizing Finished with status Accepted"); X0-TRACKER.md:9. `gh run list` confirms that run succeeded. The Apple Developer membership is paid, so it is not a cost (memory lead, owner 2026-09-05).
- X0 #2, the Apple identity: RESOLVED 2026-09-06. The owner keeps the individual identity "Indalecio Casasola"; X0-TRACKER.md:10: "Do not re-raise this".
- X0 #7, the `media.setnayan.com` updater host: FIXED by S14 #5334. `tauri.conf.json:59` and `updater.rs:60` both pin the `pub-37d64fe…r2.dev` host. The owner already ruled that `media.setnayan.com` is not being set up, so do not re-ask.
- X0 #8, the go-live guard, and the "should a failed transport probe refuse a broadcast, or only warn?" question: BUILT by S19 #5400 (`lib/encoder/go-live-guard.ts`, called from `desktop-encoder-host.tsx:7`). The rule was already owner-ruled in the DECISION_LOG 2026-09-06 row (line 3756): "probe once, record the envelope, refuse only if the transport is unusable" (latency is reported, never enforced; X0-TRACKER.md:18). ⚠ The DECISION_LOG 2026-09-09 row (line 3815) still reads "OPEN OWNER DECISION … Not decided here". It has no closing row, so the pack should not re-list it, but the log needs a one-line correction.
- X0 #5, the § 4c free-tier branding contradiction (the full-screen watermark vs "POWERED BY SETNAYAN"): RESOLVED by owner 2026-07-25 (`Live_Studio_Unified_Spec_2026-07-25.md` §4f ①: "The full-screen watermark is RETIRED … Do not re-open it"). Built in Wave 7: `lib/panood-watermark.ts:14` has a `retired` branch, and `studio/panood/broadcast/page.tsx:165` passes `retired: liveStudioRoamEnabled()`. ROAM is measured ON in prod (X0-TRACKER.md:12), so it is retired everywhere. The stale copies of the old claim are X0-TRACKER.md:13 "⏳ open", the `program-bridge.tsx:138` comment, spec lines 132–135 and 167, and memory.
- "The fork" (Path A native vs remux relay vs HLS-direct): RULED 2026-09-05. Path A was re-affirmed on corrected figures (DECISION_LOG line 3735). The memory note saying "not yet filed in DECISION_LOG" is stale. S0's Q3 HLS-CORS/WHIP spikes (S0-FINDING §5) are therefore moot.
- OS floor messaging: DONE. `apps/web/app/download/page.tsx:284-285` states "Apple-silicon Mac (M1 or newer) on macOS 14 or later, with the Safari 26 update" and "Windows 10/11 with hardware video encoding", and `tauri.conf.json:46` sets `minimumSystemVersion: "14.0"`.
- Hiding the browser key reveal on desktop (the default was "yes"): DONE in S8 #5210. `encoder-key-panel.tsx:55-60` renders the reveal only when not running in Tauri. Caveat from reading the code: the server still passes `streamKey` into this client component on desktop (`panood/control/[eventId]/page.tsx:1702`, `go-live-card.tsx:344-348`), so the key is in the page payload even though it is never shown. The component docblock's "it isn't even sent to them" is inaccurate. That is a minor follow-up, not an open owner question.
- Unified spec's "hosted channel price OPEN — reprice, fold, or retire" (spec line 26): RULED in the DECISION_LOG 2026-09-03 row ① (line 3720), "Hosted channel = ₱3,000 PER DAY", and shipped in LS8 #5143.
- Unified spec's "Event-QR overlay ⚠ OPEN: paid-only vs FREE" (spec line 79): SETTLED as FREE. `apps/web/lib/live-studio-overlays.ts:18,23`: "event QR … FREE (owner-locked)".
- Unified spec's "Cast-owner continuity / retired-SKU grandfathering" (spec lines 142, 169): BUILT. `apps/web/lib/entitlements.ts:286` has `LIVE_STUDIO: Object.freeze([...PANOOD_PAID_SKUS])`, so a Cast buyer owns Live Studio.
- The Unified spec's § 4k archive "wipe + return to pool" conflict: RULED 2026-08-31 (DECISION_LOG line 3675, "THE ARCHIVE IS NEVER WIPED").
- live-studio-system.html's "Decide: Google Workspace (Internal audience)": RULED 2026-07-27 (DECISION_LOG line 2780: "OWNER CHOSE THE INTERNAL PATH"). Whether it was executed is outside this slice and was not verified.
- LS1–LS8 Live Studio sessions: all merged. LS1 #5112 · LS2 #5110 · LS3 #5118 · LS4 #5122 · LS5 #5125 · LS6 #5134 · LS7 #5136/#5138 · LS8 #5143.

## FILES THE PACK SHOULD INCLUDE

- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/README.md — S-series plan of record; what's merged and left (read via origin/main)
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/X0-TRACKER.md — owner/infra register: secrets, cert, flags
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/X0.md — original owner checklist and CA options
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S15.md — ready prompt: first real publish, verify by object
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S16.md — ready prompt: real YouTube publish, grace window
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S13.md — ready prompt: physical end-to-end rehearsal steps
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S13-PREFLIGHT.md — why S13 can't run from coding Mac
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S17.md — sustained-load and Windows measurement brief
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S17-FINDING.md — hidden-hour data, exact resume commands
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/S0-FINDING.md — WebKit/IPC measurements, matrix cells left undone
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/build-sessions/encoder/live-studio-system.html — whole Live Studio system, end to end
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/Live_Studio_Encoder_Scope_2026-09-03.md — encoder scope, corrected relay figures (§ Corrections)
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/src-tauri/src/stream_key.rs — where own-channel ingest URL is empty
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/apps/web/lib/live-studio-encoder-claims.ts — backup ingest null; exchange shape
- /Users/icecasasola/Documents/Claude/Projects/setnayan-platform/.github/workflows/build-desktop.yml — dispatch-only build, signing and R2 gates
- /Users/icecasasola/Documents/Claude/Projects/Setnayan/Live_Studio_Unified_Spec_2026-07-25.md — Live Studio spec; §4f resolves §4c
- /Users/icecasasola/Documents/Claude/Projects/Setnayan/DECISION_LOG.md — rows 3713, 3720, 3735, 3756, 3815 (encoder/Live Studio)
