# 03 · Invite link themes · Event Hub Pro (the bundle it joins) · the cinematic reveal on the invite

Measured against `origin/main` @ `9663550cb` (2026-09-11 16:24 +0800). Repo read only through `git show` / `git grep` / `git ls-tree`. Prod was NOT queried (rules forbid it), so anything that depends on a saved row in prod is marked as such.

## WORKSTREAM: Invite link themes (Capiz · Velvet · Galeriya · Abaca · House) + the Event Hub Pro finishing touches

Plan docs: `Design_Invite_Themes_2026-09-10/00_README.md` + `01_WHAT_IS_LEFT_SESSIONS_2026-09-10.md` (5 sessions). **Current.** The owner answered all seven questions on 2026-09-11 (DECISION_LOG row 3886: 1A · 2A · 3A · 4B · 5A · 6B · 7A), so none of the build rows below waits on a question. The last invite/theme commit on main is #5410 (merged 2026-09-10 23:17). `git log origin/main --since=2026-09-09 --grep=invite|theme -i` shows nothing after it, and no remote branch for velvet, galeriya or abaca exists (`git ls-remote --heads origin | grep -iE "velvet|galeriya|abaca|invite-theme"` → none).

| Item | What a person would get (plain English) | State | Evidence | What's needed to finish | Owner question (if any) + choices the docs already list |
|---|---|---|---|---|---|
| S1a · Show Capiz working on setnayan.com | The owner opens a Capiz invite on a phone and sees the whole thing: the reveal, then all three doors in Capiz over the couple's photo with their monogram as the seal | BLOCKED ON OWNER | The code is on main: `lib/invite-themes.ts` capiz `ready: true`; `invite-skin.tsx` has `case 'capiz'`; `[slug]/invite/page.tsx:146` mounts `RevealOverlayServer`, gated by `inviteRevealPlays` (`lib/invite-reveal.ts:45`). No commit after #5410. Memory note says 0 events carry a theme. **I did not re-check prod.** | The owner picks Capiz and presses Save on Cale & Ice (Guests → Invite link). A session then screenshots the reveal + doors 01/02/03 at 375×812 and at desktop width, and fixes or writes up any defect | This is an action for the owner, not a question: "Ask the owner to pick Capiz and press Save on an event that owns Event Hub Pro" (01_WHAT_IS_LEFT:111) |
| S1b · Show a couple without Pro still gets House | A couple without Event Hub Pro who saved Capiz somehow still shows guests the plain House door | BLOCKED ON OWNER | The code gate exists: `resolveInviteTheme` returns `house` when `tier==='pro' && !ownsPro` (`lib/invite-themes.ts`). `load-invite-look.ts` reads ownership through `eventCoupleWebsiteProActive`. Not tested on prod yet | Needs a non-internal TEST event and the owner's yes in chat to set `invite_theme='capiz'` on it, fetch `/<slug>/invite`, then set it back to NULL. Internal accounts get Pro free, so they can't prove this (the is_internal trap) | Owner yes needed: "only with the owner's yes in chat: set invite_theme = 'capiz' on it" (01_WHAT_IS_LEFT:119-121) |
| S2 · Velvet (Classy) | Couples with Pro can pick Velvet: an engraved card on velvet in their colour, opening with the four-flap reveal | NOT STARTED | `lib/invite-themes.ts` velvet `ready: false`. `invite-skin.tsx` switch has only `case 'capiz'`. `git ls-tree origin/main -- 'apps/web/app/[slug]/invite/_components/themes'` → only `capiz.tsx`, `capiz.module.css`, `invite-skin.tsx`, `themes-stay-skins.test.ts` | Port `designs/velvet.html` into `velvet.tsx` + `velvet.module.css`, add one switch case, set `ready: true`, load fonts on the invite doors only, and add a test that every ready theme has a skin. No migration needed (the CHECK already allows `velvet`) | — (Q1 = A covers the Jost download) |
| S3 · Galeriya (Sophisticated), print shortened | Couples with Pro can pick Galeriya: their photo hung as an artwork. Per Q4 = B the print is shortened so Continue sits on the first phone screen | NOT STARTED | `galeriya` `ready: false` (`lib/invite-themes.ts`). No `galeriya.*` under `themes/` (ls-tree above) | Port `designs/galeriya.html`, shorten the print until Continue sits above 640 px at 375×812 (measure before and after), add the switch case, set `ready: true`, add Schibsted Grotesk | — (Q4 ruled B) |
| S4 · Abaca (Rugged) | Couples with Pro can pick Abaca: their photo on kraft, a stamped date, steps as tags on twine, with the four-flap reveal | NOT STARTED | `abaca` `ready: false`. No `abaca.*` under `themes/` | Port `designs/abaca.html`, add the switch case, set `ready: true`, add three faces. Measure page weight before and after, and check Oswald at 12 px against lint:legibility | — |
| Q1 · Five theme fonts, committed with licences | Velvet, Galeriya and Abaca show in their designed typefaces instead of fallbacks | NOT STARTED | `git ls-tree -r --name-only origin/main \| grep -iE "jost\|schibsted\|alfa.?slab\|bitter\|oswald"` → 0 hits. Bodoni Moda exists only as `apps/web/assets/cipher-fonts/bodoni-moda.ttf` (not under `app/_fonts/`). No OFL file under `app/_fonts/` | Download Jost, Schibsted Grotesk, Alfa Slab One, Bitter and Oswald from github.com/google/fonts (Q1 = A is the owner's yes). Commit them as woff2 under `app/_fonts/<face>/`, each with its OFL.txt, and load them through `next/font/local` in the theme module only. Copy Bodoni Moda into `app/_fonts/` | — (ruled 1A) |
| Q2 · The invite button in the couple's colour | On a Pro theme, the one button on the invite doors takes the couple's own button colour. It falls back to Setnayan terracotta when their colour is unreadable. House stays terracotta | NOT STARTED | `git grep -n "site_button_color\|buttonColor\|button_color" origin/main -- 'apps/web/app/[slug]/invite' apps/web/app/_components/door apps/web/lib/invite-*.ts` → 0 hits (exit 1). `load-invite-look.ts` selects only `invite_theme, std_background, monogram_text, monogram_color`. The column itself exists (`20270930244819_events_site_custom_colors.sql`) | Carry `events.site_button_color` through `load-invite-look.ts` into the DoorShell skin. Validate it as `#rrggbb`, pick white or ink text by luminance, fall back to `#C24E25` below 4.5:1. Add a line-keyed exception to `doors-are-designed.test.ts` | — (ruled 2A) |
| Q3 · Invite theme counted as Event Hub Pro's EIGHTH item | Everywhere Event Hub Pro lists what it includes, the invite theme appears as one of eight (not "seven") | PARTLY BUILT | Done: buy page BENEFITS (`studio/website-pro/page.tsx:86` "A Pro theme for your invite link…"), catalog blurb (`lib/add-ons-catalog.ts:488`), `says-what-it-includes.test.ts:144`. Still says seven: `lib/website-pro-items.ts` WEBSITE_PRO_ITEMS = 7 entries and docblock "THE SEVEN PRO ITEMS"; `lib/event-hub-pro.ts:217` `ctaLabel: 'Unlock all seven'`, and PITCH has no invite entry; `editor-shell.tsx:411` "Seven upgrades, one unlock"; `pro-panels.tsx:52` "One unlock covers all seven"; `hub-pro-offer-renders.test.ts:123` asserts "Unlock all seven" | Add the eighth item to WEBSITE_PRO_ITEMS and a PITCH headline + blurb (drafted with Fable, the owner's standing preference). Change every "seven" to "eight" in copy and tests. Update corpus `EVENT_HUB_CONTROLLER_DESIGN_2026-09-02.md` §5.3 (table counts 7). Price and SKU stay the same | — (ruled 3A) |
| Q6 · No second reveal right after arriving | A guest who just watched the reveal on invite door 01 is not shown it again when they land on the Event Hub in that visit. The Save-the-Date film still starts | NOT STARTED | `git grep -n "sessionStorage" origin/main -- apps/web \| grep -iE "reveal\|invite\|veil\|seen"` → 0. `git grep -iE "storage\|seen\|arriv"` in `reveal-overlay.tsx` / `reveal-overlay-server.tsx` / `veil-reveal.tsx` → only unrelated comments | Door 01's overlay records "seen" for that event (the plan suggests sessionStorage). The Event Hub overlay steps aside on mount when it sees the mark, but still fires the "first lift" that starts the film. Prove it on both the save-the-date and invitation stages | — (ruled 6B) |
| Q7 · Pro themes for weddings only | Only celebrations that carry the Save-the-Date film (weddings) can choose Capiz/Velvet/Galeriya/Abaca. Every other kind of event gets House | NOT STARTED | Only the REVEAL is fenced today (`lib/invite-reveal.ts:45` uses `resolveWeddingOnlyParts`). The THEME is not: `git grep -n -iE "wedding\|event_type\|resolveWeddingOnlyParts"` over `dashboard/[eventId]/guests/invite` + `lib/invite-themes.ts` → 0 hits. `setInviteTheme` (`guests/invite/actions.ts:117-138`) checks only `ready` + Pro. A Pro birthday can pick Capiz today | Fence `pickableInviteThemes`, the `setInviteTheme` refusal and `resolveInviteTheme`'s House fallback on `resolveWeddingOnlyParts(profile).save_the_date_film` | — (ruled 7A) |
| S5-1 · Links to change the colour and background | The theme picker tells the couple where to change the invite's colour (monogram colour) and background (Save-the-Date studio) | NOT STARTED | `invite-theme-picker.tsx:37-38` says "It opens on your reveal background, in your colour." Its only link is `/studio/website-pro` (line 81). No href to `/invitation` or `/studio/save-the-date` | Add two links: colour → `/dashboard/[eventId]/invitation`, background → `/dashboard/[eventId]/studio/save-the-date` | — (the plan says no decision is needed) |
| Corpus follow-through after S4/S5 | The design folder and memory stop saying three themes are "left" | NOT STARTED | `00_README.md` table lists only #5403/#5409/#5410. Controller design §5.3 counts seven | Update `00_README.md`, the memory note and the controller design once S4 and S5 merge | — |

## OPEN OWNER QUESTIONS

- **Invite themes: no open questions.** All seven (Q1–Q7) are ruled in DECISION_LOG row 3886 (2026-09-11). The four "⚠ OPEN, NOT RULED" items in the 2026-09-10 arrival row (DECISION_LOG:3839) are the same questions and are closed by row 3886. What remains are two owner ACTIONS, not questions:
  - "Ask the owner to pick Capiz and press Save on an event that owns Event Hub Pro. Cale & Ice (/cale-ice) is the owner's own internal account" — Source: /Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Invite_Themes_2026-09-10/01_WHAT_IS_LEFT_SESSIONS_2026-09-10.md:111. Checked DECISION_LOG `grep -n "Capiz"` → only rows 3839/3886, nothing saying it was done.
  - "Use a non-internal TEST event only, and only with the owner's yes in chat: set invite_theme = 'capiz' on it" — Source: same file:119-121. Choices: yes (session writes, then reverts to NULL) / no (gate stays unit-tested only).
- "**The audience switcher's blast radius.** … that already happens on the widgets page — is it intended, and should the Hub match it or should the widgets page be narrowed?" (a host can "Preview as ⟨real guest name⟩", which opens `/{slug}?invite=⟨their real token⟩`; the Hub uses a fabricated guest behind `NEXT_PUBLIC_HUB_NAMED_GUEST_PREVIEW_ENABLED`, OFF) — Source: /Users/icecasasola/Documents/Claude/Projects/Setnayan/EVENT_HUB_CONTROLLER_DESIGN_2026-09-02.md:653-668. Checked DECISION_LOG `grep -i "HUB_NAMED_GUEST\|named guest preview\|widgets page be narrowed"` → no ruling. Choices: keep the widgets page as-is and let the Hub match / narrow the widgets page / leave the Hub on the fabricated guest.
- "**Is the free Editorial Pro ruling still what you want?** It went free on 2026-08-23 … It decides whether the story is a **reason to buy** Event Hub Pro or just a room the controller opens" — Source: /Users/icecasasola/Documents/Claude/Projects/Setnayan/EVENT_HUB_CONTROLLER_DESIGN_2026-09-02.md:677-680. Checked DECISION_LOG `grep -i "EDITORIAL_PRO\|free Editorial"` → partly answered by row 3657 (2026-08-29): "EVENT HUB PRO IS NOT REPRICED — IT GROWS … do not treat the Editorial PRO freeing as a reason to cut the price". That keeps editorial free in practice, but nothing re-rules it after the 09-02 question. Choices: keep free (code `NOT_SOLD_ON = ['Editorial editing']`, `lib/website-pro-items.ts`) / make it paid again.
- Controller-layout questions in the same doc, outside this slice but still unruled there: "**The stage-first order.** … the controller opens on the **dark stage** with the facts fused to its lower edge" (:647-649). "**The five-column matrix vs. three.** … Recommendation: **five in the document, three in the code**" (:650-652). "**Enforce the room grid per kind of event?** Still open from 2026-08-17" (:713-714). Source: /Users/icecasasola/Documents/Claude/Projects/Setnayan/EVENT_HUB_CONTROLLER_DESIGN_2026-09-02.md. Checked DECISION_LOG `grep "stage-first\|dark stage"` → no ruling. `grep "room grid"` → row 3598 (2026-08-28) calls the grid "approved" and ships slice 1 ("only its own rooms"), so the room-grid question is at least partly ruled.
- Pahina guest-site RSVP (shipped with the builder's choice, never ruled): "**RSVP option labels** changed to the design spec's §7 wording … → **"Joyfully accepts / Undecided, for now / Regretfully declines."** … Revert = three lines in `rsvp-widget.tsx`" and "**The ask was NOT removed after replying.** … If the owner meant it literally, delete the disclosure in `site-body.tsx`." — Source: /Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Premium_Guest_Site_2026-07-25/BUILD_RESUME_2026-07-26.md:40-48. Live on main: `rsvp-widget.tsx:230-232` and `site-body.tsx:1645` ("Need to change your reply or your details?"). Checked DECISION_LOG `grep "Joyfully accepts"` → only row 2689, which records the build and has no ruling. Choices: keep / revert labels; keep the disclosure / delete it.
- Pahina "Needs owner call (… NOT in the 5Tab base — not added)": "**Pabati 5-second video greeting** on the guest site", "**Wall-tile lightbox** (5Tab tiles morph but don't open)", "**Lock-flip unlock ledger**" — Source: /Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Premium_Guest_Site_2026-07-25/Premium_Guest_Site_Design_Spec_2026-07-25.md:323-328. Checked DECISION_LOG `grep -i "lightbox\|unlock ledger\|lock-flip"` → no ruling on these items. Low priority. I did not check the code for these three.

## SUPERSEDED / ALREADY DONE

- **Three doors (Name · Reply · Enter)**: #5403 merged (`d175606df`, 2026-09-10). Guard `app/[slug]/invite/the-arrival-has-three-doors.test.ts`.
- **Theme column + picker + Pro gate + Capiz skin**: #5409 merged (`1f739c68f`). Migration `20271219583821_the_invite_link_wears_a_theme.sql`. Its CHECK already allows all five ids, `IN ('house','capiz','velvet','galeriya','abaca')` (:33-34), so **Velvet/Galeriya/Abaca need no migration**. It also has the authenticated SELECT grant (:56) and the events_host rebuild.
- **Reveal on invite door 01 under the Event Hub's own rule** (not on the day, not for a wake, weddings' film fence): #5410 merged (`0d3a1e0c9`). `lib/invite-reveal.ts` → `cinematicRevealPlays`. Guard `the-reveal-opens-the-invite.test.ts`.
- **Q5 feel → theme pre-selection**: already built. `suggestedInviteTheme` in `lib/invite-themes.ts`, used at `guests/invite/page.tsx:64` with `mood_feel_key`. Ruled 5A = keep, nothing to build.
- **Buy-page / catalog copy already names the invite theme** (half of Q3): `studio/website-pro/page.tsx:86`, `lib/add-ons-catalog.ts:488`, `says-what-it-includes.test.ts:144`.
- **The four "OPEN, NOT RULED" items in DECISION_LOG row 3839** (button colour, eighth item, Galeriya fold, feel default) are ruled by row 3886.
- **Event Hub controller** EH1–EH6 all merged 2026-09-02 (#5102 #5107 #5108 #5106 #5111 #5116 #5130). Controller-doc decisions 2 (Launch retires), 6 (catalog title "Event Hub Pro") and 9 (one door) are answered in the doc itself.
- **Pahina Wave A** merged to main: #3760 (`a39ce88ee`). `git merge-base --is-ancestor origin/wave/a-pahina-reskin origin/main` → 0.
- **Pahina "Editor's 5th preview tab — OWNER-GATED"** (BUILD_RESUME:55-62): shipped as PR #3773, DECISION_LOG row 2706 (2026-07-26).

## FILES THE PACK SHOULD INCLUDE

/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Invite_Themes_2026-09-10/00_README.md — what shipped, theme table, faces per theme
/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Invite_Themes_2026-09-10/01_WHAT_IS_LEFT_SESSIONS_2026-09-10.md — five session prompts, owner answers Q1–Q7, traps
/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Invite_Themes_2026-09-10/designs/velvet.html — Velvet design to port (session 2)
/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Invite_Themes_2026-09-10/designs/galeriya.html — Galeriya design to port (session 3, shorten print)
/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Invite_Themes_2026-09-10/designs/abaca.html — Abaca design to port (session 4)
/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Invite_Themes_2026-09-10/designs/capiz.html — shipped Capiz design, the pattern reference
/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Invite_Themes_2026-09-10/designs/house.html — free House door reference
/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Invite_Themes_2026-09-10/designs/five-invite-doors.html — the lineup page the owner reviewed
/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Invite_Themes_2026-09-10/designs/reveal-bg-sample.jpg — stand-in reveal background for testing skins
/Users/icecasasola/Documents/Claude/Projects/Setnayan/EVENT_HUB_CONTROLLER_DESIGN_2026-09-02.md — §5.3 "seven" count to change; §7 open decisions
/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Premium_Guest_Site_2026-07-25/BUILD_RESUME_2026-07-26.md — two unruled RSVP choices on the guest site
/Users/icecasasola/Documents/Claude/Projects/Setnayan/Design_Premium_Guest_Site_2026-07-25/Premium_Guest_Site_Design_Spec_2026-07-25.md — §12 "needs owner call" guest-site leftovers
/Users/icecasasola/.claude/projects/-Users-icecasasola/memory/setnayan-invite-themes-2026-09.md — lead: traps (wedding-word guard, stale reveal prices)
