# The DECISION_LOG row for this design

⚠ **This row is appended to `DECISION_LOG.md` in the working copy but that file is NOT committed**
— it carries 44 uncommitted rows from other sessions dated 2026-09-04 → 09-07, and the remote has
moved twice since. The row is kept here so it survives if that shared file is ever reverted.

Paste it at the bottom of `DECISION_LOG.md` if it is missing.

---

| 2026-09-07 | 📐 **THE PUBLIC STORY BECOMES A CLOCK — "the editorial, by the minute", six owner locks + five in-session rulings.** The spine of `/[slug]`'s published story changes from a page of sections to the event's own timeline: every photo, voice, vendor credit, film timecode and table is filed under the moment it happened, the light of the page moves from morning to night as you read, and the colours are derived from the host's SAVED mood-board palette (contrast-corrected — a colour off a board is never trusted to be readable). **Content locks of `Editorial_Experience_Spec_2026-06-18` stand** (three voices, the locked close, A3 keepsake, edition number); only the spine changes. **Locks:** (1) grows privately, publishes once — the host's own layer is public as it happens, the guests' layer is QR-only until publish, the edition publishes once at the same URL; a written minute does not exist until the host writes it; (2) colours from `sanitizeRolePalette(events.role_palette).reception`, neutral fallback; (3) vendor tiers LISTED (free, credited at every minute, auto-collected on their portfolio) vs FEATURED (paid: a note to the host, own day-of clips, Follow, outside links, reach numbers) — **paying never changes WHETHER a vendor is credited, and `#1 match` is a credit, not a tier**; (4) a few written minutes + EVERY bar on the dial opens; (5) a per-guest 9:16 share card; (6) **assigned seats exist only while the reception venue is in use** (owner caught this himself — the prototype had been lighting tables during the ceremony). **Rulings added in session:** a person's day is exclusive to their own account — **there is no name field, for anyone, ever**, and the public seating plan carries table numbers and photo-heat, never names (the earlier design let a stranger type any first name and learn who attended and where they sat); the capture's minute is the SHUTTER, not the upload — owner, verbatim: *"when we get the photos and snippets, we know. but the guest does not need to know"* → read it from the capture on ingest, never ask the guest, never surface the mechanism; print-ready (A3 broadsheet · PDF · A4 per-minute) and social share (FB · Messenger · Pinterest · link · 9:16 card) are first-class; the teaser for the next event is the **BACK COVER, outside the locked close** — the edition still ends on the host's words then their song, and starting the next event there links it as No. 2 with "Previously · No. 1"; a guest cannot author a chapter and does not need to (their voice arrives as photos and snippets) — chapters stay with the host and booked vendors, and appear only once the host includes them. **A 7-lens review (101 raw → 45 merged → 39 confirmed + 11 gap findings, adversarially verified) caught three blockers before any code:** the pre-publish views leaked the entire guests' layer (the index, the dial's bar HEIGHTS, the minute sheet, the cover counts, Relive, the last word — only the day's entries were being hidden); every voice was named and role-badged although the only shipped naming opt-in is `guest_columns.author_named_publicly`; and the public seating plan published 108 first names. Also corrected: one 2h48 film cannot span 2:38→9:47 PM (three broadcast sessions; timecode = clock − went-live), table attribution before the reception, the edition number arriving before publish, "who tapped them" promising identity the analytics model forbids, a light→dark crossfade passing through a ~1.05:1 illegible midpoint, derived palettes failing AA, dial labels squashed to 35% width on a phone, 1.3px tap targets, a page resting at 65% opacity, and a vendor card that was one `<a href="#">` wrapping three fake actions. ⚠ **Five data requirements are NOT met today** — see the design doc's table; the load-bearing one is that `loadVendorFeaturedStories` still filters `.eq('event_type','wedding')` while the showcase was opened to every kind on 2026-08-15, **so the free tier's portfolio promise is false right now for a debut, reunion or wake**. 🔑 **Four questions are the owner's and are NOT decided here:** are aggregate counts/bar heights public before publish (defaulted to QR-only) · does the Kwento naming opt-in match the letters' (the DPO ruled on guest columns; photo messages were never put to them) · does by-the-minute refuse the solemn register outright as the shipped recap does, or ship the quiet arm the prototype demonstrates · the Featured price. Design doc + prototype: `Design_Editorial_By_The_Minute_2026-09-07/README.md`. | The market has time-based video (YouTube chapters), time-based reporting (live blogs), and time-based travel (Polarsteps) — nobody has put an event's photos, words, vendors, room and film on one clock. It is also entirely derivable: every capture already has a timestamp, every guest a table, every vendor a booked moment, every chapter a film timecode. |
