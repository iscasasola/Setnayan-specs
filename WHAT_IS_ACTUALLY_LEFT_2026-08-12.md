# WHAT IS ACTUALLY LEFT — the register re-verified, 2026-08-12

> **This supersedes the counts in the code repo's `WHAT_IS_LEFT.md` (written 2026-08-07).**
> That file's own first rule is *"a handoff is not evidence — verify every line"*. This is that verification.

**All 58 claims re-checked against shipped code at `origin/main` and the live production database**
by 6 independent readers, then each group's verdicts attacked by a skeptic instructed to look for BOTH
failure modes: shipped work reported as open (this project has paid twice to rebuild existing pages),
and broken work reported as done. **1 verdict was overturned by the refute pass.**

## The count

| | |
|---|---|
| checked | **58** |
| **already done** — do NOT rebuild | **17** |
| still open | **41** |
| …of which need the OWNER, not engineering | **14** |
| …need engineering | **24** |
| …blocked on an outside party | **3** |

🔑 **Nearly a third of the register was already finished.** Five days of work closed 17 items, and
nothing in the register knew it. That is exactly the drift the file warned about, measured.

## ✅ ALREADY DONE — do not rebuild any of these

- Extra photo cameras can be switched on without paying
- A receipt attached to a vendor payment is silently thrown away
- The photo sweep's own warning label said it was in safe mode when it is not
- A disconnected Google account still had its access key stored
- Cameras past the fourth were dropped from the broadcast with nobody told
- The list deciding Launch vs Buy was empty for the photo service
- The privacy notice promised we automatically delete connected TikTok and Google Drive accounts a month after the event
- A guest who buys extra shots without making an account was not described anywhere in the compliance record
- The Filipino front-page wording you approved on 31 July is now live on the homepage
- The couple now has an on/off switch for the photo wall on guests' phones
- The founder perk can now be granted — an admin has a button for it
- Two Live Studio cards sit side by side and one sells a retired product
- The unplug-my-YouTube-account button exists only on the old screen
- No printable sheet for handing cameras to helpers on the current screen
- A dead feedback screen nobody can leave feedback on
- The couple's own coordinator has no button to message the emcee
- On the onboarding screen the AI planner card is a dead end

## 🔴 NEEDS YOU — no engineering left

*Each needs a signature, a price, a ruling or a sentence. Building around them does not unblock them.*

**14 items**

- **Compromised-password checking is still switched off at sign-up** *(medium)*
  - Someone can create a Setnayan account today with a password already known to be stolen and published, and nothing warns them. It is one switch in the database console — not a code change.
  - <sub>verified 2026-08-12: Supabase security advisor pulled live 2026-08-12 for project njrupjnvkjkitfctetvi still returns lint auth_leaked_password_protection (WARN): 'Supabase Auth prevents the use of compromised passwords by checking against HaveIBeenPwned.org. Enable this feature to enhance security.' Unchanged from the register's 2026-08-05 reading.</sub>
- **Face-matching files still load from a free public address that throttles under load** *(medium)*
  - Every guest phone that uses face matching downloads a 1.3 MB file from a free public address the provider says is not for production traffic. At a packed 300-guest wedding those downloads can slow or fail, and photos stop finding people. The fix means moving the website's address records to the storage provider — a real infrastructure decision the owner has deliberately not taken.
  - <sub>verified 2026-08-12: Nothing changed. Measured live today: https://pub-37d64fe618584c2981a88610a55dd439.r2.dev/face-models/face-api.js → HTTP 200, 1,333,943 bytes; the weights manifest → 200, 28,233 bytes. The recommended replacement host does NOT exist — curl https://media.setnayan.com/face-models/face-api.js fails to resolve (exit 6). No same-origin proxy was built: apps/web/app/api has no face/model route and 'face-models' has zero non-test code hits. git log origin/main --since=2026-08-06 on lib/face-embed.ts, lib/face-embed-core.ts, lib/face-gate.ts returns nothing. OWNER_ACTIONS.md:1238 still carries the open follow-up. This is NOT moot: prod events GROUP BY papic_face_mode = mode_a × 5, so face matching i</sub>
- **Guest photo-taking is selling while the two privacy sign-offs for it are still open** *(medium)*
  - Guests can already shoot at an event and a couple can already buy shot credits (₱50 / ₱1,000 / ₱3,000 / ₱5,000 are all switched on and buyable right now, and 14 photos already sit in the live system). Two things you have to sign off as data-protection officer have never been signed: the written record describing that guests' own phones are the main camera and that photos are delivered by face-matching, and a confirmation that the tick-box guests agree to at RSVP actually says both of those things. Nothing a guest sees warns them; the gap is only in our paperwork.
  - <sub>verified 2026-08-12: Read at origin/main (7d7eb1a7d): apps/web/lib/papic-event-access.ts:149-153 still records gates 0d/0e as 'likewise open while the pool sells'. Corpus Papic_Access_Scope_Council_Verdict_2026-07-20.md:161-162 defines 0d (guest-media ROPA row) and 0e (DPO confirms RSVP consent text names guest-phone capture AND face-sorted delivery). apps/web/lib/privacy-coverage.ts:119-122 has papic_pool_gallery declaredIn: [] (DPS-16 only DRAFTED 2026-08-02). The shipped filing artifact apps/web/assets/npc-docs/03_Records_of_Processing_Activities.pdf last changed 2026-07-23 (commit f4e275784) — before those amendments. Prod SQL: select * from npc_filing_tasks → all 15 rows status='not_started', note/evidence/</sub>
- **Nobody has ever streamed anything — the whole path is unproven** *(medium)*
  - Every piece of livestreaming is built, but no wedding has ever actually gone out — not once, not even a test. The first person to try it live will be a paying couple on their wedding day. Somebody on the team needs to run a complete fake event first, and allow for the one-off 24-hour wait Google imposes before a brand-new channel is allowed to stream.
  - <sub>verified 2026-08-12: Prod SQL (project njrupjnvkjkitfctetvi), superuser so an empty read is genuinely empty: live_studio_roam_channel_pool=0, live_studio_channel_grants=0, live_studio_roam_streams=0, panood_broadcasts=0, live_studio_highlights=0, live_studio_overlay_settings=0. Also orders total=0 and orders for LIVE_STUDIO/LIVE_STUDIO_ROAM/PANOOD_SYSTEM/PANOOD_SYSTEM_MOBILE=0; events.panood_watch_url set on 0 of 5 events; events.panood_manual_on_air_at set on 0. Setup HAS been exercised: live_studio_roam_zones=1, panood_camera_operators=16 (8 seats each on 2 events, 0 claimed), panood_control_state=2, and oauth_grants provider='youtube' = 1 row, all revoked (revoked_at not null) — the one connect-and-revoke rou</sub>
- **On a Setnayan-owned channel the couple gets a watch link, not a file** *(medium)*
  - After the wedding the couple gets a private link to watch the recording. If the broadcast went out on their own YouTube channel they can save the video file themselves, and the app now tells them so. If it ever goes out on a Setnayan channel instead, a link is all they get — there is no way for anyone to hand them the actual file, and no code has been written for one. Whether a link is enough is your call.
  - <sub>verified 2026-08-12: origin/main @7d7eb1a7d, apps/web/app/_components/live-studio-recordings-card.tsx:81-85 renders 'These are watch links — if your broadcast went out on your own YouTube channel, you can also download the file from YouTube Studio', and its header comment (lines 32-34) records that the spec's word 'download' was deliberately softened to 'watch' because YouTube only offers a file download to the channel's OWNER. The only per-recording control is `href={rec.watchUrl}` at line 129. apps/web/lib/live-studio-recordings.ts:32-40 confirms § 6 ruled out a parallel stored copy for V1 and that the module 'deliberately moves no bytes and writes nothing to R2'. No re-upload or admin hand-off path exists any</sub>
- **One live privacy capability is running with no approval recorded — the only one of twenty** *(medium)*
  - Twenty privacy-sensitive capabilities are switched on in production. Nineteen carry your name and the date you approved switching them on. One — the anti-fraud scoring that can automatically hide a vendor's listing without a person deciding — has both the approver and the date blank, and has never been touched since it was seeded on 22 July. If a vendor ever asks who authorised the machine that hid them, there is no answer on file.
  - <sub>verified 2026-08-12: Prod SQL (project njrupjnvkjkitfctetvi): select control_key, status, approved_by, approved_at, created_at, updated_at from data_privacy_controls → 20 rows, every one status='active'; 19 carry approved_by=5599d399-b4f8-459d-9080-8069824dec96 with an approved_at; 'antifraud_trust_signals' has approved_by NULL and approved_at NULL, created_at = updated_at = 2026-07-22 17:42:49.444575+00 (never modified). Corroborated by prod platform_compliance_facts.automated_decisions, which names this as the one automated decision that can suspend a vendor listing.</sub>
- **Photos are being matched to people by face on every event, and the page selling the service still says that does not happen** *(medium)*
  - Every event in production is set to the mode where photos find people by face, the privacy switch for it is on, and the face model is live and serving. But the public Papic page a couple reads before buying never says so — it only says a guest can 'add a selfie so their photos are ready to be matched', which sounds like a filing cabinet, not a feature. We are hiding a capability we paid for and switched on, and the next person to read that page will keep other screens narrow for a reason that expired.
  - <sub>verified 2026-08-12: PROD (Supabase njrupjnvkjkitfctetvi): `select papic_face_mode, count(*) from public.events group by 1` → mode_a: 5 (all five events are in the face-matching mode). `select control_key,status from public.data_privacy_controls` → face_enrollment = active (updated 2026-07-16). Model assets live: curl of https://pub-37d64fe618584c2981a88610a55dd439.r2.dev/face-models/{face-api.js, ssd_mobilenetv1_model-weights_manifest.json, face_recognition_model-weights_manifest.json} → 200 / 1,333,943 bytes, 200, 200. Matcher is wired, not orphaned: apps/web/lib/face-match.ts exports autoTagCapture, imported and called at apps/web/app/papic/actions.ts:11; app/papic/seat/[token]/_components/papic-seat-capture.</sub>
- **Six pages that explain and sell your services still have no way in from your own site** *(medium)*
  - The 3D plan page, the event-website page, the Patiktok page, the Live Studio page, the animated-monogram page and the memories page all load fine and look finished — but nobody browsing setnayan.com can get to them. The homepage, the top menu and the footer never point at any of them. Four of the five service pages are reachable only from inside the memories page, and the memories page itself is linked from nowhere at all, so that route is a dead end too. The Patiktok page has no way in from anywhere. Only someone typing the address or arriving from Google ever sees them. Note that Papic and the free monogram maker ARE linked and are not part of this.
  - <sub>verified 2026-08-12: Live: /pa3d /pawebsite /patiktok /panood /palogo /alaala all return HTTP 200 with real titles (e.g. 'Pa3D — Walk Your Reception in 3D Before the Day', 'Palogo — Your Animated Wedding Monogram', 'Alaala — A New Way to Remember'). Extracted every internal href from the live homepage: 41 hrefs, including /papic and /monogram, and NONE of the six. Footer at apps/web/app/_components/marketing/reskin-footer.tsx:78-102 lists 17 destinations, none of the six. The new top-bar menu (commit 643d8a43d, 2026-08-09, 'the front door gets a menu') added only /explore, /realstories and /blog. Live-fetched /pricing /about /how-it-works /features /our-story and the five service pages themselves and scanned the</sub>
- **The corrected legacy-preservation brief for the lawyer: nothing anywhere records it being sent** *(medium)*
  - We had told a lawyer in writing that keeping a dead relative's memories would be free forever. That is now a paid subscription that stops when someone stops paying, and the document was rewritten on 30 July to say so, with the payment questions marked as the priority ones. There is still no record anywhere that the packet was ever actually sent to a lawyer, and no lawyer's reply on file. Only you know whether it went out.
  - <sub>verified 2026-08-12: Corpus: 03_Strategy/Phase3_Minors_and_Legacy_Counsel_Brief_2026-07-05.md and .docx both mtime 2026-07-30 23:47; line 50-51 reads '🔴 CORRECTED 2026-07-30 — PRESERVATION IS PAID… If a payer lapses, that account's copy of the memories is discontinued'; committed as c040a59 (2026-07-30 23:48). Prod SQL: select * from npc_filing_tasks where task_key='t0-1' ('Route the full packet to external PH counsel') → status='not_started', note NULL, evidence NULL, resolved_by NULL, resolved_at NULL, updated_at 2026-07-16. Prod platform_compliance_facts.breach_team still reads 'No external DBRT members yet; add an external PH counsel liaison when engaged'.</sub>
- **The older drawings still show the retired colours** *(medium)*
  - The problem the register described was fixed and then immediately overtaken. The old gold buttons in the drawings were repainted terracotta at 2:50pm on 8 August; at 7:00pm the same day you ruled that gold is the action colour after all. Nobody has gone back. A builder opening any of those drawings today builds terracotta buttons where you said gold — the same trap, pointing the other way, and none of the drawings carries a note saying so.
  - <sub>verified 2026-08-12: Corpus commit 5d0f081 (2026-08-08 14:50:26 +0800) 'design(#4): reconcile pre-lock prototypes' — diff confirms gold fills replaced by #C24E25 in prototypes/{event_dashboard_v2,user_home_final,vendor_dashboard_v2}_2026-07-15.html (e.g. `.btn-a{...background:var(--gold-500)` → `background:var(--cta)` with `--cta:#C24E25`). Code commit 27fc9ec1f (2026-08-08 19:00:41 +0800) 'design: GOLD is the action colour — owner override of the handoff' reverses the rule ('stick to gold to all first'); Design_Warm_Editorial_Archive_2026-08-08/ACTION_COLOUR_OVERRIDE_2026-08-08.md sets primary fill to #8C6932. Re-measured today: 12 of 28 prototype .html files contain #C24E25 (all 5 archetype_* files plus the 3 </sub>
- **Do wedding recordings stay on the channel forever, or get wiped when the channel is reused?** *(low)*
  - Nothing is being deleted today — the code deliberately keeps every recording. But two of your own written specs say a channel should be wiped before it is handed to the next wedding, and one says recordings stay up indefinitely. Until you say which one wins, nobody should touch it, because deleting a wedding cannot be undone.
  - <sub>verified 2026-08-12: origin/main @7d7eb1a7d, apps/web/lib/live-studio-recordings.ts lines 41-52 state the conflict verbatim ('Live_Studio_Cast_and_Roam_2026-07-23.md § 4 and Live_Studio_Unified_Spec_2026-07-25.md § 4h both end the handoff with "the channel is then wiped + returned to the pool", while § 6 above says the archive stays… at indefinite retention') and close with 'Owner settles which doc wins.' No delete call exists: grepping deleteVideo|videos.delete|wipe across apps/web/lib/panood-youtube*.ts, apps/web/lib/live-studio-*.ts and apps/web/app/admin/live-studio-channels/actions.ts returns only that comment plus apps/web/lib/live-studio-channel-pool.test.ts:407-411, a test that asserts release is NOT aut</sub>
- **The couple still cannot pin their website to one phase — it always follows the clock** *(low)*
  - A couple can never say 'show the save-the-date version' or 'show the RSVP version' — the site always decides from the date. That matters for an evening reception, which never falls inside the automatic live window. This was designed as the escape hatch for exactly that and the screen was never built.
  - <sub>verified 2026-08-12: origin/main 7d7eb1a7d: grep of all of apps/web for either column name (launch_mode, manual_phase) returns ZERO hits — no reader, no writer; I also ran the repo's own write-detector for both → 0 writers each. The only place they exist is supabase/migrations/20270426100000_event_launch_mode.sql, whose own comment says "launch_mode = 'manual' -> the couple PINS one phase via manual_phase". Live prod: 5 events, all launch_mode='auto', all manual_phase NULL, and 0 database functions mention either name.</sub>
- **The features page is the one page two approved documents disagree about** *(low)*
  - One of the main marketing pages is frozen because two signed-off documents describe it as two different shapes. Nobody can port it until someone picks one.
  - <sub>verified 2026-08-12: The contradiction is live and unchanged. 03_Strategy/Design_Gap_Pass_2026-08-01.md line 104 assigns `/features` → '**A** Comparison'. CLAUDE_DESIGN_05_Complete_Page_Map_2026-08-07.md lines 144-145 assigns `/features` and `/tl/features` → 'A02 editorial'. Neither doc has been amended (gap pass last commit ebece02, 2026-08-01). The page is also still unported: apps/web/app/features/ = page.tsx (48 lines) + _PageBody.tsx + 10 _sections files, 1,652 lines total, shared with the Tagalog twin at app/tl/features/page.tsx; git log on apps/web/app/features shows the last touches are copy/product fixes (LED removal e14ab6ccd, retention 80610bf95/6c0806d52) — no design commit.</sub>
- **The photo service still shows no 'what this would otherwise cost you' comparison** *(low)*
  - The Papic page shows what a couple pays — ₱50, ₱1,000, ₱3,000, ₱5,000 for shots — but never says what they would otherwise pay a professional for the same coverage. The old comparison figures were pulled because they were sized against products we no longer sell. If you want a comparison back, the number has to come from you; nobody else can invent one honestly.
  - <sub>verified 2026-08-12: Fetched https://www.setnayan.com/papic (HTTP 200, 120,920 bytes) and scanned the stripped text: the only peso figures on the page are the ladder (₱50 adds 100 shots · ₱1,000 adds 3,000 · ₱3,000 adds 10,000 · ₱5,000 adds 20,000). No 'was', 'instead of', 'usually', '75,000' or '32,000' anywhere. Repo-wide grep at origin/main for 75,000 / 75000 / 32,000 / 32000 / priceAnchor / compareAt across app/papic, app/_components/marketing and lib/papic-*.ts returned nothing. The single strikethrough on the page (apps/web/app/papic/page.tsx:405) is the feature comparison 'a photo wall vs Papic', not a price anchor.</sub>

## 🛠 NEEDS ENGINEERING

*No decision required — these are builds and fixes.*

**24 items**

- **A supplier can now say which venues and ceremonies they serve — but every shop still starts narrowed** *(medium)*
  - The missing screen shipped: a shop owner can now tick which reception settings and which ceremonies they work with, on My Shop, and it saves. What did not change is where a shop starts: a brand-new business is still born saying it only serves ballrooms, gardens and heritage houses, and nothing prompts them to fix it — so a couple with a beach, resort, tent, city-hall or restaurant reception still sees fewer suppliers than exist until each shop opens that card. Both live shops are still on that starting value.
  - <sub>verified 2026-08-12: origin/main 7d7eb1a7d: apps/web/app/vendor-dashboard/shop/venue-match-actions.ts writes both columns through the vendor's own client; the card is rendered at apps/web/app/vendor-dashboard/shop/page.tsx:915 (<VenueMatchCard>), and apps/web/lib/vendor-compatibility.test.ts:63 fails the build if it stops being rendered. I re-ran the codebase's own write-detector regex over every non-test .ts/.tsx: compatible_venue_settings → 1 writer. BUT live prod: the column default is still ARRAY['banquet_hall','garden','heritage'] (pg_attrdef), both vendor rows still hold exactly that, and the marketplace filter at apps/web/app/explore/page.tsx:1641 is 'is.null OR contains{setting}' — so the default is a re</sub>
- ✅ **CLOSED 2026-08-30 (C10) — A supplier can only send the coordinator six canned buttons, never a sentence** *(was medium)*
  - ~~On the night, a band or photographer can tap one of six fixed messages — On site, Setup done, Ready to start, Packed up, Running late, Need help. They still cannot type 'the cake is melting near the lights'. The coordinator is the only person on the floor who gets a free-text box.~~ **Fixed 2026-08-17, PR #4505**, five days after this row was verified: `stage-note-compose.tsx` mounts the pre-existing `submitDayRequest` free-text action for a plain supplier too, not just the coordinator. Re-confirmed live 2026-08-30.
  - <sub>verified 2026-08-12: apps/web/lib/day-requests.ts line 82: VENDOR_STATUS_PRESETS is still exactly six entries. The free-text server action DOES exist (apps/web/app/vendor-dashboard/on-the-day/actions.ts line 470 submitDayRequest, which files a supplier on the vendor lane) — but its only caller is requests-inbox.tsx, and that component is mounted at apps/web/app/vendor-dashboard/on-the-day/page.tsx line 1081 behind `kind === 'coordinator'`. A plain supplier gets only VendorStatusUpdates (page.tsx line 534), whose own docblock says 'nothing here can post arbitrary text'. Prod check: select control_key, status from public.data_privacy_controls where control_key like 'coordinator%' → coordinator_requests_inbox = act</sub>
- **Four more surfaces still have no design: the photo service's public pages, the marketplace, the tour, and the quiz questions** *(medium)*
  - The place people shop for suppliers, the walkthrough that shows the product off, the deepest photo-service pages and the questions the onboarding quiz asks are all still built-but-undrawn.
  - <sub>verified 2026-08-12: Route counts re-measured at origin/main and they match the gap pass exactly: /explore = 3 page.tsx, /tour = 5, /papic = 11. All four are still listed as unresolved gaps 1, 2, 4, 5 in 03_Strategy/Design_Gap_Pass_2026-08-01.md lines 116-123 (file untouched since 2026-08-01). git log origin/main --since=2026-08-07: /tour has ZERO commits of any kind; /papic has only functional work (clip pricing, split gate, captcha, 720p); /onboarding has feature work (the Papic picker, the AI purchase line) but nothing touching the persona-quiz questions/order/reveal. The one exception is /explore, which got a single small design unit — 9a489e82a design(#4) 'sorting you can see, and why a shop has no name' (2</sub>
- ✅ **CLOSED 2026-08-30 (C10) — Four rows of the compliance register still say the wedding photos are stored in the Philippines** *(was medium)*
  - ~~We corrected the public privacy page to stop saying photos sit on Philippine soil, because they do not — they are in Singapore and the wider Asia-Pacific. The internal record we would actually file still says the Philippines in four places, and one of them is the row about the wedding photos themselves.~~
  - **Re-measured 2026-08-30:** `grep -n APAC` against
    `NPC_Compliance/02_Records_of_Processing_Activities_DRAFT_2026-07-05.md` now returns **zero**
    uncorrected `APAC/PH (R2)` rows (all four cited here — DPS-01, DPS-02, DPS-05, DPS-07 — are
    fixed), and `pdftotext` on the shipped
    `apps/web/assets/npc-docs/03_Records_of_Processing_Activities.pdf` also returns zero matches.
    Both closed since this row was written. **What was still open on 2026-08-30, in two files this
    row never named:** corpus `README.md:134` and `API_Integration_Checklist.md:212` both still
    said "R2 (PH region)" — corrected to "Asia-Pacific (APAC) — R2 has no PH region."
- **Guest capacity can now be entered; the venue's kind still cannot** *(medium)* — ⚠ **restated 2026-08-30 (C10):** the claim below is accurate about the WRITE screen but understates it — `venue_type` is not merely a column nothing reads; it IS read by the public vendor profile API and by Explore. This is session **C2**, not an unstarted gap.
  - Half of it landed. A venue can now type its room size and how many it seats, on My Shop, and head count starts narrowing properly. The other half did not: there is still nowhere for a business to say what kind of venue it is, so a couple who said 'church' or 'hotel ballroom' still gets no narrowing on that at all. Both live shops have both fields blank.
  - <sub>verified 2026-08-12: origin/main 7d7eb1a7d: apps/web/app/vendor-dashboard/actions.ts:342 allowlists a 'venue_size' field, :547-559 validates and writes capacity_min/capacity_max; inputs exist at apps/web/app/vendor-dashboard/shop/_components/editable-row.tsx:618/632; the row is added to the checklist for shops whose services include 'venue' at apps/web/lib/vendor-profile.ts:513; apps/web/lib/venue-room-size.test.ts:201 guards that a form actually posts every name the action reads. For venue kind: grep of apps/web for vendor_profiles.venue_type finds only readers — the sole reader is apps/web/app/onboarding/wedding/actions.ts:1191 (venueTypeFit); every /admin/venues hit writes the separate venue_directory table. </sub>
- **More than half the database still hands out a read permission nothing uses** *(medium)*
  - Nothing today — no data is leaking. The risk is that most of the database has only one lock rather than two, so a single mistake in a single rule turns into a leak instead of a near-miss. Anyone can get an account with one tap, which is what makes the second lock worth having.
  - <sub>verified 2026-08-12: Live prod SQL over pg_class/pg_policies, 2026-08-12: 383 public tables · 306 grant anonymous SELECT · 216 of those have no policy admitting an anonymous reader (register said 212 on 2026-08-05 — it has gone up, not down) · anonymous role holds TRUNCATE on 307 tables and the signed-in role on 344 · row-level security is enabled on all 383 (0 disabled), and row rules are never consulted for TRUNCATE. Anonymous sign-in confirmed enabled via /auth/v1/settings. No sweeping migration since; the only grant narrowing that landed (20271132024116) covers 8 views, not tables.</sub>
- **The admin console redesign has not started** *(medium)*
  - The Setnayan team's own screens are still ninety-odd hand-built tables. They got the new card edges and colours in the app-wide sweep, and nothing structural.
  - <sub>verified 2026-08-12: Read apps/web/app/admin/_components at origin/main: still exactly the same 12 files the register listed — admin-bottom-nav, admin-command-palette, admin-nav-descriptions, admin-nav-fab, admin-nav-groups(+test), admin-search-parity.test, admin-sidebar, admin-sidebar-menu, kpi-stat-card, mobile-landing-accordion, mobile-landing-grid. No shared console table; `find` for *console-table* / *data-table* returns nothing. Raw <table> count: 45 files app-wide, 33 of them under admin (register said 34 of 48 — the ratio is unchanged). git log origin/main --since=2026-08-07 on apps/web/app/admin returns only functional work (address correction, queue ordering, compliance sheet, verification screen) — no</sub>
- **The browser protection is still measuring only, and the measurements still go nowhere** *(medium)*
  - If someone ever managed to inject a script into a page, the site would watch it happen rather than block it. And nobody can look at what it saw, so the moment to switch real protection on never arrives on its own.
  - <sub>verified 2026-08-12: Live headers from https://www.setnayan.com/ (curl -I, 2026-08-12): the ENFORCED content-security-policy is frame-ancestors/frame-src only; the wide policy is sent as content-security-policy-report-only. Source at origin/main 7d7eb1a7d: apps/web/next.config.ts:248 (enforced, frame-only) and :436 (report-only). The sink apps/web/app/api/csp-report/route.ts ends at a single console.warn on line 57 — its own comment above it says 'Sentry when configured, the platform log otherwise', but there is no Sentry call, no capture, no write in the file (grepped for sentry/insert/from( — no hits). Live prod has no table whose name contains 'csp' (information_schema count = 0).</sub>
- **The compliance pack we would hand a lawyer or the privacy commission is three weeks out of date** *(medium)*
  - If the Setnayan team downloaded the compliance pack today to answer a lawyer or the National Privacy Commission, it would describe a product we no longer run. It lists fourteen activities when we now do nineteen — the guests' public write-ups, the shared photo pool, the same-date demand signal, the in-app video calls and the coordinator's day-of desk are all simply absent. It also states in black and white that we destroy wedding photos after five years, which we do not and never will. Handing that over would look like nothing had been done.
  - <sub>verified 2026-08-12: Files at origin/main: apps/web/assets/npc-docs/*.pdf — `git log origin/main -- apps/web/assets/npc-docs/` shows last change f4e275784 on 2026-07-23; corpus mirror ~/Documents/Claude/Projects/Setnayan/NPC_Submission_PDF_2026-07-16/ files also all dated Jul 23. Meanwhile the markdown source moved through 2026-08-08 (`git log NPC_Compliance/02_Records_of_Processing_Activities_DRAFT_2026-07-05.md` → 6c5c31a 2026-08-08, 4b87618 2026-08-08, 3a82178 2026-08-02). Proved by reading the shipped PDF, not its date: pdftotext of 03_Records_of_Processing_Activities.pdf lists only DPS-01 … DPS-14 — zero hits for 'Guest-Written Columns', 'Shared Pool', 'Same-Date Demand', 'Live Video Connections', 'Coordina</sub>
- **The couple's dashboard has not been touched by the redesign** *(medium)*
  - The couple's home Overview genuinely got redesigned this week — the frosted panels became flat cream cards, the headline card is now solid ink, and the digest gained proper rows plus an RSVP line and a 'Meanwhile' card. But the four screens they open every day — guest list, vendor comparison, budget and gallery — got new card edges and readable text, and nothing else.
  - <sub>verified 2026-08-12: Overview WAS redesigned: commits b6962a3f7 (skin swap: .sn-tile fill/border/radius 20px→14px/blur removed, 417 uses across 186 files), 063b5505c, c62aa2913, cb3682eb3, 2c58ba55f, ace4e4205, 8e54ec59a — all 2026-08-08, all on dashboard/[eventId]/_components/event-dashboard.tsx. The four named surfaces were NOT: git log origin/main --since=2026-08-07 on dashboard/[eventId]/{guests,vendors,budget,alaala} returns only 52c15f957 (the 76-place contrast sweep), f34fc750c (a QR bug), 8c5bf2dae / ec362cb46 (copy + shop-live fixes) and aa8dea513 (the budget receipt bug) — no redesign commit. The design#5 named offenders build-compare.tsx and plan-budget-accordion.tsx were last touched 2026-07-30 and 2</sub>
- **The emcee is not offered as a recipient when they were booked inside a bundle** *(medium)*
  - If the band who also emcees was booked as one package, the coordinator's message box lists nobody and the whole section silently disappears. It looks like the wedding has no host at all — no error, no explanation.
  - <sub>verified 2026-08-12: Read apps/web/lib/stage-notes-recipients.ts at origin/main 7d7eb1a7d. fetchEmceeRecipients selects only 'vendor_name, category, linked_vendor_profile_id' from event_vendors and then hardcodes `serviceCategories: null`, with the in-file comment still admitting: 'The per-service categories need a second read through requested_service_ids… a miss here means the send box does not offer them'. The pure resolver pickEmceeRecipients already accepts serviceCategories and eventTilesForBooking already unions both sources — only the fetcher is short. File last touched ea2280e9f (2026-08-05); no change since.</sub>
- **The first screens anyone ever sees — sign in, sign up, invite — have no design at all** *(medium)*
  - Every couple, vendor and guest still passes through undesigned sign-in, sign-up, password-reset, claim and join screens before they see anything else.
  - <sub>verified 2026-08-12: Confirmed the gap is still listed as gap #3 of five in 03_Strategy/Design_Gap_Pass_2026-08-01.md line 118 ('Auth screens (~8 routes) — login, signup, reset, claim, join. Low complexity, high traffic'), a doc unchanged since commit ebece02 (2026-08-01). Counted at origin/main: 12 page.tsx files across /login, /signup, /claim/[token], /vendor/claim/[token](+finalize), /join/[eventId](+check-email, set-password, success), /papic/join, /papic/claim, /samahan/join. git log origin/main --since=2026-08-07 on those trees returns only c2460f09f and 8e80a8c8a (auth lockout/captcha fixes) plus the blanket contrast sweep 52c15f957 — zero design commits.</sub>
- **The safety net for this whole group still checks a hand-typed list, not the database** *(medium)*
  - Nobody sees this directly — it is the check meant to stop settings shipping with no screen behind them. It has grown from two entries to five and got better at spotting a guest-side save, and it now also proves the founder button is really on a page. But it is still a list somebody types by hand, so a new switch is only covered if the person who added it remembered to register it. And it still cannot see a save that is assembled in a variable first, so it would report a working screen as missing.
  - <sub>verified 2026-08-12: origin/main 7d7eb1a7d, apps/web/lib/gates-have-handles.test.ts (350 lines): SWITCHES is still a hard-coded array — now 5 entries (live_media_public, papic_face_mode, author_named_publicly, is_founder, live_photo_wall_visibility) — with no enumeration of the schema anywhere in the file; MIGRATIONS is read only to confirm one RPC parameter assignment. Genuine gains since the register: rpcWritersOf() handles guest-side RPC-parameter writes, a positive/negative control test proves the detector distinguishes a read from a write, and a separate test asserts the founder form actually renders. Blind spot unchanged and proven: I ran the file's own writersOf regex over every non-test .ts/.tsx and it r</sub>
- **The vendor's dashboard has not been touched by the redesign** *(medium)*
  - Vendors got the new flat cream cards everywhere, and their calendar now says where each booking came from. The other sixty-odd screens they work in are otherwise unchanged.
  - <sub>verified 2026-08-12: Global card treatment reached vendor-dashboard: the skin-swap changelog (git show b6962a3f7:changelog.d/skin-swap-glass-to-flat.md) measures .sn-tile at 417 uses / 186 files, 'vendor 56'. Two real design units landed: 8fc74866d 'the vendor calendar says where the work came from' (customers-calendar.tsx, +100 lines) and c3376777c 'one line under Publish' (service-wizard.tsx), both 2026-08-08. Everything else on vendor-dashboard since 2026-08-07 is functional (shop address, publish guards, tier caps, payouts). apps/web/app/vendor-dashboard/_components still holds only 20 one-off files (nav, QR, accordions, tier-gate) — no shared kit. 63 vendor routes, 3 raw <table> files. The register's other </sub>
- **The we-couldn't-load-this fix was built and no screen uses it** *(medium)*
  - When a screen fails to load someone's information it still says there is nothing there. A guest opening an invitation that failed to load sees an abandoned-looking page; a couple with three real pending requests can still be told they have none.
  - <sub>verified 2026-08-12: apps/web/app/_components/states/ exists on origin/main with denied-state.tsx, empty-state.tsx, error-state.tsx, loading-skeleton.tsx, locked-state.tsx and surface-state.ts. Grepped the whole of apps/web for `_components/states` and for `from '.*states/(empty|error|denied|locked|loading|surface)` — ZERO matches. Grepped each exported name (ErrorState, DeniedState, LockedState, SkeletonList): zero hits outside the folder itself. The only EmptyState hits are separate locally-declared components in admin/connection-logs/connection-logs-client.tsx:418 and explore/page.tsx. Kit created e43651b40 (2026-08-02); its only subsequent touch is the blanket contrast sweep 52c15f957 (2026-08-08) — 10 days </sub>
- **There is still no avatar maker for guests, couples or suppliers** *(medium)*
  - Everyone in the 3D room is still a randomly-coloured stranger. Nobody can make their little figure look like them, so nobody recognises themselves or anyone else when they walk the venue — which is most of the reason to walk it.
  - <sub>verified 2026-08-12: origin/main 7d7eb1a7d: grep across all of apps/web finds ZERO importers of the chibi figure component outside its own file, and ZERO writes of the guest avatar column (I ran the repo's own write-detector regex for avatar_config across every non-test .ts/.tsx → 0 writers). apps/web/lib/chibi-config.ts:167 still describes 'the future maker writes it'. Live prod: 39 guests, 0 with an avatar; 0 database functions mention avatar.</sub>
- **Two different promises about how long we keep photos — 'forever' to guests, 'deleted after 5 years' to the regulator, and nothing enforcing either** *(medium)*
  - A couple now reads one consistent promise on the privacy page: we hold the big original files for 6 months (never less than 3 months after the celebration ends), then swap them for a smaller copy, and we never delete the photo. That is now true — the job that does the swapping actually ran on 10 August. What is left is three lines on the guest-facing pages that still say 'keep them forever', when the honest version is 'free for 5 years, then we will ask you to pay to keep storing them'. A guest could be told 'forever' today and get a bill in year six.
  - <sub>verified 2026-08-12: LIVE curl https://www.setnayan.com/privacy — retention section now reads '6 months from the event's first photo (never less than 3 months after the event ends)... compressed gallery stays online, free, for 5 years — we do not delete your photos'; the old '5 years then purged / 90-day hot' text is gone. Enforcement is REAL, not just written: apps/web/lib/papic-fullres-drop.ts:718 registers claimPeriodicJob('papic-fullres-drop', WEEKLY_GAP_MS) and apps/web/app/admin/layout.tsx:134 actually calls it via after(); prod RPC claim_periodic_job(p_job_key text, p_min_gap interval) exists in pg_proc with matching arg names, and `select job_key,last_run_at from public.cron_job_runs` shows job_key='papi</sub>
- **A supplier can still only look back at their own captures during the wedding day itself** *(low)*
  - The photographer can check what they shot while the reception is still going. At midnight the view shuts. The next morning, when they actually want to confirm a shot landed, the door is closed — even though the photos are still theirs and the permission rules already let them look on any day.
  - <sub>verified 2026-08-12: origin/main 7d7eb1a7d: the only place the 'what you shot' strip is mounted is apps/web/app/vendor-dashboard/on-the-day/live/[eventId]/papic/page.tsx:87, and that same page redirects away at line 50 — `if (!booking || booking.bookedDate !== phToday()) redirect(back)`. The parent console page carries the identical gate at apps/web/app/vendor-dashboard/on-the-day/live/[eventId]/page.tsx:183. Live prod policy check (pg_policies on vendor_papic_captures): vendor_papic_captures_vendor_read is `vendor_profile_id IN (current_vendor_profile_ids()) OR is_admin()` — no date condition at all, so this is purely a screen limit. (Note: the whole route is additionally behind a counsel flag, isVendorPapicCap</sub>
- **Internal company figures readable through summary tables that the usual rules don't cover** *(low)*
  - The two leaks named are shut. A stranger can no longer read how many suppliers are stuck waiting on us, how slow support is, or how many signups came in last week — and can no longer work out how many of a supplier's finished jobs we wrote off as fake. What is left: anyone who makes an account (and accounts can be created with one tap, no email) can still read a supplier's unfiltered job count next to the public one and subtract. Harmless right now because nothing has been written off.
  - <sub>verified 2026-08-12: Live prod pg_class.relacl (2026-08-12): bottleneck_signals_current = {postgres, service_role} only — 'authenticated' gone; vendor_full_completed_events_stats = {postgres, service_role, authenticated} — 'anon' gone. Confirmed at the API layer with the live anon key: GET /rest/v1/bottleneck_signals_current → 401 42501, GET /rest/v1/vendor_full_completed_events_stats → 401 42501, GET /rest/v1/vendor_public_completed_events_stats → 200. Fixed by supabase/migrations/20271116295515_revoke_bottleneck_signals_matview_from_authenticated.sql and 20271132024116_anon_view_grants_narrow.sql (both applied). Residual is stated in that second migration verbatim: 'RESIDUAL, deliberately left: a matview canno</sub>
- **Suppliers are still told they can set how many date-holds they take, and still cannot** *(low)*
  - A busy supplier is permanently stuck at three couples holding the same date. Once three are holding it, the fourth is refused — and there is no screen where the supplier can raise or lower that, even though the rule was written assuming they could.
  - <sub>verified 2026-08-12: origin/main 7d7eb1a7d: the limit is read and enforced at apps/web/app/dashboard/[eventId]/vendors/actions.ts:1325-1337 and the comment at :1107 says 'Vendors can configure max_soft_holds_per_date on their profile (default 3, range 1-20)'. Grep across all of apps/web finds no write and no form input for it; I ran the repo's own write-detector → 0 writers. The route its own column comment names does not exist: there is no apps/web/app/vendor-dashboard/settings directory. Live prod: the column comment still reads 'Vendor adjusts in /vendor-dashboard/settings/availability (UI ships V1.x)', and both vendor rows are on the default 3.</sub>
- **The Live Photo Wall section on the couple's own website still can never have photos in it** *(low)*
  - The wall on the projector at the venue works. The copy of it on the couple's own wedding website is permanently blank, because there is no screen anywhere where anyone picks the photos for it — the section only appears once photos are in it, so nobody ever sees it.
  - <sub>verified 2026-08-12: origin/main 7d7eb1a7d: the reader is apps/web/app/[slug]/_components/editorial/data.ts:1305-1324 and it only sets photoWallActive when the stored array is non-empty. Grep of all of apps/web for that column returns readers only — I ran the repo's own write-detector for photo_wall_photos across every non-test .ts/.tsx → 0 writers. Live prod: 0 of 5 events have a non-empty array, and 0 database functions mention the column.</sub>
- **The host still cannot see who is holding which camera** *(low)*
  - On the day the couple opens the camera crew page and sees four cameras marked Claimed and two marked Open — but not one name, and no idea which friend is about to run out of shots.
  - <sub>verified 2026-08-12: Read apps/web/app/dashboard/[eventId]/studio/papic/crew/page.tsx lines 345-420 at origin/main. Each seat renders only `const claimed = Boolean(s.claimer_user_id)` → a 'Claimed' or 'Open' pill, then the generic sentence 'A friend has this seat and can shoot.' No name, no shots-left. Grepped claimer_user_id across apps/web/app and apps/web/lib: every one of the 18 non-test hits is an authorization check or a count (crew/page.tsx:149 claimedCount, studio/papic/page.tsx:505 claimLinkUnclaimed) — nothing anywhere joins a seat to a person's name for display. The separate papic-cameras-card.tsx does show shots per camera ('N of its own') but has no holder name either.</sub>
- **The old camera screen still promises 3 free cameras and tells a paying couple they are on the free plan** *(low)*
  - The serious half is fixed: someone who paid for Live Studio is no longer told on that old page that they are on the free plan. What is left is small and cosmetic — the page still opens with the sentence "You have 3 cameras free to test with" while listing eight camera slots underneath it, because the number in the sentence belongs to the old way of counting cameras and the list does not. Only reachable by an old bookmark; nothing in the app links there any more.
  - <sub>verified 2026-08-12: origin/main @7d7eb1a7d, apps/web/app/dashboard/[eventId]/studio/panood/cameras/page.tsx. FIXED half — lines 63-81 now resolve ownership with BOTH questions (`resolvePanoodTier` AND `eventSkuActive(admin, eventId, LIVE_STUDIO_SKU)`), `unlocked = castTier !== 'free' || liveStudioOwned`, read through the admin client because orders RLS is purchaser-scoped; pinned by apps/web/lib/live-studio-cast-retirement.test.ts ('a LIVE_STUDIO owner is never told they are on the free tier', which also asserts the old `{tier === 'free' &&` shape is gone). REMAINING half — line 131-136 still renders "You have {PANOOD_FREE_CAMERA_COUNT} cameras free to test with" (=3, apps/web/lib/panood-camera-seats.ts:104) fo</sub>
- **The written photo-keeping rule and the product's rule disagree — 90 days on paper, six months in the app** *(low)*
  - The main rows were fixed: the register and the adopted privacy manual now describe the real rule (big originals for six months, floored at three months after the celebration ends, then a smaller copy, nothing deleted). But two side rows still point at the old '90 days then purge' rule, and every document in the downloadable pack still carries it in full. So we would still be telling a regulator one number while doing another.
  - <sub>verified 2026-08-12: CORRECTED in source: 02_Records_of_Processing_Activities_DRAFT_2026-07-05.md line 118 (DPS-05 Retention) and 01_Privacy_Manual_ADOPTED_2026-07-24.md line 200 both now read 'the later of 6 months from the event's first capture and 3 months after the event date, then replaced by a compressed web copy... Nothing is deleted at 5 years', each carrying an explicit '⚠ CORRECTED 2026-08-07' note. STILL WRONG: same ROPA file line 283 (DPS-16 Papic Shared Pool) 'Follows DPS-05 (90 days hot + 5 years IA cold)' — a cross-reference to a rule DPS-05 no longer states; and NPC_Compliance/01_Privacy_Manual_DRAFT_2026-07-05.md line 180 'Photo/video originals | 90 days hot → 5 years cold | 5-year hard limit'. </sub>

## ⏳ BLOCKED ON SOMEONE OUTSIDE

*Nothing in the product is waiting on engineering; someone external has to reply.*

**3 items**

- **Child-abuse image matching: the code now ships, but no provider has taken us on, so nothing is checked** *(medium)*
  - Photos that merely look explicit are still screened automatically — that has always worked and still does. What we cannot do is recognise a photo already confirmed by authorities as child-abuse material, because that requires an outside organisation to accept us and hand over their list. Nothing in the product is waiting on engineering any more; the plumbing is now built, merged and live, and it honestly records 'nothing was checked' rather than pretending a check passed.
  - <sub>verified 2026-08-12: Correction to the register's own evidence: the hook is NO LONGER a draft PR — apps/web/lib/known-hash-match.ts is on origin/main, landed commit edbf45967 (2026-08-01), and is wired into the shared upload path at apps/web/lib/nsfw-screen.ts:290-310. But apps/web/lib/known-hash-match.ts:312 resolveKnownHashProvider() returns null unconditionally, and apps/web/lib/known-hash-match-flag.ts:23 requires CSAM_HASH_MATCH_ENABLED==='true' (default off). Prod SQL: select count(*) from media_hash_checks → 0 rows, i.e. not a single object has been checked. Remaining steps are enrolment with PhotoDNA/NCMEC/IWF and signing the NPC Circular 16-02 processor agreement — both contracts, not code.</sub>
- **Nobody has ruled on whether YouTube's terms allow hosting paying customers' weddings on our own channels** *(low)*
  - The question has still never been answered by anyone. But it is no longer the thing the whole product rests on: you already decided last month that weddings stream on the couple's own YouTube channel, not on Setnayan's, precisely to avoid this question. It only comes back if you switch to putting everyone on Setnayan channels — and nothing is switched that way today.
  - <sub>verified 2026-08-12: No answer recorded anywhere: grep for the ToS question across DECISION_LOG.md and the corpus returns only the original 2026-07-25 row and the register's own restatement in WHAT_IS_LEFT_2026-08-05.md:222. THE REGISTER'S FRAMING IS STALE, and I checked the source it came from: DECISION_LOG.md:2661 (2026-07-25) reads 'LIVE STUDIO CHANNEL MODEL LOCKED — the broadcast goes on the COUPLE'S OWN YouTube channel; the "Setnayan-owned channel pool" is RETIRED as the default… It also sidesteps an unresolved YouTube API Services ToS question'. So the claim 'the owner-locked pool model depends entirely on this' is no longer true. Code matches the lock: apps/web/app/api/oauth/youtube/start/route.ts keeps t</sub>
- **The zero-cost YouTube route is still blocked by the Google account suspension** *(low)*
  - Still stuck with Google, and nothing on our side can move it. The company Google account was suspended before anyone got into the admin console, so the one thing we wanted to check — whether YouTube is even available to that kind of account — is unanswered rather than answered no. The appeal is still sitting there. Everything else on this list can be finished while it waits.
  - <sub>verified 2026-08-12: The appeal's own verification record is still live in DNS, which I checked directly rather than reading a note: `dig +short 73857927.setnayan.com CNAME` returns google.com. on both 8.8.8.8 and the authoritative nameserver ns09.domaincontrol.com. No resolution recorded anywhere in the corpus: the newest mentions are DECISION_LOG.md:2859 (2026-07-30, the suspension row), :2882 (2026-07-31) and :2978 (2026-08-03), plus WHATS_NEXT_INDEX.md:367 and CLAUDE.md:162, all still describing it as blocked; a grep for 'reinstat' across the corpus returns only the two forward-looking lines in WHATS_NEXT_Live_Studio_2026-07-29.md. No related code has landed: `git log --since=2026-08-06` over the live-studio</sub>

## How to use this

Source-of-truth order is unchanged: **live site → shipped code at `origin/main` → live prod DB →
this file → anything older.** This file is five days fresher than the register it corrects, and it
will rot at the same rate. **Re-verify before acting on any line.**
