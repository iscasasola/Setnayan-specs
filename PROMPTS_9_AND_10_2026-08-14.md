# Sessions 9 and 10 — refreshed 14 August 2026

Both **run alone**. Paste the shared header from
[`WEBSITE_ADJUSTMENT_PROMPTS_2026-08-14.md`](WEBSITE_ADJUSTMENT_PROMPTS_2026-08-14.md) above each.

**Rewritten today** — the originals were drafted before ten PRs landed, and both of their
dependency lines are now wrong in the safe direction (things they waited on are done) and one is
wrong in the *unsafe* direction (session 9 has a second blocker nobody had noticed).

| | gate | state |
|---|---|---|
| **9 · retire the old sidebar** | slices 1·2·3 merged **and** the one-top-bar PR #4438 merged | 1·2·3 ✅ · #4438 armed, one check left |
| **10 · the lock that asks the supplier** | none — but **re-plan before building** | ready whenever |

---

# SESSION 9 · Retire the old sidebar
### ⛔ DO NOT START until PR #4438 is merged. Confirm with `git merge-base --is-ancestor`.

```
Retire `app/_components/nav/sidebar-shell.tsx` now that all five trees wear the shared rail.

WHAT A PERSON GETS: nothing. Zero visible change is the SUCCESS CONDITION here. If anything
moves on screen, you removed something that was still doing a job.

🔴 IT STILL HOLDS ONE JOB, AND IT IS NOT THE OBVIOUS ONE.
Its `<main>` carries `.sn-vt-page` — THE ONLY ELEMENT IN THE APP with
`view-transition-name: sn-page`. The mobile bottom-nav carousel slide freezes the document
around exactly that element, and the docked sub-nav keys its padding off it. It wraps content
at ALL WIDTHS, not just desktop. Delete it without rehoming that and the PHONE CAROUSEL
BREAKS while every desktop test stays green.
⇒ FIRST give `.sn-vt-page` a new owner and prove the phone still slides. THEN delete.
(#4438 took the OTHER job — the sticky hide-on-scroll top bar — off it. That is why this
session was blocked until now, and why it is unblocked the moment #4438 lands.)

COUNT BEFORE YOU CUT. Earlier counts of "16 consumers" and "20 mounts" were STRING HITS IN
COMMENTS, CSS AND TESTS — there were three real mounts. Print the count of real imports
before and after, and do not trust a grep that matches a filename.

ALSO DELETE, only once nothing references them: the collapse-state localStorage key, and the
Atelier glass styles with no consumers.

⛔ DO NOT DELETE, both named deliberately in the Marketplace plan as NOT sliced:
- the legacy `PlanBudgetAccordion` — it is the `BUDGET_BUILD_ENABLED` kill-switch path.
- the Explore replan flag — it gates an entire shipped wave, not two labels.

TRAPS:
- A deleted component with a live test is a RED CI, not a silent pass. Run the full suite.
- `npx tsx --test "app/dashboard/[eventId]/..."` prints "# tests 0 … # fail 0" and runs
  NOTHING — the brackets are a glob character class. A green from that command is meaningless.
- Removing a file can strand a CSS class that nothing then defines. Grep the stylesheets for
  every class the deleted component owned.
- 🔑 SIXTH GATE WITH NO HANDLE, FOUND 2026-08-14 AND CREATED BY A DELETION: `HomeReskin.tsx`
  was deleted with the cinematic homepage and took with it the ONLY five call sites that
  could open the Papic / Live Studio / 3D Plan demos. The demos, their transport, their QR
  minting and their landing pages all survived, switched on, unreachable. CI was green.
  ⇒ FOR EVERY EXPORT THE DELETED FILE OWNED, ASK WHAT OPENED IT — not just what imported it.
  `gates-have-handles.test.ts` cannot see this shape: it watches a COLUMN with no writer,
  and this is a MOUNTED COMPONENT WITH NO OPENER.

STOP AT: the deletion. No behaviour change should be observable by anyone.
```

---

# SESSION 10 · A lock that asks the supplier
### 🔴 THE BIG ONE. Runs alone. **Plan before you build.**

```
Build PR-H, the vendor-agrees step. Spec:
~/Documents/Claude/Projects/Setnayan/PR_H_Lock_Request_Handshake_BUILD_SPEC_2026-08-04.md.
All four owner answers are already in it. BUILD FROM THAT SPEC ONLY.

WHAT HAPPENS TODAY, AND WHY IT IS WRONG: a couple presses Lock and the supplier is BOOKED
OUTRIGHT — while the words around the button promise the supplier agrees first. Owner ruled
2026-07-27 that a lock is a REQUEST. Handshake steps 1, 3, 4 and 5 all ship. STEP 2 DOES NOT
EXIST. `build-locked.tsx` carries the literal empty-slot comment where its tracker belongs.
This is the ONE functional hole on the whole Marketplace surface — everything else there is
presentation.

🔴 DO NOT START CODING. The spec's own adversarial review left 14 HIGH plan defects open, and
the FIRST is that the vendor cannot reach the page the agree card was specced onto. RE-PLAN
THOSE 14 FIRST — use Fable — and report the revised plan before writing any code.

TRAPS:
- ONE LOCK PATH: `AccordionLockButton` → `finalizeVendor`. Every gate rides it. Do not add a
  second — two paths is two answers to one question.
- `max_soft_holds_per_date` is ENFORCED at lock and HAS ZERO WRITERS, and the vendor-settings
  route its own column comment names does not exist. Do not surface "N holds left" anywhere
  until a writer exists. That is a gate-with-no-handle sitting on this exact path — the class
  has now been found SIX times, most recently on 2026-08-14.
- A FORWARD PRIMITIVE WITH NO INVERSE: whatever this writes, ask at write time what un-does
  it. An auto-block with no counterpart once left a vendor reading BUSY to everyone, forever,
  and the waitlist built for that moment could only send couples to a date they still could
  not book.
- THE ROW IS YOURS, THE FIELD IS NOT. The couple's request and the vendor's answer are two
  sides of one state machine. Constrain it in the POLICY on **both INSERT and UPDATE** — a
  guard attached to one verb is not a guard, and a PERMISSIVE `FOR ALL` policy admits INSERT
  and DELETE too. Delete-then-reinsert has defeated a correct-looking guard here before.
- READ THE COLUMN DEFAULT BEFORE YOU REVOKE. A revoke on a column whose default is the
  privileged value ships silent universal auto-approval — worse than the bug.
- Prod is PRE-LAUNCH EMPTY. "The table is empty" proves nobody has used it, never that
  nothing can write it. Trace every writer.

STOP AT: the handshake. Deposit-at-lock stays flag-dark — a separate owner call.
```
