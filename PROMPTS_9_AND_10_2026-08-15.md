# Sessions 9 and 10 — re-verified 15 August 2026

> # 🛑 STOP — THIS FILE WAS OBSOLETE WITHIN HOURS OF BEING WRITTEN
>
> **SESSION 9 IS DONE. DO NOT RUN IT.** PR
> [#4451](https://github.com/iscasasola/setnayan-platform/pull/4451) —
> *"retire SidebarShell — the last two trees take over its three remaining jobs"* — **merged
> 2026-08-15T10:08:18Z**. Verified: `git show origin/main:apps/web/app/_components/nav/sidebar-shell.tsx`
> → **file does not exist.** Running the block below would ask someone to delete a file that is
> already gone.
>
> **SESSION 10 IS STARTED, NOT WAITING.** PR
> [#4470](https://github.com/iscasasola/setnayan-platform/pull/4470) — *"the supplier's yes is what
> makes the booking (PR-H slice A, flag-dark)"* — **merged 2026-08-15T16:00:32Z**, and
> [#4459](https://github.com/iscasasola/setnayan-platform/pull/4459) fixed the promise copy.
> Re-measured at the new head: `vendor_agree_to_lock` now has **3** app callers,
> `vendor_decline_lock` **2**, `lock_request_state` **4** — the gate-with-no-handle described below
> is **CLOSED**. Still open: `cancel_vendor_lock_request` has **0** callers, and
> `LOCKED_STATUS = 'contracted'` still stands at `vendors/actions.ts:638` because slice A is
> **flag-dark** — so a couple pressing Lock today still books the supplier outright.
> ⇒ **Re-scope session 10 against the current head before using the block below.** Its "half of it
> is already built with zero callers" premise is now wrong in the safe direction.
>
> ## 🔑 WHY THIS FILE ROTTED IN SIX HOURS — the lesson is worth more than the file
>
> The audit behind it pinned a read-only worktree to `784c41139`, the commit production served at
> **07:54Z**, and every measurement in it is correct **for that commit**. #4451 merged at 10:08Z and
> #4470 at 16:00Z. **A snapshot is only evidence about the moment it was taken**, and 20+ PRs merged
> between the snapshot and delivery.
>
> 🔴 **Worse: the answer was already written down and went unread.** The auto-loaded memory index
> carried the line *"`SidebarShell` IS DELETED — PR #4451, 2026-08-15"* the whole time. The audit
> was run against a frozen checkout instead of reconciled against what was already known.
> ⇒ **Re-fetch `origin/main` and re-check every "still open" claim immediately before you deliver
> it — not when you start.** Pin a worktree for *reading code*, never for *asserting current state*.
>
> Full register: [`WHAT_IS_LEFT_VERIFIED_2026-08-15.md`](WHAT_IS_LEFT_VERIFIED_2026-08-15.md)
> (carries the same correction banner).

> **Supersedes [`PROMPTS_9_AND_10_2026-08-14.md`](PROMPTS_9_AND_10_2026-08-14.md)**, which is now
> wrong in three ways: session 9's gate is CLOSED, its size is overstated by ~8×, and **its most
> alarming trap is FALSE** and would send a session hunting a bug that does not exist.
>
> Both **run alone**. Paste the SHARED HEADER from
> [`WEBSITE_ADJUSTMENT_PROMPTS_2026-08-14.md`](WEBSITE_ADJUSTMENT_PROMPTS_2026-08-14.md) above each
> block, with one correction: **`origin/main` is now `784c41139`, which is exactly what production
> serves** (`/api/health` → `"version":"784c411"`), not `3ca1af296`.

| | gate | state 2026-08-15 |
|---|---|---|
| **9 · retire the old sidebar** | #4438 merged | ✅ **OPEN — unblocked 2026-08-14T15:59:40Z, unstarted.** Much smaller than the old brief |
| **10 · the lock that asks the supplier** | none | **Ready — but RE-PLAN FIRST.** Half of it is already live in the database with zero callers |

**Everything below was measured by hand on 2026-08-15**, not carried over from the previous draft.

---

# SESSION 9 · Retire the old sidebar

### ✅ THE GATE IS CLOSED. PR #4438 merged 2026-08-14T15:59:40Z. Start whenever.

```
Retire `apps/web/app/_components/nav/sidebar-shell.tsx` now that all five trees wear the
shared rail.

WHAT A PERSON GETS: nothing. ZERO VISIBLE CHANGE IS THE SUCCESS CONDITION. If anything moves
on screen, you removed something that was still doing a job.

📏 IT IS SMALLER THAN YOU HAVE BEEN TOLD. Measured 2026-08-15 at origin/main 784c41139:
281 lines, and EXACTLY TWO real imports —
  apps/web/app/dashboard/[eventId]/layout.tsx:18
  apps/web/app/vendor-dashboard/layout.tsx:13
28 files contain the string. Earlier briefs said "16 consumers" and "20 mounts"; those were
string hits in comments, CSS and tests. BOTH real imports already pass `desktopRailExternal`,
so the entire sidebar branch inside the component is UNREACHABLE today. You are deleting dead
chrome plus one live job — see below.
⇒ Print the real-import count before and after. Do not trust a grep that matches a filename.

🔴 THE ONE LIVE JOB IT STILL HOLDS, AND IT IS NOT THE OBVIOUS ONE.
Its `<main>` carries `.sn-vt-page` — THE ONLY ELEMENT IN THE APP with
`view-transition-name: sn-page` (apps/web/app/globals.css:2134). The mobile bottom-nav
carousel slide freezes the document around exactly that element, and the docked sub-nav keys
its padding off it. It wraps content AT ALL WIDTHS, not just desktop. Delete it without
rehoming that and THE PHONE CAROUSEL BREAKS while every desktop test stays green.

✅ THE PATTERN IS ALREADY IN THE REPO — COPY IT, DO NOT INVENT ONE.
The admin tree already did this exact migration. apps/web/app/admin/layout.tsx:360 rehomes it
onto a plain wrapper in the layout itself:

    <div className="sn-vt-page pb-[calc(env(safe-area-inset-bottom)+92px)] lg:pb-0">
      {children}
    </div>

Its docblock at :71 explains the landmark + slide rule. Do the same in the two remaining
layouts, prove the phone still slides, THEN delete the component.

🛡 FOUR GUARDS WILL GO RED, AND THAT IS THEIR JOB — THEY EXIST TO STOP A CARELESS DELETE.
Confirmed by hand 2026-08-15:
  apps/web/app/dashboard/[eventId]/_components/one-shell-event-rail.test.ts:98
      'SidebarShell is still mounted — dropping it breaks the PHONE, not the desktop'
      …and :123, which asserts it renders `.sn-vt-page` EXACTLY ONCE, unconditionally
  apps/web/app/_components/frontdoor/one-main-per-page.test.ts:144
      'SidebarShell still carries the landmark for the trees that delegate to it'
  apps/web/app/_components/frontdoor/one-top-bar.test.ts  (several assertions)
  apps/web/app/vendor-dashboard/_components/vendor-rail-context.test.ts:262
      'SidebarShell is STILL mounted, standing its rail down rather than leaving'
⇒ REWRITE each to assert the NEW owner of `.sn-vt-page` — exactly one per tree, unconditional.
   DO NOT delete an assertion to go green. The rule they protect survives the component.
   apps/web/app/admin/_components/admin-rail-context.test.ts:231 shows how admin rewrote its
   equivalent after its own migration. Copy that shape.

⛔ THE "UNREACHABLE DEMOS" TRAP IN THE PREVIOUS PROMPT IS FALSE. DO NOT CHASE IT.
The 2026-08-14 draft claimed deleting HomeReskin.tsx orphaned the only five call sites that
could open the Papic / Live Studio / 3D Plan demos, leaving them switched on and unreachable
— billed as the "sixth gate with no handle". RE-MEASURED ON THE LIVE SITE 2026-08-15: the
demos render and work. "Try it now with a friend" on the live Papic page opens the two-QR
overlay. The original measurement was taken in a browser tab that never painted — a scheduled
frame callback never fired (rafFired:false, vis:hidden), so the page was reported blank
because nothing had drawn yet, not because nothing was there.
🔑 A MEASUREMENT TAKEN IN A TAB THAT NEVER PAINTED IS NOT A NEGATIVE RESULT. Before reporting
   anything absent from a rendered page, prove the page rendered.
(The genuine sixth gate-with-no-handle is session 10's — see below.)

ALSO DELETE, only once nothing references them: the collapse-state localStorage key, and the
Atelier glass styles with no consumers. Grep the stylesheets for every class the deleted
component owned — removing a file can strand a CSS class that nothing then defines.

⛔ DO NOT DELETE, both named deliberately as NOT sliced:
- the legacy `PlanBudgetAccordion` — it is the `BUDGET_BUILD_ENABLED` kill-switch path.
- the Explore replan flag — it gates an entire shipped wave, not two labels.

TRAPS:
- A deleted component with a live test is a RED CI, not a silent pass. Run the FULL suite.
- `npx tsx --test "app/dashboard/[eventId]/..."` prints "# tests 0 … # fail 0" and runs
  NOTHING — the brackets are a glob character class. A green from that command is meaningless.
- pnpm build CANNOT run on this machine. CI is the only valid build claim.

STOP AT: the deletion. No behaviour change should be observable by anyone.
```

---

# SESSION 10 · A lock that asks the supplier

### 🔴 THE BIG ONE. Runs alone. **Re-plan before you build — the ground has moved under the spec.**

```
Build PR-H, the vendor-agrees step. Spec:
~/Documents/Claude/Projects/Setnayan/PR_H_Lock_Request_Handshake_BUILD_SPEC_2026-08-04.md.
All four owner answers are in it.

WHAT HAPPENS TODAY, AND WHY IT IS WRONG: a couple presses Lock and the supplier is BOOKED
OUTRIGHT — while the words around the button promise the supplier agrees first. Owner ruled
2026-07-27 that a lock is a REQUEST. There is no screen anywhere on which a supplier can
agree or decline. This is the ONE place the product says something untrue, and the one
functional hole on the whole Marketplace surface — everything else there is presentation.
  apps/web/app/dashboard/[eventId]/vendors/actions.ts:625  LOCKED_STATUS = 'contracted'
  …written at :1430 by finalizeVendor
  the promise copy: apps/web/lib/explore-info-copy.ts:55

🚨 READ THIS BEFORE THE SPEC — HALF OF PR-H IS ALREADY BUILT AND LIVE IN PRODUCTION, WITH
   ZERO CALLERS. THE SPEC DOES NOT KNOW THIS AND IS WRONG ABOUT SCOPE BECAUSE OF IT.
Migration 20271107090000_vendor_agrees_to_lock.sql is APPLIED. In prod right now:
  · event_vendors carries NINE lock_* columns
  · vendor_agree_to_lock / vendor_decline_lock / cancel_vendor_lock_request all EXIST and are
    EXECUTE-granted to `authenticated`
  · trigger event_vendors_guard_lock_handshake is ATTACHED and ENABLED
  · 45 rows, every single lock_request_state NULL
Measured 2026-08-15: a repo-wide grep for all four symbols returns ZERO app files. The state
machine, its guard and its permissions are waiting for a caller that was never written.
This is the SIXTH gate-with-no-handle. It is also good news — you are wiring, not authoring.
⇒ FIRST establish what the shipped trigger already enforces and what the functions already
   refuse. Do NOT re-derive a state machine that exists. Do NOT write a second one beside it.

🔴 DO NOT START CODING. The spec's own adversarial review left 14 HIGH plan defects open.
DEFECT #1 IS CONFIRMED AGAINST PROD 2026-08-15: the vendor cannot reach the page the agree
card was specced onto — they are not permitted to open it until they are already booked, which
is the state the card exists to prevent. THE OTHER 13 WERE NOT RE-CHECKED; treat them as
unverified in both directions.
⇒ RE-PLAN ALL 14 FIRST — use Fable — against origin/main 784c41139 and the live DB, WITH the
   already-live schema above as an input. Report the revised plan before writing any code.

TRAPS:
- ONE LOCK PATH: `AccordionLockButton` → `finalizeVendor`. Every gate rides it. Do not add a
  second — two paths is two answers to one question.
- `max_soft_holds_per_date` is ENFORCED at lock (one reader,
  apps/web/app/dashboard/[eventId]/vendors/actions.ts:1325) and HAS ZERO WRITERS; both prod
  vendors sit at the default 3, and the vendor-settings route its own column comment names
  does not exist. Do not surface "N holds left" anywhere until a writer exists — that is a
  seventh gate-with-no-handle sitting on this exact path.
- A FORWARD PRIMITIVE WITH NO INVERSE: whatever this writes, ask AT WRITE TIME what un-does
  it. An auto-block with no counterpart once left a vendor reading BUSY to everyone, forever,
  and the waitlist built for that moment could only send couples to a date they still could
  not book. A request that can be made must be withdrawable, expirable, and reversible.
- THE ROW IS YOURS, THE FIELD IS NOT. The couple's request and the vendor's answer are two
  sides of one state machine. Constrain it in the POLICY on BOTH INSERT AND UPDATE — a guard
  attached to one verb is not a guard, and a PERMISSIVE `FOR ALL` policy admits INSERT and
  DELETE too. Delete-then-reinsert has defeated a correct-looking guard here before.
- READ THE COLUMN DEFAULT BEFORE YOU REVOKE. A revoke on a column whose default is the
  privileged value ships silent universal auto-approval — worse than the bug. On such a
  column, "the forgery is refused" proves nothing: assert that an insert naming nothing reads
  back the SAFE value.
- Prod is PRE-LAUNCH EMPTY. "The table is empty" proves nobody has used it, NEVER that nothing
  can write it. Trace every writer.
- A rejected query is not a thrown error. A phantom column, enum value or function argument
  gets the call REJECTED, not thrown — the only symptom is an absence.
- Verify the migration reached production BY QUERYING THE OBJECT, never schema_migrations.

⚖ RELATED, DELIBERATELY NOT YOURS: a supplier currently cannot withdraw their own
VERIFICATION application (a different state machine — the UPDATE policy admits only
draft/pending_review while the action writes 'withdrawn'). It is one policy line but widening
a policy is a product call, and it is question 25 to the owner. Do not fold it in.

STOP AT: the handshake. Deposit-at-lock stays flag-dark — a separate owner call.
```

---

## What changed from the 14 August draft, and why

| Line in the old prompt | Status |
|---|---|
| "⛔ DO NOT START until PR #4438 is merged" | **Gate closed** 2026-08-14T15:59:40Z |
| "#4438 armed, one check left" | Merged |
| Session 9 is blocked | **Unblocked** |
| "16 consumers" / "20 mounts" | **2 real imports.** The rest are comments, CSS, tests |
| "the demos survived, switched on, unreachable — sixth gate with no handle" | **FALSE.** Demos work; the measurement came from an unpainted tab |
| Session 10 "STEP 2 DOES NOT EXIST" | True of the APP. **The database half is fully live with zero callers** — the spec is wrong about scope |
| PR-H defect #1 unverified | **Confirmed against prod 2026-08-15** |
| origin/main 3ca1af296 | Now **784c41139** = what prod serves |

---

*Re-verified 2026-08-15 against the live site, `origin/main` 784c41139, the production database
and GitHub. Import counts, guard line numbers, the admin rehoming pattern and the applied
migration were each re-measured by hand. Full register:
[`WHAT_IS_LEFT_VERIFIED_2026-08-15.md`](WHAT_IS_LEFT_VERIFIED_2026-08-15.md).*
