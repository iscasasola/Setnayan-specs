# Event Overview — the VERIFIED delta against shipped code

> Produced 2026-08-08 by READING THE SHIPPED PAGE, not the spec. The handoff is a
> restyle-and-extend; this file records which blocks are which, so nobody rebuilds
> something that already works.

**0 already match · 4 need restyle · 2 genuinely new** (of 6 blocks)

## Per-block verdict

### ① Event header — `ships_needs_restyle`

- **insert point valid:** False
- **evidence:** /Users/icecasasola/wt-win/apps/web/app/dashboard/[eventId]/layout.tsx:321 (plaqueName), :312 (eventPlaqueMeta), :338-339 (topBar div is `justify-end` — badges only, NO title); /Users/icecasasola/wt-win/apps/web/app/_components/nav/sidebar-shell.tsx:122 (sidebar is `hidden lg:flex`); /Users/icecasasola/wt-win/apps/web/app/_components/account-switcher/account-switcher.tsx:113 (href="/dashboard" — the Home exit that already exists); /Users/icecasasola/wt-win/apps/web/app/dashboard/[eventId]/_components/event-dashboard.tsx:1034 (focalDateLabel), :1392-1412 (the countdown numeral)
- **delta:** The design's supporting claim is WRONG and will mislead the builder. layout.tsx does NOT render a 'sticky plaque header' carrying the title — the plaque (plaqueName + eventPlaqueMeta) is the SIDEBAR header (DoorwaySidebarHeader → SwitcherPlaqueTrigger), which is `hidden lg:flex`, i.e. INVISIBLE ON MOBILE, the exact viewport this widget is drawn for. layout.tsx's own comment says the old `sticky top-0 z-20 backdrop-blur` wrapper was RETIRED and SidebarShell owns it now; its topBar is `justify-end` badges. What ships vs. spec: TITLE ships but desktop-sidebar-only, needs a mobile home. META ships as "Wedding · Dec 12" (type + short date, no year) and must become "127 days · 12 Dec 2026 · Tagaytay". DATE FORMAT differs everywhere — layout emits "Dec 12", the focal emits "Friday, December 18" (Intl weekday/long-month, no year); neither is "12 Dec 2026". VENUE is unavailable — layout.tsx's events select carries no venue_name/region, so a new column read is required. BACK ARROW does not exist in any form. Restyle in place; do not build a second header.
- ⚠ **risk:** 🚨 THE DAY COUNT IN THE HEADER COLLIDES WITH A SHIPPED, DELIBERATE FIX. event-dashboard.tsx:1338 and :1352 state the rule — "One home per number (rollout § 3.1): the countdown lives in the focal" — and record WHY: a duplicate countdown previously let the focal and a second tile disagree about whether a date was even set (owner screenshot, 2026-07-15). Putting "127 days" in the header re-creates that duplicate, and the two would derive from DIFFERENT predicates: layout.tsx has no `event_date_precision` in its select, so a month/year placeholder date would print a confident day count in the header while the focal correctly says "Narrow to a single day to start your countdown." Get an explicit ruling on the day count; date + venue + back arrow are safe. Second risk: the back arrow is spec'd as "the ONLY exit to Home" but the AccountSwitcher exit stays mounted — two exits, or delete one, is an undecided question.

### ② "Needs you this week" — decision rows — `ships_needs_restyle`

- **insert point valid:** False
- **evidence:** /Users/icecasasola/wt-win/apps/web/app/dashboard/[eventId]/_components/event-dashboard.tsx:1571-1626 (the "Decisions · waiting on you" digest tile: openDecisionCount + flatDecisions.slice(0,3) + "All N decisions ↗"), :862 (flatDecisions), :787-846 (DecisionItemView build; payItems.chip already carries formatPeso money at :815), :999-1007 (chipToneStyle hot/warm/calm), :1692 (the full <section id="decisions"> board), /Users/icecasasola/wt-win/apps/web/app/globals.css:3088-3099 (.sn-eye = 700 / 0.14em / uppercase / gold-700 #8A6B39)
- **delta:** The named insert point is stale: page.tsx contains NO section array and no decisions markup. The digest lives in event-dashboard.tsx:1571; page.tsx only passes `slotAfterBento` (page.tsx:532). Build there, not in page.tsx. Changes: (a) add the 8px dot — the tone already exists per row as `chipTone`, but shipped colours are hot #B77E2E amber / warm #8A6B39 gold / calm #4E6C82, vs the spec's #C24E25 / #A9834B / #3B4E67, so it is a token remap not a new field; (b) surface the MONEY — already computed (payItems.chip = formatPeso(...) + ' pending') but the digest renders only label+sub+CTA and drops `chip`, so the peso figure ships TODAY only on the full board; (c) collapse label+sub into the one-line row contract; (d) swap the CTA pill for a → chevron; (e) reuse `.sn-eye` for the eyebrow — already 700/0.14em/uppercase/#8A6B39 vs the spec's 600/0.16em, so write no new CSS; (f) the CONDITIONAL lifetime nearly holds — `flatDecisions.length > 0` gates the rows, but the zero case still renders the tile with a sentence instead of hiding the section.
- ⚠ **risk:** ⛔ TWO STANDING LOCKS ARE ALREADY BROKEN IN THIS SHIPPED BLOCK. (1) GOLD IS A BUTTON: event-dashboard.tsx:1600-1605 renders the first decision's CTA as a FILLED pill `background: var(--sn-gold-500)` = #A9834B (globals.css:2526, verified by arithmetic) with cream #FFFDF8 text. Same violation at :1748 ("PRIORITY N"), :1783-1789 (board row CTAs) and :1974 (Today's one thing). The chevron removes it from the digest and leaves three gold buttons on the same page. (2) TAP TARGET: that pill is `py-1.5 text-[11.5px]` ≈ 27px tall, under the 44×44 floor. (3) The spec's dot is colour-ONLY with no text label — the shipped chip carries WORDS ("pick one", "not booked yet", "₱X pending"), so trading chip for dot is an accessibility regression unless the sentence absorbs the meaning.

### ③ "Where you stand" — four counters — `ships_needs_restyle`

- **insert point valid:** False
- **evidence:** /Users/icecasasola/wt-win/apps/web/app/dashboard/[eventId]/_components/event-dashboard.tsx:1131 (const miniTiles), :1132-1160 (Guests), :1161-1200 (Budget), :1201-1230 (Schedule), :1231-1272 (Papic), :1275-1300 (Messages), :1318-1319 (const MAX_MINIS = 4 + hard truncation), :1632-1633 (`grid grid-cols-2` render), :681 (lockedVendorCount already computed), :1956 ("N of M booked" ALREADY RENDERED on the "Your team" card); /Users/icecasasola/wt-win/apps/web/app/dashboard/[eventId]/checklist/page.tsx:102 (doneCount exists — only there, never on Overview)
- **delta:** Same stale insert point as ②: miniTiles is in event-dashboard.tsx:1131, not page.tsx. The 2×2 grid, gold eyebrows, Space Mono values, per-tile hub links and sn-tile chrome ALL ship — rebuilding the grid is the expensive error. Per counter: GUESTS ships, restyle only ("86 attending of 141 invited" → "86/141 guests confirmed"; the spec also drops a live 46px ProgressRing). BUDGET ships, restyle — formatPeso emits "₱312,000" and the spec wants "₱312k/480k", needing a new abbreviating formatter; the spec also drops the gold progress bar. VENDORS BOOKED — the number is already computed (:681) and already rendered (:1956), so this is a move/duplicate, not new data. CHECKLIST % — genuinely absent from Overview, no reader on this page at all; would need doneCount/rows.length lifted out of checklist/page.tsx:102. The spec also silently drops the live SCHEDULE, PAPIC and MESSAGES tiles.
- ⚠ **risk:** 🚨 THREE HARD COLLISIONS, none cosmetic. (1) MAX_MINIS = 4 IS AN OWNER RULING. The comment at :1303-1317 is explicit: the blur budget is "focal(1) + digest(1) + ≤4 minis + chrome(2) ≤ 8" because backdrop-filter is the expensive part, AND the owner locked 2026-07-30 that "Papic ALWAYS HOLDS A SLOT… since that is the foundation of the app." Four fixed counters evict Papic. Do not raise the cap or drop Papic without the owner. (2) ONE-HOME-PER-NUMBER again: a "checklist done %" tile sits beside the focal's "% planned" gold bar (:1450-1458, the vendor-categories-locked share) — two different percentages of "done" on one screen — and "vendors booked" duplicates :1956 verbatim. (3) THE HOLLOW-DASH LOCK IS NOT SATISFIED AND CANNOT BE BOLTED ON. Every tile today is gated on `stats.total > 0`, `committedCentavos > 0`, etc., so a failed or empty read makes the tile VANISH — which reads as "you have nothing", not "not measured". That is the `count === null` ≠ zero trap in a new costume. Fixed-position counters MUST distinguish loaded-zero from failed-to-load, and the current readers return plain numbers with no not-measured value — a signal change, not a render tweak.

### ④ Add-ons rail — Setnayan services — `genuinely_new`

- **insert point valid:** True
- **evidence:** SEARCHED, FOUND NOTHING on Overview: `grep -n overflow-x` across /Users/icecasasola/wt-win/apps/web/app/dashboard/[eventId]/_components/event-dashboard.tsx returns ONE hit, :1828 — the AI-only "What's next" deadline rail, not commerce. No "from ₱" string, no ADD_ONS render, no art band in the whole 2192-line file (ADD_ONS imported at :64 is used only by serviceLabel() at :127 to NAME an order row). The only services surface is the "Your services" ExpandCard at :2038-2082, which lists PLACED ORDERS and links to /orders — it sells nothing. Reuse claim disproved: `grep -rn "ServicePoster"` across the whole app returns 3 hits and ZERO JSX consumers — the definition (/Users/icecasasola/wt-win/apps/web/app/dashboard/[eventId]/studio/_components/service-poster.tsx:89), a `export type { PosterStyle }` re-export (studio/page.tsx:38) and a comment (lib/add-ons-catalog.ts:9). The shipped Studio hub uses StudioAppRow + StudioFeaturedCard (studio/page.tsx:13-14).
- **delta:** The rail does not exist and would be a from-scratch build. `slotAfterBento` is real (page.tsx:532 → event-dashboard.tsx:1681-1683) and lands exactly where the design asks — immediately after the top grid (③), before the Decisions board — and it is the PROVEN pattern here: SetDateNudge and PapicReadyNudge already ride it as dismissible one-band nudges. If anything ships, ship a nudge in that slot, not a permanent commerce band.
- ⚠ **risk:** 🔴 RECOMMEND NOT BUILDING IT — the design's own justification does not survive contact with the code. (1) "REUSE, ZERO NEW CSS" IS FALSE: ServicePoster has zero consumers, it is dead code the Studio already moved off, so "reusing" it means reviving a retired component — and the spec's 150×72px art-band card is nothing like its 4:5 cinema poster. New CSS either way. (2) THE PRICE SOURCE IS WRONG: the spec says "prices read live from /pricing", but lib/add-ons-catalog.ts:107-115 states in terms that it is "never a price source"; live prices come from `platform_retail_catalog_v2` (studio/page.tsx:125-126, :198-199). Hardcoding "from ₱2,500" produces a dead number the moment the owner reprices — the exact failure the corpus bans. (3) THE ROUTE CLAIM IS OFF: /dashboard/[eventId]/launch is real but it is the DAY-OF launch hub ("Go live", "Hand out cameras"), not the add-ons store; the store is /studio. (4) The corpus answer file (Design_Warm_Editorial_Archive_2026-08-08/OPEN_QUESTIONS_ANSWERED_2026-08-08.md — confirmed present) already recommends NO rail on Overview and Q2 is still open. Treat as blocked on the owner, not buildable.

### ⑤ "Meanwhile" — memory arrival — `genuinely_new`

- **insert point valid:** False
- **evidence:** SEARCHED AND FOUND NOTHING. (1) Copy string: `rg -i "meanwhile"` across apps/web (excl. node_modules/.next) returns ZERO UI strings — only code comments (rigid-webgl.tsx:157, papic-tier-copy.ts:14, ugat/graph.ts:655 …) and one blog paragraph (lib/blog-batches/guests-paper.ts:153). Same for "photos arrived" / "newly delivered" / "just arrived": zero. (2) The Overview composition: app/dashboard/[eventId]/page.tsx (all 538 lines read) imports no delivery/gallery/alaala reader; `rg -i "galler|photo|deliver|alaala|handover" event-dashboard.tsx` returns only line 117 ('delivered' as a VENDOR STATUS in CONFIRMED_VENDOR_SET), line 984 (order status mapping) and lines 1233-1261 (the Papic mini-tile's shot COUNTS). No media-arrival card, no thumbnail, no unseen-state anywhere in the 2192-line dashboard. (3) The signal itself DOES exist, just not on the Overview: booking_handovers with kind='gallery_link' is read at app/dashboard/[eventId]/vendors/[vendorId]/workspace/page.tsx:409 and rendered by that route's handover-inbox.tsx:166; a notification `${vendorName} delivered your handover` is emitted at app/vendor-dashboard/clients/[eventId]/actions.ts:633; and app/dashboard/[eventId]/galleries/page.tsx is the existing 'collecting → ready' hub. So the mechanism ships — the OVERVIEW SURFACING does not. (4) The dismissible-conditional-band pattern to copy already exists: _components/papic-ready-nudge.tsx (per-event localStorage key, three self-retire conditions) and set-date-nudge.tsx.
- **delta:** BUILD a new conditional band, but NOT against the stated data source and NOT at the stated position. (a) DATA SOURCE IS WRONG. app/dashboard/[eventId]/alaala/page.tsx is a CATALOG-driven marketing arc — a hardcoded 6-stage ARC of ADD_ONS chips — plus Kwento story density. Its only reads are photo_messages (line 93), papic_guest_captures (line 127) and guests (line 166). It has NO vendor-delivery reader at all, so a section built 'from alaala/' surfaces nothing like '84 from Studio Hiraya'. The real sources are booking_handovers (kind='gallery_link', payload = the vendor's link) joined to event_vendors for the vendor NAME, and/or the /galleries hub's per-source counts. Note also that alaala/page.tsx's own docblock still says 'no per-event data yet' — stale; it reads three tables. (b) INSERT POINT IS WRONG. page.tsx has exactly ONE slot — `slotAfterBento` (page.tsx:532) — and event-dashboard.tsx:1681 renders it between the At-a-glance bento and the Decisions board, i.e. the MIDDLE of the page, not the end. The genuine last content section is the journey rail 'Read your progress' (event-dashboard.tsx:2163-2176); nothing follows it inside the master column. Appending a sibling after <EventDashboard> in page.tsx drops the card OUTSIDE <InspectorLayout> (event-dashboard.tsx:2184), losing the `space-y-10` rhythm and the desktop master/inspector grid. Honest fix: add a second prop (e.g. `slotAtEnd`) rendered inside the `div.space-y-10` after the journey-rail <section>, and pass it from page.tsx exactly as `slotAfterBento` is passed today. (c) One more content note: '84 from Studio Hiraya' embeds a NUMERAL in prose — the standing Space-Mono-for-numerals lock. Every shipped numeral on this page uses `font-mono` (event-dashboard.tsx:1152, 1175, 1248, 1286).
- ⚠ **risk:** COLOUR NAMING TRAP IS LIVE ON THIS EXACT PAGE. The 'gold eyebrow' must be #8A6B39 (readable-text gold), available as `--sn-gold-700` / `--m-nav-active`. A builder reaching for Tailwind `text-terracotta` gets rgb(169,131,75) = #A9834B (globals.css:140) — the LARGE-TEXT-ONLY gold — which fails at eyebrow size; the shipped bento already does this correctly via `color: var(--sn-gold-700)` (event-dashboard.tsx:1126). The rust CTA is `--color-mulberry` / `--m-mulberry` = #C24E25 (globals.css:158, 957). The design's raw hexes DO map to shipped tokens — #E1DCD1 = `--m-line` (globals.css:902), #6E6A62 = `--m-slate-2` (globals.css:909) — so use the vars, not the literals. Second risk: the card is CONDITIONAL + DISMISSIBLE, so the 'zero is a hollow dot, never a bare 0' lock is satisfied by absence — but do NOT let a FAILED read render as 'no deliveries'. Supabase resolves `{ error }` rather than throwing, and an RLS denial returns an empty set with no error; a fail-soft catch here would silently tell a couple nothing arrived on the day their photographer delivered. Third risk: the vendor NAME in the copy is other people's business data — the couple's own workspace already shows it, so scope is fine, but the card must be couple/host-scoped like the rest of the Overview.

### ⑥ Bottom nav — floating pill — `ships_needs_restyle`

- **insert point valid:** True
- **evidence:** THE COMPONENT SHIPS AND IS OWNER-LOCKED — DO NOT FORK IT. Canonical primitive: app/_components/nav/bottom-nav.tsx (1068 lines, docblock 'UNBREAKABLE TEMPLATE, owner-locked 2026-06-13'), with the four knobs --bn-dur/--bn-grow/--bn-glow/--bn-stretch set in NavShell (lines 694-697), the traveling pill (TravelPill, line 709) and the press-flash bloom (PressFlash, line 749). Guarded by scripts/lint-bottom-nav.mjs: check (A) any `*bottom-nav*.tsx` must import the canonical module, check (B) the primitive must still contain --bn-dur/--bn-grow/--bn-glow/--bn-stretch, `nav-pill-stretch`, `nav-press-flash`, `aria-label="Primary navigation"` and the literal `rgba(248, 246, 240, 0.92)`. The in-event bar is app/dashboard/[eventId]/_components/customer-bottom-nav.tsx:88 (`return <BottomNav items={items} />`), fed by buildCustomerMenuTree in lib/customer-menu.ts, mounted at app/dashboard/[eventId]/layout.tsx:468. The centre ➕ has a shipped counterpart too — app/_components/nav/nav-fab.tsx (56px circle, `background: var(--m-mulberry)` = #C24E25) wrapped by _components/customer-nav-fab.tsx ('Add guest' → /guests/new), mounted at layout.tsx:472.
- **delta:** RESTYLE IS NOT THE MAIN ISSUE — THE DRAWING DESCRIBES A DIFFERENT COMPONENT AND A DIFFERENT ITEM SET. (1) The drawn active state is `.sn-seg`, not the nav. globals.css:403-442 gives EXACTLY the drawn values — container padding 3px, gap 2px, `color-mix(ink 5%)` fill, 0.5px --m-line border; active item `background: var(--m-paper)` (#FDFBF7) + `box-shadow: 0 1px 3px rgba(30,34,41,0.12)`. That is the SEGMENTED CONTROL. The bottom nav's real active indicator is a translucent DARK stadium pill `rgba(30, 34, 41, 0.15)`, min(52px, cell-8px) wide, no white fill and no shadow (bottom-nav.tsx:737); the active LABEL is `var(--m-ink)` at weight 600 (lines 821, 853), and only the ICON is gold-700 via `--m-nav-active` = #8A6B39 (globals.css:923). So 'exactly the shipped .sn-seg/pill grammar' is true of .sn-seg and false of the bar. (2) Geometry differs: shipped bar is FIXED and floating — `left-[14px] right-[14px] bottom-[calc(env(safe-area-inset-bottom)+12px)] rounded-full backdrop-blur`, fill rgba(248,246,240,0.92), shadow `0 10px 30px -12px rgba(30,34,41,0.35)` (line 679-691). There is no '1px top rule' and no `8px 10px 10px` section padding — those are prototype-page artifacts. (3) Items differ: shipped cells are ICON-OVER-LABEL, min-h 56px/44pt, label 10px, inactive `var(--m-slate)` = #3A382F (bottom-nav.tsx:819-861) — not text-only pills at 12.5px/#6E6A62 (that hex is `--m-slate-2`). (4) THE FIVE DRAWN ITEMS ARE NOT THE SHIPPED ITEMS. lib/customer-menu.ts builds FOUR plan-phase menus: home 'Overview' (:186) · guests 'Guests' (:202) · explore 'Marketplace' (:215) · studio 'Studio' (:276). Budget as a tab was REMOVED BY THE OWNER 2026-07-10 (customer-menu.ts:340-344 — it lives inside the Merkado now). 'Find' = explore, whose shipped label is Marketplace. STUDIO IS MISSING FROM THE DRAWING ENTIRELY — porting the drawn five deletes the couple's whole services/AI/website doorway from the phone. Day-of phase is a different five (now/checkin/seats/services/schedule, :162-166), after phase a different four (:175-178). (5) The ➕ is a SIBLING, not a centre tab. nav-fab.tsx's own docblock: 'isolated at the far right (the Shazam layout · owner-tuned 2026-06-22) … a SIBLING of the locked BottomNav pill — never a 7th tab'; globals.css:2190 shrinks the pill to `right: 78px` when `html[data-sn-fab]` is set. Drawing it centred inside the pill reverses that tuning. (6) Every label/icon is admin-tunable at runtime through `customer.bottom-nav.<key>` nav-registry slots (customer-bottom-nav.tsx:59-69) — hardcoding five labels in a port breaks the override path.
- ⚠ **risk:** TWO OWNER LOCKS WOULD BE REVERSED by porting the drawing literally: the 2026-07-10 removal of Budget as a top menu, and the 2026-06-22 far-right FAB placement. Both are owner territory, not port detail — the design's own Q1 says so and it is correct. A LIVE LOCK COLLISION EXISTS TODAY, INDEPENDENT OF THIS PORT: nav-fab.tsx:55 sets the glyph with `text-white` (pure white), against the standing 'labels cream #FDFBF7, never pure white' lock — a one-line fix to `color: var(--m-paper)`, worth doing whoever touches this. Tap targets are safe (56px FAB, min-h-[44pt] cells). Two STALE DOCBLOCKS sit in the exact files the design cites and will mislead the builder: customer-menu.ts:5 says 'There are SIX top menus' while line 140 says 'now 5' and the code builds 4 — the deep dive's 'SIX phase-aware top-level menus' came from that comment, not the code; and bottom-nav.tsx:140 claims 'Customer doorway opts into the accordion path', but customer-bottom-nav.tsx:88 passes `items`, so the FLAT path runs and the accordion is dead code for the couple. Finally, note globals.css:923 comments `--m-nav-active: #8A6B39; /* = --color-mulberry (light) */` — that comment is FALSE by arithmetic (--color-mulberry is rgb 194,78,37 = #C24E25); it is the naming trap written into the token file itself. Do not treat any colour comment in this repo as evidence.


---

# The build plan

**Verified at `/Users/icecasasola/wt-win`, branch `feat/storage-billing-math` @ `53037a9e0`, whose merge commit `af8c84e26` IS `origin/main`'s tip — so the tree read is equivalent to the handoff's anchor. Re-verify against `origin/main` before writing code.**

**Headline: most of this already ships. Only widget ⑤ is unambiguously new, ④ should not be built, and the largest single piece of real work is a signal change (loaded-zero vs not-measured), not a restyle.**

---

## 1. TOUCH NOTHING — already ships and matches

| Thing | File · line | Why it's off-limits |
|---|---|---|
| `<BottomNav>` primitive (travel-pill, press-flash, `--bn-dur/--bn-grow/--bn-glow/--bn-stretch`) | `apps/web/app/_components/nav/bottom-nav.tsx` | Owner-locked 2026-06-13; `apps/web/scripts/lint-bottom-nav.mjs` fails CI on a fork or a missing knob. |
| In-event bar wiring | `apps/web/app/dashboard/[eventId]/_components/customer-bottom-nav.tsx:88` (`<BottomNav items={items} />`) | Correct already. Only an `items` array could ever change, and that is Q1/owner. |
| Phase-aware menus | `apps/web/lib/customer-menu.ts` — day-of 5 (`:162-166`), plan 4 (`:191/203/220/277`), post-event 4 (`:175-178`) | Confirmed by reading: plan phase builds **Overview · Guests · Marketplace · Studio**. Admin-tunable at runtime via `customer.bottom-nav.<key>`. Hardcoding the drawn five deletes Studio and kills the override path. |
| FAB geometry + placement | `apps/web/app/_components/nav/nav-fab.tsx:54,59` — `h-14 w-14` (56px), `background: var(--m-mulberry)`, `fixed right-[14px]` | Owner-tuned far-right 2026-06-22 ("never a 7th tab"). Only its glyph colour is wrong — see D1. |
| The 2×2 bento chrome itself | `event-dashboard.tsx:1131-1319`, render `:1632` | Grid, `.sn-tile`, gold eyebrow via `--sn-gold-700`, `font-mono` values, per-tile hub links — all already what the design draws. Restyle **contents**, never the grid. |
| `.sn-eye` eyebrow | `globals.css:3088-3102` | 700 / 0.14em / uppercase / gold-700. Write no new eyebrow CSS. |
| The full decisions board | `event-dashboard.tsx:1692` (`<section id="decisions">`) | The design only touches the **digest**. Leave the board alone. |
| `slotAfterBento` plumbing | `page.tsx:532` → `event-dashboard.tsx:1681-1682` | Exists, works, already carries SetDateNudge / PapicReadyNudge. |
| The focal countdown | `event-dashboard.tsx:1392-1412` | One-home-per-number, from an owner screenshot 2026-07-15. |
| Every destination route | `guests · budget · vendors · schedule · website · seating · alaala · orders · studio · launch · messages · activity` under `dashboard/[eventId]/` | All exist. Nothing here invents a route. |

---

## 2. THE DELTA, ORDERED — smallest and safest first

**D1 · XS · standing-lock fix, ungated**
`apps/web/app/_components/nav/nav-fab.tsx:54` — the glyph is `text-white` (pure white) against the "labels cream `#FDFBF7`, never pure white" lock. Drop the class, add `color: 'var(--m-paper)'` to the style object already at `:59`. One line, zero behaviour, no open question attached.

**D2 · XS · colour-only**
`event-dashboard.tsx:999-1007` (`chipToneStyle`) — remap hot/warm/calm from shipped `#B77E2E / #8A6B39 / #4E6C82` to `--m-mulberry` (#C24E25 money/urgent) · `--sn-gold-500` (#A9834B waiting-on-people) · `--color-link` (#3B4E67 informational). The per-row `chipTone` field already exists; this is a token remap, not a new field.

**D3 · S · kills two violations in one edit**
`event-dashboard.tsx:1600-1605` — the first decision's CTA renders as a **filled `var(--sn-gold-500)` pill** (gold-is-a-button, confirmed by reading) at `py-1.5 text-[11.5px]` ≈ 27px (under the 44px floor). Replace with a `→` chevron in a 44×44 hit area. ⚠ Three more filled-gold CTAs remain on the same page (`:1748`, `:1783-1789`, `:1974`) — sweep them or the page still shows gold buttons.

**D4 · S · no new query**
Surface the money in the digest. `payItems.chip` (`formatPeso(...) + ' pending'`) is already built at `:815` and **dropped** by the digest render — the peso figure ships today only on the full board. Render it right-aligned in `font-mono`.

**D5 · S**
Row contract + lifetime, `event-dashboard.tsx:1571-1626`: collapse `label` + `sub` into one line; when `flatDecisions.length === 0` the section must **not render** — today it still renders the tile with a sentence.

**D6 · S**
Counter copy/format, `event-dashboard.tsx:1132-1200`: Guests → "86/141 guests confirmed"; Budget needs a new abbreviating formatter for "₱312k/480k" (`formatPeso` emits "₱312,000"). Keep the 46px ProgressRing and the gold bar — the design's silence is not a decision to delete shipped signal.

**D7 · M · the real work in ③, a signal change not a render tweak**
Every mini is gated on `stats.total > 0` / `committedCentavos > 0`, so a **failed or empty read makes the tile vanish** — which reads as "you have nothing", the `count === null ≠ zero` trap in a new costume. Fixed-position counters must distinguish loaded-zero from not-measured. Readers must return `number | null`; the tile renders a hollow `—`. Files: `event-dashboard.tsx:1131-1300` plus the loaders in `page.tsx` that feed them.

**D8 · M · header, the safe two-thirds only**
The event title is **desktop-only**: the plaque (`plaqueName` `layout.tsx:321`, `metaLine` `:423`) lives in `SidebarShell`, `hidden lg:… lg:flex` (`sidebar-shell.tsx:122`). The mobile `topBar` at `layout.tsx:339` is `justify-end` — badges, no title. Build: (a) a mobile title + meta block near `:338`; (b) a `"12 Dec 2026"` formatter — layout emits `"Dec 12"`, the focal emits `"Friday, December 18"`, neither is the spec; (c) **add a venue column** to layout.tsx's events select — no `venue_name`/`region` is read today; (d) the 44×44 back arrow (does not exist in any form). **Ship without the day count** — see L2.

**D9 · L · the only genuinely new section**
⑤ "Meanwhile", four parts, none of them optional:
- **Slot:** add a `slotAtEnd` prop rendered inside the `div.space-y-10` **after** the journey rail (`event-dashboard.tsx:2163-2176`), passed from `page.tsx` exactly as `slotAfterBento` is. Appending a sibling after `<EventDashboard>` drops the card outside `InspectorLayout` (`:2184`) and loses the rhythm + master/inspector grid.
- **Reader:** `booking_handovers` where `kind='gallery_link'` (`apps/web/lib/booking-handovers.server.ts` already exists; consumers at `vendors/[vendorId]/workspace/_components/handover-inbox.tsx`) joined to `event_vendors` for the vendor name. **Not `alaala/`** — see §3.
- **Dismissal:** copy `_components/papic-ready-nudge.tsx` (per-event localStorage + self-retire conditions).
- **Failure mode:** Supabase resolves `{ error }` rather than throwing, and an RLS denial returns an empty set with no error. A fail-soft catch here tells a couple nothing arrived on the day their photographer delivered. Distinguish error from empty explicitly.
- Numerals in the prose ("84 from Studio Hiraya") take `font-mono`, matching `:1152, 1175, 1248, 1286`.

---

## 3. STALE INSERT POINTS — traps, named

1. **`page.tsx` for ②, ③ (and any "section array")** — does not exist. `page.tsx` has no decisions markup, no minis, no section array; it passes exactly one slot (`:532`). All of ②/③ live in `_components/event-dashboard.tsx`. Following the design here sends an engineer to the wrong 538-line file.
2. **"layout.tsx renders a sticky plaque header with the title"** — false, twice. `:335`'s own comment says the `sticky top-0 z-20 backdrop-blur` wrapper was **retired** to SidebarShell; the topBar at `:339` is `justify-end` badges; the plaque is the **sidebar** header, invisible below 1024px — the exact viewport widget ① is drawn for.
3. **`alaala/` as ⑤'s data source** — `alaala/page.tsx` is a catalog-driven marketing arc reading `photo_messages` (:93), `papic_guest_captures` (:127) and `guests` (:166). **No vendor-delivery reader at all.** Its own docblock ("no per-event data yet") is also stale. Confirmed: **zero** `alaala` references in `page.tsx` or `event-dashboard.tsx`.
4. **"Overview composition, last content section"** — `slotAfterBento` is the **middle** (between bento and decisions board). The last content section is the journey rail.
5. **`_components/nav/` for the in-event bar** — the primitive is there; the in-event bar is `dashboard/[eventId]/_components/customer-bottom-nav.tsx`.
6. **"ServicePoster already ships — reuse, zero new CSS"** — false. `grep` returns the definition (`studio/_components/service-poster.tsx:89`) and two `globals.css` comments. **Zero JSX consumers.** Studio moved to `StudioAppRow` / `StudioFeaturedCard`.
7. **`/launch` as the add-ons store** — it is the **day-of** launch hub ("Go live", "Hand out cameras"). The store is `/studio`.
8. **"prices read live from `/pricing`"** — `lib/add-ons-catalog.ts:107-115` says in terms it is *never a price source*; live prices come from `platform_retail_catalog_v2` (`studio/page.tsx:125-126`).
9. **Three stale docblocks in the very files the design cites** — `customer-menu.ts:5` says "SIX top menus" (code builds 4 for the plan phase; the deep dive's "six" came from that comment); `bottom-nav.tsx:140` claims the customer opts into the accordion path (it passes `items` ⇒ the flat path runs); `globals.css:923` comments `--m-nav-active: #8A6B39; /* = --color-mulberry */` — **false by arithmetic**. Treat no colour comment in this repo as evidence.

---

## 4. LOCK COLLISIONS — owner only, do not build

- **L1 · Q1 nav re-scope (Event·Guests·➕·Budget·Find).** Reverses two owner locks: Budget removed as a top tab 2026-07-10 (`customer-menu.ts:340-344`) and the far-right FAB 2026-06-22. Also deletes **Studio** — the couple's whole services/AI/website doorway — from the phone, destroys phase-awareness and the nav-registry override path, and `lint-bottom-nav.mjs` would block a fork in CI. The drawn active state (white raised pill + shadow) is `.sn-seg` (`globals.css:403-442`), a **different component**; the bar's real indicator is a translucent dark stadium pill. Owner call. **Also: "Find" is genuinely new work** — no command palette / cmdk / ⌘K anywhere.
- **L2 · "127 days" in the header.** `event-dashboard.tsx:1338, :1352` state the rule ("one home per number — the countdown lives in the focal") and record why: a duplicate countdown once let two tiles disagree about whether a date was even set (owner screenshot 2026-07-15). `layout.tsx` has no `event_date_precision`, so a month-only date would print a confident day count in the header while the focal correctly asks the couple to narrow to a day. Needs an explicit ruling; date + venue + back arrow are safe without it.
- **L3 · `MAX_MINIS = 4` (confirmed `:1318-1319`).** Four fixed counters evict two shipped tiles, and the owner locked 2026-07-30 that **Papic always holds a slot**. The cap exists for a stated blur budget, not taste. Owner picks which two die — or rules the cap up and pays the cost. Note `lockedVendorCount` already renders verbatim at `:1956`, so "vendors booked" is a duplicate, not new data; and a "checklist done %" tile sits beside the focal's "% planned" gold bar (`:1450-1458`) — two different percentages of "done" on one screen.
- **L4 · Two exits to Home.** The design says the back arrow is the ONLY exit; `account-switcher.tsx:113` (`href="/dashboard"`) stays mounted. Keep both or delete one — owner.
- **L5 · Q2, the add-ons rail on Overview.** The corpus answer file recommends **no rail**. Owner.
- **L6 · Dot instead of words.** The shipped chip carries text ("pick one", "not booked yet", "₱X pending"). A colour-only dot is an accessibility regression unless the sentence absorbs the meaning. Copy decision, cheap to get wrong.

---

## 5. WHAT I WOULD NOT BUILD

- **④ the add-ons rail.** Blocked on Q2, recommended against by the corpus, and **all three build justifications are false**: the poster "reuse" is dead code with zero consumers, `/pricing` is explicitly banned as a price source (hardcoding "from ₱2,500" produces a dead number the day the owner reprices), and `/launch` is the wrong route. If discovery is the actual problem, ship a **dismissible nudge in `slotAfterBento`** — the proven pattern on this page.
- **A second header.** ① is a restyle in place.
- **Any fork of `<BottomNav>`, any chip strip, any `.sn-seg` grammar on the bar.**
- **Reviving `ServicePoster`.**
- **A checklist-% tile** until someone rules which percentage of "done" the couple sees.
- **Duplicating "vendors booked."**
- **Hardcoded nav labels** — breaks `customer.bottom-nav.<key>` runtime overrides.

**Traps for a future session:**
- `--color-terracotta` holds **GOLD** (rgb 169,131,75 = #A9834B). The CTA is `--color-mulberry` / `--m-mulberry` (#C24E25). Reaching for "terracotta" gets gold.
- Eyebrows need `--sn-gold-700` (#8A6B39); `--sn-gold-500` fails at eyebrow size. The shipped bento gets this right at `event-dashboard.tsx:1126`.
- Use the vars, not the literals: `#E1DCD1` = `--m-line` (`globals.css:902`), `#6E6A62` = `--m-slate-2` (`:909`).
- Supabase resolves `{ error }`, never throws; an RLS denial ≡ an empty read.
- `npx tsx --test "app/[slug]/…"` prints `# tests 0 … # fail 0` — the brackets are a glob character class. Quote or escape.
- CI's "lint" is ~12 scripts. Run every `apps/web/scripts/lint-*.mjs`, especially `lint-bottom-nav.mjs`, before opening the PR.
