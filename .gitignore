# macOS
.DS_Store

# LibreOffice / OpenOffice lock files (transient, present only while a doc is open)
.~lock.*

# Local Claude Code config + agent worktrees (machine-local, never part of the spec corpus)
.claude/

# Stray nested git worktree left by an old agent task (a full repo copy with its own .git)
remove-time-of-day-greeting/

# Build artifacts / dependencies (never belong in the spec corpus)
node_modules/
.next/

# Separate Next.js landing-page project living in this folder (not part of the specs;
# committed by accident via `git add -A`. Track it in its own repo, not here.)
setnayan-landing/
