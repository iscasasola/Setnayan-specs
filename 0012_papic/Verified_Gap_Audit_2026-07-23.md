# Setnayan — Verified Gap Audit (12-lens adversarial hunt)

**2026-07-23 · session c54fa75d.** 12 independent lenses swept `apps/web` + `supabase/migrations` on `origin/main`; every candidate faced 3 adversarial refuters told to default-refute. **79 findings survived** (of 142 raw). 4 of the 6 criticals were then spot-checked by hand against shipped code — all confirmed. 42 more findings reached <3 votes before the run was stopped (listed at the end, UNVERIFIED).

> Evidence discipline: each row cites `file:line` a reader can open. `LIVE` = reachable on main today; `latent` = behind an off flag/unbuilt path (gate named).

## Excluded (already known / fixed this session)
The 7 gaps fixed in PRs #3464/#3465/#3466/#3467 and their corrections #3470/#3472/#3473/#3475 were filtered out of the finder inputs and do not appear below.

## A · RLS — current_event_ids() has no member_type filter (guest == full member)  — 10 (2 critical · 5 high)

- **[critical·LIVE]** `supabase/migrations/20260516261000_oauth_grants_per_couple.sql:86` — oauth_grants RLS lets ANY event member (including guests) read the couple's plaintext Google/YouTube refresh + access tokens
  - *fails when:* A guest claims their invite for event E. apps/web/lib/link-guest-account.ts:78-86 upserts an event_members row (member_type='guest') for that user. public.current_event_ids() (supabase/migrations/20260512000000_setnayan_base.sql:185-186) returns every event_id in event_members for auth.uid() with NO member_type filter, so the guest's JWT now satisfies the policy. From any browser, using the publis
  - *fix:* Narrow the policy to couples (public.current_couple_event_ids()), and add column-level defense-in-depth exactly as vendor_ig_connections does: REVOKE ALL ON public.oauth_grants FROM authenticated, anon; then GRANT SELECT on the non-secret columns only. The token columns should never be reachable by 
- **[critical·LIVE]** `supabase/migrations/20260513010000_iteration_0001_guests.sql:188` — Any event member can read every guest's qr_token and mint that guest's session — full guest impersonation
  - *fails when:* Guest A scans an event QR and joins via /join/[eventId]; apps/web/app/join/[eventId]/actions.ts:57 inserts an event_members row with member_type='guest'. From the browser (same Supabase session, anon key) A issues `select guest_id,qr_token from guests where event_id=eq.<event>` — policy `event_member_can_read_guest` (line 188) gates only on `event_id IN (SELECT public.current_event_ids())`, and cu
  - *fix:* Restrict the guests SELECT policy so a non-couple member can only read their own linked guest row (the `guest_reads_own_row` policy already exists for that), or at minimum exclude qr_token from member-wide read by moving it to a side table / column-level grant. Independently, /[slug]/redeem should r
- **[high·LIVE]** `apps/web/app/dashboard/[eventId]/studio/papic/moderation/actions.ts:351` — blockKwentoGuest has no role check and its RLS INSERT path admits any event member — a guest can permanently silence another guest
  - *fails when:* Mallory joins an event by QR as an ordinary guest, so apps/web/app/join/[eventId]/actions.ts:56 inserts her event_members row with member_type='guest'. She POSTs the `blockKwentoGuest(eventId, guestId)` server action with the victim's guest_id. The action performs NO membership or role check at all (lines 351-360) — it inserts straight into guest_message_blocks under her own RLS session. The polic
  - *fix:* Add `await requireCouple(eventId)` at the top of blockKwentoGuest (matching every sibling action in the file), and tighten the policy's WITH CHECK to repeat the couple/coordinator EXISTS predicate instead of relying on current_event_ids(). Also add an unblock action so the lever is reversible.
- **[high·LIVE]** `supabase/migrations/20270129279924_orders_cohost_read.sql:40` — orders_owner_read admits every event member, not just co-hosts — a wedding guest can read the couple's full order ledger
  - *fails when:* A guest who claims their invite for event E gets an event_members row with member_type='guest'. current_event_ids() has no member_type filter, so from the browser with the anon key that guest runs GET /rest/v1/orders?select=service_key,amount_php,reference_code,status,admin_notes&event_id=eq.<E> and reads the couple's entire purchase history — every SKU bought, every peso amount, the payment refer
  - *fix:* Replace current_event_ids() with a host-scoped set (current_couple_event_ids(), plus an explicit co-host membership list if co-hosts are a distinct member_type), so guests and vendor members fall out of the predicate.
- **[high·LIVE]** `supabase/migrations/20260901000000_iteration_0012_guest_face_enrollments.sql:67` — Any event member — including a fellow guest — can read every guest's face vector and selfie ref via the anon-key PostgREST endpoint
  - *fails when:* Guest A joins event E via the join flow and gets an `event_members` row. `current_event_ids()` returns every event the caller is a member of regardless of `member_type`, and the `event_member_can_read_face_enrollment` SELECT policy is `USING (event_id IN (SELECT public.current_event_ids()))`. Guest A opens devtools on any Setnayan page (the browser client ships `NEXT_PUBLIC_SUPABASE_ANON_KEY`) and
  - *fix:* Drop `event_member_can_read_face_enrollment` or narrow it to `member_type = 'couple'` (the couple already has `couple_writes_face_enrollment`), keep `guest_reads_own_face_enrollment`, and add `revoked_at IS NULL` to both. Server matchers already use the service-role client so nothing app-side regres
- **[high·LIVE]** `supabase/migrations/20270202160005_event_vendor_payment_plan.sql:106` — `event_vendor_payment_plan` "host" policies use `current_event_ids()`, so any event member — including a plain RSVP guest — can read, edit and DELETE the couple's frozen vendor payment schedule
  - *fails when:* A wedding guest accepts an event invite; apps/web/app/join/[eventId]/actions.ts:57 inserts their `event_members` row with `member_type: 'guest'`. `public.current_event_ids()` (20260512000000_setnayan_base.sql:178-186) selects event_id from event_members with NO member_type filter, so the guest is inside the predicate. From the browser Supabase client they can `DELETE /rest/v1/event_vendor_payment_
  - *fix:* Replace `public.current_event_ids()` with `public.current_couple_event_ids()` (defined 20260513040000_fix_rls_infinite_recursion.sql:45) in both policies, or drop the write policy entirely — the header at :31-33 states the snapshot is written by the service-role admin client, so the authenticated wr
- **[high·LIVE]** `supabase/migrations/20270202160005_event_vendor_payment_plan.sql:106` — event_vendor_payment_plan is FOR ALL to any event member — a plain guest can rewrite or clear the couple's frozen vendor payment schedule
  - *fails when:* A user who joined the event as a plain guest (event_members.member_type='guest', seeded by apps/web/app/join/[eventId]/actions.ts:57) hits PostgREST directly. The SELECT policy at line 100 lets them read every event_vendor_payment_plan row for the event (which hands them the otherwise-unreadable `event_vendor_id` — event_vendors itself is couple-only, supabase/migrations/20260513100000_iteration_0
  - *fix:* Swap current_event_ids() for current_couple_event_ids() in event_vendor_payment_plan_host_write (and tighten the select policy the same way), matching the event_vendors precedent. Consider dropping the write policy altogether — the only writer in app code is the service-role lock snapshot at apps/we
- **[medium·LIVE]** `supabase/migrations/20260824000000_budget_allocation_decisions.sql:88` — `budget_allocation_decisions` — table comment says "Couple-own-only under RLS" but SELECT and DELETE are scoped to `current_event_ids()` (any event member)
  - *fails when:* A guest on event E issues `DELETE /rest/v1/budget_allocation_decisions?event_id=eq.<E>`. The table is described in its own comment as the RA-10173-erasable behavioral-capture log feeding Layer-2 analytics and as having deliberately no UPDATE policy because snapshots are immutable — a guest can nonetheless erase the couple's entire decision history, and read every per-leaf default-vs-final amount (
  - *fix:* Use `public.current_couple_event_ids()` in the read and delete policies.
- **[medium·LIVE]** `apps/web/app/api/crew/register-device/route.ts:161` — /api/crew/register-device trusts a client-supplied vendor_profile_id — any event QR holder can exhaust a named vendor's 5 crew-device slots
  - *fails when:* The route's only credential is the event's master_qr_token (constant-time compared at lines 115-150). It never establishes that the caller belongs to the vendor named in the body — `vendor_profile_id` is only regex-checked for UUID shape at line 95 and then written verbatim into the upsert at line 161-172. Two live abuse paths: (a) any Setnayan account that joined the event as a plain guest can re
  - *fix:* Require an authenticated caller and verify membership in the named vendor org (vendor_profiles.user_id = auth.uid() OR a vendor_team_members row) before the upsert — the same check /api/v1/manpower/sync-device attempts at lines 82-102. Failing that, bind the device to the pairing session rather than
- **[medium·LIVE]** `supabase/migrations/20270713200000_event_appointments.sql:109` — event_appointments INSERT/UPDATE is open to any event member — a plain guest can cancel or reschedule the couple's vendor meetings
  - *fails when:* A user with an event_members row of member_type='guest' reads every appointment on the event via the SELECT policy at line 91, then issues `UPDATE event_appointments SET status='cancelled'` (or SET scheduled_at to a new time) on any of them: the UPDATE policy at line 109 checks only `event_id IN (SELECT public.current_event_ids())` for both USING and WITH CHECK, and that helper (supabase/migration
  - *fix:* Gate the INSERT/UPDATE policies on current_couple_event_ids() plus an explicit moderator branch with the relevant area grant, mirroring how event_vendors and event_floor_plan are scoped.

## B · MONEY — unguarded paid-order / payment write  — 9 (3 critical · 3 high)

- **[critical·LIVE]** `apps/web/app/dashboard/[eventId]/orders/actions.ts:99` — createOrder self-comp branch mints a status='paid' order + runs SKU activation on ANY event_id — the event-membership check is explicitly skipped
  - *fails when:* Mallory signs up, opens a shop at /open-shop (self-serve, no verification: apps/web/app/open-shop/actions.ts:150-158 upserts her as vendor_team_members role='admin' on her own vendor_profile). She then POSTs the `createOrder` server action with `event_id` = a stranger's event UUID, `self_purchase_action='comp_for_myself'`, `self_purchase_vendor_profile_id` = her own profile, `service_key='PAPIC_UN
  - *fix:* Move the event-membership check above the self-comp branch (or repeat it inside it): require the caller to hold an event_members/event_moderators row on `eventId` before createSelfCompOrder is called. Independently, make the self-comp path stop writing status='paid' directly — route it through the s
- **[critical·LIVE]** `apps/web/app/dashboard/[eventId]/orders/actions.ts:99` — Vendor self-comp mints a `status='paid'` order against an arbitrary, unvalidated `event_id` — a second writer of 'paid' with no event scoping
  - *fails when:* Any user who signs up as a vendor gets a `vendor_team_members` row with role='owner' automatically (supabase/migrations/20260514010000_iteration_0022_vendor_dashboard_expansion.sql:223-225 inserts role 'owner' on profile creation). That user opens their own new-order form and POSTs the `createOrder` server action with `self_purchase_action=comp_for_myself`, `self_purchase_vendor_profile_id=<their 
  - *fix:* Move the `event_members` membership check above the `isSelfComp` branch so it applies to both paths, or add an explicit event-scope check inside the self-comp branch (the buyer must be an event member OR the target event must have an `event_vendors` row for the self-comping vendor). Do not rely on t
- **[critical·LIVE]** `supabase/migrations/20260513150000_iteration_0034_payments.sql:77` — `orders_owner_write` is FOR ALL with only `user_id = auth.uid()` — any authenticated user can INSERT/UPDATE an order row with `status='paid'` on any event, bypassing the entire paywall
  - *fails when:* A logged-in couple opens devtools and, with the browser Supabase client (`apps/web/lib/supabase/client.ts:5-6`, NEXT_PUBLIC_SUPABASE_URL + anon key, user JWT), POSTs to PostgREST `/rest/v1/orders` with `{user_id: <their own uid>, event_id: <their event>, service_key: 'PAPIC_SYSTEM', description: 'x', requested_total_php: 0, reference_code: 'X1', status: 'paid'}`. The WITH CHECK only requires `user
  - *fix:* Split `orders_owner_write` into an INSERT-only policy whose WITH CHECK pins `status = 'submitted'` (or 'draft') and `event_id IN (SELECT public.current_couple_event_ids())`, and drop the authenticated UPDATE/DELETE grant entirely (admin reconciliation already runs service-role). Add a BEFORE INSERT 
- **[high·LIVE]** `apps/web/app/dashboard/[eventId]/orders/actions.ts:99` — Vendor self-comp creates a 'paid' order and activates the SKU for an arbitrary, unverified event_id
  - *fails when:* Anyone who registers a (free) vendor profile becomes `owner` in `vendor_team_members`. They POST to the `createOrder` server action with `self_purchase_action=comp_for_myself`, their own `self_purchase_vendor_profile_id`, `service_key=PAPIC_GUEST_10K`, and `event_id` set to ANY event UUID — a stranger's wedding, or a colluding couple's. The vendor-role check at lines 87-97 validates only the vendo
  - *fix:* Move the `event_members` membership check ABOVE the self-comp branch so every order path — comped or not — proves the buyer belongs to `event_id`; and validate `service_key` against an active catalog row before comping.
- **[high·LIVE]** `app/dashboard/[eventId]/orders/actions.ts:367` — Second writer of order status 'paid' bypasses the event-membership check, so a vendor owner can activate paid SKUs on any event id
  - *fails when:* A vendor-team owner POSTs the `createOrder` server action with `event_id` = a stranger couple's event UUID, `self_purchase_action=comp_for_myself`, `self_purchase_vendor_profile_id` = their own vendor, `service_key=SETNAYAN_AI`. The self-comp branch validates only vendor-team role (lines 88-97) and returns at line 109 — before the `event_members` membership check at lines 116-124 that exists preci
  - *fix:* Inside the `isSelfComp` branch, before calling `createSelfCompOrder`, verify the caller is an `event_members` row on `eventId` (or that the event is one the vendor legitimately owns), and add a guard script (or a `lib/`-hosted unit test over an extracted pure predicate) asserting that no file outsid
- **[high·LIVE]** `supabase/migrations/20260513150000_iteration_0034_payments.sql:119` — `payments_owner_insert` does not constrain `status` or `order_id`, so a buyer can insert a `status='matched'` payment that satisfies the admin shortfall guard
  - *fails when:* A customer owes ₱2,999 on order O. They transfer ₱1 and upload proof. Before the admin reviews it, they POST to `/rest/v1/payments` with `{order_id: <O>, user_id: <their uid>, amount_php: 3400, channel: 'gcash', status: 'matched'}` — the WITH CHECK only asks `user_id = auth.uid()`, so it lands. The admin then approves the real ₱1 payment with "Also mark order as paid" ticked; the shortfall guard a
  - *fix:* Tighten the WITH CHECK to `user_id = auth.uid() AND status = 'pending' AND order_id IN (SELECT order_id FROM public.orders WHERE user_id = auth.uid())`. Reconciliation already runs service-role, which bypasses RLS, so nothing legitimate breaks.
- **[medium·LIVE]** `apps/web/lib/v2-catalog.ts:138` — PAPIC_ADDON_STORIES and PAPIC_ADDON_THANK_YOU are is_active=TRUE and labelled 'live' with zero fulfillment code — and Stories already ships free
  - *fails when:* A couple opens /pricing, sees "Stories (Papic Add-on) ₱2,000" and "Thank You (Papic Add-on) ₱1,500" in the "Papic & its add-ons" group with a 'Live' build chip, and sees them again as priced picks in wedding onboarding. There is no studio page, no checkout drawer, and no entitlement gate for either code, so nothing in the app ever reads ownership of them and nothing is ever delivered. A couple who
  - *fix:* Set `is_active = false` for PAPIC_ADDON_STORIES (the feature is free) and for PAPIC_ADDON_THANK_YOU (no fulfillment), or hold both at BUILD_STATUS 'partial' like CAMERA_BRIDGE/LIVE_WALL until delivery exists. Add a lint/test asserting that every `is_active` catalog row either has an activation hook 
- **[medium·latent]** `supabase/migrations/20260918000001_vendor_token_purchase_webhook.sql:44` — Superseded-but-not-reversed: `_apply_token_purchase_credit` still credits only the founder's store wallet, so the webhook confirm path pays a teammate's purchased tokens into the founder's balance
  - *fails when:* Migration 20270401611377 added `vendor_token_purchases.holder_user_id` and split the credit into two destinations: founder → `vendor_wallets.purchased_tokens`, teammate → `vendor_member_token_wallets`. It rewrote `approve_vendor_token_purchase` accordingly (20270401611377:311-360) but never touched `_apply_token_purchase_credit`, which the webhook path calls. So: a vendor team member buys a 25-tok
  - *fix:* Port the holder branch from 20270401611377:335-353 into `_apply_token_purchase_credit` and have `approve_vendor_token_purchase` delegate back to it, so both paths again share a single credit core.
- **[medium·latent]** `apps/web/app/api/webhooks/token-purchase/route.ts:75` — Token-purchase webhook credits tokens on any payload that does not explicitly match a failure keyword — a `pending`/`chargeable`/`authorized` provider event credits the wallet
  - *fails when:* `isFailureStatus()` is a deny-list: it uppercases one of five candidate status fields and tests /FAIL|CANCEL|VOID|EXPIRE|DECLINE|REFUND|CHARGEBACK/. Every other value — including a missing status field entirely (the fallback is the empty string), `PENDING`, `AUTHORIZED`, or PayMongo's `source.chargeable` — falls through to the credit call at :128-131. A provider that fans out a lifecycle event (pa
  - *fix:* Invert to an allow-list: require the resolved status to match the provider's explicit success token (e.g. /^(PAYMENT_SUCCESS|SUCCEEDED|PAID)$/ or `event.type === 'payment.paid'`) and ack-without-crediting on everything else, so an unrecognised lifecycle event is a no-op rather than a credit.

## C · AUTHZ — self-granted / un-reversed paid tier  — 10 (1 critical · 5 high)

- **[critical·LIVE]** `supabase/migrations/20260513120000_iteration_0022_vendor_dashboard.sql:63` — Vendors can self-grant any paid tier: `vendor_profiles` owner policy is FOR ALL with no column guard, and `tier_state` is the whole subscription ladder
  - *fails when:* A vendor owner opens the browser console on any Setnayan page (the client Supabase client is built with `NEXT_PUBLIC_SUPABASE_ANON_KEY` + their own session, apps/web/lib/supabase/client.ts:6) and runs `supabase.from('vendor_profiles').update({tier_state:'custom', tier_expires_at:null, extra_agent_seats:50, verification_state:'verified'}).eq('user_id', <their uid>)`. RLS admits it: `vendor_profiles
  - *fix:* Add a BEFORE UPDATE guard trigger on vendor_profiles in the pattern already used on events (supabase/migrations/20261214000000_guard_pax_finalize_columns.sql:21-40 and 20270821110200_papic_mini_cap_and_caps_guard.sql:40-72): when `auth.uid() IS NOT NULL AND NOT public.is_admin()`, revert NEW.tier_st
- **[high·latent]** `supabase/migrations/20260514100000_vendor_reviews.sql:238` — vendor_profiles_public_read grants anon SELECT on the whole row — hq_address, geo coords, fraud flags, is_demo and BIR/TIN columns are all publicly enumerable
  - *fails when:* Postgres RLS filters rows, not columns, and there is no column-level GRANT on vendor_profiles. So anyone holding the publishable anon key (shipped in every page bundle) can run GET /rest/v1/vendor_profiles?select=business_name,hq_address,hq_latitude,hq_longitude,contact_phone,fraud_suspended_at,fraud_banned_at,fraud_tombstoned,is_demo,tin_number,registered_address&is_published=eq.true and dump, fo
  - *fix:* Apply the vendor_ig_connections pattern to vendor_profiles: REVOKE ALL FROM anon (and narrow authenticated), then GRANT SELECT on only the marketplace-card columns. Keep hq_address/hq_latitude/hq_longitude, all fraud_*, is_demo, and every BIR column off the anon grant.
- **[high·LIVE]** `apps/web/app/admin/payments/actions.ts:321` — SKU activation fires on every payment approval, outside the promote/shortfall guard — a partial payment provisions the full SKU
  - *fails when:* Couple submits a PAPIC_GUEST_10K order (₱X, 10,000 points) and transfers ₱1. Admin opens /admin/payments and approves the ₱1 payment with "Also mark order as paid" UNCHECKED — which is exactly what the code's own notice string at line 194 instructs an admin to do to record a partial transfer. `if (promoteOrder)` at line 150 is skipped, so neither the shortfall guard (line 166) nor the promote-to-p
  - *fix:* Gate `activateOrderSku` on the same coverage test the promote block uses: compute `matchedTotal >= owed` once, before the `if (promoteOrder)` block, and require it for activation as well as promotion. Alternatively pass the covered amount into the hooks and let point-granting hooks scale/refuse.
- **[high·LIVE]** `apps/web/app/explore/page.tsx:1961` — Vendor tier lapse only fires from that vendor's own dashboard visit, and the public marketplace reads tier_state with no expiry guard
  - *fails when:* A vendor buys Pro; sku-activation stamps tier_state='pro', tier_expires_at = now+28d (apps/web/lib/sku-activation.ts:609-622). Recurring billing does not exist, so lapse is the normal path, not an edge case. They don't renew and stop opening /vendor-dashboard. The ONLY downgrade path is `sweep_vendor_tier_expiry(p_vendor_id)` — a single-vendor RPC fired from that vendor's own dashboard layout (app
  - *fix:* Add tier_expires_at to the explore enrichment select and treat a past-due row as its lapsed tier before calling tierCaps — or, better, add a fleet-wide `claimPeriodicJob('vendor-tier-lapse', DAILY_GAP_MS)` wrapper over a set-based sweep so the DB state is correct for every reader instead of requirin
- **[high·LIVE]** `supabase/migrations/20270512705572_vendor_custom_tier_and_plans.sql:127` — Vendors can flip their own `vendor_custom_plans` row to status='active' with any composition — self-granting Custom ceilings and the paid /api/v1 access
  - *fails when:* A vendor (owner or vendor-admin team member — `current_vendor_profile_ids()`, migration 20260821000000:23-35) PATCHes their own plan row: `supabase.from('vendor_custom_plans').update({composition:{nationwide:true,seats:999,slotsPerCategory:999,photos:99999,api_access:true}, status:'active', quoted_28d_php:0})`. The policy `vendor_custom_plans_vendor_access` is `FOR ALL … USING/WITH CHECK (vendor_p
  - *fix:* Split the vendor policy: allow the vendor SELECT plus INSERT/UPDATE restricted to non-terminal statuses only, and move status='active' behind a SECURITY DEFINER RPC gated on is_admin()/service-role (or add a BEFORE UPDATE trigger that reverts status and composition changes for non-admin, non-service
- **[high·LIVE]** `supabase/migrations/20260917000000_setnayan_ai_entitlement.sql:18` — Couples can self-grant the paid Setnayan AI SKU by PATCHing `events.setnayan_ai_active` — the flag IS the entitlement and is unguarded
  - *fails when:* A couple runs `supabase.from('events').update({setnayan_ai_active:true}).eq('event_id', <their event>)` from the browser. `couple_can_update_event` (supabase/migrations/20260513040000_fix_rls_infinite_recursion.sql:90-96) is FOR UPDATE over the whole row with no column restriction, and no trigger protects this column. Every AI gate then passes: `isSetnayanAiActive` returns `event.setnayan_ai_activ
  - *fix:* Extend the events guard-trigger pattern to the entitlement columns: revert (or 42501) non-admin, non-service-role changes to setnayan_ai_active, setnayan_ai_active_until and setnayan_ai_intro_used, exactly as papic_caps_admin_only() does for the price caps.
- **[medium·LIVE]** `apps/web/public/llms.txt:92` — llms.txt promises a 7-day renewal email before vendor-subscription expiry — the reminder job reads public.orders, and vendor subscriptions never land there
  - *fails when:* A Pro vendor buys a 28-day prepaid block. create_vendor_subscription inserts into public.vendor_subscriptions and approval stamps vendor_profiles.tier_expires_at — nothing writes a public.orders row with expires_at. subscriptions_due_for_renewal_reminder only scans public.orders, so it returns zero rows for that vendor and no email is ever sent. On day 28 the lapse sweep demotes the tier with no e
  - *fix:* Extend subscriptions_due_for_renewal_reminder (or add a sibling RPC) to UNION vendor_profiles.tier_expires_at joined to the vendor's owner email, keyed on a vendor-scoped reminder_window so the idempotency lock still holds. Until then, drop "email" from llms.txt:92.
- **[medium·LIVE]** `apps/web/lib/sku-activation.ts:597` — Custom-tier activation binds to the most-recently-updated plan row, not the plan the paid order quoted — pay the cheap quote, receive the expensive composition
  - *fails when:* 1) Vendor composes a minimal Custom plan → plan row P1 written as pending_payment with the cheap composition, order O1 at ₱9,000 (apps/web/app/vendor-dashboard/subscription/custom/actions.ts:160-186, 191-206). 2) Vendor pays O1 externally. 3) Before the admin reconciles, the vendor hits "Adjust" with a maxed composition (nationwide, 999 seats, api_access) — that action REUSES the same newest non-a
  - *fix:* Add `custom_plan_id` to the orders row (or `order_id` to vendor_custom_plans) at quote time and have the activation hook resolve the plan by that id, refusing to activate when it is absent; and make "Adjust" always INSERT a new plan row rather than mutating a row that already has a live order.
- **[medium·LIVE]** `apps/web/lib/sku-activation.ts:729` — Refunding a vendor extra-seat order never lowers `extra_agent_seats` — the seat survives the refund permanently
  - *fails when:* A vendor buys 3 extra team seats (3 × ₱250, service_key `vendor_extra_seat__{vendor_profile_id}`). Admin approves → the prefix hook recomputes `vendor_profiles.extra_agent_seats = 3` (sku-activation.ts:554-563). The vendor then disputes and the admin refunds all three orders at /admin/payments. `refundOrder` flips each order to 'refunded' and calls `deactivateOrderSku` (apps/web/app/admin/payments
  - *fix:* Add a prefix branch to deactivateOrderSku that re-runs the same `COUNT(orders WHERE service_key = seatServiceKey(vendorId) AND status='paid')` recompute after the reversal — the recompute is already idempotent, it just never runs on the reversal side.
- **[medium·LIVE]** `apps/web/lib/subscriptions.ts:48` — Extra team seats are sold as a 28-day recurring add-on but nothing ever expires them: no expires_at is stamped and the lapse sweep is an exact-match SKU allowlist
  - *fails when:* A vendor buys one extra seat. The order is created with `description: 'Extra Team Seat (28-day)'` (apps/web/app/vendor-dashboard/team/actions.ts:371) and the customer is told it is a ₱250/28-day add-on (SEAT_PERIOD_DAYS = 28, lib/vendor-seats.ts:33). On approval the seat hook writes `extra_agent_seats` but — unlike the sibling branch hook, which stamps `orders.expires_at` 28 days out (sku-activati
  - *fix:* Either stamp `orders.expires_at = now()+28d` in the seat hook and change the sweep to also match `service_key LIKE 'vendor_extra_seat__%'` (and re-derive the count from non-lapsed paid orders), or restate the SKU as a one-time purchase in the description and pricing copy.

## D · COPY vs CODE — public promise not kept  — 12 (0 critical · 7 high)

- **[high·LIVE]** `apps/web/app/privacy/page.tsx:580` — Published privacy policy promises automatic OAuth-grant purges (30 days post-event, expired refresh tokens) that no code performs
  - *fails when:* A couple connects Google Drive to their wedding event; a `oauth_grants` row stores `refresh_token` + `access_token` in plaintext. The wedding happens. 30 days pass, then a year. The published policy told them the grant would be "kept until the earlier of ... (c) 30 days after the event ends. Refresh tokens past their expiry are purged automatically." No sweep, trigger, or RPC ever deletes it. The 
  - *fix:* Either add a claimPeriodicJob('oauth-grant-retention', DAILY_GAP_MS) sweep that revokes-at-Google and hard-deletes grants whose event ended >30d ago or whose refresh token is past expiry, or amend the three privacy-page paragraphs to describe what actually happens (revocation is user-initiated). The
- **[high·LIVE]** `apps/web/public/llms.txt:74` — llms.txt advertises the retired Papic camera ladder (₱30/₱50/₱100, "Unli = unlimited") — catalog now charges ₱100/₱200 with Ltd deactivated
  - *fails when:* An LLM/answer engine reads /llms.txt and tells a couple "Papic Mini is ₱30 per camera per day, Papic Unli is ₱100 and gives unlimited shots." The couple opens /pricing and is quoted ₱100 (Mini) and ₱200 (Papic Max, capped at 500 points/day). A couple who picks "Papic Ltd ₱50" finds no such rung — it is is_active=FALSE and publicPapicLadder() filters it out entirely. Every one of the six numbers on
  - *fix:* Rewrite llms.txt:74 to the shipped ladder (first 3 cameras free; Papic Mini ₱100/cam·day ≈200 points, Papic Max ₱200/cam·day ≈500 points; Ltd retired) and update APPROVED_LLMS_PRICES. Better: add llms.txt to PAPIC_COPY_FILES in lib/papic-copy-guardrails.test.ts so a spelled Papic rate/points literal
- **[high·LIVE]** `apps/web/public/llms.txt:58` — llms.txt sells two Live Studio device SKUs (₱1,299 / ₱2,499 per day) — the catalog has one "Live Studio" at ₱2,500/day and the Mobile row is deactivated
  - *fails when:* A couple (or an answer engine quoting us) budgets ₱1,299/day for "Live Studio Mobile, up to 3 cameras". No such product exists: PANOOD_SYSTEM_MOBILE is is_active=FALSE and no code path could ever sell it, and the one real SKU is ₱2,500/day — so the quoted price is both a phantom product and, for the Desktop rung, ₱1 under the charged amount while promising a device restriction ("phone-run, up to 3
  - *fix:* Collapse llms.txt:58 and :150 to a single "Live Studio — ₱2,500 / day, up to 8 cameras, laptop or phone; single-camera stream free" and drop ₱1,299/₱2,499 from APPROVED_LLMS_PRICES.
- **[high·LIVE]** `apps/web/public/llms.txt:94` — llms.txt promises verified vendors 100 complimentary tokens until 2027-01-31 — the grant trigger was dropped 2026-06-17
  - *fails when:* A vendor reads /llms.txt (or an LLM quoting it), completes verification expecting 100 free tokens worth ₱20,000 at the flat ₱200/token rate, and receives zero. Their wallet is empty, so the first couple inquiry they try to answer fails on INSUFFICIENT_WALLET_BALANCES until they buy a pack.
  - *fix:* Delete llms.txt:94. Tokens now come only from admin grants, subscription bundles, and purchases.
- **[high·latent]** `apps/web/app/_components/pilot-mode-banner.tsx:20` — NEXT_PUBLIC_PILOT_MODE_FREE_UNTIL renders a sitewide "everything is free" banner but no pricing or checkout path consults it
  - *fails when:* Owner sets NEXT_PUBLIC_PILOT_MODE_FREE_UNTIL="2026-08-31T23:59:59+08:00" to run a closed pilot. Every visitor on every page (the banner is mounted in the root layout) now reads "Pilot mode — every add-on and subscription is free for testing through Aug 31." A couple then buys Papic Seats: the checkout action resolves the price from the admin catalog (platform_retail_catalog_v2 / v2-catalog.ts), wh
  - *fix:* Either wire `getEffectivePriceCentavos`/`isFreeNow` into the checkout price resolution (and into `resolveBundleChargeCentavos` / `resolvePaxPricedOrderCentavos`), or delete the banner and the three dead helpers. Note the same dead helpers are the only enforcement of `LAUNCH_PROMO_SKU_CODES` — that p
- **[high·LIVE]** `apps/web/lib/help.ts:403` — Help centre describes account deletion as an immediate soft-delete with a 30-day admin restore window; the code files a pending request and, when approved, hard-deletes irreversibly
  - *fails when:* A user exercising their RA 10173 right to erasure reads help.ts:403 — "We soft-delete the account and sign you out. Internal admins can restore within 30 days; after that, deletion becomes permanent" — types DELETE, and believes they are deleted and can change their mind for 30 days. In reality `requestAccountDeletion()` only INSERTs a `pending` row into `account_deletion_requests` and redirects b
  - *fix:* Rewrite help.ts:403 to state the real flow (request → admin review within 24h → permanent deletion, no restore window), or build the soft-delete + 30-day restore it promises. As written it is a false statement about a statutory data-subject right.
- **[high·LIVE]** `apps/web/app/privacy/page.tsx:468` — Privacy notice promises a T+27d "your wedding goes public in 3 days" reminder email; no such job, template, or notification type exists
  - *fails when:* A couple opts into the public summary at signup (apps/web/app/signup/actions.ts:75, `public_summary_consent`). Their event page becomes publicly indexable on /realstories at event_date + 30 days, because loadPublishedShowcases() computes `cutoff = now - 30d` at read time (apps/web/lib/showcase-db.ts:92,127-129) — no schedule needed, it just happens. The published RA 10173 § 16(e) safeguard #4 says
  - *fix:* Either implement the T+27d job as a sixth claimPeriodicJob('showcase-going-public', DAILY_GAP_MS) entry with a per-event send lock, or delete safeguard #4 from the published privacy notice — a documented RA 10173 safeguard that does not exist is worse than one never promised.
- **[medium·LIVE]** `apps/web/lib/help.ts:471` — Help article promises "your first 5 guest cameras are free" — PAPIC_FREE_CAMERA_COUNT is 3
  - *fails when:* A couple reads the help article, plans to hand out 5 free crew cameras, opens Studio → Papic and finds only 3 free seats; cameras 4 and 5 land on the paid ladder at ₱100/camera/day. The same file is emitted as schema.org FAQPage answers (app/help/page.tsx:38-50), so the wrong number is also fed to search engines.
  - *fix:* Either derive the sentence via papicFreeCameraCount() or hard-correct to 3, and add 'lib/help.ts' + 'public/llms.txt' to PAPIC_COPY_FILES.
- **[medium·LIVE]** `apps/web/lib/help.ts:126` — Help article "What is Papic?" still sells two retired SKUs with an unkeepable 24-photos-plus-10-videos split promise
  - *fails when:* A couple reads that Papic has "two purchase modes" — Papic Guest at 24 photos + 10 five-second videos per guest, and Papic 5 Seats with unlimited photos and video for 5 hours — then finds neither in Studio → Papic. Both catalog rows were deactivated as couple buy paths; PAPIC_GUEST has since been repurposed into a per-event points bucket ("Papic One — 3,000 shots", ₱500). The per-guest split is al
  - *fix:* Rewrite the article against the shipped model (3 free cameras, then Papic Mini/Max per camera per day on a points budget) and bring lib/help.ts under the papic-copy guardrail file list.
- **[medium·LIVE]** `apps/web/lib/help.ts:178` — Help article guarantees "old slugs auto-redirect for 90 days" — the redirect resolver self-noops behind a default-OFF flag, so renamed slugs 404
  - *fails when:* A couple with printed invitations/QR codes pointing at setnayan.com/maria-and-juan renames their slug to maria-juan-2026 on the Invitation tab. updateEventSlug writes the slug_change_log row (redirect_until = now + 90 days) as promised, but on the next visit fetchEventBySlug misses, resolveRenamedEventPath returns null at the flag check before it ever queries slug_change_log, and the request falls
  - *fix:* Ungate resolveRenamedEventPath from the /u/ nesting cutover — the rename redirect is orthogonal to the URL-shape cutover and can emit the legacy bare `/{slug}` path — or remove the 90-day promise from the help article until the flag flips.
- **[medium·LIVE]** `apps/web/public/llms.txt:88` — llms.txt sells Enterprise as "unlimited team" / "unlimited team accounts" — the tier caps agent seats at 10
  - *fails when:* An Enterprise vendor pays ₱79,999/year on the strength of "unlimited team", then hits a hard stop inviting the 11th agent seat because VENDOR_TIER_CAPS.enterprise.agentAccounts === 10. The /vendors tier matrix (which derives from the same caps) correctly shows the finite number, so the public site and the AI-crawler surface contradict each other.
  - *fix:* Change llms.txt:88 and :128 to "up to 10 team seats" (and note Custom as the truly-unlimited tier).
- **[low·LIVE]** `apps/web/lib/help.ts:81` — Help article calls the Event Website and RSVP "optional paid software" while /pricing and llms.txt list both as free
  - *fails when:* A couple reading /help's "Is Setnayan free for couples?" concludes the wedding website and RSVP cost money and either budgets for them or bounces; /pricing's own free list, on the same site, names "Your free website" with "unlimited RSVP collection". Two live public surfaces give opposite answers to the single most-asked pricing question.
  - *fix:* Rewrite help.ts:81 to name the actually-paid à-la-carte touches (Editorial PRO, Cinematic Reveal) instead of "the Event Website, and premium RSVP".

## E · FAIL-OPEN / points leak  — 3 (0 critical · 3 high)

- **[high·LIVE]** `apps/web/app/papic/actions.ts:501` — Papic capture points are reserved before the papic_photos insert and never released when the insert fails
  - *fails when:* A paparazzo on a free-tier camera (papic_tier_config.points_per_day = 20) taps the shutter. Lines 364-378 book 1 point in papic_seat_day_usage via papic_reserve_camera_points, and lines 397-405 book 1 point in papic_event_pool_usage via papic_reserve_event_points (on a flat-pass event). The subsequent `papic_photos` insert at lines 479-498 runs on the CLAIMER'S session client under RLS — if it fai
  - *fix:* Hoist `seatBooked` / `eventBooked` and the release block out of the gate scope, and call the two release RPCs (best-effort, as the existing block does) in the `if (insertError)` branch at line 501 before returning.
- **[high·LIVE]** `apps/web/app/papic/actions.ts:501` — Reserved capture points are never released when the papic_photos insert fails
  - *fails when:* A paparazzo on a 'mini' seat (200 pts/day) shoots a clip. recordSeatCapture books 3 points on the seat ledger (papic_reserve_camera_points, actions.ts:365) AND 3 points on the event lifetime pool (papic_reserve_event_points, actions.ts:395). Both return true, so combinePointsGates → 'allow' and the unwind block at actions.ts:415-450 (which only runs `if (gate !== 'allow')`) is skipped. The papic_p
  - *fix:* Hoist the release logic out of the `gate !== 'allow'` branch into a helper and call it on every non-ok exit after a successful reserve — in particular the insertError return at actions.ts:501 (and any later early return). Track `seatBooked`/`eventBooked` in the outer scope so the insert-failure path
- **[high·latent]** `apps/web/lib/vendor-papic-grants.ts:138` — Vendor capture-points meter fails OPEN: a failed read reports 0 points spent, granting a full fresh budget
  - *fails when:* A vendor on the Lite tier (20 pts) has already shot 20 photos at a booked event. On the next capture, /api/vendor/papic-capture calls fetchVendorPapicPointsSpent (route.ts:141). That function destructures only `data` — `const { data } = await client.from('vendor_papic_captures')...` — and discards the PostgREST `error`. Any query failure (statement timeout, transient 5xx, a schema/column drift ret
  - *fix:* Destructure and check `error`; on any error (or a thrown exception) return a sentinel that fails CLOSED — e.g. return the tier's full cap so canCapture refuses, or change the signature to `number | null` and have the route 503 on null, mirroring resolvePointsGate's 'blocked' verdict. Separately, rep

## F · PERIODIC / RETENTION never fires  — 13 (2 critical · 6 high)

- **[critical·LIVE]** `apps/web/lib/daily-email-jobs.ts:405` — The mandatory 14-day "we're about to delete your full-res originals" email can never send — wrong join column on public.users
  - *fails when:* A couple's Papic photo turns 76 days old. runPapicDropWarning (daily-email-jobs.ts:352) finds the event, resolves the couple's event_members row (member.user_id = a UUID), then queries `admin.from('users').select('email').eq('id', member.user_id)` at line 405. `public.users.id` is BIGSERIAL (supabase/migrations/20260512000000_setnayan_base.sql:78); the UUID column is `user_id` (line 79). PostgREST
  - *fix:* Change line 405 to `.eq('user_id', member.user_id as string)`. Separately, surface the query error instead of swallowing it, and make runFullResDropSweep refuse to drop any photo whose event has `full_res_drop_warned_at IS NULL` — so a broken notifier can never authorize a deletion (same fail-closed
- **[critical·LIVE]** `apps/web/app/admin/users/actions.ts:277` — Biometric face vectors + full-res selfies survive account deletion entirely (`guest_face_enrollments` has no user FK)
  - *fails when:* A guest enrolls a selfie at RSVP; `guest_face_enrollments` stores `asset_url` (the full-res R2 selfie ref) and `face_vector`. They later delete their Setnayan account. `deleteUser` purges only owned-event birth data and authored chat, then deletes `auth.users`, which cascades `event_members` — the ONLY user→guest bridge — away. `guest_face_enrollments.guest_id` FKs to `guests`, and `guests`/`event
  - *fix:* Add a `purgeUserBiometrics(admin, targetUserId)` step to `deleteUser`/`blacklistUser` that reads `event_members.guest_id` for the user BEFORE the auth delete (same ordering rationale as the existing purges), then reuses the withdrawal logic: null `face_vector`/`vector_model`, set `revoked_at`, and `
- **[high·LIVE]** `apps/web/lib/papic-fullres-drop.ts:211` — Drive-deferred photos permanently occupy the full-res drop sweep's oldest-500 window, so the sweep can converge to zero drops forever
  - *fails when:* One couple connects Google Drive, then disconnects it (or the Drive sync jams / the token dies / an `oauth_grants` read errors). `DRIVE_INTENT_PROVIDERS` deliberately does not filter `revoked_at` (line 52 comment), so the event is still 'Drive in play'; `loadEventDriveCopyState` returns `connected` with an empty confirmedKeys set (or `unknown`), and `isDriveDeferred` defers every one of their phot
  - *fix:* Make deferral advance a cursor instead of re-reading the same head: either add a `full_res_drop_deferred_at` (or `drop_check_attempts`) column stamped on each defer and order by it, or exclude events whose deferred count exceeded a threshold on the previous pass and page past them. Also emit a Sentr
- **[high·LIVE]** `apps/web/app/dashboard/[eventId]/studio/photo-delivery/actions.ts:166` — Google Drive photo delivery permanently stalls in 'uploading' past ~240 photos — the tick that was supposed to drain it never ran and the retry path is status-gated shut
  - *fails when:* A couple with 400 Papic photos clicks "Release to Drive". enqueueRelease flips `events.photo_delivery_status='releasing'` and creates 400 artifact rows. The after() drain at actions.ts:166 loops at most 40 times × batchSize 6 = 240 uploads (fewer in practice — 240 sequential multi-MB Drive multipart uploads will hit the Vercel function ceiling first), leaving ~160+ artifacts with `drive_file_id IS
  - *fix:* Add a claimPeriodicJob-driven drainer (or a self-continuing after() that re-enqueues on a fresh request) so 'releasing'/'uploading' events keep advancing without a user click, and admit 'uploading'/'releasing' into enqueueRelease's status gate as a resume (the artifact upsert is already idempotent o
- **[high·latent]** `apps/web/app/[slug]/_components/editorial/data.ts:818` — The public editorial recap's consent / FaceBlock exclusion filters on two `moderation_state` values that no code path ever writes, so a guest who revokes photo consent stays on the permanent public page
  - *fails when:* A guest asks the couple to withdraw photo consent. The host unticks it on the guest record; `updateGuest` sets `guests.photo_consent = false`, revokes the face enrolment and clears the selfie (app/dashboard/[eventId]/guests/[guestId]/actions.ts:222, 253-275). The venue Live Wall correctly drops every tile tagged with that guest on the very next read — `wall_visible_photos` joins `photo_tags → gues
  - *fix:* Add the same `photo_tags → guests.photo_consent = FALSE` NOT EXISTS veto the wall uses to every seat-capture read in the editorial data loader and the teaser (an RPC or a view mirroring `wall_visible_photos`'s predicate would keep it in one place), and either start writing `consent_withheld`/`facebl
- **[high·LIVE]** `apps/web/lib/force-majeure.ts:112` — Unattended force-majeure disputes silently self-close as "resolved"; the documented ESCALATED path does not exist
  - *fails when:* A couple files a typhoon force-majeure flag on day 0. `force_majeure_flags.auto_resolve_at` defaults to NOW()+7 days (supabase/migrations/20260514110000_force_majeure_flags.sql:64). Nobody acts. On day 8 an admin opens /admin/force-majeure to triage the backlog — `sweepAutoResolveStaleFlags(admin)` runs FIRST at app/admin/force-majeure/page.tsx:81 and UPDATEs every `open`/`under_review` flag past 
  - *fix:* Either add an 'escalated' status and make the sweep set that (leaving the flag in the admin queue filter), or delete the auto-resolve sweep and fix help.ts:568 + tours.ts:272 to describe what actually happens. Do not leave a destructive status change firing from the admin's own triage pageview.
- **[high·LIVE]** `apps/web/lib/nsfw-screen.ts:338` — NSFW re-screen heal sweep is permanently starved by poster-less clips, so genuinely stuck photos stay invisible forever
  - *fails when:* An event's guests record 10+ clips whose poster-frame upload failed (apps/web/app/api/papic/guest-capture/route.ts:200-216 explicitly treats poster upload as best-effort: `catch { posterRef = null }`). Those rows sit at moderation_state='unscreened' forever, because screenCapture() bails at nsfw-screen.ts:276 (`if (!posterRef) return;`) WITHOUT writing any state. reScreenStuckCaptures then selects
  - *fix:* Either (a) add a terminal state for poster-less clips (e.g. stamp moderation_state='unscreenable' or a `screen_attempted_at` column) so they leave the candidate set, or (b) exclude clips-without-poster from the sweep query and add `.order('created_at')` plus an attempt counter so the window advances
- **[high·LIVE]** `apps/web/lib/daily-email-jobs.ts:318` — Renewal reminder inserts its idempotency lock then ignores sendEmail's failure result, permanently suppressing the reminder
  - *fails when:* The Resend key is unset or unresolvable at the moment the daily job wins its claim (sendEmail returns `{ok:false, reason:'not_configured'}` at apps/web/lib/email.ts:68-70 — it does NOT throw), or Resend returns a 429/5xx (email.ts:88-91, also returns rather than throws). runRenewalReminders has already inserted `renewal_reminder_log (order_id, '7d')` at line 305-308. sendEmail's result is never in
  - *fix:* Bind the result (`const result = await sendEmail(...)`) and mirror the anniversary pattern: `if (result.ok) sent += 1; else await admin.from('renewal_reminder_log').delete()...` so a failed send retries on the next daily claim.
- **[medium·latent]** `apps/web/lib/patiktok-reel-emails.ts:44` — "Your Patiktok reel is ready" email never sends — same wrong join column on public.users
  - *fails when:* A couple's Patiktok render job finishes. sendPatiktokReelReadyEmail resolves the couple's `event_members.user_id` (UUID) then runs `.from('users').select('email, display_name').eq('id', coupleUserId)`. `public.users.id` is BIGSERIAL, so PostgREST rejects the UUID (22P02); the error is not destructured, `userRow` is null, `to` is empty, and the function returns at line 47. The couple is never told 
  - *fix:* Change to `.eq('user_id', coupleUserId)`. Worth adding a lint rule or a guardrail test asserting that no `from('users')` query filters on `id` with a UUID-typed value — two independent regressions of the same shape suggests it will happen a third time.
- **[medium·latent]** `supabase/migrations/20270324681685_inquiry_outcomes_won_lost.sql:85` — purge_expired_chat aborts on inquiry_outcomes' anchor CHECK — ON DELETE SET NULL nulls the row's only anchor
  - *fails when:* A vendor records a Won/Lost outcome on a chat thread. recordInquiryOutcome ALWAYS inserts with `chat_thread_id` set and `vendor_proposal_id` left NULL (apps/web/app/vendor-dashboard/messages/[threadId]/outcome-actions.ts:92-96), so that row's only anchor is the thread. Later, purge_expired_chat(5) tries to DELETE that chat_threads row (the event has no orders row and its event_date is older than 5
  - *fix:* Change inquiry_outcomes.chat_thread_id to ON DELETE CASCADE (an outcome with no inquiry has no meaning), or have purge_expired_chat explicitly DELETE the dependent inquiry_outcomes rows before deleting the threads. Separately, move the claimPeriodicJob stamp to after a successful runRetentionSweep, 
- **[medium·LIVE]** `supabase/migrations/20270303900000_papic_unlock_guest_cap.sql:18` — The guest-capture RPC silently dropped the UGC block + terms gates that migration 20261108000000 installed as authoritative
  - *fails when:* A couple blocks an abusive guest (row in event_blocked_users) or a guest has never accepted the UGC terms. Migration 20261108000000 (§4, lines 211-241) re-created papic_record_guest_capture specifically to enforce both gates server-side, returning 'blocked' / 'terms_required'. The 6-arg overload created by 20270216612756 and re-created by 20270303900000 — which is the signature apps/web/app/api/pa
  - *fix:* Re-add the event_blocked_users lookup and the guests.ugc_terms_accepted_at NULL check to the 6-arg overload (returning 'blocked' / 'terms_required' before the quota branch), and add a regression test that asserts both statuses come back from the RPC — not just from the route.
- **[medium·latent]** `apps/web/lib/anon-draft-sweep.ts:49` — Anon-draft RA 10173 deletion sweep has a 50-row unordered batch with permanent-skip paths that can wedge it forever
  - *fails when:* The candidate query selects `users` rows whose email still ends in the anon placeholder domain and whose created_at is older than 30 days, `.limit(50)` with no `.order()` — so Postgres returns the same physical-order rows every run. Three paths inside the loop `continue` WITHOUT mutating the row: :66 (auth user is no longer anonymous — a converted account whose best-effort public.users email overw
  - *fix:* Add `.order('created_at', { ascending: true })` plus a durable skip marker (a `sweep_skipped_at` / attempt counter column, or exclude rows whose auth user is non-anonymous by reconciling their email) so the window advances past unresolvable rows instead of re-reading them every day.
- **[medium·LIVE]** `apps/web/app/dashboard/[eventId]/studio/papic/actions.ts:405` — couple_approved_for_showcase can only ever be set on CLIPS, so every public read that requires it on photos is permanently empty
  - *fails when:* A couple buys Papic Guest (the disposable-camera SKU: 24 photos + 10×5s clips), guests opt in to public sharing, and the couple approves shots in the studio. The public recap section '5b-quater. Papic GUEST captures … join the day' requires `couple_approved_for_showcase = true` AND `media_type='photo'`, but the ONLY two writers of that column both hard-filter to clips: setClipShowcaseApproval ends
  - *fix:* Either add a photo-scoped approval action (and surface the toggle for photos in papic-gallery-grid.tsx), or drop the three photo-scoped public reads so the code stops advertising a path that cannot fire.

## G · FAKE DOOR / broken route  — 4 (0 critical · 2 high)

- **[high·LIVE]** `apps/web/lib/shortlist-taxonomy.ts:326` — Three registries emit /dashboard/[eventId]/vendors/[vendorId], a route with no page.tsx
  - *fails when:* A couple opens /dashboard/<evt>/vendors on a phone (viewport < xl) and taps any shortlisted vendor card. shortlist-categories.tsx:260 passes `v.href` to InspectorTrigger, which at sub-xl viewports does NOT preventDefault (inspector-column.tsx:254-256 returns early when `!isXl`) and navigates to `/dashboard/<evt>/vendors/<vendorId>`. That path has no page — `app/dashboard/[eventId]/vendors/[vendorI
  - *fix:* Point all three at the canonical room: `/dashboard/${eventId}/vendors/${vendorId}/workspace` (the 33-call-site convention), or add a `page.tsx` at `[vendorId]` that redirects to `workspace`.
- **[high·LIVE]** `apps/web/app/dashboard/[eventId]/_components/day-of-mode/video-guestbook-card.tsx:103` — Day-of Video Guestbook 'Manage' button links to /gallery; the route is /galleries
  - *fails when:* On the wedding day the couple's dashboard switches to day-of mode (DayOfModeGrid, imported at app/dashboard/[eventId]/page.tsx:36 and rendering VideoGuestbookCard via day-of-mode/grid.tsx:10). They tap 'Manage' on the Video Guestbook card to review incoming guest clips and land on `/dashboard/<evt>/gallery` — a 404. The real surface is `/dashboard/<evt>/galleries` (plural). Worst possible moment f
  - *fix:* `/dashboard/${eventId}/galleries`.
- **[medium·LIVE]** `apps/web/app/dashboard/[eventId]/studio/panood/cameras/page.tsx:126` — Live Studio 'Print the QR sheet' button targets a cameras/print route that was never built
  - *fails when:* A couple who bought Live Studio opens the camera-provisioning page to print QR claim sheets for their camera operators, clicks 'Print the QR sheet', and gets a 404. `app/dashboard/[eventId]/studio/panood/cameras/` contains only `actions.ts`, `page.tsx`, `reissue-button.tsx` — no `print/` segment. The sibling feature that this was patterned on does have one (`app/dashboard/[eventId]/studio/papic/cr
  - *fix:* Build `studio/panood/cameras/print/page.tsx` mirroring `studio/papic/crew/print/page.tsx`, or drop the button until it exists.
- **[medium·LIVE]** `apps/web/lib/payouts.ts:575` — Payout-released notification tells vendors "Form 2307 is issued quarterly" — the obligation was retired, the surface redirects away, and no generator exists
  - *fails when:* An admin releases a vendor payout at /admin/payouts. releasePayout() emits a `vendor_payout_update` notification whose body states "Form 2307 (BIR withholding) is issued quarterly." The vendor waits for a certificate that will never arrive: (a) the surface that used to serve it, /vendor-dashboard/tax-documents, is a bare `redirect('/vendor-dashboard')` retired 2026-05-29 because "Per RR 16-2023 1%
  - *fix:* Drop the 2307 sentence from the payout notification body at payouts.ts:575, and ship a migration that `cron.unschedule('quarterly_2307_generation')` so a future pg_cron enablement doesn't start POSTing at a dead route.

## H · PRIVACY — PII in URL/log  — 1 (0 critical · 1 high)

- **[high·LIVE]** `apps/web/app/admin/users/actions.ts:422` — Admin password reset puts the plaintext temporary password and the target's email in a URL query string
  - *fails when:* An admin clicks "Reset password" for user bob@x.com. `resetUserPassword` sets a new 12-char password on the account and then redirects the browser to `/admin/users?temp_password=<plaintext>&for_email=bob%40x.com`. That full URL is written to the Vercel edge/function access log, to the admin's browser history and any synced-profile cloud copy, and is emitted as the `Referer` on subsequent requests 
  - *fix:* Return the temp password from the action to the calling component and render it once from the POST result (no navigation), or stash it in a short-lived one-time-read server-side store keyed by an opaque id and pass only the id in the URL. Never place the credential or the email in the query string.

## Z · other  — 17 (3 critical · 6 high)

- **[critical·latent]** `supabase/migrations/20270826385580_papic_event_capture_pool.sql:412` — papic_release_event_points / papic_reserve_event_points are SECURITY DEFINER with zero authorization and GRANTed to anon — the capture-points fence can be zeroed from the browser
  - *fails when:* A paparazzo on a flat-pass event holds eventId in their browser (apps/web/app/papic/seat/[token]/page.tsx:115 passes it into the client component papic-seat-capture.tsx:109). With the public anon key they POST /rest/v1/rpc/papic_release_event_points {"p_event_id":"<their event>","p_cost":2147483647}. The function (defined at :363-380) performs NO caller check of any kind — it unconditionally runs 
  - *fix:* REVOKE EXECUTE ON papic_reserve_event_points, papic_release_event_points, papic_release_camera_points FROM authenticated, anon — leave service_role only, matching how apps/web/app/papic/actions.ts actually calls them. If a client-side probe is ever needed, expose only the read-only papic_event_point
- **[critical·LIVE]** `supabase/migrations/20270513460125_person_spine_people_table.sql:35` — Account hard-delete leaves the durable `people` PII node intact, and a re-signup silently re-claims it
  - *fails when:* Bob (bob@x.com) signs up; the `ensure_person_for_user` trigger creates a `people` row holding his display_name, first/last name, email, phone, profile_photo_url and birth_date. Bob files a deletion request; an admin approves it and `deleteUser` runs. `auth.users` cascades away `public.users` and `event_members`, but `people.claimed_by_user_id` is `ON DELETE SET NULL` — the row survives with every 
  - *fix:* In `deleteUser`/`blacklistUser`, before the auth delete, resolve the user's `people` row via `claimed_by_user_id = targetUserId` and either hard-delete it or NULL every PII column (display_name, first/last, email, phone, profile_photo_url, birth_date) and set `deleted_at`, mirroring the existing bes
- **[critical·LIVE]** `apps/web/app/[slug]/_components/editorial/data.ts:903` — Public /[slug] editorial auto-plays Papic clips that were NEVER NSFW-screened
  - *fails when:* A claimed paparazzi seat records a 5-second clip and the browser's poster-frame extraction fails (or a hostile client simply omits posterR2Key). screenCapture() takes the clip branch, finds no poster_r2_key, and returns at nsfw-screen.ts:276 leaving moderation_state='unscreened' forever (reScreenStuckCaptures re-enters the same early return, so it never heals). loadEditorialData's clip read then f
  - *fix:* Change the editorial clip/photo reads to an ALLOWLIST — `.eq('moderation_state','clean')` — matching guest-live-gallery.ts and wall_ingest G1 (supabase/migrations/20261115000604_wall_p2_faceblock_bake.sql:134). Separately, make a poster-less clip terminal-withheld (write a `*_withheld` state) instea
- **[high·LIVE]** `apps/web/app/dashboard/[eventId]/hosts/actions.ts:110` — inviteHost never checks permissions_json.invite_hosts — any accepted moderator (incl. viewer) can mint a full-authority 'bride' host on the event
  - *fails when:* The couple invites Vera as a `viewer` (PERMISSION_TEMPLATES.viewer = {edit_all:false, checkout:false, invite_hosts:false, remove_hosts:false} — apps/web/lib/event-moderators.ts:163). Vera accepts, so she holds an accepted, non-removed event_moderators row. She then POSTs `inviteHost` with `event_id` = that event, `invitation_email` = her own second address, `role_subtype='bride'`. requireHostMembe
  - *fix:* In requireHostMembership, select permissions_json and require `permissions_json.invite_hosts === true` (or member_type='couple') for inviteHost and revokeHostInvite. Additionally clamp the invitable `role_subtype` set so a non-couple inviter can never mint a PRIMARY_HOST_ROLE_SUBTYPES role.
- **[high·LIVE]** `apps/web/lib/sku-activation.ts:744` — Refund/reject does not reverse the SETNAYAN_AI_SUB subscription window — refund the money, keep the subscription
  - *fails when:* A buyer purchases 12 cycles of SETNAYAN_AI_SUB (₱499 × 12). On approval the hook at lib/sku-activation.ts:306 upserts `user_ai_subscription.active_until = now + 336 days`, `source: 'paid'`. The buyer then requests and receives a full refund; the admin runs `refundOrder`, which flips the order to `'refunded'` and calls `deactivateOrderSku` (app/admin/payments/actions.ts:907, :929). But `deactivateO
  - *fix:* Add an AI_SUB reversal to `deactivateOrderSku`: when `ctx.serviceKey === AI_SUB_SKU`, look up `user_ai_subscription` by the order's `user_id`, and if `last_order_id === ctx.orderId` roll `active_until` back by the granted cycle count (or clear it when no other paid AI_SUB order remains). Same symmet
- **[high·LIVE]** `apps/web/app/dashboard/[eventId]/paperwork/page.tsx:422` — Paperwork empty-state CTAs link to /dashboard/[eventId]/settings, which does not exist
  - *fails when:* A couple with no `event_date` set opens the Paperwork tab. The page renders the 'Set a wedding date to unlock deadline math' block whose 'Set wedding date' Link points at `/dashboard/<evt>/settings`. There is no `settings` segment under `app/dashboard/[eventId]/` (the real surface is `details/`), so the couple 404s on the one CTA meant to unblock the entire paperwork deadline engine. Identical dea
  - *fix:* Change both to `/dashboard/${eventId}/details` (the event attributes surface that owns date + ceremony type).
- **[high·LIVE]** `apps/web/app/waitlist/actions.ts:82` — Waitlist upsert targets ON CONFLICT (email) but the only unique index is on LOWER(email) — every signup errors
  - *fails when:* Any visitor submits the pre-launch couple waitlist form with a valid email. PostgREST compiles the upsert to `INSERT ... ON CONFLICT (email) DO UPDATE`. Postgres resolves ON CONFLICT inference at plan time and finds no unique index whose key is the bare column `email` — the only one is the expression index on `LOWER(email)` — so it raises 42P10 ("no unique or exclusion constraint matching the ON C
  - *fix:* Either add `CREATE UNIQUE INDEX ... ON public.couple_waitlist_signups (email)` (email is already lowercased at actions.ts:20, so a plain unique index is equivalent), or drop the upsert and do an explicit select-by-email then update/insert — the same select-then-write pattern already used for the exp
- **[high·LIVE]** `.github/workflows/ci.yml:33` — `test:db` — three hermetic RLS/security DB suites — is invoked by no CI job
  - *fails when:* A PR that reverts or narrows `public.panood_rtc_can_access(topic)` (migration `supabase/migrations/20270829134804*`) or drops `private: true` from `lib/panood-webrtc.ts` re-opens the verified stream-hijack hole the suite exists to guard (a stranger holding an event id answers a camera's WebRTC offer and blacks out the couple's control room mid-ceremony). `apps/web/tests/db/panood-rtc-authz.db.test
  - *fix:* Add a `db-tests` job to `.github/workflows/ci.yml` running `pnpm --filter @setnayan/web test:db` (needs only `pnpm install` + node 22, same shape as the existing `typecheck-lint` job), and promote it to a required check.
- **[high·LIVE]** `apps/web/app/[slug]/_components/editorial/data.ts:1847` — Public Kwento anchor read pulls guest captures with no NSFW filter, on a false 'no moderation_state column' premise
  - *fails when:* A guest records a 5-second Papic Guest clip that the NSFW screen classifies as nsfw_blocked (screenCapture writes the verdict to papic_guest_captures.moderation_state). The guest opted in at capture time (consent_to_public=TRUE) and the couple approved the clip for the showcase (setGuestClipShowcaseApproval). A Kwento wish anchored to that clip is approved. The public Kwento anchor read selects th
  - *fix:* Add `.eq('moderation_state','clean')` to every public papic_guest_captures read (data.ts:976, :1000, :1847 and papic-gallery.ts:254) and delete the 'this table carries NO NSFW moderation_state' comments, which are contradicted by the migration.
- **[medium·latent]** `supabase/migrations/20260628000000_v2_additive_phase_a.sql:424` — vendor_wallets has RLS enabled with zero policies but is read through the RLS-bound user client — every vendor's token balance renders as 0
  - *fails when:* A vendor with 40 purchased tokens opens any /vendor-dashboard page. The layout reads vendor_wallets with the user-scoped server client; RLS is enabled and no policy grants SELECT, so the read returns zero rows, walletRes.data is null, and tokenBalance falls back to 0. The sidebar footer renders "0 tokens" and the /vendor-dashboard/subscription wallet card shows an empty balance, for a vendor who a
  - *fix:* Either add the canonical vendor-scoped SELECT policy (`USING (vendor_id IN (SELECT public.current_vendor_profile_ids()))`, matching vendor_payouts_team_admin in supabase/migrations/20260822000000_vendor_admin_table_access.sql:75) or switch both readers to createAdminClient() after an ownership check
- **[medium·LIVE]** `apps/web/app/papic/face-enroll-actions.ts:30` — The day-of face-enrollment server action has no 18+ backstop, so a crafted POST enrolls a minor's biometrics
  - *fails when:* `enrollGuestFace` accepts a POST with `selfie_ref` + `biometric_consent=1` and writes `guest_face_enrollments` rows carrying `face_vector`. It checks `face_recognition_excluded` but never reads `age_affirmation`. The RSVP path enforces `ageAffirmed` server-side precisely because, per its own comment, the client checkbox is not a control: "this is the server-side backstop so a crafted POST can't en
  - *fix:* Add `const ageAffirmed = clean(formData.get('age_affirmation')) === '1'; if (!ageAffirmed) return { ok: false };` to `enrollGuestFace`, and add a test asserting every writer of `guest_face_enrollments` enforces both the consent and the age affirmation.
- **[medium·LIVE]** `apps/web/app/dashboard/[eventId]/guests/[guestId]/actions.ts:256` — Host-side consent withdrawal only tombstones the enrollment — the face vector and the R2 selfie are retained
  - *fails when:* A guest tells the couple to withdraw their photo consent. The couple unticks photo_consent (or ticks face_recognition_excluded) on the guest edit form. The action sets `revoked_at` on the live `guest_face_enrollments` row and NULLs `guests.photo_url` — but leaves `face_vector` and `vector_model` populated and never deletes the full-res selfie object from R2. The biometric identifier the subject wi
  - *fix:* Extract the guest-side withdrawal into a shared `revokeFaceEnrollments(admin, eventId, guestId)` helper that nulls the vector and deletes the R2 objects, and call it from the host-side path too.
- **[medium·LIVE]** `apps/web/package.json:36` — 19 unit tests sit under app/ where the `lib/**/*.test.ts` runner glob can never collect them
  - *fails when:* Someone changes `isInPlanningWedding` in `app/dashboard/(account)/create-event/wedding-guard.ts` so an archived or past-dated wedding still counts as in-planning. The one-life-event-per-type cardinality guard then blocks a widowed or remarrying user from ever creating a new wedding. `wedding-guard.test.ts` asserts exactly that case — and never runs. Likewise a regression in `matchesPath` (`app/_co
  - *fix:* Either widen the glob (`tsx --test "lib/**/*.test.ts" "app/**/*.test.ts"`) or move both tests next to the pure functions they cover under `lib/`. Widening also removes the trap for the next colocated test.
- **[medium·LIVE]** `lib/coordinator-money-scope.test.ts:168` — The coordinator money-scope revocation/scoping tests cannot fail — the Supabase stub ignores every filter it claims to verify
  - *fails when:* Delete `.is('revoked_at', null)` from `lib/coordinator-money-scope.ts:74`. A couple who revoked a coordinator's payment permission (setting `revoked_at`) now still returns `true` for the `checkout` scope, so the revoked coordinator can submit orders and upload payment proof on the couple's event. `pnpm test:unit` stays 2425/2425 green — including the test literally named `'flag ON → revoked consen
  - *fix:* Have the stub record its filter calls (table, column, value) and assert the recorded chain in each test — or move this predicate into `tests/db/` and exercise it against the replayed schema, where `revoked_at` and `removed_at` are real columns and the filters are real.
- **[medium·LIVE]** `supabase/migrations/20270401574089_vendor_org_multi_admin.sql:122` — `vendor_team_guard` blocks demoting/removing another admin without a team vote but not reassigning that admin row's `user_id`, and the RLS policy permits column-unrestricted UPDATE
  - *fails when:* Store S has co-admins A and B. A signs up a second account A2 (not yet a team member, so the UNIQUE (vendor_profile_id, user_id) index does not block it). A issues `PATCH /rest/v1/vendor_team_members?vendor_team_member_id=eq.<B's row>` with `{user_id: <A2>}`. RLS `vendor_team_members_admin_write` (FOR ALL, USING/WITH CHECK `vendor_profile_id IN (SELECT public.current_vendor_ids('admin'))`) permits
  - *fix:* Add to the trigger: `IF TG_OP = 'UPDATE' AND OLD.user_id <> NEW.user_id AND NOT v_approved THEN RAISE EXCEPTION 'VENDOR_ADMIN_CHANGE_NEEDS_VOTE'; END IF;` — or make `user_id` immutable on vendor_team_members outright (membership changes should be delete+insert).
- **[medium·latent]** `apps/web/app/api/webhooks/token-purchase/route.ts:84` — Token-purchase webhook credits the wallet on any status that is not on a failure denylist
  - *fails when:* Once the secret is provisioned, the gateway sends its normal lifecycle events for a checkout carrying the TKN- reference — e.g. Maya PAYMENT_PENDING / AUTHORIZED, or PayMongo payment.awaiting_payment_method / checkout_session.payment.created. None of those strings match /FAIL|CANCEL|VOID|EXPIRE|DECLINE|REFUND|CHARGEBACK/, so isFailureStatus returns false, the route treats the event as a success, e
  - *fix:* Invert the check to an allowlist of terminal-success statuses per provider (e.g. PAYMENT_SUCCESS / paid / succeeded) and refuse to credit on anything else, including a missing status field.
- **[low·LIVE]** `apps/web/app/api/profile/export/route.ts:114` — The RA 10173 export returns every guest's face-enrollment records to any couple who runs it
  - *fails when:* The export's face-enrollment select carries no `.eq()` filter and relies solely on RLS, with a comment asserting `guest_reads_own_face_enrollment` scopes it. Postgres ORs permissive SELECT policies, and `event_member_can_read_face_enrollment` also matches for anyone with an `event_members` row. A couple with 200 enrolled guests who requests their own data export therefore downloads 200 other data 
  - *fix:* Resolve the caller's own `guest_id`s from `event_members` and add `.in('guest_id', myGuestIds)` (or filter by the returned `guest_id`), matching the explicit-filter pattern used for orders/payments.

## Pending — UNVERIFIED (<3 adversarial votes; treat as leads, not findings)

- [critical·LIVE] `supabase/migrations/20260513150000_iteration_0034_payments.sql:77` — orders_owner_write (FOR ALL) lets a customer set their own order status='paid' — second writer of 'paid', breaks apply-then-pay
- [critical·LIVE] `supabase/migrations/20260513010000_iteration_0001_guests.sql:188` — `guests` RLS lets any event member (including a plain guest) read every guest's email/mobile/address AND `qr_token` — the bearer credential that mints a guest session
- [critical·LIVE] `supabase/migrations/20260901000000_iteration_0012_guest_face_enrollments.sql:67` — `guest_face_enrollments` RLS exposes raw `face_vector` and the selfie `asset_url` to every event member, contradicting the code comments that claim RLS keeps vectors out of client reach
- [critical·LIVE] `supabase/migrations/20260513010000_iteration_0001_guests.sql:187` — Any joined guest can read the entire guest list's PII — including every other guest's `qr_token` bearer credential
- [critical·LIVE] `apps/web/lib/papic-cameras.ts:751` — The Papic points gate fails OPEN on PGRST202/42883, and a unit test cements it as correct
- [high·LIVE] `supabase/migrations/20270202160005_event_vendor_payment_plan.sql:106` — event_vendor_payment_plan 'host_write' policy gates on current_event_ids() — every wedding GUEST can read and rewrite the couple's vendor payment schedule
- [high·LIVE] `supabase/migrations/20260824000000_budget_allocation_decisions.sql:88` — couple_deletes_budget_allocation_decisions uses current_event_ids() while the INSERT policy two lines above correctly requires member_type='couple'
- [high·LIVE] `apps/web/lib/guest-stories.ts:174` — Guest Stories hands the guest a presigned URL to the RAW R2 original — EXIF/GPS intact — bypassing every strip path
- [high·LIVE] `apps/web/app/admin/users/actions.ts:277` — Account deletion leaves the user's whole event and its full guest roster in place forever — `events` has no owner FK and nothing purges it
- [high·LIVE] `apps/web/app/api/social/card/[postId]/route.ts:152` — `credit_mode = 'anonymous'` is honoured in the caption but ignored on the published card image and the post title — the couple's names go out anyway
- [high·LIVE] `apps/web/app/admin/users/actions.ts:277` — Account erasure purges only birth data + chat bodies; help-desk and signup tables keep the departing user's email, name and free text forever
- [high·LIVE] `apps/web/lib/add-ons-catalog.ts:406` — Deactivated COUPLE_WEBSITE_PRO umbrella is still fully purchasable in Studio (₱4,999 for ₱3,998 of standalone value)
- [high·LIVE] `apps/web/lib/fraud-detection-runner.ts:433` — Platform-wide fraud scoring and the auto-suspend sweep have zero callers — no cron, no after(), no admin button
- [high·latent] `supabase/migrations/20270826385580_papic_event_capture_pool.sql:185` — Papic One purchased point buckets are granted but never metered — the event fence only applies to PAPIC_UNLOCK* events
- [high·latent] `apps/web/app/api/papic/guest-capture/route.ts:222` — Papic One purchased point buckets are granted but never consumed — the guest capture path has no pool call, and the pool is structurally inapplicable to these SKUs
- [high·latent] `apps/web/lib/camera-bridge/papic-sink.ts:107` — Camera-bridge sink presigns with the generic client-chosen pathPrefix contract, skipping the seat auth, paid, capture-window and points gates on /api/upload
- [high·LIVE] `apps/web/lib/photo-delivery-release.ts:133` — Photo-delivery "release again" can never retry the photos that failed — attempt_count is never reset
- [high·LIVE] `apps/web/app/papic/seat/[token]/page.tsx:33` — `?bridge=demo` bypasses NEXT_PUBLIC_CAMERA_BRIDGE_ENABLED and writes mock 1×1-pixel fixture captures into the couple's real Papic gallery
- [high·latent] `apps/web/app/api/papic/kwento/route.ts:103` — Two DDL files live in `apps/supabase/migrations/` — a directory `supabase db push` never reads — so the Kwento voice-depth columns and the `get_vendor_mood_board` RPC they define do not exist in any repo-provisioned database
- [medium·LIVE] `apps/web/app/admin/users/actions.ts:422` — Admin password reset puts the plaintext temporary password and the target's email in a URL query string, across two redirects
- [medium·LIVE] `apps/web/app/[slug]/actions.ts:116` — Marketing and the site editor promise the RSVP form auto-closes at a cutoff; the RSVP write path has no deadline check and the guest portal also bypasses the guest-count lock
- [medium·LIVE] `apps/web/app/features/_sections/_VendorsLedger.tsx:53` — /features tells couples to drop the vendor's PDF in and have it OCR-scanned into the ledger; couples cannot upload contracts at all and no OCR exists in the repo
- [medium·LIVE] `apps/web/app/api/budget/[eventId]/ics/route.ts:13` — /features promises a single subscribable .ics feed that updates live, plus per-family-member feeds; the only couple-facing .ics is a session-gated one-shot download of budget dates
- [medium·LIVE] `apps/web/app/admin/repost-watch/page.tsx:439` — "Open vendor →" links on both admin moderation queues point at a route that has no page
- [medium·latent] `apps/web/lib/funnel-benchmark.ts:241` — Category Benchmarks vs Peers ships as SQL + a 254-line lib with no card, no caller, and no recompute doorway
- [medium·LIVE] `apps/web/lib/self-purchase.ts:32` — The §3.1a vendor self-purchase confirm modal and its self-comp quota reader are both orphaned — no surface renders them
- [medium·latent] `apps/web/scripts/lint-admin-chat-guard.mjs:59` — The admin chat/face-vector privacy guard scans only app/admin/**, leaving app/api/admin/** unguarded
- [medium·LIVE] `apps/web/app/admin/integrity-watch/page.tsx:473` — Three admin "Open vendor →" links point at /admin/vendors/{id}, which has no page
- [medium·latent] `apps/web/lib/llms-price-fixture.ts:21` — The llms.txt price-drift guard checks the file against a hand-typed fixture, not against the catalog it claims to reconcile
- [medium·latent] `apps/web/app/papic/actions.ts:472` — recordSeatCapture stores any client-supplied r2:// ref without checking it belongs to the seat's server-derived prefix, re-opening cross-event gallery poisoning
- [medium·latent] `apps/web/app/dashboard/[eventId]/studio/papic/moderation/actions.ts:137` — papic_photos.hidden_at is filtered by every read path but written by nothing — a paparazzi capture can never be taken down
- [medium·LIVE] `apps/web/app/dashboard/[eventId]/seating/export/route.ts:53` — Seating-plan PDF export reads the mood board with maybeSingle() on a table that is multi-row per event by design — palette is always dropped
- [medium·latent] `apps/web/app/api/v1/manpower/verify-telemetry/route.ts:115` — Manpower API team-membership check filters on columns that do not exist in the schema, so the guard can only ever deny
- [medium·latent] `supabase/migrations/20260628000000_v2_additive_phase_a.sql:138` — event_software_activations_v2 UNIQUE excludes vendor_id while the reward RPC keys on vendor_id — a second vendor on the same event silently gets nothing and a 200
- [medium·latent] `apps/web/app/dashboard/[eventId]/actions.ts:674` — BaZi birth-date/time erasure is gated behind BOTH the feature flag and `isChineseWedding()`, so stored sensitive birth data is stranded the moment either condition stops holding
- [medium·LIVE] `.env.example:444` — 36 shipped feature gates are absent from `.env.example` (including the inverted, string-exact `NEXT_PUBLIC_SEATING_3D` kill-switch), while `.env.example` documents flags no code reads
- [low·LIVE] `apps/web/app/dashboard/[eventId]/studio/papic/page.tsx:1498` — Papic studio advertises a "Battery handoff at 20%" handoff QR to the couple; there is no Battery API call and no handoff QR in the codebase
- [low·latent] `apps/web/lib/checklist-taxonomy.ts:20` — lib/checklist-taxonomy.ts is a zero-caller module that queries an events column that has never existed
- [low·latent] `turbo.json:62` — `pnpm test` at the repo root executes zero tests and exits 0
- [low·LIVE] `apps/web/lib/social/flush.ts:439` — Milestone watermark is written before the post — a failed post insert permanently suppresses that milestone with no repair path
- [low·LIVE] `apps/web/lib/songs.ts:376` — mergeSongs deletes the source rows without checking whether the re-point upsert succeeded
- [low·LIVE] `apps/web/app/dashboard/[eventId]/guests/map-actions.ts:57` — Duplicate-label recovery for guest groups uses maybeSingle() on a filter that is a strict subset of the unique key
---

## Remediation status (2026-07-23)

**Owner rulings:** fix everything · guests = read-only benign event view (never tokens/orders/faces) · token exposure fixed in code only.

**Batch A — security/money/authz/privacy (5 units, all shipping as DRAFT PRs for owner+DPO review):** dispatched `wf_f0a6d123`.
- S1 · RLS guest-scope — swap `current_event_ids()`→`current_couple_event_ids()`/`…or_coordinator…` on oauth_grants, guests(qr_token), orders, guest_face_enrollments, event_vendor_payment_plan, budget, event_appointments + blockKwento/crew-device role checks.
- S2 · Money write-guard — RESTRICTIVE policy/trigger blocking non-admin `status='paid'|'matched'`; createOrder self-comp authority; partial-payment activation guard.
- S3 · Self-grant tier — entitlement-column triggers (vendor_profiles.tier_state, vendor_custom_plans, events.setnayan_ai_active) + sku-activation refund/lifecycle reversals.
- S4 · RA 10173 erasure — delete `people` PII + biometrics + selfies on account deletion; consent-withdrawal truly deletes; temp-password out of URL.
- S5 · Public media screening — public editorial/Kwento fail-closed on unscreened + consent-withdrawn media.

**Batch B — correctness/copy/routes/periodic/fail-open (queued, mostly auto-merge after adversarial-green):**
- B1 · Wrong-join-column email bugs (`daily-email-jobs.ts:405` deletion-warning can never send; patiktok-reel email; renewal-reminder failure swallow).
- B2 · Sweeps that never converge (fullres-drop oldest-500 starvation; photo-delivery stall >240; nsfw re-screen starvation; force-majeure self-close).
- B3 · llms.txt — retired SKUs/prices/token promises.
- B4 · help.ts + privacy page — align promises with code (some legally load-bearing → draft).
- B5 · Broken routes (vendors/[vendorId], /settings, /gallery→/galleries, cameras/print).
- B6 · Papic capture points released on insert failure + vendor meter fail-closed.
- B7 · CI wires `test:db`; move 19 app/ tests under lib glob; coordinator money-scope test teeth.

---

## Batch A — SHIPPED as drafts (2026-07-23, `wf_f0a6d123`)

All 5 held as **draft PRs** — none auto-merged. Adversarial-reviewed (3 mutation-testing reviewers each) before opening.

| PR | Unit | Verdicts | State |
|---|---|---|---|
| #3604 | S1 · RLS guest-scope | SOUND/FLAWED/SOUND | draft — the FLAWED reviewer wanted the crew endpoint scoped to bookings not session; implementer kept session-derivation (brief-mandated, fails closed, no caller today) |
| #3603 | S2 · money write-guard | SOUND×3 | draft — **clean** |
| #3605 | S3 · entitlement triggers | INCOMPLETE/SOUND/INCOMPLETE→addressed | draft — **needs live-DB red/green before ready** |
| #3602 | S4 · RA 10173 erasure | INCOMPLETE/SOUND/INCOMPLETE→addressed | draft — DPO section; auto_face photo_tags + legacy selfies not scrubbed (documented) |
| #3601 | S5 · public media screening | SOUND×3 | draft — **clean** |

**S1 per-table scope decisions (reviewer-accepted):** oauth_grants→`current_couple_event_ids` (couple only) · guests(qr_token)→`…or_coordinator` · orders→`user_id OR couple` (money-wall excludes coordinator) · guest_face_enrollments→`couple` · event_vendor_payment_plan read+write→`couple` · budget SELECT **and DELETE**→`couple` (DELETE re-scope beyond brief — strict narrowing) · event_appointments write→`…or_coordinator`, read left benign. blockKwento + crew-device got action-level authz checks.

**⚠ PRE-READY GATE:** S1 (RLS re-scopes) + S3 (entitlement triggers) were NOT tested against live Postgres (no local DB; agents declined to spend on the real project). Must verify on a Supabase preview branch before marking ready: (a) a guest/browser-PATCH is BLOCKED, (b) the couple/co-host + service-role activation path still WORKS.

**Non-issue confirmed:** the S1 note that `current_couple_or_coordinator_event_ids()` lacks an accepted/removed filter is a likely false alarm — `removeHost` deletes the coordinator's `event_members` row, so removed coordinators don't appear in it. (Confirm the invite/accept lifecycle if pursued; low priority.)
