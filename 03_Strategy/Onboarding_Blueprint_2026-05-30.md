# Onboarding Blueprint — Customer + Vendor (locked 2026-05-30)

> Design lock for Setnayan's three conversion-funnel onboardings. This is the **design** source of truth; screen-by-screen copy is drafted next, engineering after. Built from the `app-onboarding-questionnaire` skill framework (Mob/Headspace/Noom archetypes), adapted to Setnayan's free-to-plan customer model and V2-publisher / token vendor model.
>
> **Status:** design locked · not yet built · engineering is a proper build (realistically post-pilot — pilot 2026-06-01 ships on the current signup → dashboard flow).
> **Register (owner pick):** full conversion-funnel *structure*, copy in Setnayan editorial voice, **soft paywalls** (every "paywall" is skippable; the free experience always continues).

---

## 1 · Scope

**Three onboardings — 1 customer + 2 vendor:**

| # | Flow | Trigger | Ends on |
|---|------|---------|---------|
| 1 | **Customer** | Customer signup | "Your Plan" — free services + matched paid boosters (soft) |
| 2 | **Vendor #1 · Signup** | Vendor signup | Starter **token bundle** + submit verification |
| 3 | **Vendor #2 · Verified** | Admin confirms the business is legit (verification approved) — days later, via first login / email deep-link | 100 free tokens + Verified/Pro/Enterprise plan choice |

Guests and admins do **not** get these funnels — they keep the lighter Driver.js guided tour (iteration **0030**). Separate, already specced.

**Folds in existing setup surfaces (owner pick: "absorb"):** customer flow absorbs event creation + the wedding-type picker (**0043**) + the mood mini-interview (**0010**) + first-vendor discovery (**0006**); vendor flow absorbs category + first package + the verification submit. It does **not** duplicate them.

---

## 2 · Cross-cutting principles

- **Soft paywall, always.** Customers keep the full free DIY experience on skip; vendors keep a free listing that still *receives* inquiries on skip. No hard gate — that would break the locked free-to-plan / Free-tier model.
- **Honest social proof.** Pre-launch = sample testimonials marked as samples now; swap for real pilot quotes after 2026-06-01. (Owner: "yes, we will change them.")
- **Setnayan editorial voice** even inside a funnel — no marketing clichés, honest comparisons, "would I say this to a friend?" test. No real dark patterns; the one "processing moment" is light anticipation, not a fake loader that lies.
- **Resumable.** Persist answers as the user goes (the skill's memory/state pattern) so a drop-off can resume.
- **One conceptual ask per screen** (Noom-style, high completion) — group only tightly-coupled micro-fields (e.g. both partners' names on one screen). *[defaulted — confirm]*
- **Account creation point:** run the personalization questions first (low friction, anonymous), then a soft account gate right before the value/plan output ("Create your free account to keep your plan"). Invest-then-gate. *[defaulted — confirm]*
- **Names auto-format to title case, live as typed (owner-locked 2026-06-03).** Every name field in onboarding — the account/full name at the soft gate (`users.full_name`) and the **four wedding-name fields** on screen 5 — Bride/Groom first + last (`events.display_name` + the four name columns) — auto-formats to **Smart PH-aware title case** while the user types (first letter up / rest down per word; particles like *de la* stay lowercase, suffixes like *III* go all-caps, *Mc* is cased, while plain *Mac* and a possessive *'s* are left alone). Same `titleCaseName()` util the guest list uses (0001) — one source of truth, re-normalized server-side on persist. Why: the couple's wedding identity + auto-monogram read cleanly without anyone having to fix CAPS-LOCK or all-lowercase entry, the UX-best default. See DECISION_LOG 2026-06-03.

---

## 3 · Flow 1 — Customer onboarding

*Reconciled to the live `events` schema 2026-05-30 (study of `apps/web/lib/events.ts` + the create-event flow + the 0043 picker). The personalization screens (**your list**) are the **customization data that actually improves results** — each writes a real column downstream surfaces read. The pain / tinder / social-proof / mood screens write **no** personalization data; they're an optional conversion layer, kept separate.*

### 3.0 · Canonical data map — what each personalization screen captures + drives

| Screen (your list) | Canonical column(s) | Captured today in | Drives |
|---|---|---|---|
| **Role** · Bride / Groom / Others *(helper)* | `event_moderators.role_subtype` | host membership / invite | multi-host (0048) · primary-host status · tailored framing. **Singular account — one person, one role (owner 2026-06-01): no "Both of us"** · the partner joins as a separate **co-host** via the 0048 invite. |
| **Kind of wedding** · Religious · Civil · Mixed | sets the `events.ceremony_type` axis (Religious→one faith · Civil→`civil` · **Mixed→*interfaith*, two faith traditions** · owner 2026-06-01) | create-event (0000 + 0043) | timeline + requirements + which vendors |
| **Religion / tradition** *(if Religious or Mixed)* | **`ceremony_type`** (+ **`secondary_ceremony_type`** when Mixed) — Catholic live; Christian · INC · Muslim · Cultural "coming soon" → notify | create-event (0043) | religion-match vendor filtering · officiant auto-resolve · faith-aware copy. **Mixed = multi-select capped at exactly 2** (rolling — a 3rd pick drops the first · owner 2026-06-01) → each tradition pre-sets its own dietary + religion-match + protocols. **HALAL pre-lock (owner 2026-06-01):** Muslim → HALAL-certified + alcohol-free pre-locked · INC → alcohol-free pre-locked (faith-correct: INC = alcohol-free, Muslim = halal) — on food/bar categories on the prefs screen. |
| **Wedding name** · Bride first + last · Groom first + last | `events.display_name` (friendly "First & First" pair) **+ `events.bride_first_name` · `bride_last_name` · `groom_first_name` · `groom_last_name`** (full names) | create-event | monogram (first-name initials) · invitation + website full names · website slug · chrome. **Four distinct fields (owner 2026-06-04):** caret auto-starts at **Bride first** · **Enter** walks first→last→first→last then closes the keyboard · the **monogram's gold frame shows from the start (muted draft) and brightens once all four are in** (gentle pop + "✦ Your monogram" caption). |
| **Wedding date** | `events.date_mode` (`specific`/`window`) + `events.event_date` (final, resolved) + `event_date_precision` · `events.date_candidates DATE[]` (specific, 1–4) · `events.date_window_start`/`date_window_end` (flexible window) | create-event / wizard | **Inline calendar, limited today → +3 years (36-month cap), no past dates** (owner 2026-06-01). **TWO modes (owner 2026-06-01, merged from 3):** **Specific dates** = pick **1–4 candidate dates** (1 date = a valid set, absorbing the old "exact" single mode) · **Flexible window** = a start+end span ≤30 days inclusive. **Live-shrinking 90-day cluster (owner 2026-06-01):** in Specific mode, once ≥1 date is picked the selectable window = `[max(picked)−90d, min(picked)+90d]` (±3 months) — it shrinks as the set spreads; at 4 dates the calendar locks (picked days still removable to swap). The date isn't the couple's preference but the candidate **all chosen vendors are free** — a 1–4-date set or a contiguous span both resolve through the **same vendor-availability convergence** at shortlist/lock time, stamping the final `event_date` (highly synced to vendors as their schedules fill). **Nugget bank** surfaces what the candidates share (all Saturdays / all weekends / same month / same season / numerology / cluster span), framed around vendor scheduling. Drives countdown · Setnayan AI scheduling · vendor availability. |
| **Area(s)** *(not venue type · ⚠ amended 2026-06-04 · ⚠⚠ persistence REVERSED 2026-06-05 → see §3.0a)* | couple picks **up to 2 areas** to *search for a reception venue* AND as the **Phase-1 working anchor** (the area's city centroid drives location filtering while no venue is locked); the stored anchor is the **chosen reception venue + coords** (`events.venue_latitude/longitude`), NOT `events.region`. Region kept only as an optional display label. | onboarding → settings | **Phase 1:** area centroid = location filter (always-on, even pre-venue). **Phase 2:** **realigns** to the chosen reception coords once locked. **Phase 3 (shortlist):** vendors labeled with which area/venue they're nearest to. (Vendor_Match §2a/§2b) |
| **Estimated pax — EXACT drag line (10− → 500+) PAIRED with an always-on "Exact count" number box + a live VIBE PHOTO** (owner 2026-06-01) | `events.estimated_pax` (the **exact** number — can be **below 10 or above 500** via the box; "not sure" = **Skip**) | Setnayan AI card #2 | **sets the starting headcount we share with vendors** + the **exact-capacity** hard-filter + personalizes the step-9 totals · sub copy: *"Your starting headcount, shared with vendors — be as specific as you can for the best matches."* **Photo-driven (owner 2026-06-01 · "create a more attractive way of showing the pax · an ideal vibe of a wedding with that number of pax · so onboarding doesn't feel like an interview alone"):** a **hero PHOTO** in the viewzone shows what a wedding at that scale feels like, with a caption (tier name + one warm line) over a dark scrim — this **replaces the old cost-data nugget**. **Five photo tiers (Filipino wedding scenes, generated via Recraft · `assets/pax/t1–t5.webp` · palette-gradient fallback):** **≤25** *Intimate · civil* — "Just you and your closest few — an unhurried, personal day" (covers the **civil-of-5** case the owner raised) · **26–80** *Warm & intimate* · **81–200** *The classic size* · **201–400** *Grand* · **401+ / 500+** *A grand fiesta*. **Two synced inputs** — drag the slider OR type in the box: (i) **slider in 10–500** → the box mirrors it live; (ii) **type 10–500** → the slider moves in real time; (iii) **type >500** → the slider parks at the **500+** end + the box holds the exact figure (e.g. 800); (iv) **type <10** → the slider parks at the **10−** end + box holds it (e.g. **5**); (v) **drag *onto* an open end (10− or 500+)** → the box goes **blank with a placeholder** so they enter the precise figure. **No snapping** (continuous, 1-guest steps). Readout shows the exact number, or **"500+"** / **"Under 10"** at an open end, with **correct grammar** (*"1 guest"* singular, else *"guests"*). The count is the pricing **floor** ("they cannot go lower than this" · couples grow their list up to it). Big-serif readout + gold-fill track + mulberry thumb + the labeled "Exact count" box in the **tapzone**; default 150. *(Superseded: the 5-chip ladder, the 50-step snap, the slider-max auto-grow, the **50-guest floor** → now **10−**, and the cost-data per-head nugget — the photo + emotional caption is the nugget now; the per-head insight lives on the budget screen.)* |
| **Estimated budget — photo-driven feel bands · range = MEDIAN per-head ±20% (owner 2026-06-01)** — Essentials · Simple · Classic · Elevated · Premium · Luxury · No limit *(owner 2026-06-01: "Still figuring" removed — it duplicated No limit)* | `events.estimated_budget_centavos` (band median × pax) + a **new `events.budget_band` enum** (the chosen feel) — *eng: band → a centavos range; add `budget_band` or derive* | Setnayan AI card | **the displayed range = ±20% of the band's median per-head** — i.e. the **initial price range vendors quote** for the guest count · recommendation ranking · shortlist budget. Per-head **medians**: Essentials ₱2,000 · Simple ₱3,500 · Classic ₱5,000 · Elevated ₱7,500 · Premium ₱11,000 · Luxury ₱15,000 (each ×0.8 → ×1.2 = the shown band, then × pax, **rounded to nearest ₱50k** with a collapse-nudge so small-pax ranges stay visible). **Photo-driven (owner 2026-06-01 "show how these different budgets would differ in looks"):** each band shows a **reception look-photo** (lean → opulent · `assets/budget/{band}.webp` · Recraft) that swaps on pick so the couple *sees* the production difference; **Classic is default-highlighted** as the ideal/recommended setup; **No limit deselects all bands** → a "No ceiling · the best of everything" top-end state (shows the luxury look). |

### 3.0a · Canonical onboarding outputs — the 20 fields onboarding MUST persist (19 outputs + recommended-per-leaf · owner-locked 2026-06-05)

Owner enumerated the definitive take-from-onboarding list. Reconciled to the **live `apps/web` commit** (`app/onboarding/wedding/actions.ts › commitOnboardingWedding`), which is **ahead of §3.0 above** in places — `region`, the shortlist→reception anchor, `mood_feel_key`, and `music_playlist_seed` already ship; the §3.0 "area transient / not stored" line and the `monogram_svg` reference are **stale and superseded here**. Build plan for the gaps: **`Onboarding_Canonical_Fields_Build_Plan_2026-06-05.md`** — **G1–G4 CLOSED 2026-06-05 (PR #1040)**: role → `event_moderators`, area picks → `style_preferences.search_areas`, taxonomy picks → `style_preferences.interested_categories`, basic palette → `style_preferences.basic_moodboard`. Only the **vendor** ≤3/leaf/location recommendation fan-out under #11 remains. #20 (recommended in-app add-ons per chosen leaf) also shipped (PR #1040).

| # | Field | Persisted to | Status |
|---|---|---|---|
| 1 | Bride's name | `events.bride_name` (+ first guest-list row) | ✅ shipped |
| 2 | Groom's name | `events.groom_name` (+ second guest-list row) | ✅ shipped |
| 3 | You (if helper) | signing user = `event_members.user_id` (Google identity) + **role** → `event_moderators.role_subtype` (bride/groom direct · helper → `family_helper` · null → `partner1`; auto-accepted first host, 0048 `PERMISSION_TEMPLATES`) | ✅ **shipped (G1 · PR #1040, 2026-06-05)** |
| 4 | Monogram | `events.monogram_frame_key` + `events.monogram_font_key` | ✅ shipped *(supersedes `monogram_svg`)* |
| 5 | Type of wedding | `events.ceremony_type` + `events.is_mixed_ceremony` | ✅ shipped |
| 6 | Religion | `events.ceremony_type` (+ `events.secondary_ceremony_type` for Mixed) | ✅ shipped |
| 7 | Target dates | `events.date_mode` + `date_candidates[]` / `date_window_start`·`end` | ✅ shipped |
| 8 | Target location | `events.region` + `venue_latitude/longitude` (primary pick centroid) + the up-to-2 area picks → `style_preferences.search_areas` | ✅ **shipped (G2 · PR #1040)** *(reversed the 2026-06-04 transient lock)* |
| 9 | Pax | `events.estimated_pax` | ✅ shipped |
| 10 | Budget | `events.budget_band` + `events.estimated_budget_centavos` | ✅ shipped |
| 11 | Services to look for *(= the taxonomy picks)* | ✅ picks persisted → `style_preferences.interested_categories[]` (PR #1040). The **vendor** ≤3/leaf/location recommendation fan-out (reception-anchored #18, else the 2 areas #8) stays a follow-on | 🟡 **picks persisted (G3 · PR #1040); vendor fan-out pending** |
| 12 | Reception venue type | `events.venue_setting` (derived) + `style_preferences.reception` | ✅ shipped |
| 13 | Ceremony venue type | `events.style_preferences.ceremony` (display blob) | 🟡 display-only; matching = Phase A2 |
| 14 | Catering type | `events.style_preferences.{cuisine,serviceStyle,dietary}` | 🟡 display-only; matching = Phase A2 |
| 15 | Photo & Video | `events.style_preferences.{pvLook,pvNeed,pvIncluded}` | 🟡 display-only; matching = Phase A2 |
| 16 | Song list | `events.music_playlist_seed[]` + `event_song_picks` | ✅ shipped |
| 17 | Basic Moodboard | `events.mood_feel_key` + the derived `FEELS[feel]` palette → `style_preferences.basic_moodboard` (0010 baseline; null for `others`/none) | ✅ **shipped (G4 · PR #1040)** |
| 18 | First reception venue | `event_vendors` (status `considering`) → `recomputeReceptionAnchor` → `venue_latitude/longitude` | ✅ shipped |
| 19 | In-app services | `events.style_preferences.interested_services` (Your Plan / Boost & enhance) | ✅ shipped (G5) |
| 20 | Recommended services *(per chosen leaf)* | the pick-matched in-app add-ons (`PICK_TO_INAPP` — every leaf → ≥1, deduped to ≤14) pre-added to `style_preferences.interested_services` | ✅ shipped (PR #1040 · owner 2026-06-05) |

**Location lock reversed (owner 2026-06-05):** the 2026-06-04 "area pick is a transient search scope · NOT stored" lock (the §3.0 Area row + the two bullets under it) is **superseded** — the area pick(s) now persist. `events.region` already lands a single key; the build seeds `events.venue_latitude/longitude` from the primary area's centroid so distance filtering works **pre-venue** (the Phase-1 working anchor), with the real reception-venue coords overwriting it once a venue is shortlisted (unchanged). Matching still anchors on the reception venue when one is present.

**What the study changed (the fix):**
- **Kind of wedding → religion / tradition (two steps), owner-corrected 2026-05-30.** Couples first pick the *kind* (Religious · Civil · Mixed), then — for Religious/Mixed — the *faith* (Catholic live; others coming soon). Together they resolve `ceremony_type`. **`venue_setting` moves OUT of onboarding** — it's set later (event settings / wizard); onboarding asks **region** instead.
- **Pax + budget already exist** as `events.estimated_pax` / `events.estimated_budget_centavos` (today they're Setnayan AI wizard cards). Onboarding **pulls them forward** so the first vendor demo sizes + ranks correctly.
- **Role = `event_moderators.role_subtype`** — "Others" = parent / planner / entourage (0048), not throwaway.
- **Area search, not a region picker (⚠ reworked 2026-06-04 · supersedes the 2026-05-30 region correction).** Screen 7 is a **search field + GPS "Near me" toggle** over a static PH city/hub dataset — the couple picks **up to 2 areas** to *search for a reception venue* (e.g. Tagaytay + Quezon City). The area pick is a **transient search scope**, not a stored hard-filter: vendor matching anchors on the chosen reception-venue coords by **distance ≤ service_radius** (Vendor_Match §2b · region kept only as a display label).
- **⚠ Area pick is a transient search scope, region is NOT the filter (owner-asked 2026-06-04 · supersedes the "derive `events.region` so the area-filter works" eng note above).** Screen 7 now lets the couple pick **up to 2 areas**, and that pick **only scopes the reception-venue search** — it is **not stored** and **not a hard filter**. The persistent anchor is the **chosen reception venue + coords**; vendor "serves my area" matching is **`distance(reception_venue, vendor) ≤ vendor.service_radius`** (straight-line in V1; drive-time is V2). Out-of-range vendors are hidden with a count + names, not greyed. Region survives only as an optional human-readable display label. Full correction: [Vendor_Match_Personalization §2b](Vendor_Match_Personalization_2026-06-01.md) + DECISION_LOG 2026-06-04.
- **Account constraint:** an `events` write needs an authed user. **LOCKED — invest-then-gate:** answers held in client state, account created at "Customizing your plan…" (screen 11).

### 3.1 · Full sequence — **LOCKED (lean), 2026-05-30**

**Consolidated — 14 screens, one navigable file: `Onboarding_Customer_Flow.html`** (supersedes the split `Onboarding_Customer_01_Welcome.html` + `_Flow_PartA.html` + `_Plan.html`).

**Personalize** *(canonical data · before login)*
1 Welcome · 2 Role · **3 Kind of wedding** (Religious · Civil · Mixed) · **4 Religion / tradition** (Catholic live; others coming soon — skipped for Civil) · 5 Wedding name *(**Bride/Groom first + last** · **Enter advances field→field** · auto-monogram **shows a muted draft frame from the start, brightens once all four are in** · **luxurious recolorable SVG frames** · tap the monogram to restyle (curated combos) · **premium fonts only**)* · 6 Wedding date · **7 Area / location** *(search + GPS "Near me" · pick up to 2 · scopes the reception-venue search)* · 8 Estimated pax (**drag line 10−→500+ + exact-count box, synced** · *+ a live reception PHOTO showing the crowd-scale at that count*) · 9 Estimated budget (**photo-driven feel bands** Essentials→Luxury + No limit · default **Classic** · *extreme-contrast look-photo per band · **photo scales to pax tier** · Luxury made grander · No limit keeps its own photo · ±20%-median ranges rounded to ₱50k*) · 10 What would you love? (**picker** · per-category **photo + definition** preview · equal-width chips)
**10 What would you love?** — service picker, **interest-only (no prices)** · *sticky upper preview + description per tap* → builds the one-time package
**11 Customizing your plan… → create free account** — the **login moment** (invest-then-gate)

**Payoff** *(after login)*
12 Find your first vendor *(demo · tap to shortlist)* · 13 Your starting plan *(the congrats + stats + motivation moment)* · 14 **Your Plan** *(§3.2)*

**Part B — Convert — CUT 2026-05-30.** Owner went lean: no pain / tinder / social-proof / mood. The data does the personalizing; notifications priming folds into the dashboard, not the funnel. *(Full plot: `Onboarding_Sequence_Map.html`.)*

### 3.1a · Integrated flow — UPDATED 2026-06-01 (folds in per-category preferences + religion/certs · verified-only marketplace, vendor-pool screen removed)

The lean 14 (§3.1) predates the **Vendor Match Personalization** lock ([Vendor_Match_Personalization_2026-06-01.md](Vendor_Match_Personalization_2026-06-01.md)). The integrated flow adds **1 net-new screen** — *Your style & preferences* (the per-category preference capture) — and **folds the mood mini-interview + the HALAL/dietary faith-confirm INTO it** (no extra screens). *(The vendor-pool screen "Who should we show you?" was removed 2026-06-01 — the marketplace is verified-only by rule, so there's no all-vs-verified choice to make.)* **15 screens; a typical couple taps through ~11–13** (conditional screens skip · most preference questions are quick chip-taps). Navigable prototype: **`Onboarding_Wedding_Flow_2026-06-01.html`**.

**Trigger — this IS the wedding create-event flow (owner 2026-06-01):** the flow runs when a customer clicks **Add Event → Wedding** (iteration 0000 Add Event + the 0043 wedding-type picker), not a separate signup-only funnel. A brand-new visitor hits the account gate (screen 13) to create their account; an already-signed-in user adding another event **skips the gate** and writes straight to the new `events` row. (Other event types stay "Coming soon" — only Wedding runs this flow in V1.)

**🔒 The 7 Golden Rules — owner-locked 2026-06-01 (apply to EVERY onboarding screen; full memory: [[feedback_setnayan_onboarding_golden_rules]]):** **(1) NO SCROLLING** — every screen fits one phone viewport (≈665px body on the 375×812 frame). When a screen overflows, CUT (drop redundant copy, compact components, shrink cells) — don't accept the scroll. **No dead voids either (owner 2026-06-01 "look for dead spots"):** short choice/input screens whose controls would otherwise strand at the bottom (leaving a big empty middle) **grow their option cluster / hero to FILL** the gap — Role + Kind use big `.opt` cards spread `space-evenly`, Faith's chips fill + enlarge, Name's monogram enlarges + centers, budget's photo fills (picker / find-vendor / your-plan scroll instead). Audit fixed: **Role 417→0 · Faith 379→0 · Kind 223→0 · Name 212→0 px**. The only tolerated exceptions are the genuine "select-from-many" screens (the service **picker** + the **prefs** screen) and the **results/summary** screens (find-vendor · your-plan), which **scroll internally with the brand top-bar + Continue pinned**. **(2) Setnayan always visible** — the brand lockup (the **official gold `#cb9e4b` Setnayan monogram**, inlined as `<svg>` per PR #695, + SETNAYAN wordmark) shows in the top bar on every screen, so a screenshot always carries the brand. **No step numbers (owner 2026-06-01)** — show only the **progress bar**, never "X / 15" (avoids the awkward count when Civil skips a step). **Top-bar back + Skip · full-width CTA (owner 2026-06-01 "the let's go button needs to take the whole bottom part"):** the back ‹ (compact circle) sits **top-left** + **Skip** sits **top-right** in the brand bar (`back.style.display` toggles per screen; Skip per `canSkip[]`), so the bottom bar is a single **full-width primary CTA** (`.btn{flex:1}` is now the only child of `.bottom`). **(3) Keep words at their best** so everything fits (short h1, 1-line-or-no sub, tight chip labels, ≤2 nugget reasons). **(4) Attractive but premium** (Clean Editorial · calm motion). **(5) Photos + app icons allowed** where they earn their height. **(6) Thumb-zone split — upper half = viewing/commentary · lower half = interactive controls.** Strategically position everything: the **upper** zone holds what you read/look at (eyebrow · question · sub · nuggets · why-panels · budget compass · previews); the **lower** zone holds what you tap (chips · options · calendar + toggle · input · pickers) docked toward the bottom for thumb reach. Pattern: fixed-height phone (`height:100dvh` / desktop `880px`, `overflow:hidden`) + `.body` as the internal scroll viewport (`display:flex;flex-direction:column;min-height:0;overflow-y:auto`) + each `.screen.active{flex:1 0 auto}` with a `.viewzone` (top) and `.tapzone{margin-top:auto}` (docked bottom). On the date screen the **nugget floats to the upper zone** while the calendar deck sits lower; on budget the **compass/nugget is upper**, chips lower. **(7) Preloaded & instant — no loading time between pages (owner 2026-06-01).** The whole onboarding is preloaded and ready, so Continue→next (or Back) is **instant** — a screen never waits on a fetch. Single document (all screens live in the DOM; nav toggles `.active`, never fetches a page) + **all imagery preloaded on first load** (`<link rel="preload" as="image">` for the first-shown asset + JS `new Image()` warming the rest — the 5 pax vibe photos `assets/pax/t1–t5.webp`, plus future region thumbnails · style photo-chips · hero bands · palette strips) + preconnected/preloaded fonts. The auto-monogram is text+CSS so it has nothing to fetch, and the **brand mark is an inlined `<svg>`** (the official gold `#cb9e4b` Setnayan monogram, PR #695) — zero fetch, renders instantly on every screen regardless of server MIME. **Recommended PRODUCTION process (owner 2026-06-01 "download all the onboarding files so everybody gets a fast pre-set-up experience — anyone who presses it loads + presets the whole onboarding"): yes, fully possible — 6-layer stack, the prototype already proves layers 1+3+6:** (1) **ONE client-component onboarding route** (`/dashboard/create-event` · Add Event → Wedding) — all screens in memory, step transitions toggle state (like the prototype's `.active`), so Continue→next is **instant with zero network round-trip · no page-per-step navigation**; (2) **route-prefetch on the entry CTA** — Next.js `<Link prefetch>` (default in App Router) on the marketing "Start planning · free" + the Add-Event → Wedding buttons preloads the onboarding bundle the moment the CTA enters view, so the code is **already on the device before they tap**; (3) **eager-preload the welcome hero + background-warm the rest after first paint** — `<link rel="preload">` the first-seen hero + a `new Image()` warm-loop for every pax/budget/picker/feel/ceremony/prefs photo right after the welcome paints, so each later screen's photo is **decoded + cached before they reach it** (prototype does this today); (4) **next/image + Vercel image CDN** — every onboarding photo through next/image (AVIF/WebP · responsive sizes · edge-cached globally) so each payload is the smallest possible + served from the nearest edge; (5) **service-worker / PWA precache** — the V2 Phase G offline daemon precaches the onboarding bundle + photo set on first visit, so a **repeat visitor (or an installed PWA) gets instant onboarding even offline** — the literal "anyone who presses it loads + presets it"; (6) **inline brand mark** (done — zero fetch). Net: the prototype already delivers instant in-flow transitions + preloaded imagery; the production additions are **route-prefetch on the CTA (2), next/image+CDN (4), and the service-worker precache (5)**. **Corollary:** no redundant "Not sure yet" defer-ghosts — the bottom CTA bar already has **Skip**, which is the defer path (also reclaims height for Rule 1).

**Premium UX direction (owner 2026-06-01):** the build should feel **premium with a genuinely good UX** — tasteful micro-interactions (chip + calendar-day pop, the why-date panel rising in, CTA depth, tap-scale feedback, smooth screen transitions), calm Clean-Editorial motion (no flashy parallax), and `prefers-reduced-motion` honored. The prototype demonstrates the baseline; the build should match or exceed it.

**Photo placeholders (owner 2026-06-01):** imagery is placed where it makes the flow feel more personal — a **welcome hero band**, **region cards** (destination thumbnails on screen 7), and the **visual style picks** on screen 11 rendered as **photo-chips** (venue setting · cuisine · cake · florals · photo-video look — a 2-col image grid) plus a **palette inspiration strip**. In the prototype most are stylized gradient placeholders with content-hinting glyphs; the build sources real imagery from the same owned-asset / Higgsfield pipeline as the mood board ([CLAUDE.md 2026-05-21](../CLAUDE.md)). **Screens 0, 5, 8, 9, 10 and 11 already ship real generated photos** (all Recraft · ~14–130KB each · palette gradient as the load fallback): screen 0 = the **full-bleed welcome hero** `assets/welcome.webp` (owner 2026-06-01 "use this as the onboarding for the customer" + a golden-hour Filipino-couple reference photo — a hilltop couple at sunset overlooking the sea · edge-to-edge `object-fit:cover` with a bottom scrim carrying the italic "Let's plan your wedding." headline + sub + the full-width "Let's go" CTA); screen 5 = the **4 premium hand-built gold monogram frames** `assets/mono/{wreath,crest,square,oval}.svg` (vector SVG · inlined as data-URIs · the initials overlay live in `.mono-letters`); screen 8 = `assets/pax/t1–t5.webp` (elevated reception **crowd-scale** 25→500+); screen 9 = `assets/budget/{essentials…luxury}.webp` + `nolimit.webp` **+ 30 pax-tier × band** `assets/budget/{t1–t5}_{band}.webp` (the band's **look** lean→opulent, **scaled to the guest count** — the same budget reads lean at 500 guests, lavish at 25); screen 10 = the **~53 per-service picker photos** `assets/picker/<service>.webp` (a distinct hero per service · keyed off the tapped chip's `data-cat`); screen 11 = the **16 style-card photos** `assets/prefs/{setting_*, cuisine_*, pv_*}.webp` (7 reception settings · 4 cuisines · 5 photo-video looks · each `.pcard` background keyed off the option's photo key, gradient+emoji fallback) **+ the 7 feel example photos** `assets/prefs/feel_*.webp` (Timeless · Modern · Boho · Rustic · Glam · Royalty · Filipiniana — **owner 2026-06-01 redo: each is now a full-blast reception scene in the SAME wide eye-level angle, couple centered showing their attire + the styled reception background, per the owner's reference photo** — shown on the palette/mood screen) **+ the 5 Filipino-couple ceremony photos** `assets/prefs/ceremony_{church,garden,beach,civil,same_reception}.webp` (a Filipino couple saying "I do" in each setting · on the ceremony screen) **+ Italian & Fusion cuisines** `assets/prefs/cuisine_{italian,fusion}.webp` (catering now 6). *(The 6 music-act photos `assets/prefs/music_*.webp` are now **orphaned** — music became a text **top-50 song picker** on 2026-06-01, no images.)* All warmed on first load (Rule 7). **Every photo carries a small SETNAYAN logo watermark (owner 2026-06-01 "each photo overlayed with a setnayan logo"):** a CSS `::after` corner wordmark on the pax / budget / picker hero / feel photos + the picker & prefs cards (the real build composites the actual brand mark server-side). **One photo size (owner 2026-06-01):** the picker preview hero (screen 10) sits at `clamp(196px,30vh,224px)`. *(The budget look-photo (screen 9) originally matched this, but owner 2026-06-01 "stretch the photo so it fills the screen" → the budget photo now **grows to fill** its viewzone (`flex:1 1 auto`, ~341px), superseding the shared size for budget only.)* Text-first screens (role, date, account gate) stay clean — photos only where they add warmth, never decoration for its own sake.

| # | Screen | Writes / drives | Notes |
|---|---|---|---|
| 1 | Welcome | — | "Let's plan your wedding — free." Sets the few-quick-questions expectation. |
| 2 | Role | `event_moderators.role_subtype` | **Singular account — one person, one role (owner 2026-06-01): Bride · Groom · Someone helping** (parent / planner / entourage). **No "Both of us"** — a Setnayan account is one person; the partner joins as a **co-host** via the multi-host invite (0048). Sub: "This account is just you — your partner can join as a co-host anytime." |
| 3 | Kind of wedding | ceremony_type axis | **Religious** (one faith) · **Civil** · **Mixed** (two faith traditions — an *interfaith* wedding, e.g. Catholic & Muslim · owner 2026-06-01). |
| 4 | Religion / tradition *(adaptive)* | `ceremony_type` (+ `secondary_ceremony_type` for Mixed) | **Adapts to step 3 (owner 2026-06-01):** *Religious* → **single-select** one faith · *Mixed* → **multi-select capped at exactly 2** (rolling — a 3rd pick drops the first · `data-max="2"`; writes `ceremony_type` + `secondary_ceremony_type`) · *Civil* → **skipped entirely (owner 2026-06-01)** — nav jumps step 3 → step 5 (no faith screen shown), back jumps 5 → 3. Catholic live; Christian / INC / Muslim / Cultural coming-soon → notify. **Pre-sets dietary + religion-match + ceremony setting — for *each* tradition picked.** **HALAL pre-lock (owner 2026-06-01):** carried to the prefs screen — Muslim → HALAL-certified + alcohol-free chips pre-checked + locked on food/bar categories (catering · cake · stations · mobile_bar); INC → alcohol-free pre-locked (faith-correct: INC = alcohol-free, Muslim = halal). |
| 5 | Wedding name | `events.display_name` | **Auto-monogram delight (owner 2026-06-01 · "can we give them a Monogram here? or later?" → here, as a free auto-mark · styling later).** A gold double-ring **monogram medallion** fills the viewzone and is **made live from the name** as they type ("Maria & Juan" → **M & J**; single name → first 2 letters), Cormorant-italic over a cream radial — so the screen feels like *their* wedding instead of an empty interview field (Golden Rule 5 imagery · Rule 6 medallion-in-viewzone / input-in-tapzone). It's the **free auto mark** (iteration 0037 baseline). **This free auto-mark IS the couple's switcher icon (owner-locked 2026-06-03):** the chosen frame + font + initials persist to `events.monogram_svg`, which the **upper-left event-switcher monogram** in app chrome renders ([0000](../0000_app_shell_and_navigation/0000_app_shell_and_navigation.md) § event switcher) — so the couple's gold mark identifies their event in chrome from onboarding completion, replacing the plain-initials fallback (rendered letters-forward at the ~28px switcher size · the ornate frame reads here in the medallion + the Website editor · the upper-right profile avatar per 0021 §2.0c stays the "me / home" initials-or-photo affordance, a deliberately separate element). **Tap-to-REGENERATE · no controls (owner 2026-06-04 · supersedes the 2026-06-01 Frame/Font two-button restyle):** the **Frame + Font buttons are removed** — the couple **taps the medallion to regenerate**, cycling the next **curated combination**; a muted "↻ Tap the monogram for another style" hint sits where the buttons were. The **app picks the pairing** (frame + font + colours) so every result is a strategically good-looking, on-brand combination — no choice-overload. **LUXURIOUS frames (owner 2026-06-01 "we want frames that would look premium for the guests · the letter need to fit properly inside the frame" + 5 ornate-gold reference frames):** the 4 frames are **premium hand-built wedding frames** (**inline vector SVG** · perfectly symmetric · **flat — no shadow, no gradient · "just design"** (owner 2026-06-04) · **recolorable**: every line/fill is class-coloured from the active combo via `--fc`/`--fc2`, so the **frame colour is paired to complement the initials** (gold ⟷ mulberry ⟷ obsidian duos from the brand palette) · clear center holds the initials · crisp at any size · `assets/mono/{wreath,crest,square,oval}.svg`) cycling **round floral wreath → crowned heraldic crest → art-deco gold border → ornate oval cartouche** (the crest honors the interlocked-rings-with-crowns reference). The initials render **inside each frame's clear center** (on the paper, since the center is transparent) in `.mono-letters` (the crest's center sits below the crown → letters nudged down `translateY(11%)`); sizes tuned so even the widest case ("M & J" in script) fits the narrowest frame (oval/crest ~79px clear) — verified across all 4 frames × 4 fonts. **PREMIUM-only fonts (owner 2026-06-01 "we will never offer basic fonts even if this is for free · that is fine"):** **Cormorant Garamond italic · Cinzel engraved caps · Playfair Display italic · Great Vibes calligraphy script** — **DM Mono dropped** (basic). Frames + 3 web-fonts are warmed on first load (Rule 7). This is a small **free** set; the **deeper premium frame/font library + Animated/Bespoke** stay the paid upgrade (0004 Pro Hero / 0037 Bespoke) — the dashboard Website editor + the Setnayan AI **"Design Monogram"** card. No new SKU; no scroll (665/665 · verified). **Four fields · Enter-to-advance · generate-on-complete (owner 2026-06-04):** the screen captures **Bride first + last** and **Groom first + last** as four fields (2×2 grid · `label · first · last` per row). The **text caret auto-starts at Bride first name** on entry (mobile keyboard opens straight onto it); pressing **Enter** advances first→last→first→last, and **Enter on Groom last name closes the keyboard**. The gold frame shows **from the start as a muted draft** so couples can preview + cycle frame styles while typing (Frame/Font live immediately); once **all four names are entered** the mark **brightens to full + a "✦ Your monogram" caption fades in + a gentle pop** = the "it's done" moment (owner 2026-06-04 "show while choosing" · supersedes the same-day no-frame-until-complete reveal; also fires on Continue if all four are filled). First-name initials still drive the mark ("Maria … Juan" → **M & J**); the **full names** (incl. last) feed the invitation + website. Names still auto-title-case live via the shared `titleCaseName()` (§ above · production). |
| 6 | Wedding date | `events.event_date` (final, resolved) · `events.date_candidates DATE[]` (specific 1–4) · `events.date_window_start`/`date_window_end` (window) · `events.date_mode` (`specific`/`window`) + precision | **Inline calendar — limited today → +3 years** (no past dates · 36-month cap · owner 2026-06-01) · **redesigned to fit one screen (≈613px, no-scroll · Golden Rule 1)**. Month nav disabled at the bounds; out-of-range days greyed. **Readout + helper sit ABOVE the mode toggle (owner 2026-06-01)** — the "Your date(s): …" readout + the "Up to 4 dates within ~3 months — we lock the one all your vendors share" helper render at the top of the lower deck, above the Specific/Flexible toggle + calendar. **TWO-mode toggle (owner 2026-06-01, merged single+specific):** *Specific dates* = pick **1–4 candidate dates** (1 date = a valid set — the meaningful-candidates case: anniversary, lucky date, long weekend, when relatives can fly in) · *Flexible window* = pick a start + end, **capped at 30 days inclusive** (over-cap taps auto-trim with a notice). **Live-shrinking 90-day cluster:** once ≥1 specific date is picked, the selectable window = `[max(picked)−90d, min(picked)+90d]` and shrinks as the set spreads; at 4 dates the calendar **locks** (picked days still removable to swap). **The wedding date isn't the day they'd pick — it's the candidate the chosen vendors all share** (highly synced to vendors as schedules fill), resolved at shortlist/lock time (1–4-date set or contiguous span → the same convergence engine — see Vendor Match §2 + the vendor soft-hold model; the screen-13 demo + the Plan+Budget accordion converge on the shared-free date). **Date nuggets:** 1 date → "✦ Why this date works" (5-layer taste — liturgical · numerology · folklore · weather · astrology from [18_Concierge_Brain/09_Date_Selection_Cultural_Logic.md](../02_Specifications/18_Concierge_Brain/09_Date_Selection_Cultural_Logic.md) · tone chip + 1 reason + "see all 5 layers with Setnayan Concierge →" teaser); 2–4 dates → "✦ What your dates share" **nugget bank** (all Saturdays · all weekends · same month · same season · numerology 8/18/28 · cluster span — every nugget framed around vendor scheduling, "a vendor only needs one open slot · as schedules fill, we lock the one they all share"). *(No "Not sure yet" ghost — the bottom-bar **Skip** is the defer path · Golden Rule corollary.)* |
| 7 | **Area / location** *(⚠ reworked 2026-06-04 · supersedes the single-select region picker)* | transient **search scope** → resolves to the **reception-venue anchor** (`events.venue_latitude`/`venue_longitude`); region = display label only, never the filter (§2b) | **Search field + GPS "Near me" toggle pinned at the BOTTOM** (owner 2026-06-04 · results rise above it · thumb-zone Rule 6). **City is the pick · region is a grey subtitle label** (disambiguates "San Fernando · Pampanga" vs "· La Union" + signals distance · never the filter). Default (idle) = a **Top-30 PH wedding-cities CAROUSEL** sitting just above the search bar (expanded from 10 · owner 2026-06-04 *"do top 30"*) — a horizontal scroll-snap strip of **City cards** ranked 1–30 (**Tagaytay · Cebu · Boracay · El Nido · Baguio · Nasugbu · Panglao · Manila · Makati · Vigan · Quezon City · BGC · Davao · Iloilo · Bacolod · Puerto Princesa · Coron · Siargao · Dumaguete · Bohol · Mactan · Subic · La Union · Clark · Calatagan · Antipolo · Cagayan de Oro · Siquijor · Legazpi · Sagada** — City, not region), each with a **per-city wedding nugget** (one-line local flavor — e.g. Tagaytay *"cool-climate gardens & ridges over Taal"*) AND a **real generated photo** behind the card scrim (**30 Recraft photos shipped** · `assets/cities/{key}.webp` · 428×760 WebP · ~1.2MB total · preloaded per Rule 7). The carousel **slides DOWN out of view the moment you start typing, and rises back UP when the box is cleared** (owner 2026-06-04 · smooth transform+opacity transition · the results list cross-fades in). **"Near me"** reads device GPS (free) and re-sorts **nearest-first** with `· N km` hints (haversine); **typing searches EVERY PH locality** — the prototype now **bundles the complete PSGC set** (**all 81 provinces + 1,634 cities/municipalities = 1,665 places across all 17 regions**, incl. every archipelagic province — Batanes · Sulu · Tawi-Tawi · Dinagat Islands · Basilan · Guimaras · Marinduque · Siquijor · Romblon · Sarangani — so **no spot in the country is unavailable**, owner 2026-06-04; bundled as `assets/ph_places.js` ~80KB on-device · ₱0, no Places API, instant per Golden Rule 7 · graceful Metro-Manila fallback if GPS denied) — swapping the carousel for a **results list, each row with a per-place nugget** (curated for the well-known cities · a graceful *"venues & vendors who serve {place}"* fallback otherwise). The 30 curated carousel cities keep precise coords + nuggets + real photos; the long-tail PSGC places carry a region-centroid coord for the Near-me sort and resolve to the same anchor model on pick. **Photo scope (owner 2026-06-04 "do top 30"):** real photos are the **Top-30 carousel's** treatment only — the search-results long tail (all 1,665 PSGC localities) stays **nuggets/text** (no per-municipality photo banks · doesn't scale · nuggets are ₱0 + infinite); a **17-image region-level fallback** is the path *if/when* a downstream "your place" hero is added, never per-municipality. **Pick up to 2** areas → removable chips. This only **scopes the reception-venue search** — once a venue is chosen, vendor matching anchors on its coords by **`distance ≤ service_radius` OR the vendor's explicit served-area tag** (HYBRID coverage · a La-Union caterer who tags Quezon City serves a QC wedding · Vendor_Match §2b), so "Tagaytay + Quezon City" means *search venues in both*, not a 2-region hard-filter. **Location = "which services can serve here"** (owner 2026-06-04) — the area surfaces the venues + the vendors whose coverage reaches it. Prototypes: `Onboarding_Wedding_Flow_2026-06-01.html` (screen 7) · `Reception_Anchored_Services_2026-06-04.html` (reception-anchor demo). *(Supersedes the 2026-06-01 Top-5-regions + "Somewhere else"→13-more single-select picker.)* |
| 8 | Estimated pax | `events.estimated_pax` (**exact** · can be <10 or >500) | **EXACT drag line (10−→500+) + an always-on "Exact count" box + a live VIBE PHOTO (owner 2026-06-01 · supersedes the 5 chips, the 50-step snap, the auto-grow, and the 50-guest floor).** A **stretched, responsive hero photo** (`height:clamp(244px,38vh,296px)` · `object-fit:cover`, never distorted · fills the screen neatly · owner 2026-06-01 "stretch the photo so it is nice and neat") in the viewzone shows the **crowd-scale** of a wedding at that size — *an elevated/aerial reception shot where you can see how many guests it looks like* (owner 2026-06-01 "a photo where we can see how many the guests looks like on their reception"; the 5 tiers regenerated as elevated wide reception shots, the crowd visibly filling 2 tables → a packed banquet hall as 25 → 500+), caption = tier + one warm line over a scrim, so the screen **inspires rather than interrogates** — 5 Filipino-wedding tiers generated via **Recraft** (`assets/pax/t1–t5.webp` · gradient fallback): **≤25** *Intimate · civil* (the **civil-of-5** case) · **26–80** *Warm & intimate* · **81–200** *The classic size* · **201–400** *Grand* · **401+/500+** *A grand fiesta*. Slider 10–500 in range mirrors the box live; **type 10–500 → slider moves real-time; type >500 → slider parks at 500+ end + box keeps the exact figure; type <10 → slider parks at 10− end + box keeps it (e.g. 5); drag onto an open end → box goes BLANK + placeholder.** **No snapping.** Readout shows exact / "500+" / "Under 10" with **correct grammar** (*"1 guest"* singular). "not sure" = **Skip**. The count **sets the starting headcount shared with vendors** + is the **pricing floor** ("they cannot go lower than this" · grow your list up to it); sub: *"Your starting headcount, shared with vendors — be as specific as you can for the best matches."* Big-serif readout + gold-fill track + mulberry thumb + labeled box in the **tapzone** (Golden Rule 6); default 150. Drives the **exact-capacity** hard-filter AND the **personalized** step-9 totals. *(The vibe photo + caption replaces the old cost-data nugget; the per-head "eases as you add guests" insight now lives on the budget screen.)* |
| 9 | Estimated budget | `events.estimated_budget_centavos` + new `events.budget_band` (eng note) | **Photo-driven feel-band ladder · range = MEDIAN per-head ±20% (owner 2026-06-01).** **7 chips** (3+3+1): **Essentials · Simple · Classic · Elevated · Premium · Luxury · No limit** — *"Still figuring" removed (owner 2026-06-01: duplicated No limit; Skip is the defer path).* **Photo-driven (owner 2026-06-01 "show how these different budgets would differ in looks"):** a viewzone **look-photo** shows the band's reception aesthetic — lean/simple → opulent — and **swaps on pick** so the couple *sees* the production difference (`assets/budget/{essentials…luxury}.webp` · Recraft · gradient fallback) — regenerated 2026-06-01 for **extreme contrast** (owner "show extreme differences · a wide shot of how much they differ"): a **bare community hall with folding chairs** (Essentials) → a **grand draped ballroom with chandeliers** (Luxury), each step a clear visual leap; caption = band tag + the pax-scaled ₱ range. **Luxury made grander (owner 2026-06-01 "a more grand photo for the working budget luxury · make it more grand · no limit's photo will stay"):** the **Luxury** band photo was regenerated even more opulent (a vast ballroom under a sprawling crystal chandelier, hundreds of guests), and **No limit got its OWN preserved photo** (`assets/budget/nolimit.webp` = the prior luxury look) so making Luxury grander didn't change the No-limit look. **Default-highlight the IDEAL band (owner 2026-06-01):** **Classic** ("the sweet spot") is pre-selected on entry — *"the ideal setup they want from the working budget"* — so the couple lands on a recommended view + photo, then picks what they want. **No limit deselects all bands (owner 2026-06-01)** → a **"No ceiling · the best of everything"** state (shows its own preserved `nolimit.webp`). **On pick**, the band's range = **±20% of its median per-head × exact pax, rounded to nearest ₱50k** (collapse-nudge keeps small-pax ranges visible), labelled "₱X – ₱Y · ~N guests." **Per-head MEDIANS** (PH 2026 — Storia/Nuptl/Kasyon): **Essentials ₱2,000** *Lean & intentional* (most DIY/outside/free + Setnayan's free planning · the bare floor where the app still works) · **Simple ₱3,500** *Comfortable* · **Classic ₱5,000** *The sweet spot* (the most common · the ideal default) · **Elevated ₱7,500** *Polished* · **Premium ₱11,000** *Entry luxury* · **Luxury ₱15,000** *No-compromise* · **No limit** → "No ceiling." Verified 665/665 no-scroll (Classic ₱600K–₱900K → Luxury ₱1.8M–₱2.7M @ 150 guests). **Pax-tier-aware photo (owner 2026-06-01 "different photos for each [pax tier] · how much better it will look when this is their working budget for that pax"):** the band's look-photo also keys on the couple's **pax tier** — `assets/budget/{t1–t5}_{band}.webp` (30 photos = 5 tiers × 6 bands · Recraft) — so the **same band visibly changes by guest count**: lean at 500 guests, lavish at 25 (e.g. `t5_premium` = a packed ~450-guest grand reception with a flower-lined aisle; `t1_premium` = an intimate lavish room). No limit → the tier's grandest look (`{tier}_luxury`); the generic `{band}.webp` is the no-pax fallback. *This is the couple's whole-wedding budget compass (real pax) — distinct from the Setnayan-SKU 100-pax pricing floor.* **Photo fills the screen (owner 2026-06-01 "stretch the photo so it fills the screen"):** the look-photo **grows to fill the dead space** between the sub-text and the chips — scoped `#screen-budget .viewzone{flex:1 1 auto;display:flex;flex-direction:column}` + `.budgetphoto{flex:1 1 auto;height:auto;min-height:210px}` (object-fit:cover → just reveals more of the image, no distortion); verified ~341px tall, flush above the chips, 665/665 no-scroll. **This supersedes the "one photo size" lock for the budget screen** — budget now fills; the picker hero stays at `clamp(196px,30vh,224px)`. |
| 10 | **What would you love?** | picker (interest) | **All child categories** — every service across the 10 parents (~53 tiles · grouped Venue → Planning → Feast → Design → Program → Documentary → Look → Booths → Prints → Transport), **not a curated subset** (owner 2026-06-01). Multi-select → drives screen 11 + the package. **Sticky preview + PHOTO + description (owner 2026-06-01 "there is something peeking between the SETNAYAN and venue · this is where the photos should be · the upper half should be with photo and definition on what this category serves"):** a pinned upper card (sticks **flush** under the brand bar while the chip list scrolls) leads with a **prominent full-width PHOTO hero (182px banner · owner 2026-06-01 "increase the height of the photo so the services can be seen better" — bumped 134→182px · redone from the earlier small left-thumbnail into a proper hero, matching the pax/budget hero style)** with the **category + service name overlaid** on a bottom scrim + the **1-line "what it provides" definition below the photo** — **a distinct photo per service** (owner 2026-06-01 "the photos must change for each — reception venue, ceremony venue, coordinator, catering, cake, food stations, stylist/decorator, florist, lights and sounds, and everything else") — ~53 photos `assets/picker/<service-key>.webp` (Recraft · keyed off the tapped service's own `data-cat`, **not** the parent group · so reception ≠ ceremony, catering ≠ cake ≠ food stations, etc.), name + definition from the `PICK_INFO` map (all ~53 services), updating on every tap so the couple **sees what that specific service looks like + reads what it serves**. All preloaded on first load via `Object.keys(PICK_INFO)` (Rule 7). **Peek fixed (owner's "something peeking"):** the card is opaque + a `::before` paper cap covers the `.body` 14px top-padding band so no chip shows between the brand bar and the pinned card. **Equal-width chips (owner 2026-06-01 "stretch each button equally · 1 in a row → full row · 2 → evenly divide"):** picker chips `flex:1 1 150px` → each row's chips grow to fill it evenly (a lone chip = full row, two = halves). **Per-group solo/pair layout (owner 2026-06-01 "realignment of buttons"):** each group is split into explicit `.chips` rows so solo services span a full row and the rest pair into even halves — **Feast:** Catering (solo) · Cake + Food stations · **Design:** Stylist/decorator (solo) · Lights&sound + Florist · Dance floor + LED wall · Fireworks + Outdoor · **Digital Services** (solo · NEW 2026-06-03 — the Setnayan digital-productions category; selecting it suggests Pakanta · Animated Monogram · Pro Website · Live Venue Photo Wall · Pailaw on Your Plan; see [Digital_Services_Cross_Surface_Map_2026-06-03.md](Digital_Services_Cross_Surface_Map_2026-06-03.md)) · **Program:** Host/MC (solo) · Live band + Orchestra · Choir + Wedding singer · DJ + Choreographer · Performers (solo) · **Documentary:** Photo & Video (solo) · Livestream + Editorial feature · **Look:** Bride's attire (solo) · Groom's + Grooming · Hair&makeup + Wellness · Filipiniana & Barong (solo) · Women's + Men's · Jewellery & accessories (solo) · **Booths:** Photo booth (solo) then 7 pairs (incl. "**Calligraphy / Caricature / Live Art**" + Tarot — the one deliberate relabel) · **Transport:** Bridal car (solo) · Guest shuttle + Escort. Defaults to *Reception venue*. |
| 11 | **Your style & preferences** *(NEW · adaptive · sub-stepper)* | `event_vendor_preferences` (+ derived) | **ONE FOCUSED SCREEN EACH (owner 2026-06-01 — picked over one-scrolling-page + swipe-deck).** A sub-stepper inside `#screen-prefs`: the picks build a **lean queue** of style dimensions, one focused screen per dimension — big **multi-pick photo cards** (preferences *sort, never exclude*, so "open to Garden **or** Beach" floats both · `.ck` checkmark per pick) + a **STYLE n of N** progress header + dots. **Back / Continue / Skip walk the focused screens** (last → account; Back at the first → picker). **LEAN set only — never one screen per picked category** (the owner's "too many pages?" worry): just the high-signal dims, each only if picked + de-duped — **reception** setting (7 photo cards · **reordered most-usual: Hotel ballroom → Events place → Heritage → Restaurant → Garden → Beach → Resort · indoor/outdoor row dropped, the setting implies it · owner 2026-06-01**) · **ceremony** where (owner 2026-06-01: now **5 Filipino-couple "saying I do" photo cards** — Church · Garden · Beach · Civil registrar · Same as reception · `assets/prefs/ceremony_*.webp` · **single-select** via a `[data-single]` wrapper, fills the previously-empty viewzone) · **catering** cuisine (now **6 cards** · Filipino · Asian · International · Spanish · Italian · Fusion · a **horizontal film-strip** so the screen fits · owner 2026-06-01) + service + **HALAL/dietary inline** (faith pre-locks per screen 4) · **photo/video** look (5 cards as a **horizontal film-strip** · owner 2026-06-01) + **coverage** (Pre-nup, Wedding day, Same-day edit, Drone, Save-the-date, Album — **Photo booth dropped** [it's its own Booths category] · multi-pick, Wedding day pre-selected · feeds vendor matching AND which Setnayan services get suggested on Your Plan) · **music** **top-100 song picker** (owner 2026-06-01 — supersedes the same-day 6 vibe photo cards; expanded 50 → 100): the 100 most-popular PH wedding songs (OPM + international staples); the couple taps **at least 10** they love, **checked songs pin to the top and stay there** (stable pick order — earlier picks don't reshuffle · the rest keep original top-50 order · the dashboard vendor-category stacking style) with a live count bar (Picked N · "pick at least X more" → **"✓ we'll build the rest of your playlist"** at 10) · **at 10 picks the unpicked choices disappear** (you're left with your 10) · a **search bar under the count** filters the 100 by title/artist **live as you type — no Enter needed (Enter is a no-op)** and is the path to add an 11th+ after the browse list collapses · **Setnayan builds the full playlist from the seed** · the list scrolls (select-from-many exception), the count bar + search pin) · **palette/mood** (any aesthetic cat → one screen · **NEW owner 2026-06-01**: pick a **feel** — Timeless · Modern · Boho · Rustic · Glam · Royalty · Filipiniana · Others — each shows an **example photo in its suggested colors** [`assets/prefs/feel_*.webp`] that swaps on pick — **owner 2026-06-01 redo: every feel photo is now a full-blast reception scene in the SAME wide eye-level angle (couple centered, showing their attire + the styled reception background, per the owner's reference photo) so the feels compare apples-to-apples; only the styling, colors & attire change**; **Others** → no preset, built later in the mood board; the 5 emoji INSP icons removed. *Royalty + Filipiniana added per the feel-alignment review; American skipped — too broad, Modern/Timeless cover it.* Derives stylist/florist/cake/gown — so those get *no* own screen). **Coordinator help-level dropped from onboarding (owner 2026-06-01 — that's settled after they book a coordinator).** Typically **3–5 screens**, max 6; picked nothing-with-a-style-question → a friendly "all set on style" screen. The long tail (attire acquisition · cake/florals/booth sub-types · bridal-car type · certs beyond HALAL) lives in **Personalize my matches** on Home, not onboarding. Every focused screen now fits one viewport (dropping reception's indoor/outdoor row closed its scroll — overflow ~2px). |
| 12 | Customizing your plan… → **create free account** | account + writes all the above to `events` | the login moment (invest-then-gate) · Google / Facebook / email |
| 13 | **Find your first vendor** *(demo)* | shortlist | The aha — the personalized, **sorted** results for their top category: *Matches your preference* float to top, *More to consider* below, **[ Expand search ]** for the date/area/budget-excluded. Tap → shortlist. (Every vendor shown is already verified — see the verified-only marketplace rule below.) **Bring-your-own (owner 2026-06-01):** a gold-dashed "Already have a vendor you love?" card sits at the bottom (after the matches + Expand) — **add your own vendor + invite them to Setnayan** (the locked `vendor_invites` flow · 0006/0022 · decision-logged 2026-05-19) so a couple whose vendor isn't on the app can still run chat · files · their wedding website · day-of in one place AND use Setnayan's in-app services (Papic · Panood · …) with that vendor. Onboarding plants the concept; the full add + invite mechanism lives post-onboarding in the dashboard Plan-grid. |
| 14 | **Your starting plan — the congrats moment** | — | **Owner 2026-06-01: a celebration + motivation screen, not a dry recap.** Eyebrow "You did the hard part" + serif **"Congratulations, {names}."** + a milestone/mission line ("you've done the most crucial part — your whole wedding is on track · from here we help you finish, so you can focus on everything else") + a **3-stat strip** (gold tiles): **hours saved** ("200+ hrs the average couple spends deciding all this") · **vendors curated** ("48 best-fit vendors curated from 2,400+") · **days to finalize** ("200 days — you're ahead") + the **event/shortlist recap card** (Wedding · Date · Where · Guests · Style · Shortlisted) + the **where-to-change note**: "Change or switch off any of your personalization anytime in **Personalize my matches** on your Home." Fits one screen (overflow 0). **The 200+ hrs benchmark + the 48-of-2,400 curation counts are PLACEHOLDERS — owner to confirm the real figures (Vendors Plan+Budget §6 hours-saved / market-pool open item); days-to-wedding computes from the date.** |
| 15 | **Your Plan** *(§3.2)* | — | Free baseline + matched paid boosters + package / Pro Bundle |

> **Step 12 "Who should we show you?" removed (owner 2026-06-01).** The all-vendors-vs-verified-only choice is gone because the **marketplace is now verified-only by rule** — an unverified vendor never appears at all (see the vendor-tier remodel in the decision log + Vendor Match Personalization §5). There's nothing to toggle: every vendor a couple sees is verified. `events.vendor_pool_preference` is retired.

**Why focused screens + the lean trim (owner 2026-06-01):** the owner chose **one focused screen each** over a single scrolling page (the old design — most form-like screen in the flow) and over a swipe-deck (a second horizontal axis). To keep "one screen each" from ballooning into 15+ pages, the onboarding asks only the **lean** high-signal dims (above) — so a couple who picks broadly still gets ~3–5 focused screens, not one per category. Multi-pick everywhere (preferences *sort, never exclude*). Preferences they don't set leave that category on the pure ladder (graceful — picking nothing-with-a-style-question shows a friendly "all set on style" screen). The long tail (attire acquisition · cake/florals/booth sub-types · bridal-car type · certs beyond HALAL like drone CAAP / pyro) is captured later in the **Personalized Customization surface** or inline while browsing — not onboarding.

**Religion + certifications in the flow** (per the owner's 2026-06-01 add): screen 4 (faith) pre-sets HALAL / alcohol-free + religion-match; screen 11 surfaces the **HALAL / dietary confirm inline** (+ opt-in). Category-compliance certs (drone CAAP · pyro · venue insurance) are **not** onboarding questions — too niche — they're inline-refine filters when browsing that category (Vendor Match Personalization §5b).

### 3.2 · "Your Plan" + the in-app-services flow — the end screens (owner-REDESIGNED 2026-06-05)

Not a paywall, a **plan summary** — free-first so trust is built before any price appears. **Reframed 2026-06-05** (shipped: PR #1016 free value + opt-ins · PR #1018 powerful Freebies · PR #1021 the à-la-carte services flow) to **lead with the free value, then ask, then offer — across three screens (14→16)**.

**Screen 14 · Your Plan.**
1. **Free-value showcase** — a swipeable card per free tool, each with **⏱ time saved** + the **market-equivalent "what you'd pay elsewhere"**, the elsewhere price **struck through → a gold *Free*** (apparatus rule: a tool, not hired people / DIY toil); a big serif ₱-total + hours **hero** counts up; closes on a mulberry **seal** tally. Numbers come from the locked **Time & Money Saved model §H/§I** (`computeOnboardingSavings` per-driver breakdown — single source, no invented figures). Typical ≈ **₱63.5K · ≈290 hrs** → round-down *"₱60,000+ · 250+ hours — saved, free."* Free **website** + **monogram** are labelled **"Basic"** (vs the paid Pro Website / Animated Monogram).
2. **"Keep guiding me" — free opt-in (default ON).** The free per-service deadline timeline + what-to-do-next guidance (₱0 → `events.style_preferences.guidance_opt_in`). **KEPT free guidance — NOT the removed paid planner SKU** (the old paid planner retired 2026-06-05; the free timeline carries no paid-planner / "Concierge" branding in the UI).
3. **"Reach my best matches" — opt-in (default OFF · explicit consent · RA 10173)** + a **1–5 "inquiries per category" stepper** (default 3). When ON, the commit fans the first inquiry to the **top-N best-fit vendors per picked category** (idempotent — one thread per (event, vendor); the founder-only pool caps the real count). When OFF, nothing is sent.

**Screen 15 · Boost & enhance** *(replaces the retired bundle).* A browsable carousel of paid in-app services; tapping a card opens a **full detail** — benefit copy + Setnayan price + **"you save ₱X vs hiring [role]"** (apparatus rule). Multi-select adds to the couple's interested set. (Live = buyable; in-build/coming-soon = "notify me" per §3.5. Card prices follow the v2 customer catalog in production; the in-shell `SVC` demo prices are a flagged stand-in.)

**Screen 16 · Services you're interested in** *(terminal).* A summary of the selected services + a running **total + total saved**, then **[Purchase Now]** and a quiet **"continue with the free plan"** link. Purchase Now commits the event, persists the picks (`events.style_preferences.interested_services`), and routes to the dashboard **Services** tab to complete payment **per service via the existing 0034 apply-then-pay** (no in-onboarding charge; no multi-item cart). Continue-free drops the picks → Home.

**Retired 2026-06-05:** the single budget-matched **bundle** (`MatchedBundle`) + its 10%-off one-payment pre-order — superseded by the à-la-carte screens 15–16. The §3.3 apparatus rule + §3.5 add-on library still govern the paid services shown on screen 15; the **free-tool** money+time figures live in `Time_and_Money_Saved_Model_2026-06-01.md` §H.

*Sync status (2026-06-05):* the prototype `Onboarding_Wedding_Flow_2026-06-01.html` #screen-plan + this file's `.docx` mirror still show the pre-redesign layout — flagged in DECISION_LOG as a follow-up mirror.

### 3.3 · Customer services — price comparison + the apparatus rule (owner data 2026-05-30)

The "what would you love?" picker (§3.1 screen 9 · interest-only) and "Your Plan" (§3.2) sell against a **"vs hiring elsewhere"** comparison — the customer-side equivalent of the vendor 0%-vs-15% gut-punch. Owner-supplied research (Kasal.com · Nuptials.ph · Bride and Breakfast PH · Prodigital · Kwyzer · EventNest · Vidlens · Gifty PH · Gear Rental PH).

**🔑 The apparatus rule — LOCKED, non-negotiable honesty.** Every paid service is a **Setnayan tool, NOT hired people.** The "elsewhere" price = hiring the person + crew + gear; the Setnayan price = the software that delivers the outcome — the couple brings their own people + phones. **Every service card + package line MUST carry the tool-not-people framing** ("this is the app, not a hired photographer/coordinator/crew"). It's what makes the "you save" number truthful and defensible. Lineage: the long-locked apparatus-pricing principle — *"the phone is not included, the crew is not included."*

| Service | Setnayan | Elsewhere | You save | Elsewhere = hiring… |
|---|---|---|---|---|
| Animated Monogram | ₱2,499 | ₱15,500 | ₱13,001 | designer + motion studio |
| Pro Website | ₱5,499 | ₱25,000 | ₱19,501 | freelance web dev |
| Panood (livestream/day) | ₱3,499 | ₱17,500 | ₱14,001 | livestream videographer |
| Custom QR per guest | ₱1,499 | ₱5,000 | ₱3,501 | invitation designer |
| Setnayan AI | ₱1,499 | ₱20,000 | ₱18,501 | day-of coordinator (lightest) |
| Indoor Blueprint | ₱1,499 | ₱12,500 | ₱11,001 | floor-plan + seating service |
| Call-Time Escalator | ₱1,999 | ₱10,000 | ₱8,001 | OTD vendor-management fee |
| Patiktok | ₱2,499 | ₱18,000 | ₱15,501 | 360 / social video booth |
| Pabati | ₱999 | ₱12,000 | ₱11,001 | video guestbook booth |
| Pakanta | ₱2,499 | ₱12,500 | ₱10,001 | custom original song |
| Papic Guest | ₱2,999 | ₱32,000 | ₱29,001 | 20+ disposable cams + dev/scan |
| Guest Stories | ₱1,999 | *(no market equiv)* | — | ~₱500–1,000/guest manual |
| Thank You Video | ₱5,499 | ₱60,000 | ₱54,501 | 5-min cinematic highlight |
| SDE | ₱3,499 | ₱35,000 | ₱31,501 | same-day-edit videographer |
| Papic (5 seats) | ₱2,999 | ₱75,000 | ₱72,001 | 5 photographers × 5 hrs |
| Camera Bridge | ₱1,999 | ₱17,500 | ₱15,501 | extra DSLR operator |
| Live Venue Photo Wall | ₱2,499 | ₱18,000 | ₱15,501 | onsite live-slideshow team |
| Live Background | ₱2,499 | ₱20,000 | ₱17,501 | LED wall rental + crew |
| High Res Archive | Free | ₱5,000 | ₱5,000 | 1-month high-res window (3-mo real) + sync to your own Google Drive |

**Free baseline** (owner list — the "Everything you get free" block of "Your Plan"): another-bidder alerts · unlimited bids · compare quotes side-by-side · best-vendor recommendations · invite an off-platform vendor · chat + video-call vendors · scheduler · your wedding website · personal monogram · checklist · budget tracker · seat plan · inspiration board · guest-list maker · real reviews · Verified-badge safety · your media synced to your own Google Drive (high-res · 1-month window).

**Live vs coming-soon (per `apps/web/lib/v2-catalog.ts` `BUILD_STATUS`, 2026-05-30):** only **Setnayan AI** is `live`/buyable today; the rest are `partial`/`not_built`. "Your Plan" therefore **sells the comparison on live services**, and shows the rest as **"coming soon · notify me"** — the comparison is the teaser, no charge. Each flips to buyable automatically as the feature ships (the onboarding reads `BUILD_STATUS`, same as `/pricing`). Mockup of the screen: `Onboarding_Customer_Plan.html`.

### 3.4 · Admin onboarding manager — curate what's offered (owner request 2026-05-30)

A new admin surface lets the owner pick **which services appear in the onboarding** (picker §3.1 screen 9 + "Your Plan" §3.2) — manual curation **on top of** the automatic `BUILD_STATUS` filter. Lives in the admin console (iteration 0023, beside Add-on Management § 3.12). Single-admin authority · audit-logged · **plot: `Onboarding_Admin_Manager.html`**.

**The page** — every customer service as a row: `Service · Build status · Offer (toggle) · Order · Featured · Blurb override`. Drag to reorder · ★ to feature · toggle to offer · rewrite the picker card's one-liner inline.

**Composition (honesty preserved):**
- **On + `live`** → buyable in the package.
- **On + `coming-soon`** (`partial`/`not_built`) → shows as "notify me" (comparison teaser), **never charged**.
- **Off** → hidden entirely.

So the owner curates the onboarding **without a deploy** — push a service the day it's ready, pull one for a season, reorder, feature. The admin can *offer* anything, but `BUILD_STATUS` still decides what's *charged* — the apparatus + honesty rules hold.

**Schema:** small config table `onboarding_offerings` (`service_code` PK · `enabled` BOOL · `sort_order` INT · `featured` BOOL · `blurb_override` TEXT) — or a few columns on the catalog. The onboarding reads it × `BUILD_STATUS`. The service-by-service cards (§3.3 + the add-on review) are the **library** this page toggles from.

**Sequencing:** new admin surface = **V1.x post-pilot.** Pilot can ship with a hardcoded default offering (catalog × `BUILD_STATUS`); the manager lands as a fast follow.

### 3.5 · The add-on card library — canonical per-service cards (locked 2026-05-30)

The **library** the picker (§3.1 screen 9), "Your Plan" (§3.2), and the admin manager (§3.4) all read from. One canonical card per customer service — 19 total. Source of truth for prices + build status is `apps/web/lib/v2-catalog.ts`; comparison figures are §3.3. Visual gallery: **`Onboarding_Addon_Library.html`** (grouped Live / In build / Coming soon, each card rendered exactly as the couple + admin see it).

Each card carries **four canonical fields**: a **picker blurb** (interest-only, italic, no price — what shows on screen 9), a **what-it-is** apparatus line (the tool), a **tool-not-people** tag (the role the elsewhere-price hires), and the **comparison** (Setnayan → elsewhere → save) revealed on "Your Plan." Build status decides charged vs notify-me.

| Service (`code`) | Picker blurb | Setnayan | Elsewhere | Tool, not… | Build |
|---|---|---|---|---|---|
| **Setnayan AI** (`TODAYS_FOCUS`) | "Never wonder what to do next." | ₱1,499 | ₱20,000 | a hired day-of coordinator | 🟢 Live |
| **Pro Website** (`PRO_WEBSITE`) | "Your wedding, on its own website." | ₱5,499 | ₱25,000 | a hired web developer | 🟡 In build |
| **Custom QR per Guest** (`CUSTOM_QR_GUEST`) | "One scan, and your guest finds everything." | ₱1,499 | ₱5,000 | a hired invitation designer | 🟡 In build |
| **Indoor Blueprint** (`INDOOR_BLUEPRINT`) | "Your whole venue, mapped and seated." | ₱1,499 | ₱12,500 | a hired layout service | 🟡 In build |
| **Animated Monogram** (`ANIMATED_MONOGRAM`) | "Your initials, drawn live." | ₱2,499 | ₱15,500 | a hired motion studio | 🟡 In build |
| **Panood** (`PANOOD_SYSTEM`) | "Family abroad watches it live." | ₱3,499/day | ₱17,500 | a hired livestream crew | 🟡 In build |
| **Patiktok** (`PATIKTOK_COMPILER`) | "A video booth guests run themselves." | ₱2,499 | ₱18,000 | a hired 360 / social-video crew | 🟡 In build |
| **Papic Guest** (`PAPIC_GUEST`) | "Every guest's phone, a candid camera." | ₱2,999 | ₱32,000 | 20 disposable cams + developing | 🟡 In build |
| **Papic · 5 Seats** (`PAPIC_SEATS`) | "Turn five friends into your photo crew." | ₱2,999 | ₱75,000 | 5 hired photographers × 5 hrs | 🟡 In build |
| **High Res Archive** (`HIGH_RES_ARCHIVE`) | "Keep every original, full quality." | Free | ₱5,000 | a USB-and-delivery service | 🟢 Free baseline (1-mo high-res window + Google Drive sync) |
| **Live Background** (`LIVE_BACKGROUND`) | "Design the screen behind your stage." | ₱2,499 | ₱20,000 | an LED wall rental + crew | 🟡 In build |
| **Pabati** (`PABATI`) | "A guestbook that talks back." | ₱999 | ₱12,000 | a hired booth + attendant | ⚪ Coming soon |
| **Pakanta** (`PAKANTA`) | "Your wedding's own song." | ₱2,499 | ₱12,500 | a hired composer + singer | ⚪ Coming soon |
| **Guest Stories** (`PAPIC_ADDON_STORIES`) | "Everyone gets their own highlight reel." | ₱1,999 | *no market equiv* | per-guest manual editing | ⚪ Coming soon |
| **Thank You Video** (`PAPIC_ADDON_THANK_YOU`) | "A cinematic thank-you, edited for you." | ₱5,499 | ₱60,000 | a hired cinematographer | ⚪ Coming soon |
| **Same-Day Edit** (`SDE`) | "A highlight reel before the night ends." | ₱3,499 | ₱35,000 | a hired SDE crew | ⚪ Coming soon |
| **Camera Bridge** (`CAMERA_BRIDGE`) | "Bring a DSLR — bridge it in." | ₱1,999 | ₱17,500 | a hired second shooter | ⚪ Coming soon |
| **Live Venue Photo Wall** (`LIVE_WALL`) | "Photos on the wall, live." | ₱2,499 | ₱18,000 | a hired onsite slideshow team | ⚪ Coming soon |
| **Call-Time Escalator** (`CALL_TIME_ESCALATOR`) | "Your vendors, reminded on the day." | ₱1,999 | ₱10,000 | a hired on-the-day manager | ⚪ Coming soon |

**Composition recap:** picker shows the blurb only (no price). "Your Plan" reveals the comparison; **Live → buyable**, **In build / Coming soon → "notify me"** (teaser, never charged). The admin manager toggles each row on/off and can override the blurb. As each `partial`/`not_built` SKU ships, it flips to buyable automatically — no copy change needed (onboarding reads `BUILD_STATUS`, same as `/pricing`).

**Pakanta price — RESOLVED 2026-05-30:** owner-confirmed **₱2,499** (matches v2.1 brief § 5 catalog + §3.3; supersedes the earlier ₱3,499 single-tier from CLAUDE.md fourth 2026-05-28 row). Still verify the live `platform_retail_catalog_v2` row matches at engineering time.

---

## 4 · Flow 2 — Vendor onboarding #1 (Signup)

*List free → create first package → token economics → starter token bundle → submit verification. Vendor is fully live on **Free** during the verification wait (listing up, receiving inquiries, can spend tokens to answer).*

**Mockup (12 navigable screens):** `Onboarding_Vendor1_Signup.html` — phone-frame walk-through with the live package-builder (screen 5) and the 0%-vs-15% kill-shot (screen 10).

| # | Screen | Funnel role | What it does in Setnayan |
|---|--------|-------------|--------------------------|
| 1 | Welcome | Hook | "Run your business, not your DMs." End-state: an inbox of ready-to-book couples. **List free** |
| 2 | HQ location | Personalize | City/province → location + radius (Free 10km) + screen-name ("Manila Wedding Photographer #…") |
| 3 | Years in weddings | Trust | Start year → "X years in weddings" badge |
| 4 | Primary category | Goal | Pick main service from the taxonomy |
| 5 | **Create first package** | Build | Name + price (or range) + inclusions + 3–5 photos → a **live marketplace listing** |
| 6 | Other services? | Tease | Add more categories (Free = 1; more with Verified/Pro/Enterprise) |
| 7 | Pain points | Empathy | "What's hard about getting wedding clients?" (ads, ghosting, DMs) |
| 8 | Tour of the free stuff | Value | Free listing + microsite + portfolio + chat + inquiries + **0% commission, ever** |
| 9 | Notifications priming | Permission | "Never miss an inquiry" |
| 10 | **Demo: answer an inquiry with a token** | Aha + economics | The 0%-vs-15% moment — see §4.1 |
| 11 | Profile live | Output | Their marketplace card + microsite preview |
| 12 | **Starter token bundle** *(soft)* | Convert | Token packs — see §4.2 + submit verification |

### 4.1 · The 0%-vs-15% pitch (vendor demo screen)

**Beat 1 — the inquiry lands:**
> *Maria & Juan are looking for a wedding photographer in Manila for Dec 18. They sent you an inquiry.*  →  **[ Reply · 6 tokens ]**

**Beat 2 — why those tokens are nothing:**
> ## Your sale is 100% yours.
> Other platforms take a cut of every booking — often **15%** — just for handing you a lead. On a ₱1,000,000 wedding, that's **₱150,000 gone.**
>
> We don't want a peso of your sale. You pay a **flat region token cost — 6 tokens (₱600) in Metro Manila, fewer elsewhere** — to answer an inquiry from a couple who's *actually looking to book*. That's the whole cost — even on a ₱1,000,000 booking, it's the same flat token burn. It just helps us run the app.

**Kill-shot table:**

| On a ₱1,000,000 booking | Other platforms | Setnayan |
|---|---|---|
| Commission taken | −₱150,000 (15%) | **₱0** |
| Cost to reach the lead | (inside the 15%) | 6 tokens · ₱600 (NCR) |
| **You keep** | ₱850,000 | **₱999,400** |

*Comparative-claim safety:* attribute "15%" to a category, not a named rival (*"platforms that take a cut of your sale charge up to 15%"*), or use a range (*"10–20%"*). The ₱1M math stays just as devastating.

### 4.2 · Starter token bundle (end-screen)

> ## Start with tokens.
> *Each token answers one inquiry — one real chance to get booked.*
>
> Token packs: **4 · 10 · 25 · 50 · 100** — **₱100/token flat**; bigger packs come with **bonus tokens on top** (no per-token discount · bonus ladder owner-to-set · locked 2026-06-04).
>
> **+100 free tokens** when you verify your business before launch.
>
> **[ Get started ]** · *Stay on the free plan*

Free skip is honest: a Free vendor still gets a listing and still *receives* inquiries — tokens just let you *answer* them.

**Do not sell Pro/Enterprise here.** Tokens are the only thing a vendor needs to start; the tier sell belongs in onboarding #2 (post-verification), when they've felt a real inquiry land.

---

## 5 · Flow 3 — Vendor onboarding #2 (Post-verification)

**Trigger (locked 2026-05-30):** loads automatically **the first time the vendor logs in after their business is verified** (12-doc verification approved by admin, 3–5 business-day SLA). It's a **one-time** flow — gated by a seen-flag on the vendor record (e.g. `verified_onboarding_seen_at`), so it shows once and never re-fires on later logins. The *"Your business is verified — see what's unlocked"* email/notification is the **nudge to come back and log in**; its deep-link routes into this same flow. If the vendor dismisses it, it stays reachable from the dashboard (*"What's unlocked"*) — never orphaned.

**Mockup (5 screens):** `Onboarding_Vendor2_PostVerification.html` — verified-seal + the 100-token wallet drop (screen 1), the good/better/best benefits cards (screen 3), and the monthly/yearly billing toggle (screen 4).

### Spine
1. **"You're verified."** Badge live + **drop the 100 free founder tokens into their wallet here** (the dopamine moment lands here, not at signup).
2. **What verification unlocked** — star ratings now show, 20km reach, up to 20 inquiries/week, up to 3 categories.
3. **"Want to go further?"** — the benefits comparison (§5.1).
4. **Billing choice** — monthly or annual, with the savings shown.
5. **"Stay Verified — free"** soft skip, always.

### 5.1 · Benefits screen — good / better / best (felt benefits, not a feature dump)

- **Verified · Free** — *"Get found and trusted."* Badge, star ratings, 20km, up to 20 inquiries/week, up to 3 categories, earn bonus tokens by recommending Setnayan services.
- **Pro · ₱2,499 / 28 days** *(or ₱24,999/yr — save ₱7,488)* — *"Run your business."* Your name shown in browse, **unlimited inquiries**, 50km, video calls, **all** reviews visible, your own website + slug, file sharing, specialized tools, bundle maker, share-inquiry links, **3 team logins**.
- **Enterprise · ₱5,499 / 28 days** *(or ₱54,999/yr — save ₱16,488)* — *"Run it at scale."* Everything in Pro, plus **your whole team (unlimited logins)**, **unlimited categories**, **unlimited daily bookings**, **100km reach**. For studios, multi-branch, high-volume vendors.

The real Pro→Enterprise jump (§6) is **team + scale + volume**, never just radius.

---

## 6 · Canonical vendor tier matrix (reconciled from owner spreadsheet 2026-05-30)

> This supersedes the partial matrix in CLAUDE.md's V2.1-amendment-#2 row, which was **missing the team-accounts and daily-capacity rows.** The owner's spreadsheet is now source of truth; the v2.1 brief + iteration 0022 should be reconciled to match (queued).

| Feature | Free | Verified | Pro | Enterprise | Booster (tokens) |
|---|---|---|---|---|---|
| Reach radius | 10km | 20km | 50km | 100km | 4 |
| Create packages | ✓ | ✓ | ✓ | ✓ | 10 |
| Chat | ✓ | ✓ | ✓ | ✓ | 25 |
| Categories | 1 | up to 3 | up to 3 | **Unlimited** | — |
| Team accounts | 1 | 1 | **3** | **Unlimited** | — |
| Video call | — | — | ✓ | ✓ | 50 |
| Scheduling | Manual | Hybrid | Hybrid | Hybrid | 100 |
| Vendor name | Hidden until first reply | Hidden until first reply | **Shown** | **Shown** | — |
| Daily capacity | — | — | up to 2/day | **Unlimited** | — |
| Inquiries (bids)/week | 10 | 20 | **Unlimited** | **Unlimited** | — |
| Token bonus qualified | — | ✓ | ✓ | ✓ | — |
| Portfolio | ✓ | ✓ | ✓ | ✓ | — |
| Editorial tagging | — | — | ✓ | ✓ | — |
| Show star ratings | — | ✓ | ✓ | ✓ | — |
| Show all reviews | — | — | ✓ | ✓ | — |
| Website | Basic | Custom (name hidden) | Custom | Custom | — |
| Custom slug | — | — | ✓ | ✓ | — |
| Inquire on website | ✓ | ✓ | ✓ | ✓ | — |
| Onboarding bundle maker | — | — | ✓ | ✓ | — |
| Specialized tools | — | — | ✓ | ✓ | — |
| File sharing | — | — | ✓ | ✓ | — |
| Bring in outside clients | ✓ | ✓ | ✓ | ✓ | — |
| Share inquire link | — | — | ✓ | ✓ | — |
| Recommend Setnayan services | — | ✓ | ✓ | ✓ | — |
| **Price** | ₱0 | ₱0 | **₱2,499–₱3,999 / 28 days** (region · built pilot ₱499/wk → sunsetting) | **₱5,499–₱8,499 / 28 days** (region · not yet built) | — |

*Reading notes:* "Token bonus qualified" = earn free tokens by recommending Setnayan services to your booked couples. "Onboarding bundle maker" = a vendor's own custom couple-intake tool (distinct from these three onboardings). Booster column = token cost to temporarily boost that feature for a window.

---

## 7 · Token economics (reference)

- **₱100 per token, flat** (sizes 4/10/25/50/100 · **no per-token discount** · **bulk orders earn bonus tokens on top**, ladder owner-to-set). Owner-locked 2026-06-04 ("no more 250" + flat-price-with-bonus-tokens). Canonical source `Token_Economy_Flow_Map_2026-06-01.html`.
- **Region-weighted burn to answer an inquiry** — banded by the wedding region's **minimum wage**: lowest-wage regions **1** · regional hubs **2** · NCR/CALABARZON/Central Luzon **3** tokens (= **₱100 / ₱200 / ₱300**, ₱300 ceiling), keyed to the wedding's region, flat within a band, **never by booking size** (repriced 2026-06-05, supersedes 3‑4‑5‑6). Answering unlocks chat + reveals your name + video + quote.
- **0% commission** on bookings — tokens are the *only* per-engagement charge, flat regardless of booking size (on a ₱1M booking you still pay only the region's token burn, never a percentage).
- **Earn** bonus tokens (not just buy) via "Recommend Setnayan services" (Token Bonus Qualified, Verified+) — credited back into the same wallet you spend on leads.
- **100 free founder tokens** on verification before 31 Jan 2027 — delivered in onboarding #2.

---

## 8 · Open / defaulted decisions + drift flags

| Item | State |
|---|---|
| Soft vs hard paywall | **Soft**, locked |
| Customer end = personalized bundle (free + paid boosters) | Locked |
| Social proof | Sample now, real later — locked |
| Keep both Pro + Enterprise | **Yes**, locked (matrix §6 differentiates them) |
| Account-creation point | Defaulted: questions first, gate before output — *confirm* |
| Screen granularity | Defaulted: one concept/screen — *confirm* |
| Customer "Your Plan" contrast line | Optional — *owner to decide* |
| **Verified price** | **₱0 — RESOLVED 2026-05-30.** Verified is genuinely free (no one-time badge fee). Onboarding #2's "stay Verified — free" line is accurate. |
| **Pro/Enterprise cadence** | **28-day prepaid block — RESOLVED 2026-05-30.** Shown as "₱2,499 / 28 days" + "₱5,499 / 28 days." Annual ₱24,999 / ₱54,999 (savings ₱7,488 / ₱16,488 = ~3 cycles, already computed on 13 blocks/yr). **Superseded 2026-06-03 → region-tiered:** Pro ₱2,499–₱3,999 · Enterprise ₱5,499–₱8,499 / 28 days (base→NCR); pilot ships flat base. See `Token_Economy_Flow_Map_2026-06-01.html`. |
| **Pakanta price** | **₱2,499 — RESOLVED 2026-05-30** (owner-confirmed; matches catalog + §3.3). Still reconcile vs the live `platform_retail_catalog_v2` row at engineering time. Other booster + token-pack prices still pulled from `v2-catalog.ts` / `Pricing.md` at copy-draft time. |

---

## 9 · Cross-iteration impact

Touches: **0000** (signup/shell), **0006** (vendors/marketplace), **0010** (mood), **0015** (marketing/conversion voice), **0016** (Setnayan AI), **0021** (couple dashboard), **0022** (vendor dashboard/tiers), **0030** (guided tour — stays separate), **0043** (wedding-type picker), plus the V2 token model + v2.1 tier model.

**Corpus reconciliation owed (queued):** update the vendor tier matrix in the v2.1 brief (`CLAUDE-CODE-BRIEF-v2.1_2026-05-28.md` §3) + iteration `0022` to add the **team-accounts** and **daily-capacity** rows from §6 above, so the corpus stops trailing the owner's spreadsheet.

---

## 10 · Next steps

1. Owner confirms the §8 flags (Verified price, cadence, the two defaults).
2. Draft screen-by-screen **copy** word-for-word (skill Phase 4) — start with the two end-screens (highest-leverage): Customer "Your Plan" + Vendor #1 token bundle + Vendor #2 benefits.
3. Engineering build (skill Phase 5) — sequence per owner; realistically post-pilot. Strong candidate for one shared onboarding engine (progress bar + answer persistence + screen framework) driving three content configs.
