# Vendor Benefits — "Soon" Build Plan (2026-06-29)

> **Purpose:** a ready-to-run brief to BUILD every "Soon" (not-yet-shipped) vendor benefit from the catalog, in a fresh session. Each benefit below is grounded in a real read-only audit of the codebase (cited files/tables). Source pitch + status: `03_Strategy/Vendor_Benefits_Catalog_2026-06-29.md`. These are surfaced in the prototype's Vendors panel today tagged "Soon"; the goal is to make them real and drop the tag as each ships.

## How to use this (new session)
1. Open a session **rooted in the app monorepo** (see Repo facts) — not the spec corpus.
2. Work the **PR-wave order** below (prerequisites first). One benefit (or one tightly-coupled cluster) per PR.
3. For each: read the cited current-state files, build per the spec, honor the locks, test with a **prod build**, add a `changelog.d/` fragment, open the PR, enable auto-merge.

## Repo facts (critical)
- The monorepo **`setnayan-platform` is checked out AT THE HOME DIR**: `git -C /Users/icecasasola rev-parse --show-toplevel` = `/Users/icecasasola`. ("Setnayan-App" is dormant — do not look for it.)
- Web app = `/Users/icecasasola/apps/web` · Migrations = `/Users/icecasasola/supabase/migrations` · Native = `~/Setnayan-Native`.
- The repo is **outside the default sandbox** — use `dangerouslyDisableSandbox: true` on Bash for repo/git/build commands.
- The **main checkout is dirty** — never commit there; always `git worktree add -b <slug> <path> origin/main` (clean worktree off origin/main).
- Ship: implement → **prod build** (`next build`) → `changelog.d/<slug>.md` fragment → push ALL commits → `gh pr create` → `gh pr merge <#> --auto --merge`.
- Stale CLI DB creds: `supabase db push` may 401 — the **migration FILE in the PR is what matters** (CI applies). For data/price changes use the Supabase MCP (project `njrupjnvkjkitfctetvi`) + a manual ledger row.

## Locks to honor (do not violate)
- **RLS at `CREATE TABLE` time**, using the canonical patterns (`02_Specifications/RLS_Policy_Pattern.md`; helpers `current_vendor_ids`/`current_vendor_profile_ids`/`current_event_ids`/`is_admin`). No invented patterns.
- **Prices are admin-managed — NEVER hardcode.** Thresholds/SLAs/weights that gate behavior are admin-managed config rows too, not constants.
- **Off-platform pay / 0% commission / no percentage:** Setnayan is NOT in the vendor↔couple payment chain. Do **NOT** build anything that makes Setnayan the withholding agent, issue the vendor's Official Receipt, or compute their tax (no 2307 — those benefits were deliberately removed).
- **Behavioral data = first-party, edge, min-N de-identified.** Any "what couples are doing" signal shown to a vendor must be aggregated (min-N >= the locked floor) with **no couple identity** leaked.
- **Cron-free:** no polling crons — use Next 15 `after()` / `waitUntil` (e.g. recompute response latency incrementally on message-send).
- **Architect mandate:** a feature spans couple + vendor + admin surfaces where relevant, and connects to what exists.
- **Build for the long term** (root-cause, no band-aids). Reviews must stay **event-bound + anti-fake**; vendor identity follows the hybrid-anonymity rules.


## Shared prerequisites (build these first — several benefits depend on them)

- P0 — Public vendor profile surface as a deep-link target (S, foundational). /v/[slug] ALREADY EXISTS (apps/web/app/v/[slug]/page.tsx) and renders tier-gated credits, reviews, stats. It does NOT need to be built — but several Discovery items assume a richer landing with (a) tappable deep-link anchors and (b) reusable badge/credit slots. Ship a thin pass first: add stable anchor IDs + a generic <VendorBadgeSlot/> + <VendorCreditChip/> render primitive on /v/[slug] and on explore/_components/vendor-card.tsx so Style-Twin, Shortlist Radar, Featured-in-Real-Stories, Spotlight Awards and Certified-Partner all hang badges/links off ONE component instead of each inventing its own. NOTE the route is /v/[slug], NOT /vendors/[slug] which several specs name — reconcile the specs to the real route, do not create a second route.
- P1 — vendor_activity_stats INVOCATION wiring (S–M, foundational, partly already done). CORRECTION TO SPECS: recomputeVendorActivityStats() is ALREADY fully implemented in apps/web/lib/vendor-activity.ts (552 lines: avg_response_minutes, response_rate_pct, inquiry_to_booking_pct, finalized_booking_count) and a chat_threads first-reply migration (20270110320018) already feeds latency. The genuinely-missing piece is the INVOCATION: call recomputeVendorActivityStats() from message-send / inquiry-accept / status-change server actions via Next 15 after()/waitUntil (cron-free lock), incrementally. This single wiring unblocks First-Look Window AND most of the Data & Analytics group (they read this table). Do this before any analytics surface.
- P2 — Canonical 'two-party acknowledgement' RPC + table convention (S, foundational doc/scaffold, no new infra). Codify ONE reusable pattern from the live respond_vendor_proposal (SECURITY DEFINER + SELECT FOR UPDATE + status precondition, single-winner) in supabase/migrations/20261209000000_concurrency_guards.sql and the orthogonal-marker column pattern from contract_booking_link. Every Booking & Operations item (Deposit Lock-Free, Change-Order Trail, Day-Of Handover) and No-Show Protection ship a propose→acknowledge row keyed off this. Write it as a tiny helper/template + a checklist so all four waves use identical RLS helpers (current_couple_event_ids / current_vendor_booked_event_ids / current_vendor_profile_ids / is_admin) and the same serialization, instead of re-deriving it four times.
- P3 — Shared analytics rollup substrate + min-N de-identification helper (M, foundational for Data & Analytics). One migration that establishes the de-identified-aggregate convention: a reusable min-N suppression SQL helper (per the behavioral_data_edge memory lock) + the recompute-via-after()/waitUntil pattern (cron-free) + a single admin-managed config table for thresholds/weights (per prices-are-admin-managed lock — NO hardcoded thresholds anywhere). Demand Radar, Price-Position bands, Quote-to-Booking funnel, Won/Lost, Peso-per-Lead all build on this. Ship the helper + config table before any analytics surface so each item is a query + a card, not a fresh privacy/threshold debate.
- P4 — Admin-managed config rows for every tunable (XS, cross-cutting rule, fold into P2/P3). Prices-are-admin-managed + no-hardcoded-thresholds locks apply to: First-Look SLA window + boost weight, Radar thresholds, referral-cut %, resell markup, certification fee, off-season discount values, white-label tier entitlement. Establish ONE admin_config-style table (or reuse the existing token-bands admin pattern) up front so no wave hardcodes a number. This is a rule, not a build — but stating it as a prerequisite prevents rework.

## PR-wave plan

### Wave 1 · Wave 1 — Quick wins on already-shipped rails
_Benefits:_ Right-of-Reply on every review (Trust) — ALREADY-EXISTS; ship only the two polish calls: (a) decide immutable-vs-editable for lock_vendor_reply trigger, (b) relax the Pro/Enterprise showComments gate so a reply renders for ALL tiers on /v/[slug] (today a Free/Verified vendor's reply is hidden). Surface both to owner — they change shipped behavior., Featured in Real Wedding Stories (Marketing) — ALREADY-EXISTS; verify migrations 20261221000000 + 20270129232225 are applied in prod, optionally add vendor_profiles.website_url dofollow backlink + a 0028 'you got featured' email., Double-Booking Guard (Booking) — ALREADY-EXISTS + DB-enforced; ship only the named-calendars UI completion (flag flip NEXT_PUBLIC_NAMED_CALENDARS_ENABLED, owner-gated) + a couple-visible 'date held' state on plan-card-lock.tsx + a read-only admin pool-occupancy view. Defer crew-role lanes unless owner asks., Style-Twin Discovery (Discovery) — S, pure wiring; render the existing VendorCredit[] from editorial data.ts as tappable chips deep-linking to /v/[slug] on /realstories cards and /[slug] editorial, honoring tier + consent gates (Free excluded). Consumes P0's credit-chip primitive.
_Shared work:_ These ride existing, shipped schema with ZERO new tables. They share P0 (the /v/[slug] deep-link + badge/credit-chip primitive) and the explore vendor-card badge slot. Shipping them first proves the P0 component, delivers visible vendor value in the first PR batch, and clears the four 'already-exists / S' items off the board so later waves aren't padded with trivia. No serialization, no analytics, no money.

### Wave 2 · Wave 2 — Stats backbone + responsiveness signals (Discovery + First-Look)
_Benefits:_ First-Look Window (Discovery) — M; add responsivenessBoost input to compat-score.ts (admin-tunable weight, keep COMPAT_WEIGHTS summing to 1), fed by vendor_activity_stats; eligibility = replied in-region within admin-set SLA; 'Replies fast' badge on explore (reuse compat tier render) + earned/at-risk chip on vendor-dashboard., Shortlist Radar (Discovery) — M; SECURITY DEFINER count_saves_for_vendor() (keeps guest_saved_vendors owner-only — do NOT expose user_id) + rival_signals_for_vendor() de-identified aggregate (min-N, no couple identity) over event_vendors/chat_threads in the vendor's category+hq_region; radar card on vendor-dashboard.
_Shared work:_ Both are powered by P1 (vendor_activity_stats invocation wiring) and the P3/P4 min-N + admin-config substrate. First-Look needs the stats table populated (the recompute fn is already written — just wire its invocation). Shortlist Radar reuses the SAME min-N de-identification helper and admin-managed-threshold config that the whole Data & Analytics group will reuse — so building the de-id helper here, against a real consumer, hardens it before Wave 6 leans on it heavily. Both honor RLS Pattern E (current_vendor_ids gate). vendor_follows already has its vendor-read RLS shipped; guest_saved_vendors stays locked behind the count fn.

### Wave 3 · Wave 3 — Booking lifecycle: deposit + change orders (Booking & Operations + No-Show)
_Benefits:_ Deposit Reservation Lock-Free (Booking) — M; additive event_vendors columns (deposit_recorded_at / deposit_acknowledged_at / deposit_proof_url) as orthogonal markers (NEVER repurpose status enum); recordDeposit() reuses the live acquire_schedule_pools RPC to hold the date; acknowledgeDeposit() uses the P2 FOR-UPDATE+precondition pattern. Triad surfaces on the existing couple + vendor workspace pages., Change-Order Trail (Booking) — M; new vendor_change_orders (+ optional lines) propose→accept/decline state machine modeled exactly on event_schedule_suggestions/vendor_proposals; accept atomically writes an event_vendor_line_items row so the budget ledger stays single-source. Greenfield table, RLS at CREATE., No-Show Downpayment Protection (Money) — M; policy fields on vendor_service_payment_schedules seq-0 + new event_vendor_policy_acknowledgements (snapshot policy text at lock so edits can't rewrite history); couple ack gate in the lock/build flow; admin sees frozen evidence in the existing disputes surface.
_Shared work:_ All three are 'one side proposes / the other acknowledges, never a 2-way write' rows that ride P2's canonical serialization (respond_vendor_proposal FOR-UPDATE+precondition) and the orthogonal-marker column convention (contract_booking_link precedent). They all touch the SAME two workspace pages (apps/web/app/dashboard/[eventId]/vendors/[vendorId]/workspace + apps/web/app/vendor-dashboard/clients/[eventId]) and the SAME event_vendor_line_items settle target, so the couple/vendor/admin surfaces are built once and shared. Deposit + Change-Order pair naturally (a change order can re-trigger a deposit top-up). No-Show is the policy/evidence layer over the same deposit. Grouping avoids three separate passes over the workspace UI + RLS-helper wiring. PH-counsel review of No-Show forfeit wording is the only external gate.

### Wave 4 · Wave 4 — Money read-models + day-of operations
_Benefits:_ Payday Calendar & Cash-Flow View (Money) — M; a vendor-scoped SECURITY DEFINER read over event_vendor_payment_plan (do NOT loosen its host-only RLS) joined to event_vendor_payments confirmed-state; lib/vendor-cashflow.ts assembles the timeline; new 'Payday' tab + optional .ics export. Pure read/aggregation, no money movement., Booked-Out Waitlist (Discovery, placed here for the email+notify rail) — M; new vendor_date_waitlist table; couple joins when vendor_calendar_blocks shows their date unavailable; vendor one-click 'a slot opened' flips status→notified and fires a 0028/Resend email on block-delete via after() (email-only lock)., Day-Of Run-of-Show & Handover (Booking) — L; (a) additive actual_start_at/actual_end_at + run_state on event_schedule_blocks + an 'advance block' RPC + Supabase-realtime now/next header shared across couple/vendor/guest; (b) new booking_handovers table (delivered→acknowledged→disputed) riding the P2 ack RPC, on-ack advancing event_vendors.status→delivered (reusing the existing delivered transition + review-request emit).
_Shared work:_ This wave is the 'read the booking + notify against it' cluster. Payday reuses the Wave-3 payment-plan tables (built/locked) — so it MUST follow Wave 3. Waitlist and Handover both depend on the 0028/Resend email rail + the cron-free after()/waitUntil notify pattern (block-delete trigger for Waitlist; realtime channel for Run-of-Show), so the notification plumbing is established once and shared. Handover reuses the shared-timeline + booked-vendor RLS (current_vendor_booked_event_ids) and the delivered-status transition already wired in updateVendorStatus. Day-Of is the heaviest single item (L) — isolating it here keeps it from blocking the lighter money/waitlist reads.

### Wave 5 · Wave 5 — Trust track-record + editorial/marketing spotlights
_Benefits:_ Receipt-Backed Reviews (Trust) — M; add booked_through_setnayan provenance flag on vendor_reviews (server-populated, couples can't set it) + a vendor_completed_events dated-line view extending vendor_public_completed_events_stats (apply the SAME self-review/team exclusions). Per-review pill + 'Track record' list on /v/[slug] and vendor-dashboard., Editorial & Journal Spotlights (Marketing) — M (roadmap-larger framing, but DB-overlay keeps it M); new journal_vendor_spotlights overlay joined by blog_slug to the existing file-based Journal (decide: stay file-based + overlay vs. full CMS — overlay recommended); 'Featured partner' dofollow block in blog/[slug]; two-admin gate + 'Sponsored' badge for paid slots., Spotlight Awards (Marketing) — M; persist computeVendorBadges() winners into a new vendor_spotlight_awards table (monthly recompute via after()/admin 'run now', cron-free); admin confirm/override + homepage feature toggle; homepage Spotlight strip + vendor-dashboard banner. Owner sign-off needed (homepage is product-led today)., Off-Season Promos (Marketing) — M; wire the ALREADY-shipped vendor_services off_peak discount columns + wedding_season_factors into a 'your lean months' vendor nudge + a couple-facing 'off-season savings' explore filter/band. Mostly wiring; optional small vendor_promo_windows table only if window-scoped.
_Shared work:_ All four hang vendor-credit/badge/feature surfaces off P0's /v/[slug] + explore badge-slot primitive and reuse the vendor_partnerships admin-verify pattern (clone its admin/vendor/explore UI wholesale). Receipt-Backed Reviews + Spotlight Awards both extend the SAME stats/exclusion views (vendor_public_completed_events_stats, public_stats_exclusion). Editorial Spotlights + Spotlight Awards share the two-admin approval gate (0023 §9.1) and the 'Sponsored' badge rule (0038). Off-Season reuses the explore vendor-card + badge-row built in Wave 1. Grouping the 'make a vendor look good publicly' items lets them share badge components, admin-verify scaffolds, and the homepage/marketing layout negotiation in one go.

### Wave 6 · Wave 6 — Vendor self-serve analytics dashboard
_Benefits:_ Quote-to-Booking Funnel (Data) — M; only the 'views' stage is net-new (vendor_profile_views table + capture on /v/[slug] + explore cards, hashed viewer, min-N); inquiries/quotes/booked already populated; clone the FunnelTable component from /admin/funnels scoped to the vendor, sliced by event_vendors.source., Won & Lost Reasons (Data) — M; new inquiry_outcomes keyed to chat_thread/vendor_proposal with an admin-managed reason_codes taxonomy (taxonomy-DB-driven, never hardcoded); hook capture into respond_vendor_proposal decline + thread-close; vendor roll-up + admin report in /admin/insights., Peso-Per-Lead Scorecard (Data) — S; mostly a reporting VIEW joining vendor_event_unlocks token spend + subscription tier cost ÷ finalized_booking_count; token price read from DB (admin-managed). Note: burn is 'economically inert in pilot' — reports ₱0/lead until burn-on-answer is activated., Demand Radar (Data) — M; de-identified materialized rollups of events + chat_threads + vendor_event_unlocks by region/month/event_type/style, above min-N; vendor 'Demand Radar' card + fuller admin dashboard alongside /admin/funnels., Price-Position Meter (Data, roadmap-larger) — L; new market_price_bands precomputed table (category+region+pax_bucket, low/median/high, min-N suppressed) + recompute_market_price_bands() over published vendor_packages/vendor_services; vendor percentile-in-band card + admin band-review (clone /admin/token-bands).
_Shared work:_ Every item in this wave reads vendor_activity_stats (P1, wired in Wave 2) and rides the P3/P4 substrate: the SAME min-N de-identification helper, the SAME after()/waitUntil cron-free recompute pattern, the SAME admin-managed-threshold config table, and the SAME vendor-stats-panel.tsx card grid for the vendor surface. They cluster naturally as ONE vendor analytics dashboard built incrementally (Peso-per-Lead is S and ships first as the warm-up; Price-Position is L and ships last). Clone /admin/funnels' FunnelTable + /admin/insights scaffolds once and reuse across all five. Doing analytics LAST is deliberate: the marketplace is founder-only today, so most bands/rollups stay min-N-suppressed (empty) until vendor density exists — these surfaces are real but low-yield at launch, so they shouldn't block the operational waves.

### Wave 7 · Wave 7 — Ecosystem & crew (gated on multi-vendor density)
_Benefits:_ Couple Referral Rewards (Marketing, roadmap-larger) — L; new referral_codes + referral_redemptions with anti-self-referral trigger + qualification gate; on qualify, MINT two single-use discount_codes via the EXISTING voucher system (the perk rail already ships); couple 'refer a couple' card + admin reward-value config. Standalone — does NOT need multi-vendor density, so ship this part of the wave first., Resell Setnayan Productions (Ecosystem) — M; bridge vendor_package_items/vendor_service_links to platform_retail_catalog_v2 service_codes + new vendor_setnayan_resell + setnayan_resell_attributions (referral cut, admin-managed %); hook submitOrderAction to stamp referring_vendor. Ship behind the founder-only publish gate., Crew-Rate Marketplace (Ecosystem) — M; generalize manpower_gigs to vendor-posted slots + new crew_referrals + optional crew_directory; reuse the token handshake + vendor_team_members; referral cut admin-managed, cash off-platform (preserve BIR-exempt stance)., Setnayan-Certified Service Partner (Ecosystem) — M; new setnayan_certified_partners keyed to platform_retail_catalog_v2 SKUs + a derived badge + getCertifiedPartnersForSku routing at SKU checkout; clone the vendor_partnerships admin-verify queue., White-Label Couple Tools (Ecosystem, roadmap-larger) — L; (a) small: mood-board vendor-read SECURITY DEFINER mirroring get_vendor_seat_plan (closes the 3rd tool gap); (b) large: vendor_white_label_config + co-branded render mode swapping Setnayan chrome for vendor logo/accent on seating/mood/schedule, tier-gated, MUST NOT paywall the couple's own tool access (seat-plan-stays-free lock).
_Shared work:_ These all add a referral/attribution/cut + admin-verify dimension on top of EXISTING primitives: vendor_partnerships (clone its admin/vendor/explore UI for Resell + Certified), manpower_gigs + token handshake (Crew), the voucher/discount_codes payout rail (Referral), Feature_Access_By_Vendor_Category booked-vendor reads + the 0021 theme system (White-Label). They share an admin-managed referral-cut config and the off-platform/no-2307 money stance. CRITICAL: the marketplace is founder-only-vendor today (one published vendor), so Resell/Crew/Certified/White-Label are 'theoretical until 3rd-party vendors publish' — gate them behind the publish flag. This wave is LAST because its value only materializes with vendor density; Couple Referral Rewards is the one density-independent item and can be pulled forward if the owner wants an early growth lever.

> **Sequencing notes:** VERIFIED AGAINST CODE (repo root = /Users/icecasasola, 502 migrations). Three corrections to the specs' stated assumptions that change the plan: (1) The public vendor route is /v/[slug] (apps/web/app/v/[slug]/page.tsx), NOT /vendors/[slug] as several Discovery/Ecosystem specs name — do NOT create a second route; reconcile the specs to /v/[slug]. This makes P0 a thin badge/credit-slot pass on an existing page, not a new route build. (2) recomputeVendorActivityStats() is ALREADY fully implemented (apps/web/lib/vendor-activity.ts, 552 lines, upserts avg_response_minutes/response_rate_pct/inquiry_to_booking_pct/finalized_booking_count) and migration 20270110320018_chat_threads_first_reply already feeds latency — so First-Look's 'the population job is the missing piece' is STALE; the missing piece is only the INVOCATION wiring (call it from message-send via after()). This de-risks Wave 2 and the entire analytics group. (3) respond_vendor_proposal + concurrency_guards (FOR UPDATE x6) + acquire_schedule_pools are all live and reusable — every two-party-ack item rides them, justifying P2 as a doc/template prerequisite rather than new infra.

SEQUENCING LOGIC: Quick wins / already-exists items first (Wave 1) to clear trivia and prove the shared /v/[slug] primitive. Stats wiring + responsiveness next (Wave 2) because it unblocks both First-Look AND the analytics group. Booking-lifecycle two-party-ack items cluster (Wave 3) because they share ONE serialization pattern + the SAME two workspace pages + event_vendor_line_items settle target. Money-read + day-of (Wave 4) follow Wave 3's payment-plan tables and share the email/notify rail. Public-facing vendor spotlights cluster (Wave 5) sharing badge components + vendor_partnerships admin-verify scaffold + two-admin gate. Analytics LAST among the high-value waves (Wave 6) because they all read the Wave-2 stats table and share the min-N + cron-free recompute substrate — and because founder-only marketplace means most rollups stay min-N-suppressed (empty) at launch. Ecosystem/crew dead last (Wave 7) because their value is gated on 3rd-party vendor density that doesn't exist yet (marketplace_founder_only memory lock) — ship behind the publish flag; Couple Referral Rewards is the lone density-independent item and can be pulled forward as an early growth lever.

CROSS-CUTTING LOCKS honored throughout: prices/thresholds always admin-managed (never hardcoded) — First-Look SLA+weight, radar thresholds, referral-cut %, resell markup, cert fee, off-season values; behavioral-data min-N + de-identification on every aggregate (Radar, bands, funnel, Won/Lost); cron-free (after()/waitUntil, no pollers); email-only (Resend, no SMS); seat-plan-stays-free (White-Label must not paywall couple's own tools); off-platform money / no-2307 (deposit, crew, no-show are policy+evidence layers, not money movement); architect mandate (every item names couple+vendor+admin surfaces). OWNER SIGN-OFFS to surface before building: Right-of-Reply immutable-vs-editable + relax-tier-gate (changes shipped behavior); Spotlight Awards homepage slot (homepage is product-led today); No-Show forfeit wording (PH counsel); named-calendars flag flip; Journal file-based-vs-CMS decision.


---

# Per-benefit build specs (grounded in the audit)

## Discovery

### Shortlist Radar (live count of couples who saved you + rival-on-your-date alert)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Three real save signals exist but NONE is exposed to vendors as a count. (1) public.vendor_follows (supabase/migrations/20260514150000_iteration_0019_follow_gate.sql) — couple→vendor follow; CRUCIALLY it already has a vendor_follows_vendor_select RLS policy so a vendor CAN read rows pointing at their own vendor_profile_id, and is indexed vendor_follows_by_vendor(vendor_profile_id, followed_at DESC). (2) public.guest_saved_vendors (20270226218747_guest_saved_vendors.sql) — account-level bookmark, but RLS is owner-only (guest_saved_vendors_owner_select USING user_id=auth.uid()) so a vendor canNOT currently read who saved them. (3) saveVendorToPicks (apps/web/app/explore/actions.ts) writes the couple's pick into event_vendors. The 'rival on your date' half needs events.event_date + events.date_status (events table, date precision migrations) joined to event_vendors/chat_threads on the SAME category+region — that join is computable but no surface does it. Vendor dashboard has no 'who saved you' card (apps/web/app/vendor-dashboard/ — no radar/saved surface found). vendor_activity_stats exists for service-role-written metrics.
- **What to build:** Vendor: a 'Shortlist Radar' card on /vendor-dashboard (home or a new /vendor-dashboard/radar) showing live saved-count (follows + bookmarks) and a feed 'A couple planning a <date> wedding in <region> just added a vendor in your category to their shortlist'. Schema: extend guest_saved_vendors RLS with a NON-IDENTIFYING vendor read — do NOT expose user_id; instead add a SECURITY DEFINER aggregate fn count_saves_for_vendor(vendor_profile_id) (GRANT to authenticated, internally gated to current_vendor_profile_ids()) returning just a count, preserving the table's owner-only privacy lock. For the rival alert add a view/fn rival_signals_for_vendor() that aggregates anonymized (count + date-bucket + region_code, min-N>=1, NO couple identity per the behavioral-data edge/min-N memory lock) over event_vendors/chat_threads created in the vendor's category+hq_region. Server action getShortlistRadar() reads both via service role. Admin: a read-only radar-config row (thresholds, on/off) in an admin-managed settings table — no hardcoded thresholds. Couple: no new surface (their saves already exist). Honor RLS Pattern E (current_vendor_ids gate) and the min-N de-identification rule.
- **Reuse:** vendor_follows (+ its vendor-read RLS already shipped), guest_saved_vendors, event_vendors, vendor_activity_stats (write-by-service-role pattern), current_vendor_profile_ids()/current_vendor_ids() helpers, regions/region_code resolution (lib/regions.ts, vendor_profiles.hq_region), events.event_date/date_status, the vendor-dashboard card layout + notifications surface (/vendor-dashboard/notifications).
- **Dependencies:** Behavioral-data min-N + de-identification lock (memory: behavioral_data_edge); a SECURITY DEFINER count fn to keep guest_saved_vendors owner-only privacy; region resolution already in place. No external services.

### First-Look Window (reply-fast → head-start slot for new in-region couples)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Strong foundation. chat_threads carries inquiry_status enum (pending/accepted/declined) via 20260722000000_chat_inquiry_accept_gate.sql with index chat_threads(vendor_profile_id, inquiry_status); the accept-to-reply gate is the lead/inquiry flow (apps/web/app/vendor-dashboard/messages/page.tsx renders 'New inquiry · accept to reply'). vendor_activity_stats ALREADY computes avg_response_minutes + response_rate_pct (service-role written) — the exact 'reply-fast' signal needed, though no job populates it yet (it's a schema-only foundation). Region match is available: vendor_profiles.hq_region/region_code/travel_radius + events region via regions/wedding_destinations; the compat-score engine (apps/web/lib/compat-score.ts) already ranks survivors and the explore matcher (apps/web/app/explore/page.tsx) GATEs by region/ceremony/venue. NO 'head-start slot' / early-surfacing boost logic exists — compat-score has a 'boosted' input (ad_rank) but nothing tied to response speed, and there is no first-look queue table.
- **What to build:** A ranking boost, not a new marketplace. Add a responsiveness-derived nudge to compat-score.ts (new optional input e.g. responsivenessBoost, weight added to COMPAT_WEIGHTS — keep weights summing to 1, admin-tunable later) fed by vendor_activity_stats.avg_response_minutes/response_rate_pct. Define eligibility: vendor replied to recent in-region inquiries within an admin-set SLA → gets a head-start placement window for NEW couples (created within N days) in the vendor's hq_region. Build the background job that actually POPULATES vendor_activity_stats from chat_threads/chat_messages timestamps (compute first-reply latency from chat_messages on accepted threads) — this is the missing piece. Vendor: a 'First-Look' status chip on /vendor-dashboard (Earned / At-risk) + explainer. Couple: in /explore, a subtle 'Replies fast' badge on boosted cards (reuse compat tier rendering). Admin: SLA window + boost weight as admin-managed config rows (never hardcode — per pricing/prices-admin-managed lock). RLS: read vendor_activity_stats (already public-read); job writes via service role.
- **Reuse:** compat-score.ts (boosted/score architecture, NEVER-hide gate), vendor_activity_stats (avg_response_minutes/response_rate_pct columns), chat_threads.inquiry_status + chat_messages timestamps, explore matcher GATE+SCORE (apps/web/app/explore/page.tsx), region helpers, vendor-dashboard chip/badge components.
- **Dependencies:** Requires the (currently unwritten) vendor_activity_stats population job — that job is itself a prerequisite shared with vendor quality/rating workstream. Admin-managed SLA/weight config. Cron-free constraint (memory: cron_free) — use Next after()/waitUntil on message-send to recompute latency incrementally.

### Booked-Out Waitlist (couples join a waitlist when a vendor is full)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Nothing purpose-built. The existing couple_waitlist_signups table (20260518300000_couple_waitlist_signups.sql) + /waitlist route (apps/web/app/waitlist/) is a PLATFORM pre-launch waitlist (anon insert, admin-only read, one global email list) — it is NOT per-vendor and cannot be reused for a booked-out waitlist. The 'full' signal exists: vendor_calendar_blocks (20260518500000_iteration_0016_wizard_architecture_schema.sql) records blocked_at/blocked_until at 30-min granularity with block_source manual|setnayan_booking|synced_calendar (is_private default TRUE — couples see only 'Unavailable'). chat_threads UNIQUE(event_id,vendor_profile_id) + inquiry_status give the inquiry rail. No waitlist join, no notify-on-open, no vendor-side waitlist queue.
- **What to build:** New table public.vendor_date_waitlist (waitlist_id, vendor_profile_id FK, event_id FK nullable, requested_date DATE, user_id FK, status pending|notified|converted|cancelled, created_at, notified_at) with RLS-at-create: couple owns their row (Pattern: user_id=auth.uid() for insert/select/cancel) + vendor reads rows for their own vendor_profile_id (current_vendor_profile_ids() gate) + admin full. Couple surface: on a vendor's public/profile + in /explore, when vendor_calendar_blocks shows the couple's event_date as unavailable, swap the inquiry CTA for 'Join the waitlist for <date>'. Vendor surface: a Waitlist queue on /vendor-dashboard/calendar or /messages showing who's waiting per date, with a one-click 'a slot opened — notify them' that flips status→notified and fires email. Admin: visibility in the admin console + a config toggle. Notify path: when a vendor_calendar_blocks row covering a waitlisted date is deleted (slot freed), trigger/after() emails pending waitlisters (reuse 0028 email templates via Resend). Honor email-only (no SMS) lock.
- **Reuse:** vendor_calendar_blocks (the full/availability source), chat_threads/inquiry flow (waitlist→inquiry conversion reuses acceptInquiry), 0028 email notification templates + Resend, vendor-dashboard calendar surface, couple_waitlist_signups RLS pattern as a template (anon-insert + scoped-read) though a NEW table is required, current_vendor_profile_ids()/current_couple_event_ids() helpers.
- **Dependencies:** Relies on vendors actually maintaining vendor_calendar_blocks (availability accuracy). Email notification infra (0028/Resend). Cron-free → fire notify via DB trigger or Next after() on block-delete, not a poller.

### Style-Twin Discovery (couples who love a Real Story tap through to the vendors who made it)
- **Status:** ◐ buildable now  ·  **Effort:** S · small
- **Current state:** All the pieces exist EXCEPT the tap-through link. /realstories (apps/web/app/realstories/page.tsx) renders published editorials; each editorial already computes a vendor CREDIT list from event_vendors (apps/web/app/[slug]/_components/editorial/data.ts builds VendorCredit[] with tier-aware showcase + 'From Your Vendors' day-of media via editorial_vendor_media, 20270102000000) — so 'the vendors who made this story' is DATA WE ALREADY HAVE per editorial. BUT realstories/page.tsx only links to a generic /explore ('Browse vendors', line 180) — there is no per-story tap-through to the specific vendors. Also there is no standalone PUBLIC vendor profile route to land on (vendor pages live at /dashboard/[eventId]/vendors/[vendorId], event-scoped; the marketplace card is in /explore). vendor_profiles.tier gates which credited vendors are showcased (Free excluded per Wedding_Website_Lifecycle_Spec).
- **What to build:** Mostly a wiring + linkage job, not new data. On each Real Story (the /[slug] editorial 'Team Behind the Day' / 'From Your Vendors' block AND on the /realstories card hover/expand), render the existing VendorCredit[] as TAPPABLE chips that deep-link to that vendor's marketplace presence. Decide the landing target: either (a) a new public vendor profile route /vendors/[slug] (recommended, also unblocks other Discovery items) or (b) link into /explore pre-filtered to that vendor — (a) is the durable, root-cause choice per the build-for-long-term lock. Add 'Style-Twin' framing: a 'Loved this story? Work with the team that made it' CTA. Optionally precompute a story→vendor index for SEO/ISR. Honor the tier showcase rule (Free vendors stay excluded). Admin: editorial-review already gates publish (apps/web/app/admin/editorial-review/) — no new admin surface needed beyond ensuring credited-vendor links respect consent + tier. Couple: the tap-through itself. Vendor: appears as a credited link (attribution) — could add a 'featured in N real stories' badge on the vendor profile.
- **Reuse:** editorial data.ts VendorCredit[] + vendorMedia (editorial_vendor_media), event_vendors selection_match_rank credit logic, /realstories gallery + /[slug] editorial-content.tsx, vendor tier showcase logic, lib/showcase-db + real-weddings, admin editorial-review consent gate, explore vendor card components.
- **Dependencies:** Cleanest implementation wants a public vendor profile route /vendors/[slug] (currently only event-scoped vendor pages exist) — small but shared with Shortlist Radar/First-Look. Consent + vendor-tier gating already enforced upstream; must not surface Free-tier or non-consented vendors.

## Booking & Operations

### Deposit Reservation Lock-Free (recorded deposit holds the date, money settles off-platform)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Most of the substrate already ships. The booking ledger is public.event_vendors (PK vendor_id; status enum considering→shortlisted→contracted→deposit_paid→delivered→complete from 20260513100000_iteration_0006_vendors.sql; deposit_paid_php NUMERIC column; workspace_status payment-stepper from 20260604130000_event_vendors_workspace_status.sql). The date-hold itself is real: acquire_schedule_pools() (20261126000000_schedule_pools.sql) writes a vendor_schedule_pool_bookings row on the capacity-consuming transition, called from updateVendorStatus()/finalizeVendor() in apps/web/app/dashboard/[eventId]/vendors/actions.ts when the host marks the deposit (DOWNPAID_STATUSES gate at lines ~268-318, ~990-1023). Off-platform money is already the model: deposit is a manual host-entered figure (event_vendor_payments + event_vendor_line_items from 20260513110000_iteration_0007_budget.sql; vendor_service_payment_schedules TEMPLATE from 20270202160004) — Setnayan never holds funds (memory: vendor_payment_disclosure). What is MISSING is an explicit 'recorded deposit = date held, awaiting settlement' state distinct from confirmed-paid: today deposit_paid_php>0 and status='deposit_paid' conflate 'I recorded it' with 'it cleared', and there is no proof-of-deposit artifact or vendor-side acknowledgement that the off-platform money arrived.
- **What to build:** Thin layer on existing tables, NOT a new booking engine. (1) Add event_vendors.deposit_recorded_at + deposit_acknowledged_at + deposit_proof_url (R2) columns (additive ALTER, idempotent) so a 'recorded but unsettled' deposit is first-class and orthogonal to the locked status enum (same orthogonal-marker pattern as contract_signed_at in 20270217864104_contract_booking_link.sql — never repurpose event_vendors.status). (2) Server action recordDeposit() in vendors/actions.ts that calls the EXISTING acquire_schedule_pools RPC to hold the date the instant the deposit is logged (it already fires on the booked transition — just key it to deposit_recorded_at rather than full payment), records the event_vendor_payments row, and emits a notification to the vendor. (3) Vendor-side acknowledgeDeposit() action (vendor confirms the off-platform money landed) writing deposit_acknowledged_at — reuse the schedule_suggestions accept/decline serialization pattern (SELECT FOR UPDATE + status precondition, cf. respond_vendor_proposal in 20261209000000). Surfaces (architect mandate): COUPLE = deposit-record CTA + 'date held, awaiting vendor confirmation' chip on the vendor workspace page (apps/web/app/dashboard/[eventId]/vendors/[vendorId]/workspace/page.tsx); VENDOR = 'confirm deposit received' on the client workspace (apps/web/app/vendor-dashboard/clients/[eventId]/page.tsx); ADMIN = deposit-state column in the existing reconciliation/disputes console (0023/0034). RLS: reuse current_couple_event_ids() (couple write) + current_vendor_booked_event_ids() (vendor read/ack) + is_admin() — all canonical helpers already present. No hardcoded prices (deposit amount is host-entered PHP, schedule template % lives in vendor_service_payment_schedules).
- **Reuse:** event_vendors + deposit_paid_php + workspace_status; acquire_schedule_pools()/release_schedule_pools() RPCs; event_vendor_payments/line_items; vendor_service_payment_schedules template; updateVendorStatus()/finalizeVendor() actions; orthogonal-marker pattern from contract_booking_link; FOR-UPDATE+precondition serialization from concurrency_guards; R2 upload pattern used by contract proof.
- **Dependencies:** event_vendors + schedule-pool layer (both live). Coordinates with 0034 Payments reconciliation for the admin settlement view. No external service.

### Double-Booking Guard (held slot blocks a second booking, scoped per-service + crew-role)
- **Status:** ✅ already exists (mostly)  ·  **Effort:** S · small
- **Current state:** ALREADY BUILT and DB-enforced — this is the most mature of the four. The schedulable resource is the per-(org, leaf-category) POOL (vendor_schedule_pools), and acquire_schedule_pools() (20261126000000_schedule_pools.sql) is a race-safe atomic acquire: deterministic-order FOR UPDATE on every pool row, a partial-UNIQUE live-booking index vendor_schedule_pool_bookings_live_uniq (pool_id, event_vendor_id WHERE released_at IS NULL), capacity check (live reservations + external_client jobs) against daily_booking_capacity, closure-block check, and bundles-lock-all (the RPC takes a pool ARRAY and is all-or-nothing — owner lock 2026-06-12 'bundles lock both schedules for both category'). External off-app jobs consume capacity via vendor_calendar_blocks block_source='external_client' (idempotent import via import_external_client() in 20261209000000_concurrency_guards.sql). PER-SERVICE scoping is real and getting finer: 20270209750853_named_calendars_phase_a_service_membership.sql adds vendor_schedule_calendar_services (PK vendor_service_id → pool) so a vendor names calendars and assigns which services each covers (flag-gated NEXT_PUBLIC_NAMED_CALENDARS_ENABLED, default OFF). Vendor UI is live at apps/web/app/vendor-dashboard/calendar/page.tsx (+actions.ts). The ONLY genuine gap vs. the benefit wording is 'crew-role' scoping: pools are per-category, not per-named-crew-member — a 2-photographer studio that can shoot two weddings/day models it as daily_booking_capacity=2 on one pool, with no way to say WHICH crew/role is committed.
- **What to build:** Core guard needs nothing — surface it and (optionally) extend to crew-role. (1) FLIP/finish named calendars: complete the Phase A→B UI so service→calendar membership is the default scoping (the migration is additive + already in prod; the resolver resolvePoolIdsForService falls back to the category pool for unassigned services). (2) CREW-ROLE (optional, only if owner wants sub-pool granularity): add vendor_schedule_pool_lanes (lane_id, pool_id FK, label e.g. 'Lead shooter'/'Second shooter', capacity 1) + a lane_id column on vendor_schedule_pool_bookings, and teach acquire_schedule_pools to lock+count per-lane rather than per-pool — same FOR UPDATE pattern, RLS mirrors vendor_schedule_pools verbatim (Pattern A owner + published public-read). (3) Make the guard VISIBLE to the couple: today couples only ever see 'unavailable' (privacy lock) — keep that, but surface a clear 'this date is held' state on the plan-lock card (apps/web/app/dashboard/[eventId]/vendors/_components/plan-card-lock.tsx already consumes the acquire result codes full/blocked/no_date). ADMIN surface: a read-only pool-occupancy view in the console for dispute triage. If crew-role is NOT prioritized, mark this benefit already-exists and ship only the named-calendars UI completion.
- **Reuse:** vendor_schedule_pools + vendor_schedule_pool_bookings + vendor_schedule_pool_categories + vendor_schedule_calendar_services; acquire_schedule_pools()/resolve_schedule_pool()/release_schedule_pools(); vendor_calendar_blocks external_client capacity; import_external_client idempotency; calendar page.tsx + lib/vendor-schedule.ts + lib/schedule-pools.ts; plan-card-lock result-code handling.
- **Dependencies:** Fully self-contained on the schedule-pool layer. Crew-role lanes is the only part that touches the acquire RPC; everything else is UI. Named-calendars flag flip is owner-gated.

### Change-Order Trail (mid-plan add-ons become logged, both-acknowledged change orders)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** NOTHING PURPOSE-BUILT yet — this is the biggest greenfield of the four. Adjacent substrate exists but no change-order entity. The booking's money lines are event_vendor_line_items (label + amount_php + due_date, couple-write only, from 20260513110000_iteration_0007_budget.sql) and the vendor's TEMPLATE schedule vendor_service_payment_schedules (20270202160004) — but line items are unilaterally couple-edited with NO vendor acknowledgement and NO audit trail of who added what when. Contracts (vendor_contracts + vendor_contract_signatures, 20260518200000_vendor_contracts_dual_esign_retire_0032.sql) are upload + dual-esign with a flat status (draft/sent_for_signature/fully_signed/cancelled) and NO amendment/versioning/parent-contract linkage (confirmed: no parent_contract/amends/revision columns). vendor_contracts now links to the booking via event_vendor_id (20270217864104). The closest reusable PATTERN for 'one side proposes, the other acknowledges, never a 2-way write' is event_schedule_suggestions (20261130003000) and vendor_proposals — both are propose→accept/decline state machines with DB serialization (respond_vendor_proposal FOR UPDATE in 20261209000000). A grep for change_order/addendum/amendment across apps/web + migrations + the 0032 spec corpus returns zero real hits.
- **What to build:** New both-acknowledged change-order entity, modeled on the existing suggestion/proposal pattern (conflict-architecture lock: no 2-way writes — a change order is a row whose state both parties move through, never a direct edit of the other's data). Schema (RLS at CREATE TABLE): vendor_change_orders (change_order_id, event_vendor_id FK→event_vendors, event_id, vendor_profile_id denormalized, raised_by ENUM couple|vendor, title, description, delta_amount_php (signed, +add-on/−removal), proposed_due_date, status ENUM proposed|accepted|declined|withdrawn, proposed_by_user_id, acknowledged_by_user_id, acknowledged_at, created_at) + optional child vendor_change_order_lines for itemization. RLS: couple side via current_couple_event_ids(); vendor side via current_vendor_booked_event_ids() + current_vendor_profile_ids(); admin via is_admin() — all canonical, already in the DB. Writes go through SECURITY DEFINER RPCs (acceptChangeOrder/declineChangeOrder) using the SELECT FOR UPDATE + status-precondition pattern from respond_vendor_proposal so concurrent accept/decline is single-winner. On ACCEPT: atomically write a corresponding event_vendor_line_items row (so the budget ledger stays the single source of truth) and bump event_vendors.updated_at. Surfaces (architect mandate): COUPLE raises/accepts add-ons on the vendor workspace page (apps/web/app/dashboard/[eventId]/vendors/[vendorId]/workspace/page.tsx); VENDOR raises/accepts on the client workspace (apps/web/app/vendor-dashboard/clients/[eventId]/page.tsx) — drop it beside the existing Suggest flow which already lives there; ADMIN sees the immutable trail in the dispute/booking console for arbitration. Prices: delta_amount_php is host/vendor-entered PHP, never hardcoded. Notifications via the existing emitNotification helper used in vendors/actions.ts.
- **Reuse:** event_schedule_suggestions + vendor_proposals propose→ack state-machine pattern; respond_vendor_proposal FOR-UPDATE+precondition serialization (concurrency_guards); event_vendor_line_items as the settle target; event_vendors workspace pages (couple + vendor) that already host the Suggest flow; contract_booking_link orthogonal-marker pattern; emitNotification; canonical RLS helpers (current_couple_event_ids / current_vendor_booked_event_ids / current_vendor_profile_ids / is_admin).
- **Dependencies:** event_vendors + event_vendor_line_items (live). Stronger UX if paired with the Deposit benefit (a change order can re-trigger a deposit top-up) and optionally with contract amendment versioning, but standalone-shippable. No external service.

### Day-Of Run-of-Show & Handover (shared live timeline + delivery handover against the booking)
- **Status:** ◐ buildable now  ·  **Effort:** L · large
- **Current state:** The shared-timeline HALF is substantially built; the delivery-handover half is greenfield. RUN-OF-SHOW: event_schedule_blocks (20260513190000_iteration_0031_schedule.sql; typed blocks pre_ceremony→after_party, start/end, location, is_public for the /[slug] guest site) is the couple's timeline, AND booked vendors already get a FULL live read of it via current_vendor_booked_event_ids() + the event_schedule_blocks_booked_vendor_read policy (locked decision D2, 20261130003000_shared_timeline_suggestions.sql). Vendors PROPOSE edits through event_schedule_suggestions (propose→accept/decline, never a 2-way write), wired in the vendor client workspace (apps/web/app/vendor-dashboard/clients/[eventId]/page.tsx — suggestScheduleChange action, incl. an auto-computed 'suggested call time' from the earliest key slot minus the trade's setup lead) and the couple dashboard schedule (apps/web/app/dashboard/[eventId]/schedule/). A caterer Production Sheet (apps/web/app/vendor-dashboard/clients/[eventId]/production-sheet/) and a meal-count handover report (apps/web/app/dashboard/[eventId]/seating/caterer/route.ts) exist as category-specific artifacts. Day-of guest mode (0031) lives at /[slug] with schedule-widget + live cards. What is MISSING: (a) a LIVE run-state on the timeline (current/next block, running ahead/behind) shared across couple+coordinator+vendors on the day, and (b) any DELIVERY HANDOVER against the booking — there is no record that the vendor delivered the agreed assets; the only 'handover' hits in code are an unrelated seat-plan editor lock-handover and the caterer report.
- **What to build:** Two parts. (1) LIVE run-of-show: add event_schedule_blocks.actual_start_at/actual_end_at + a run_state ENUM(upcoming|live|done) (additive ALTER) and a lightweight 'advance to next block' RPC any host/coordinator OR booked vendor can call (auth via current_event_ids() ∪ current_vendor_booked_event_ids()); render a shared 'now / next / running ±N min' header on the couple schedule page, the vendor client workspace (it already reads the full timeline), and the day-of guest card. Realtime via Supabase channel on event_schedule_blocks (no cron — memory: cron_free; Next after()/waitUntil). Keep vendor edits as SUGGESTIONS (reuse event_schedule_suggestions) — only host/coordinator commit. (2) DELIVERY HANDOVER against the booking: new booking_handovers table (handover_id, event_vendor_id FK→event_vendors, vendor_profile_id, kind ENUM gallery_link|file|note|signoff, payload (R2 object key OR external URL OR text), delivered_at, couple_acknowledged_at, status ENUM delivered|acknowledged|disputed) — RLS: vendor write via current_vendor_booked_event_ids()+current_vendor_profile_ids(), couple read+ack via current_couple_event_ids(), admin via is_admin(); writes through a SECURITY DEFINER ack RPC with the FOR-UPDATE+precondition pattern. On couple-acknowledge, optionally advance event_vendors.status→'delivered' (reusing the existing delivered transition + review-request emit already in updateVendorStatus). Surfaces (architect mandate): VENDOR posts handover on apps/web/app/vendor-dashboard/clients/[eventId]/page.tsx (beside the Suggest flow); COUPLE confirms receipt on apps/web/app/dashboard/[eventId]/vendors/[vendorId]/workspace/page.tsx; ADMIN sees delivery+ack state in the dispute console. R2 for any uploaded deliverable (memory: storage_drive_copy — R2=record, large galleries stay Drive-linked).
- **Reuse:** event_schedule_blocks + event_schedule_suggestions + current_vendor_booked_event_ids() + event_schedule_blocks_booked_vendor_read policy; suggestScheduleChange action + vendor client workspace + couple schedule page + day-of guest schedule-widget; production-sheet/caterer-report handover-artifact precedent; updateVendorStatus delivered transition + emitNotification + review-request emit; respond_vendor_proposal serialization pattern; Supabase realtime + after()/waitUntil; R2 + Drive-copy storage model; canonical RLS helpers.
- **Dependencies:** event_schedule_blocks + event_vendors + booked-vendor membership (all live). Delivery-handover coordinates with 0009 photo-delivery/galleries and storage_drive_copy for asset hosting. Realtime relies on Supabase channels already used elsewhere. No external service.

## Money & payments

### No-Show Downpayment Protection
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Strong downpayment plumbing exists, but NO cancellation/no-show policy or couple-agreement layer exists yet. (1) supabase/migrations/20270202160004_vendor_service_payment_schedules.sql defines vendor_service_payment_schedules — a per-service template where seq 0 IS the downpayment (amount_kind percent_bps OR fixed amount_centavos; due_anchor on_lock/before_event). (2) 20270202160005_event_vendor_payment_plan.sql freezes that template into a per-booking event_vendor_payment_plan.instances_json at lock (finalizeVendor), host-RLS via current_event_ids(). (3) 20270202160006 (confirm) + 20270202160007 (clear) add event_vendor_payments.schedule_instance_seq/vendor_confirmed_at/vendor_confirmed_by + the confirm_vendor_payment()/clear_vendor_payment_plan() SECURITY DEFINER guards. (4) Couple workspace renders the full stepper (app/dashboard/[eventId]/vendors/[vendorId]/workspace/page.tsx via PaymentPlanStepper + fetchPlanForCouple). What's MISSING: grep across supabase/migrations found NO cancellation_policy, non_refundable, forfeit, or no-show field. The only 'no_show' hits are an unrelated retired payout dispute_reason enum (20260516210000_vendor_payout_model.sql) and review seed text — neither is a couple-agreed reservation/cancellation policy. There is no record of the couple ACKNOWLEDGING terms, which is what makes a forfeit defensible. Note: Setnayan never holds the downpayment (off-platform pay; event_vendor_payments are couple-logged proofs the vendor confirms), so this is a policy + mutual-acknowledgement + evidence-trail layer, not a money-movement feature.
- **What to build:** A reservation/cancellation POLICY attached to the downpayment, plus a couple-acknowledgement record that creates the defensible paper trail. Schema (RLS at CREATE TABLE): (a) ALTER vendor_services (or vendor_service_payment_schedules seq-0 row) to add policy fields — cancellation_terms TEXT, downpayment_non_refundable BOOLEAN, refund_window_days INT, no_show_forfeit BOOLEAN — owner+admin write / public-read gated exactly like the existing vendor_service_payment_schedules_owner_write + _public_read policies (mirror them verbatim). (b) New table event_vendor_policy_acknowledgements(ack_id, event_id FK events, event_vendor_id, policy_snapshot_json, acknowledged_by UUID, acknowledged_at) — RLS host-scoped via event_id IN (SELECT public.current_event_ids()) OR public.is_admin(), the canonical event-child couple pattern already used by event_vendor_payment_plan_host_select; snapshot the policy text at lock-time so later edits can't rewrite history. Server actions: at finalizeVendor lock, snapshot the policy into the ack row alongside the existing payment-plan freeze (same code path that writes event_vendor_payment_plan). Couple surface: a 'Reservation terms' acknowledgement gate in the lock/build flow (accordion-lock.tsx / build-locked.tsx) — couple ticks 'I understand the downpayment is non-refundable on no-show' before lock; rendered read-only afterward on the workspace beside PaymentPlanStepper. Vendor surface: policy editor inside the existing PaymentScheduleEditor card (app/vendor-dashboard/services/_components/payment-schedule-editor.tsx) on the downpayment row; a 'Protected by your reservation policy — acknowledged [date]' badge on the per-booking view. Admin surface (architect mandate): expose the ack snapshot + policy in the admin dispute view (app/dashboard/[eventId]/disputes already exists) so support can adjudicate a forfeit dispute with the frozen evidence. Prices stay admin-managed — no hardcoded amounts; the downpayment value continues to come from the schedule template.
- **Reuse:** vendor_service_payment_schedules + event_vendor_payment_plan tables; the finalizeVendor lock snapshot path (actions.ts / build-pick-actions.ts) that already freezes the plan — add the policy snapshot there; PaymentScheduleEditor for the vendor editor; RLS patterns copied verbatim from vendor_service_payment_schedules_owner_write/_public_read (vendor side) and event_vendor_payment_plan_host_select / event_vendor_preferences (couple side); existing disputes surface for admin adjudication; emitNotification + the 4 payment_* notification types for any policy-ack signal.
- **Dependencies:** Requires a booked/locked marketplace vendor with a downpayment defined in vendor_service_payment_schedules (off-platform/manual vendors with marketplace_vendor_id NULL have no schedule and are out of scope). Best paired with the existing disputes surface for the adjudication half. PH-counsel review of the non-refundable/forfeit wording is advisable before launch (off-platform = Setnayan is not party to the money, lowering its own exposure).

### Payday Calendar & Cash-Flow View
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** The per-installment due-date + amount data ALREADY EXISTS but is locked to the couple side; no vendor-facing aggregated timeline exists. event_vendor_payment_plan.instances_json stores resolved {seq,label,amount_php,due_date} per booking — computed by computePlanInstances() in apps/web/lib/vendor-service-payment-schedules.ts (on_lock = lockDate + offset; before_event = eventDate - offset). BUT the plan table is host-RLS ONLY (event_vendor_payment_plan_host_select gates on current_event_ids()), so a vendor cannot read their own bookings' due-dates directly — confirm/clear only reach it through SECURITY DEFINER RPCs. Vendor surfaces that exist to build on: app/vendor-dashboard/calendar/page.tsx (an availability calendar — pools/blocks/bookings, NOT payments; PoolBookingEntry in lib/vendor-schedule.ts carries dates but no amounts), and app/vendor-dashboard/earnings/page.tsx (in-app SKU earnings + retired-model payouts, NOT off-platform vendor-booking installments). So all the raw data (amounts, due-dates, confirmed/unconfirmed state via event_vendor_payments.schedule_instance_seq + vendor_confirmed_at) is present; what's missing is a vendor-scoped read path that aggregates installments across all the vendor's booked events into one timeline.
- **What to build:** A vendor-scoped aggregate read over the existing plan data + a timeline UI. Data layer: a SECURITY DEFINER read function (mirror the shape of confirm_vendor_payment/clear_vendor_payment_plan — resolve auth.uid() -> vendor_profiles, then gather event_vendor_payment_plan rows whose event_vendor -> marketplace_vendor_id = caller's vendor) OR a vendor-readable VIEW that joins event_vendor_payment_plan -> event_vendors (on marketplace_vendor_id) -> event_vendor_payments (confirmed state), returning {event_name, event_date, seq, label, amount_php, due_date, confirmed/owed} per installment. Because the plan is host-RLS, the SECURITY DEFINER route is the right one — do NOT loosen event_vendor_payment_plan's RLS. A lib/vendor-cashflow.ts assembles the timeline (group by due month/week, expected vs confirmed-received, overdue flag where due_date < today and not vendor_confirmed). Vendor UI: a new 'Payday' / cash-flow tab (or a section on the existing calendar or earnings page) showing a chronological list of upcoming installment due-dates across all booked events with running expected-cash totals, an overdue band, and a confirmed-vs-owed split; optional .ics export reusing the existing calendar export pattern. Couple side (architect mandate): no new couple surface needed — the same due-dates already render in PaymentPlanStepper on the workspace, which IS the couple's mirror of this timeline. Admin surface: surface the same aggregate in the admin console reconciliation/dispute views for support context. All amounts read-only from the frozen plan (admin-managed prices upstream; nothing hardcoded). This is pure read/aggregation — no money movement, no tax, no OR (honors the off-platform constraint).
- **Reuse:** event_vendor_payment_plan.instances_json + computePlanInstances/computeStepper (lib/vendor-service-payment-schedules.ts) for the already-resolved amount_php/due_date; event_vendor_payments.schedule_instance_seq + vendor_confirmed_at for the received-vs-owed split; the confirm_vendor_payment/clear_vendor_payment_plan SECURITY DEFINER + ownership-gate pattern as the template for the new vendor read function; the existing vendor calendar page (page shell, month nav, manilaDate helper) and its .ics-style export; fetchOwnVendorProfile for vendor scoping; PaymentPlanStepper component for per-booking detail drill-in.
- **Dependencies:** Depends entirely on the Phase-2 payment-plan lifecycle (PR-A..D, all shipped) — only bookings that were locked with a defined schedule produce installments to show; off-platform/manual vendors (marketplace_vendor_id NULL) and direct-pay bookings (empty instances_json) contribute nothing. Needs the new vendor-scoped SECURITY DEFINER read added before any UI (the plan's host-RLS blocks a plain vendor select). No dependency on No-Show Protection, though they share the same plan tables and pair naturally.

## Trust & reviews

### Receipt-Backed Reviews ("booked through Setnayan" per-rating + each finished event a dated profile line)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Strong foundation, but neither sub-feature is fully shipped as described. Schema: `vendor_reviews` (supabase/migrations/20260514100000_vendor_reviews.sql) — 5-axis ratings + body + event_id + couple_user_id, INSERT RLS gated on owning the event AND an `event_vendors` row in status delivered/complete. Provenance link EXISTS: `event_vendors.linked_vendor_profile_id` (20260515020000_public_stats_exclusion.sql) and `event_vendors.marketplace_vendor_id` (20260519200000_vendor_invites_foundation.sql) tie a couple-side booking to a platform vendor_profile. Aggregate 'through Setnayan' proof ALREADY renders: `vendor_public_completed_events_stats` materialized view counts delivered/complete linked bookings, and `vendor_activity_stats.finalized_booking_count` drives the hero line `"{n} finalized events through Setnayan"` plus the experience-tier badge (apps/web/lib/vendor-experience.ts, apps/web/app/v/[slug]/page.tsx:984). The reviews header already says "From verified couples who've booked {vendor} via Setnayan" (page.tsx:1541). GAPS: (1) NO per-review "booked through Setnayan" badge — the review INSERT RLS checks only that *a* delivered event_vendors row exists, NOT that this couple's specific booking links to THIS vendor_profile (no per-review provenance column on vendor_reviews); (2) NO per-event dated profile line / track-record timeline — only the aggregate count exists, not individual "finished {event} · {date}" lines.
- **What to build:** (1) Per-review 'booked through Setnayan' badge: add a derived/stored provenance flag. Cleanest = a generated/maintained `booked_through_setnayan BOOLEAN` on `vendor_reviews` (RLS at create-time: keep the existing public SELECT; couples can't set it — populate server-side in createReview/submitCoupleReview by resolving whether the source event_vendors row has linked_vendor_profile_id/marketplace_vendor_id = the reviewed vendor and an in-app order). Tighten the INSERT path (lib/reviews.ts createReview + the dashboard action submitCoupleReview) to stamp it; backfill via migration for existing rows. Render the small 'Booked through Setnayan' pill in ReviewRow (apps/web/app/v/[slug]/page.tsx) and the vendor-dashboard VendorReviewCard. (2) Dated finished-event track-record: add a `vendor_completed_events` SELECT-able view (or extend vendor_public_completed_events_stats from a COUNT to a row-per-event view) exposing {vendor_profile_id, event_type, completed_at/event_date} for delivered/complete linked bookings, applying the SAME self-review/team exclusions already in public_stats_exclusion.sql; grant SELECT to anon+authenticated. Render a 'Track record' dated list on the public profile and vendor dashboard. Surfaces (architect mandate): couple = unchanged review flow (provenance auto-derived, no new input); vendor = new 'Track record' section + per-review badge on /vendor-dashboard/reviews; admin = the dated lines must respect the existing self-review exclusion + the show_team_bookings_in_backend_count toggle, and feed the same /admin/reviews moderation. No prices involved.
- **Reuse:** vendor_reviews table + RLS + lib/reviews.ts; event_vendors.linked_vendor_profile_id / marketplace_vendor_id provenance links; vendor_public_completed_events_stats + vendor_activity_stats.finalized_booking_count (already counts 'through Setnayan' with self/team exclusions baked in); experienceTier() badge pattern (lib/vendor-experience.ts); ReviewRow / VendorReviewCard render components; the materialized-view refresh-trigger pattern; events.display_name + event_type for the dated line label.
- **Dependencies:** event_vendors completion handshake (completion_status confirmed/delivered/complete) which already drives finalized counts; the self-review exclusion view (public_stats_exclusion.sql) to keep dated lines honest; admin reviews moderation surface (apps/web/app/admin/reviews) already in place. No price/catalog dependency.

### Right-of-Reply on every review (one public vendor reply under any review)
- **Status:** ✅ already exists (mostly)  ·  **Effort:** S · small
- **Current state:** Already built and shipped end-to-end. Schema: `vendor_reviews.vendor_reply` + `vendor_reply_at` columns with a dedicated vendor-only UPDATE RLS policy (`vendor_reviews_vendor_reply`, scoped to vendor_profiles owned by auth.uid()) and a `lock_vendor_reply()` BEFORE-UPDATE trigger (supabase/migrations/20260514100000_vendor_reviews.sql). Lib: `submitVendorReply()` + `VENDOR_REPLY_MAX_CHARS` (apps/web/lib/reviews.ts). Action: `postVendorReply` (apps/web/app/vendor-dashboard/reviews/actions.ts) — writes the reply AND emits a `vendor_review_reply` notification to the couple. UI: vendor-dashboard reply + edit forms (apps/web/app/vendor-dashboard/reviews/page.tsx — ReplyForm / ExistingReplySection), and public render under each review via VendorReplyBlock in apps/web/app/v/[slug]/page.tsx (gated behind the Pro/Enterprise showComments tier cap). One nuance vs the benefit's literal wording: the original migration locked the reply to one-time/immutable, but the shipped action + UI now allow editing it (page copy: 'you can reply publicly and edit your response anytime'), so it's 'one reply slot, editable' rather than strictly write-once.
- **What to build:** Nothing required to ship the core benefit — it exists. Only optional polish if the owner wants the literal 'one public reply' contract enforced: (a) decide immutable-vs-editable (currently editable) and align the lock_vendor_reply trigger + UI copy accordingly; (b) ensure the reply renders for ALL viewers, not just Pro/Enterprise — today showComments gates the reply behind tier (apps/web/app/v/[slug]/page.tsx), so a Free/Verified vendor's reply is hidden on the public profile; if 'right-of-reply on every review' should always be visible, relax that gate for the reply block specifically. Surfaces already complete: vendor (compose/edit), couple (notified + sees reply), admin (replies visible in /admin/reviews moderation context).
- **Reuse:** vendor_reply column + vendor_reviews_vendor_reply RLS + lock_vendor_reply trigger; submitVendorReply()/postVendorReply; ReplyForm/ExistingReplySection/VendorReplyBlock components; emitNotification('vendor_review_reply'); SubmitButton primitive.
- **Dependencies:** None for the existing feature. Optional polish depends on the vendor-tier-caps gate (reviewCommentsViewable) decision and the immutable-vs-editable product call — surface both to owner since they change shipped behavior.

## Marketing & editorial

### Featured in Real Wedding Stories (vendor credit + backlink)
- **Status:** ✅ already exists (mostly)  ·  **Effort:** S · small
- **Current state:** Largely BUILT. Migration supabase/migrations/20261221000000_realstories_featuring.sql adds events.showcase_featured_at + showcase_feature_rank (admin curate/pin/order, RA 10173 consent-gated). Admin surface apps/web/app/admin/real-stories/page.tsx (+ actions.ts) feature/order/hero. Public index apps/web/app/realstories/page.tsx + /[slug] editorial. Vendor surface apps/web/app/vendor-dashboard/real-stories/page.tsx ('Featured in Real Stories', one-tap FB share via ShareButtons, lib/realstories-vendor.ts loadVendorFeaturedStories). The vendor CREDIT + BACKLINK already fires inside the couple editorial: apps/web/app/[slug]/_components/editorial/data.ts (~lines 580-629) joins event_vendors.linked_vendor_profile_id -> vendor_profiles, and renders logo + tier badge + profile link (business_slug) for Pro/Enterprise ('tagged'), plain-text credit for verified, hidden for free. linked_vendor_profile_id wiring + backfill fixed in 20270129232225_vendor_editorial_credit_slot_and_backfill.sql. editorial_vendor_media table (20270102000000) lets the recommended/#1 vendor submit up to 3 photos+3 clips that auto-show on the editorial.
- **What to build:** Mostly verification + polish, not a new build. (1) Confirm the two pending migrations are applied in prod (20261221000000 + 20270129232225) — admin page renders a 'migration not applied' state if not. (2) The backlink is a profile link only; if SEO 'backlink' means a dofollow outbound link to the vendor's OWN website, add vendor_profiles.website_url surfacing in the editorial credit (admin-moderated, rel as policy dictates). (3) Optional: notify the vendor (0028 email) when one of their weddings gets featured (the couple is already notified on feature per admin copy). No new table needed; couple+vendor+admin surfaces already exist.
- **Reuse:** realstories_featuring columns, lib/realstories-vendor.ts, editorial/data.ts vendor-credit join, vendor tier_state gating, ShareButtons, editorial_vendor_media, vendor_recommendations (vendors-we-loved block).
- **Dependencies:** Pending migrations applied to prod DB; vendor tier_state (Pro/Enterprise for tagged credit + logo + backlink); RA 10173 couple showcase consent (users.public_summary_consent_at).

### Editorial & Journal Spotlights (vendor featured in Journal/guides)
- **Status:** ○ roadmap (larger)  ·  **Effort:** M · medium
- **Current state:** Journal is BUILT but vendor-spotlight is NOT. /blog ('Setnayan Journal') fully shipped: apps/web/app/blog/page.tsx + /[slug] + lib/blog.ts + lib/blog-batches/*. CRITICAL: content is FILE/CODE-based (lib/blog.ts BlogArticle objects), explicitly 'no DB, no CMS, no markdown-renderer' — there is NO admin CMS and NO vendor-crediting field on BlogArticle. The 0038 spec tables editorial_articles / recommendation_pages / affiliate_links / sponsored_slot_bookings were NEVER migrated (zero matches in supabase/migrations). Articles do link to the vendor marketplace generically but cannot credit/feature a specific vendor.
- **What to build:** New DB-backed spotlight layer on top of the existing Journal. Schema (RLS at CREATE TABLE, canonical patterns): journal_vendor_spotlights(spotlight_id, public_id S89-prefix via generate_public_id, blog_slug TEXT, vendor_profile_id FK vendor_profiles, placement TEXT CHECK(in 'featured_partner'|'recommended'|'sponsored'), is_sponsored BOOL, admin_approved_at, sort_order, created_at) — admin-only write (is_admin), public read of approved rows (Pattern: public SELECT USING(admin_approved_at IS NOT NULL), admin FOR ALL is_admin()). Server actions in app/admin/journal-spotlights/* (CRUD). UI: render a 'Featured partner / In partnership with' credit block + dofollow link in blog/[slug]/page.tsx; vendor-dashboard read-only 'You're featured in' list (mirror real-stories page); admin curate surface. If sponsored, two-admin gate (per 0023 §9.1) + unambiguous 'Sponsored' badge (0038 rule). Prices for any paid sponsored slot must come from service_catalog (admin-managed), never hardcoded.
- **Reuse:** lib/blog.ts article registry + /blog rendering; vendor-dashboard/real-stories/page.tsx pattern for the vendor-facing 'you're featured' list; vendor_partnerships admin_verified pattern; ShareButtons; vendor tier_state for eligibility.
- **Dependencies:** Decision on whether Journal stays file-based (then spotlight is a join-by-slug overlay) or migrates to DB CMS (larger); service_catalog SKU if sponsored slots are paid; two-admin approval (0023) for paid placements.

### Spotlight Awards (top-performer homepage feature badge)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Badge ENGINE exists, homepage feature does NOT. apps/web/lib/vendor-badges.ts computes a 'top_pick' badge ('Setnayan's pick of the month — top 5% by review-weighted score avg_rating x ln(reviews+1)') plus 'most_booking' (top 10% completed bookings), 'verified', 'new'. Rendered via apps/web/app/explore/_components/vendor-badge-row.tsx (Award/TrendingUp icons) on explore cards. BUT: badges are recomputed live per-page against the visible set (not persisted), there is NO is_top_pick column, NO admin curation, and NO homepage surface — the marketing homepage (app/_components/marketing/*) features product narrative, not vendors. So 'top performers earn a homepage feature badge' is only half-built (badge yes, homepage feature + persistence + award no).
- **What to build:** Persist + curate + surface. (1) Schema: vendor_spotlight_awards(award_id, public_id, vendor_profile_id FK, award_type TEXT CHECK(in 'top_pick'|'most_booked'|'rising'), period_month DATE, awarded_at, awarded_by('auto'|'admin'), is_homepage_featured BOOL, UNIQUE(vendor_profile_id,award_type,period_month)) with RLS: public read (USING TRUE, aggregate-safe), admin FOR ALL is_admin(). (2) A monthly recompute that snapshots computeVendorBadges() winners into the table (Next 15 after()/waitUntil per cron-free lock, or admin 'run now' action — no polling cron). (3) Admin surface app/admin/spotlight-awards to confirm/override + toggle homepage feature. (4) Homepage 'This month's Spotlight Awards' vendor strip in app/_components/marketing reading is_homepage_featured rows. (5) Vendor-dashboard 'You earned a Spotlight Award' banner + share asset. No prices involved.
- **Reuse:** lib/vendor-badges.ts (computeVendorBadges, fetchCompletedBookingCounts, percentile math), vendor-badge-row.tsx (Award badge UI), vendor_review_stats, explore vendor-card patterns; the badges.ts V1 caveat already anticipates promoting to a persisted boolean.
- **Dependencies:** vendor_review_stats + event_vendors completed counts (exist); monthly recompute trigger (cron-free after()/waitUntil or admin action); homepage marketing layout slot (owner sign-off, since homepage is product-led today).

### Off-Season Promos (flag lean months → surfaced vendor offer)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Strong foundation, missing the 'flag lean months + surface' layer. supabase/migrations/20270108000200_vendor_service_discounts_and_exclusive.sql ALREADY adds vendor_services.discount_type CHECK including 'off_peak' (also early_booking/bundle/promo/returning) + discount_value + discount_expires_at + discount_conditions_md — so a vendor can ALREADY attach an off-peak discount to a service, set in vendor-dashboard/services. Separately, supabase/migrations/20261001000000_wedding_season_factors.sql holds per-(region,month) price multipliers (admin-seeded, EMPTY=neutral) used only by the budget allocator — it encodes seasonality but is NOT wired to vendor promos. There is NO mechanic that detects a vendor's lean months and prompts them to launch a surfaced offer, and no couple-facing 'off-season deals' surface.
- **What to build:** Wire the existing pieces into a guided 'lean month' flow. (1) Read-side: derive a vendor's lean months from their own booking calendar (vendor_schedule / event_vendors by event month) and/or wedding_season_factors troughs. (2) Vendor-dashboard nudge: 'Your June-Aug look light — launch an off-season offer' CTA that pre-fills the EXISTING vendor_services off_peak discount (no new discount schema needed). (3) Surface the live off-peak offers to couples: a filter/badge on explore vendor cards + an 'Off-season savings' band (read vendor_services where discount_type='off_peak' AND discount_expires_at>now()). (4) Admin: optional curate of a featured off-season collection. Optionally add a light vendor_promo_windows(vendor_profile_id, month_start, month_end, headline, active) table (RLS: vendor writes own via current_vendor_ids(), public read active, admin FOR ALL) if offers should be window-scoped rather than per-service. Prices/discount values are vendor-entered on their own service (already admin-policy-safe — not a platform SKU).
- **Reuse:** vendor_services off_peak discount columns (already shipped), wedding_season_factors (seasonality data), vendor-dashboard/services wizard, explore vendor-card + vendor-badge-row, retired boosted/sponsored SKUs pattern (is_active flip) if a paid 'promote this offer' upsell is wanted later.
- **Dependencies:** Vendor booking-calendar data to detect lean months; explore filter plumbing; decision whether offer is per-service (reuse existing columns, S) or window-scoped (new small table, M).

### Couple Referral Rewards (refer-a-couple, both get a perk, tracked in-app)
- **Status:** ○ roadmap (larger)  ·  **Effort:** L · large
- **Current state:** NOTHING for couple referrals. No referral table in any migration (grep for referral_code/referred_by/couple_referral = empty; all 'referral' hits are vendor token redeem-codes or HTML rel=noreferrer). No referral code on users, no in-app tracking. HOWEVER the PERK rail exists and is reusable: supabase/migrations/20260529010000_voucher_system_day1.sql ships discount_codes (code ^[A-Z0-9]{8}$, types amount_off/pct_off/free, max_uses, eligible_users, effective_from) + per-order redemption + admin surface app/admin/discount-codes. The wedding showcase already calls Editorial 'the referral loop' but that's organic word-of-mouth, not a tracked program.
- **What to build:** Net-new program (architect mandate: couple+couple+admin). Schema (RLS at CREATE TABLE, canonical patterns): referral_codes(referral_code_id, public_id, owner_user_id FK users, code UNIQUE ^[A-Z0-9]{8}$, created_at) — owner reads/creates own (USING owner_user_id=auth.uid()), admin FOR ALL; referral_redemptions(redemption_id, referral_code_id FK, referred_user_id FK, status CHECK('pending'|'qualified'|'rewarded'), qualifying_event_id, referrer_reward_code_id FK discount_codes, referred_reward_code_id FK discount_codes, created_at, qualified_at) with anti-self-referral CHECK(referred_user_id<>owner via trigger) and a qualification gate (e.g. referred couple completes signup / first paid order) mirroring the completion gates used in vendor_recommendations. Server actions: generateReferralCode, applyReferralAtSignup, and a cron-free after()/waitUntil that, on qualification, MINTS two single-use discount_codes (one each) via the existing voucher system and marks rewarded. UI: couple dashboard 'Refer a couple' card (share link + status list), referred-couple banner at signup, admin surface to view referrals + set the reward value (reward value = a discount_codes row, so admin-managed, never hardcoded). RA 10173: store minimal PII, no email harvesting.
- **Reuse:** discount_codes + voucher redemption + app/admin/discount-codes (the entire perk/reward payout rail), generate_public_id, vendor_recommendations completion-gate RLS pattern as the qualification template, 0028 email templates for 'your friend joined / you earned a perk', share-buttons.
- **Dependencies:** discount_codes/voucher system (exists); a 'qualifying event' definition (signup vs first paid order) — owner sign-off; reward amounts as admin-created discount_codes; abuse/self-referral guardrails; 0028 email for both-party notifications.

## Data & analytics

### Price-Position Meter (your package vs the live market band for category/region/pax)
- **Status:** ○ roadmap (larger)  ·  **Effort:** L · large
- **Current state:** Partial substrate, no market-band engine. Admin-managed price catalogs exist: platform_retail_catalog_v2 (supabase/migrations/20260628000000_v2_additive_phase_a.sql — retail_price_php, but these are 1st-party SKUs, not vendor market prices) and vendor-side vendor_packages (supabase/migrations/20260604110000_vendor_packages.sql) + vendor_services hold each vendor's OWN prices. Region is already a first-class, banded, admin-editable concept: token_burn_bands + public.regions.burn_band + lib/v2/region-token-burn.ts. Pax-based pricing lock exists (memory project_setnayan_pax_based_pricing: 100 floor + per-50). There is NO table that AGGREGATES vendor prices into a market band by (category, region, pax) — nothing computes percentiles/min-median-max across vendors.
- **What to build:** Schema: a precomputed market_price_bands table (PK: category + region_slug + pax_bucket; cols low_php/median_php/high_php/sample_n/computed_at) — RLS ENABLED at CREATE TABLE, public/authenticated SELECT (vendors need to read their band), admin-only write (matches token_burn_bands pattern). A SECURITY DEFINER recompute_market_price_bands() that aggregates published vendor_packages/vendor_services prices, suppressed below a min-N (reuse the min-N de-identification rule from memory project_setnayan_behavioral_data_edge). Server: a vendor-dashboard read action that takes the vendor's own package price + their (category, region, pax) and returns position-in-band. Surfaces — VENDOR: a meter card in vendor-dashboard (extend vendor-stats-panel.tsx or new pricing tab) showing 'you're in the Xth percentile / above-band / below-band'; ADMIN: a band-review surface + the recompute trigger control (clone /admin/token-bands page pattern); COUPLE: optional — none required v1, but the same bands could later power a 'fair price' hint. Honor admin-managed-prices lock: never hardcode band thresholds; all derive from the catalog/vendor tables.
- **Reuse:** token_burn_bands + /admin/token-bands page & actions (admin band-editor pattern), lib/v2/region-token-burn.ts + lib/region-source.ts (canonical region resolver), vendor_packages/vendor_services (price source), min-N suppression doctrine (project_setnayan_behavioral_data_edge), vendor-stats-panel.tsx card grid for the vendor surface.
- **Dependencies:** Needs enough published vendor packages per (category, region, pax) cell to clear a min-N — currently marketplace is founder-only (memory project_setnayan_marketplace_founder_only_vendor), so bands will be empty/suppressed until real vendor density exists. Depends on a canonical pax-bucket definition and consistent per-package pax metadata on vendor_packages.

### Demand Radar (which dates/regions/looks couples book hardest)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Raw demand signals exist but are not aggregated into a vendor-facing radar. events table carries date + region (events.region, used by region-token-burn) and event_type; 'looks' map to the Mood Board / style taxonomy and vendor refinements (memory project_setnayan_refinements_terminology, events.papic_style migration 20270302000000). Booking-intensity signals live in chat_threads (inquiry volume), vendor_event_unlocks (which events vendors paid to answer — a strong demand proxy), and event_vendors.status progression. PostHog captures behavioral events client-side (lib/analytics.ts) but there is no Supabase rollup of demand by date/region/style, and nothing vendor-facing surfaces it.
- **What to build:** Schema: a demand_rollup table or (cheaper) a set of SQL VIEWs/materialized views aggregating events + chat_threads + vendor_event_unlocks by (region, month-bucket, event_type, style/look) into inquiry_count, unlock_count, booking_count — RLS at create-time; expose only DE-IDENTIFIED aggregates above min-N (project_setnayan_behavioral_data_edge: identified vs de-identified, min-N). Server: a recompute job (Next 15 after()/waitUntil per project_setnayan_cron_free — no polling cron) + a vendor read action scoped to the vendor's own categories/regions. Surfaces — VENDOR: a 'Demand Radar' card/page in vendor-dashboard (heat by month, top regions, hot looks) so a vendor sees where to focus; ADMIN: a fuller demand dashboard alongside /admin/insights & /admin/funnels; COUPLE: not required. Aggregate-only, never expose another couple's identifiable plan.
- **Reuse:** events (date/region/type/style), chat_threads + vendor_event_unlocks (demand proxies), lib/region-source.ts (region normalization), Mood Board / refinement taxonomy for 'looks', /admin/funnels + /admin/insights page scaffolds, project_setnayan_behavioral_data_edge min-N + de-identification rules, after()/waitUntil cron-free recompute.
- **Dependencies:** Meaningful radar needs booking/inquiry volume density per region/look to clear min-N; thin in a founder-only marketplace. 'Looks/style' axis depends on style metadata being populated on events (events.papic_style + Mood Board) consistently.

### Won & Lost Reasons (why each inquiry closed yes/no)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Close-state exists but reasons are barely captured. chat_threads has inquiry_status (pending/accepted/declined) + a single decline_reason column (migration 20260722000000_chat_inquiry_accept_gate.sql) — but that captures the VENDOR declining an inquiry, NOT why a couple ultimately picked or passed on the vendor. vendor_proposals has status (sent/viewed/accepted/declined/expired) — the win/lose signal — but no reason field. event_vendors.status moves through considering→contracted etc. with no close-reason. vendor_activity_stats.inquiry_to_booking_pct gives the aggregate rate but no per-inquiry why. There is NO structured won/lost-reason capture surface.
- **What to build:** Schema: an inquiry_outcomes table keyed to chat_thread / vendor_proposal (outcome ENUM won/lost/no_response, reason_code from an admin-managed reason taxonomy, free_text, who_recorded, recorded_at) — RLS at create-time: vendor reads/writes own (current_vendor_profile_ids() per migration 20260821000000_vendor_role_aware_rls.sql), admin reads all (is_console_admin() pattern from 20260916000000). Optionally a couple-side soft signal (why they passed) gated to the couple. Server: a recordOutcome vendor action surfaced at proposal-decline / thread-close; an admin-managed reason_codes lookup (never hardcode the list — taxonomy-DB-driven per memory project_setnayan_categories_db_not_hardcoded). Surfaces — VENDOR: a quick 'mark won/lost + reason' prompt on the thread/proposal + a roll-up in vendor-dashboard; ADMIN: reason-taxonomy editor + aggregate report in /admin/insights; COUPLE: optional one-tap 'why you passed' on declining a proposal (feeds the vendor anonymized).
- **Reuse:** chat_threads.inquiry_status + decline_reason (extend the pattern), vendor_proposals.status (win/lose anchor), respond_vendor_proposal() RPC (hook the reason capture there), current_vendor_profile_ids() RLS + is_console_admin() admin gate, taxonomy-DB-driven reason codes pattern, vendor-stats-panel.tsx for the vendor roll-up.
- **Dependencies:** Win/lost truth is partly off-platform (RA 11967 / money stays off-platform per region-token-burn note + project_setnayan_vendor_payment_disclosure) — 'won' is a self-reported signal, not a verified booking, so data quality hinges on vendor adherence to marking outcomes. Builds on the proposals workstream (memory project_setnayan_linked_inquiry_build_cascade).

### Quote-to-Booking Funnel (views→inquiries→quotes→booked, with source)
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Three of four stages already have data; the 'views' top-of-funnel is the gap. INQUIRIES: chat_threads (created per couple→vendor inquiry, inquiry_status). QUOTES: vendor_proposals (status sent/viewed/accepted). BOOKED: event_vendors.status (contracted+) + vendor_activity_stats.finalized_booking_count + inquiry_to_booking_pct. SOURCE: event_vendors.source/source_category (migration 20260604120000) tracks how a pick entered (host_manual / auto_cascade_from_finalize / invite_claim). A platform-wide funnel already ships at /admin/funnels (apps/web/app/admin/funnels/page.tsx) computing signup→event→paid and vendor signup→profile→first-thread. MISSING: a vendor_profile_views / impression table — there is NO view-tracking table in supabase/migrations (grep confirms), so the 'views' stage and PostHog→Supabase mirror for per-vendor funnels don't exist; the existing admin funnels page explicitly notes the PostHog-side funnels aren't mirrored to Supabase.
- **What to build:** Schema: a vendor_profile_views table (vendor_profile_id, event_id?, source/utm, viewer-hashed, viewed_at) to seed the top-of-funnel — RLS at create-time, vendor reads aggregate of own, admin reads all; writes via a lightweight server action or PostHog→Supabase mirror. Server: a per-vendor funnel read action joining views→chat_threads→vendor_proposals→event_vendors, sliced by source (reuse event_vendors.source). Surfaces — VENDOR: a 'Quote-to-Booking funnel' panel in vendor-dashboard (clone the FunnelTable component already in /admin/funnels/page.tsx, scoped to the vendor); ADMIN: extend /admin/funnels with a per-vendor drill-down; COUPLE: n/a. Capture views privacy-safe (hashed viewer, min-N) per behavioral-data-edge.
- **Reuse:** FunnelTable component + funnel-step pattern in apps/web/app/admin/funnels/page.tsx (directly clonable), chat_threads / vendor_proposals / event_vendors / vendor_activity_stats (3 of 4 stages already populated), event_vendors.source/source_category (the 'with source' axis), PostHog client+server (lib/analytics.ts, posthog-provider.tsx) for view capture, vendor-stats-panel.tsx layout.
- **Dependencies:** Only the 'views' stage needs net-new instrumentation (vendor_profile_views table + capture on /vendors/[slug] and explore cards). proposals='quotes' stage depends on the in-chat proposal workstream being live (migration 20270225555952 unparked inquiry-stage proposals). Source attribution beyond event_vendors.source (e.g. true UTM on the inquiry) needs the view-capture to stamp referrer/utm.

### Peso-Per-Lead Scorecard (cost of each booked couple vs tier + token spend)
- **Status:** ◐ buildable now  ·  **Effort:** S · small
- **Current state:** Both cost inputs already exist in the DB — this is mostly a join/compute. TOKEN SPEND per lead: vendor_event_unlocks (migration 20260908000000) records tokens_burned + region_slug + band per (vendor, event) — i.e. the exact peso cost to answer each lead (tokens × ₱100, lib/v2/region-token-burn.ts); plus import_external_client burns (20261127000000) and earned/purchased token provenance (earned_token_vouchers, vendor_token_purchases, token_grants_log). TIER cost: vendor subscription tiers Solo ₱2,000 / Pro ₱6,000 / Ent ₱10,000 per-28d (memory project_setnayan_vendor_tier_ladder; vendor_billing_catalog + migration 20261010000000_vendor_subscription_checkout). BOOKED couples: event_vendors.status / vendor_activity_stats.finalized_booking_count. No surface joins token-spend + subscription cost against booked-couple count into a per-lead/per-booking peso scorecard.
- **What to build:** Schema: mostly none — a reporting VIEW or a thin scorecard rollup over vendor_event_unlocks (Σ tokens_burned × token price) + subscription spend (vendor_billing_catalog/subscription rows) ÷ finalized_booking_count, per period. If materialized, RLS at create-time (vendor reads own via current_vendor_profile_ids(); admin all). Server: a read action computing cost-per-lead (total token+sub spend ÷ leads answered) and cost-per-booked-couple (÷ finalized bookings); token price is admin-managed (TOKEN_PRICE_PHP, but read from DB not hardcoded — honor prices-are-admin-managed lock). Surfaces — VENDOR: a scorecard card in vendor-dashboard/subscription or stats panel ('you spent ₱X in tokens + ₱Y subscription this cycle = ₱Z per booked couple'); ADMIN: a unit-economics view in /admin/insights to watch vendor ROI/ retention; COUPLE: n/a.
- **Reuse:** vendor_event_unlocks (per-lead token cost — the core input), lib/v2/region-token-burn.ts (TOKEN_PRICE_PHP), vendor_token_purchases + earned_token_vouchers + token_grants_log (spend provenance), vendor_billing_catalog + subscription tier data (project_setnayan_vendor_tier_ladder), vendor_activity_stats.finalized_booking_count (denominator), vendor-stats-panel.tsx + subscription page for the surface, is_console_admin() / current_vendor_profile_ids() RLS gates.
- **Dependencies:** Accuracy of 'booked couple' count depends on the same off-platform-booking caveat as Won/Lost (finalized_booking_count is a signal, not a verified payment). Token burn must actually be charged — note lib/v2/region-token-burn.ts says the burn path is 'economically inert in pilot' pending owner activation, so peso-per-lead is only meaningful once burn-on-answer is live; until then it reports ₱0/lead. Subscription-cost half needs vendors actually on paid tiers.

## Ecosystem & crew

### Resell Setnayan Productions
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** Strong bundling + recommendation primitives exist, but none wire a vendor's quote to the FIRST-PARTY SKU catalog. (1) supabase/migrations/20260604110000_vendor_packages.sql ships vendor_packages + vendor_package_items + event_vendor_packages with cascade-lock + consumable budget — but package items are canonical_service TEXT taxonomy strings, NOT platform_retail_catalog_v2 service_codes. (2) supabase/migrations/20261014000000_vendor_service_links.sql ships vendor_service_links ('comes with [X][Y][Z]' on a vendor's card) + vendor_services.is_linked_only — again keyed to canonical_service taxonomy, not Setnayan SKUs. (3) supabase/migrations/20270110320016_vendor_partnerships.sql is the closest: relationship_type IN ('sponsored_included','sponsored_discounted') + additional_fee_centavos + discount_pct + covered_plan_groups[], admin_verified gate, and it is FULLY WIRED across the triad (apps/web/app/admin/vendor-partnerships/*, apps/web/app/vendor-dashboard/partnerships/page.tsx, apps/web/app/explore/_components/vendor-card.tsx). (4) First-party Setnayan services are catalogued in platform_retail_catalog_v2 (19 SKUs incl. PAPIC_SEATS/PANOOD_SYSTEM/ANIMATED_MONOGRAM/PAKANTA) — migration 20260628000000_v2_additive_phase_a.sql — and float to the top of search via vendor_market_stats.is_setnayan_service (20260607020000). The couple checkout for these SKUs is orders.service_key (apps/web/app/dashboard/[eventId]/studio/bundle/page.tsx + checkout/actions.ts). What is MISSING: a vendor cannot attach a Setnayan SKU (e.g. PAPIC_SEATS) to their own package/quote, there is NO referral attribution/cut back to the reselling vendor on the resulting order, and no 'ride first-party bundle recs' surface that recommends Setnayan productions inside a vendor's offering.
- **What to build:** Bridge the existing partnership/package primitives to the first-party SKU catalog + add referral attribution. SCHEMA (RLS at CREATE, canonical patterns): (a) ALTER vendor_package_items / vendor_service_links to optionally carry a platform_retail_catalog_v2.service_code (nullable FK) so a package line can BE a Setnayan SKU, not just a taxonomy string. (b) New table vendor_setnayan_resell(resell_id, vendor_profile_id FK, service_code FK→platform_retail_catalog_v2, vendor_markup_centavos, is_active, created_at) — RLS owner+admin write / public read when vendor published (mirror vendor_service_links policies). (c) New table setnayan_resell_attributions(order_id FK→orders, referring_vendor_profile_id FK, service_code, referral_cut_centavos, status, created_at) — RLS: vendor reads own via current_vendor_ids('viewer'), admin via is_admin(); written by a SECURITY DEFINER fn at order-create time. PRICES NEVER HARDCODED — resell price = catalog retail (admin-managed in platform_retail_catalog_v2) + vendor markup; referral cut % is an admin-managed config row, not a literal. SERVER ACTIONS: addSetnayanSkuToPackage (vendor-dashboard), and a hook in submitOrderAction (apps/web/app/dashboard/[eventId]/checkout/actions.ts) that stamps referring_vendor on Setnayan-SKU orders that originated from a vendor card. UI (architect-mandate triad): VENDOR — extend /vendor-dashboard/partnerships + the package builder to pick Setnayan productions to bundle/resell; COUPLE — show 'Setnayan productions included via {vendor}' on the vendor card (extend explore/_components/vendor-card.tsx, already renders partnership badges) + at studio checkout; ADMIN — extend /admin/vendor-partnerships to approve resell relationships + a referral-payout reconciliation view in admin payouts.
- **Reuse:** vendor_partnerships (relationship_type/covered_plan_groups/admin_verified + full admin/vendor/couple UI), vendor_packages cascade-lock, vendor_service_links 'comes with' card pattern, platform_retail_catalog_v2 + fetchV2BundleCatalog (apps/web/lib/v2-catalog.ts), orders.service_key checkout (studio/bundle/page.tsx + checkout/actions.ts), is_setnayan_service first-party float (vendor_market_stats), current_vendor_ids()/is_admin() RLS helpers.
- **Dependencies:** platform_retail_catalog_v2 (admin-managed SKU prices) + orders/checkout pipeline; vendor_partnerships admin-verify queue; an admin-set referral-cut config (new). Note: marketplace is founder-only-vendor today (one published vendor) — 'reselling' is mostly theoretical until 3rd-party vendors are published, so ship behind the same publish gate.

### White-Label Couple Tools
- **Status:** ○ roadmap (larger)  ·  **Effort:** L · large
- **Current state:** Booked vendors ALREADY get scoped read access to two of the three named couple tools, via the owner-locked 'Feature Access By Vendor Category' program (03_Strategy/Feature_Access_By_Vendor_Category_2026-06-12.md). SEATING: supabase/migrations/20261201003000_vendor_seat_plan_viewer.sql ships get_vendor_seat_plan(p_event_id) SECURITY DEFINER — a booked floor-touching vendor reads the PUBLISHED floor plan + per-table seated counts (+ meal counts for food categories), guest PII never crosses. SCHEDULE: supabase/migrations/20261130003000_shared_timeline_suggestions.sql ships current_vendor_booked_event_ids() + booked-vendor SELECT on event_schedule_blocks + event_schedule_suggestions (vendor PROPOSES, couple/coordinator approve). MOOD BOARD: supabase/migrations/20260513080000_iteration_0010_mood_board.sql is couple-private — NO vendor share path. What is MISSING is the actual 'white-label' / 'under the vendor brand' layer: today these are read-only views rendered in Setnayan chrome inside the vendor dashboard. There is NO vendor-branded chrome, NO co-brand/'powered by' theming, and NO way to hand the seating/mood-board/schedule TOOL (editable, branded) to a couple as the vendor's own product (grep for white_label/co_brand/powered_by → nothing in schema).
- **What to build:** Two distinct pieces. (1) Mood-board vendor share — small: a vendor-read SECURITY DEFINER read model for event mood boards gated on current_vendor_booked_event_ids(), mirroring get_vendor_seat_plan, plus a vendor-dashboard surface — closes the gap so all three tools are vendor-visible. (2) The white-label brand layer — larger: new table vendor_white_label_config(vendor_profile_id PK FK, brand_logo_url, accent_hex, powered_by_visible bool, enabled_tools text[], is_active) RLS owner+admin write / read by booked couples; a co-branded render mode for the seating/mood-board/schedule surfaces that swaps Setnayan chrome for the vendor's logo + accent when a couple opens the tool inside a booked-vendor context. Gate behind vendor tier (Pro/Enterprise) via admin-managed entitlement — NOT hardcoded. ARCHITECT TRIAD: VENDOR — a 'Your branded tools' config tab in /vendor-dashboard; COUPLE — branded chrome + 'powered by {vendor}' on the seating/mood/schedule surfaces for that booked vendor; ADMIN — entitlement toggle + brand-asset moderation in /admin (logos are public-facing). Honor seat-plan-stays-free lock: white-label MUST NOT paywall the couple's own access to their tools — it only re-skins the vendor-facing/co-branded view.
- **Reuse:** get_vendor_seat_plan RPC + vendor seat-plan viewer UI, shared_timeline_suggestions/event_schedule_suggestions, current_vendor_booked_event_ids() helper, mood board schema (iteration 0010), the 5-theme runtime theme picker (iteration 0021 pilot — already a theming primitive) for the accent/logo swap, vendor logo_url (mandatory at registration, iteration 0022).
- **Dependencies:** Feature_Access_By_Vendor_Category program (booked-vendor read access — shipped); vendor tier entitlement system (vendor_tier ladder) for gating; theme system from 0021 for the brand swap. Larger because the co-branded render mode touches multiple couple-facing surfaces and needs brand-asset moderation.

### Crew-Rate Marketplace
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** A host→vendor gig primitive exists and is wired, but NOT a vendor↔vendor crew marketplace with a referral cut. SHIPPED: supabase/migrations/20260704020000_v2_phase_f_manpower_gigs.sql → manpower_gigs (event_id, posted_by_user_id [HOST], vendor_profile_id [accepting vendor], cash_amount_php_centavos default ₱15k, 2-token handshake on accept, status pending/accepted/completed/cancelled, BIR-exempt note — Setnayan never touches the cash). It is fully wired: apps/web/app/dashboard/[eventId]/manpower/page.tsx (host posts), apps/web/app/vendor-dashboard/manpower/page.tsx + actions.ts + gig-card.tsx (vendor accepts via token burn). CREW MODELING: vendor_team_members (20260514010000, role owner>admin>agent>viewer via current_vendor_ids(min_role)), vendor_service_agents (20260816000000, scope an agent to services), and registered_crew_devices / events.master_qr_token (20260704000000, 5-device-per-vendor crew pairing). What is MISSING for THIS benefit: the post is by the HOST, not a vendor posting a crew slot to OTHER vendors; there is NO 'earn a referral cut' (the only money primitive is the 2-token handshake fee TO Setnayan, not a cut TO the poster); and there is NO 'vetted hand at a posted rate' directory of individual crew the way the benefit describes.
- **What to build:** Generalize manpower_gigs into a vendor-sourced crew board + add referral attribution. SCHEMA (RLS at CREATE, canonical patterns): (a) ALTER manpower_gigs to allow vendor-posted gigs — add posted_by_vendor_profile_id (nullable FK) + posting_party CHECK('host'|'vendor') + posted_rate_centavos (the 'posted rate'); keep posted_by_user_id for audit. (b) New table crew_referrals(referral_id, gig_id FK→manpower_gigs, referring_vendor_profile_id FK, accepting_vendor_profile_id FK, referral_cut_centavos, status, created_at) — RLS: each vendor reads rows where it is referrer or acceptee via current_vendor_ids('viewer'); admin via is_admin(); write via SECURITY DEFINER fn on accept. (c) Optional crew_directory(vendor_team_member-or-vendor_profile, skill_tags[], posted_day_rate_centavos, is_available, vetted_by_admin bool) for the 'pull a vetted hand at a posted rate' directory — RLS public read when vetted+available, owner+admin write. Referral cut % = admin-managed config, NOT hardcoded; cash still flows off-platform (preserve the BIR-exempt non-intermediary stance — Setnayan only records attribution + optionally burns/credits tokens). SERVER ACTIONS: postCrewGig (vendor-dashboard), acceptCrewGig (extend existing acceptManpowerGig to stamp crew_referrals), browseCrewDirectory. ARCHITECT TRIAD: VENDOR — a /vendor-dashboard/crew board (post a slot, browse vetted hands, see referral earnings); COUPLE — unchanged / optional visibility that their vendor pulled vetted crew; ADMIN — vet crew-directory entries + reconcile referral-cut ledger in /admin (extend the existing manpower admin surface).
- **Reuse:** manpower_gigs + acceptManpowerGig action + gig-card UI (apps/web/app/vendor-dashboard/manpower/*), the vendor token wallet / consume_vendor_assets() handshake economy, vendor_team_members + vendor_service_agents (crew identity + scoping), registered_crew_devices + master_qr_token (crew pairing), current_vendor_ids()/is_admin() RLS helpers.
- **Dependencies:** Vendor token wallet + consume_vendor_assets() (shipped); admin-managed referral-cut config (new); the off-platform-pay / no-2307 stance (must be preserved). Multi-published-vendor marketplace is currently founder-only, so vendor↔vendor matching only becomes real once more vendors are published.

### Setnayan-Certified Service Partner
- **Status:** ◐ buildable now  ·  **Effort:** M · medium
- **Current state:** The badge + admin-verification + search-surfacing machinery exists generically; what's missing is binding it to FIRST-PARTY in-app-service delivery + couple routing. CLOSEST PRIMITIVE: supabase/migrations/20270110320016_vendor_partnerships.sql → relationship_type='accredited' ('the recommending vendor formally certifies the other') + admin_verified gate (public only after admin marks verified) + full triad UI (apps/web/app/admin/vendor-partnerships/page.tsx + actions.ts, apps/web/app/vendor-dashboard/partnerships/page.tsx, apps/web/app/explore/_components/vendor-card.tsx renders the badge). SEARCH ROUTING already exists: vendor_market_stats.is_setnayan_service (20260607020000) floats first-party-service vendors to the top of marketplace search (explore/page.tsx orders is_setnayan_service DESC). VERIFICATION pipeline exists: vendor verification_state + admin queues (iteration 0006/0023). What is MISSING: there is NO 'Setnayan-Certified to DELIVER in-app services on the ground' concept — i.e. a badge tied to platform_retail_catalog_v2 SKUs (this vendor is certified to run Papic/Panood/monogram/Pakanta on-site) and NO couple-routing that, when a couple buys a first-party SKU, surfaces/assigns a certified local partner. Today first-party SKUs are fulfilled by Setnayan itself (founder vendor); there is no partner-delivery layer.
- **What to build:** Add a certification dimension keyed to first-party SKUs + couple routing. SCHEMA (RLS at CREATE, canonical patterns): (a) New table setnayan_certified_partners(cert_id, vendor_profile_id FK, service_code FK→platform_retail_catalog_v2, cert_status CHECK('applied'|'granted'|'revoked'), region/location_city, granted_by_admin_user_id, granted_at, created_at) — UNIQUE(vendor_profile_id, service_code); RLS: public read when granted, vendor reads own via current_vendor_ids('viewer'), admin write via is_admin() (mirror vendor_partnerships admin_verified policy split). (b) New column or flag on vendor_profiles for a 'Setnayan-Certified' badge derived from any granted cert. (c) Optional cert_routing_log for the couple-routing audit. SERVER ACTIONS: applyForCertification (vendor), grant/revokeCertification (admin), and a routing helper getCertifiedPartnersForSku(service_code, region) used at SKU checkout to surface 'certified partners near you'. ARCHITECT TRIAD: VENDOR — a 'Setnayan certifications' tab in /vendor-dashboard (apply per SKU, see status); COUPLE — at first-party SKU purchase (studio checkout), surface certified local partners + a 'Setnayan-Certified' badge on vendor cards (extend explore/_components/vendor-card.tsx, which already renders partnership/verification badges); ADMIN — a certification review/grant queue in /admin (clone the vendor-partnerships admin-verify pattern). Badge eligibility + any cert fee = admin-managed, NOT hardcoded.
- **Reuse:** vendor_partnerships admin_verified pattern + its full admin/vendor/explore UI (clone wholesale), vendor verification_state + admin verification queue (iteration 0006/0023), is_setnayan_service search float (vendor_market_stats), explore vendor-card badge rendering, platform_retail_catalog_v2 (the SKUs a partner gets certified to deliver), current_vendor_ids()/is_admin() helpers, HQ region columns (20260620000000 vendor_profiles_hq_region) for geo routing.
- **Dependencies:** platform_retail_catalog_v2 (the certifiable SKU list); vendor verification pipeline; geo/region data (vendor_profiles hq_latitude/longitude/location_city) for couple routing. Real partner-delivery only matters once first-party SKUs are fulfilled by 3rd-party vendors rather than the founder vendor — until then certification is a badge without an on-the-ground delivery network.


---
_Generated 2026-06-29 from a read-only codebase audit (7 parallel agents). Correct any drift against the live code when you build — the audit is a starting map, not gospel._